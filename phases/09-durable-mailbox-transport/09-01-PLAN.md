---
phase: 09-durable-mailbox-transport
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_mailbox.py
  - tests/multiprocess/test_mailbox_durability.py
  - tests/structural/test_phase_09_shape.py
autonomous: true
requirements:
  - MBOX-01
  - MBOX-02
  - MBOX-03
  - MBOX-04

must_haves:
  truths:
    - "tests/unit/test_mailbox.py exists with failing tests covering MBOX-02 (ordered, peek, since-exclusive), MBOX-03 (MAXLEN, MINID trim, EXPIRE), MBOX-04 (8-field payload completeness)"
    - "tests/unit/test_mailbox.py includes test_since_excludes_already_seen that verifies the '(' exclusive-range prefix works through redis-py, gating MBOX-02 --since correctness"
    - "tests/structural/test_phase_09_shape.py exists and covers file-presence, symbol-presence, forbidden-import, and SUMMARY coverage checks for Phase 9"
    - "tests/multiprocess/test_mailbox_durability.py exists with a failing test for MBOX-01 CLI-boundary durability"
    - "All three test files fail (RED phase) because message/_ops.py does not exist yet"
  artifacts:
    - path: "tests/unit/test_mailbox.py"
      provides: "Unit tests for mailbox ops (MBOX-02, MBOX-03, MBOX-04) and exclusive-range probe"
      contains: "def test_since_excludes_already_seen"
    - path: "tests/multiprocess/test_mailbox_durability.py"
      provides: "CLI-boundary durability test (MBOX-01)"
      contains: "def test_mailbox_persists_for_offline_session"
    - path: "tests/structural/test_phase_09_shape.py"
      provides: "Phase 9 structural invariant checks"
      contains: "def test_message_ops_module_exists_and_has_required_functions"
  key_links:
    - from: "test_since_excludes_already_seen"
      to: "mailbox_inbox(since=last_msg_id)"
      via: "assert returned messages do NOT include entry at last_msg_id"
      pattern: "test_since_excludes_already_seen"
    - from: "test_phase_09_shape.py"
      to: "src/em_proj/message/_ops.py"
      via: "Path.read_text() source assertion"
      pattern: "MBOX_KEY_PREFIX.*mbox:"
---

<objective>
Create the Wave 0 test scaffolds for Phase 9. All three test files must exist and fail
in RED phase before any implementation is written.

Purpose: Establish the Nyquist test contract for MBOX-01..04. Critically, includes
the redis-py exclusive-range probe (test_since_excludes_already_seen) which gates
whether the '(' prefix works through redis-py 7.4.0 or needs a Lua fallback — this
must be written before the ops module, so the test drives the implementation decision.

Output:
  - tests/unit/test_mailbox.py — unit tests for ops-layer mailbox behavior
  - tests/multiprocess/test_mailbox_durability.py — CLI-boundary durability test
  - tests/structural/test_phase_09_shape.py — structural invariant assertions
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/09-durable-mailbox-transport/09-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces the test scaffolds will call (from existing code). -->

From tests/conftest.py:
```python
# clean_db fixture: function-scoped FLUSHDB on redis db=15; returns redis.Redis client
# redis_precheck fixture: session-scoped ping check; skip on Redis unreachable
# multiproc_race fixture: parallel subprocess.Popen launcher; returns list[RaceResult]
# TEST_DB: int = 15
# EM_PROJ_BIN: str = "em-proj"
# RaceResult(returncode, stdout, stderr, duration_ms)
```

From src/em_proj/session/_ops.py (used by multiprocess test for session_id):
```python
def session_register() -> dict: ...  # returns dict with "session_id" key
```

From tests/unit/test_session.py (autouse fixture pattern to mirror):
```python
@pytest.fixture(autouse=True)
def _reset_client_between_tests():
    rc._reset_for_tests()
    yield
    rc._reset_for_tests()

@pytest.fixture(autouse=True)
def _point_session_at_test_db(monkeypatch):
    monkeypatch.setenv("EM_PROJ_REDIS_DB", "15")
```

From tests/structural/test_phase_08_shape.py (structural test pattern to mirror):
```python
REPO_ROOT = Path(__file__).resolve().parent.parent.parent
PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "09-durable-mailbox-transport"
# pytest.skip() when PHASE_DIR absent (planning worktree not attached)
# Source-text assertions via Path.read_text()
# Import-line filtering for forbidden imports
```

Target interface (message/_ops.py — does NOT exist yet; tests will import and fail):
```python
# These are the imports tests will attempt:
from em_proj.message._ops import (
    MBOX_KEY_PREFIX,      # "mbox:"
    MBOX_MAXLEN,          # 500
    MBOX_TTL_SECONDS,     # 3600
    MAX_BODY_CHARS,       # to be defined
    mbox_write,           # (session_id: str, msg: dict) -> str  (returns msg_id)
    mailbox_inbox,        # (session_id: str, since: str|None, peek: bool) -> list[dict]
)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/unit/test_mailbox.py — unit test scaffold with exclusive-range probe</name>
  <files>tests/unit/test_mailbox.py</files>

  <read_first>
    - tests/unit/test_session.py lines 1-80 — autouse fixture pattern (_reset_client_between_tests, _point_session_at_test_db); import structure
    - tests/conftest.py — clean_db fixture; TEST_DB constant; _reset_for_tests usage
    - .planning/phases/09-durable-mailbox-transport/09-RESEARCH.md sections "Key Scenarios" and "Pitfall 1" — the exclusive '(' range test is the gating probe for the --since implementation
  </read_first>

  <behavior>
    - Test: importing from em_proj.message._ops raises ImportError (module does not exist yet — confirms RED)
    - Test: test_mbox_write_returns_msg_id — calls mbox_write; asserts returned value is a non-empty string (the stream entry ID)
    - Test: test_mailbox_inbox_ordered_reads — writes N messages; asserts XRANGE returns them in ascending msg_id order
    - Test: test_peek_does_not_consume — write 3 messages; call mailbox_inbox(peek=True); call mailbox_inbox(peek=False); assert second call returns same 3 messages; third call returns []
    - Test: test_since_excludes_already_seen — write msg_A then msg_B; consume both (peek=False); write msg_C; call mailbox_inbox(since=msg_B["msg_id"], peek=True); assert result contains msg_C but NOT msg_B (exclusive '(' range working); this test gates the Lua fallback decision
    - Test: test_mbox_maxlen_bound — write MBOX_MAXLEN + 20 messages; assert client.xlen(mbox_key) <= MBOX_MAXLEN + 50 (approximate trim allows slight overage)
    - Test: test_mbox_payload_has_all_eight_fields — write one message; read back; assert all 8 MBOX-04 fields present: msg_id, from_session, pattern, scope, topic, body, sent_at, ttl
    - Test: test_topic_field_is_none_for_non_topic_pattern — write message with pattern="broadcast", topic=None; read back; assert msg["topic"] is None
    - Test: test_consume_removes_from_stream — write 2 messages; call mailbox_inbox(peek=False); call again; assert second call returns []
    - Test: test_max_body_chars_enforced — attempt mbox_write with body exceeding MAX_BODY_CHARS; assert raises ValidationError
  </behavior>

  <action>
    Create tests/unit/test_mailbox.py. Module docstring must state: "Unit tests for
    em_proj.message._ops mailbox operations. Covers MBOX-02, MBOX-03, MBOX-04 at the
    ops layer (no subprocess). Includes the redis-py exclusive-range probe
    (test_since_excludes_already_seen) per 09-RESEARCH.md Pitfall 1."

    Imports required (all at module top):
    - from __future__ import annotations
    - import time, pytest, import em_proj.redis_client as rc
    - from em_proj.message._ops import (MBOX_KEY_PREFIX, MBOX_MAXLEN, MBOX_TTL_SECONDS,
      MAX_BODY_CHARS, mbox_write, mailbox_inbox)
    - Import ValidationError from the appropriate location once message._ops defines it
      (use em_proj.state.kv.ValidationError as the same class, mirroring claim.py).

    Autouse fixtures (mirror test_session.py pattern exactly):
    - _reset_client_between_tests: calls rc._reset_for_tests() before and after each test
    - _point_session_at_test_db: monkeypatch.setenv("EM_PROJ_REDIS_DB", "15")

    Helper used across tests:
    - _make_msg(from_session="test-sender", pattern="direct", scope="machine",
      body="hello", topic=None, ttl=3600) -> dict: builds the msg dict argument for mbox_write.
      All fields present; sent_at = time.time(). This is a plain function, not a fixture.

    SESSION_ID constant: SESSION_ID = "test-session-09"

    Implement all tests from the behavior block above. For test_since_excludes_already_seen,
    the assertion comment must state: "If this test fails (msg_B returned), redis-py does NOT
    pass '(' prefix through XRANGE; Plan 09-02 Task 1 must use the Lua fallback instead."

    For test_mbox_maxlen_bound: the slack of +50 accommodates Redis's approximate trim
    behavior (macro-node boundaries). Do NOT assert xlen == MBOX_MAXLEN exactly.

    For test_consume_removes_from_stream: after mailbox_inbox(peek=False), assert that
    client.xrange(MBOX_KEY_PREFIX + SESSION_ID, min="-", max="+") returns [] (not
    xlen == 0, per 09-RESEARCH.md Pitfall 4 — XDEL may not update xlen immediately).

    ValidationError import: use "from em_proj.state.kv import ValidationError" —
    same class reused per claim.py precedent. If message._ops re-raises from a different
    location, update import accordingly after Plan 09-02.
  </action>

  <verify>
    <automated>bash /Users/emonical/projects/personal/ai-tools/em-proj/scripts/test.sh collect --tail 30</automated>
  </verify>

  <done>
    tests/unit/test_mailbox.py exists; pytest collects all test functions listed in the
    behavior block; running the tests fails with ImportError on em_proj.message._ops
    (confirming RED phase); no syntax errors in the test file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/structural/test_phase_09_shape.py and tests/multiprocess/test_mailbox_durability.py</name>
  <files>tests/structural/test_phase_09_shape.py, tests/multiprocess/test_mailbox_durability.py</files>

  <read_first>
    - tests/structural/test_phase_08_shape.py — full file (exact pattern to mirror: Path assertions, read_text() source checks, import-line filtering, SUMMARY coverage check, pytest.skip() for missing PHASE_DIR)
    - tests/multiprocess/test_session_registry.py lines 1-60 — multiprocess test pattern: subprocess.Popen via multiproc_race fixture, EM_PROJ_BIN, clean_db usage, RaceResult assertions
    - .planning/phases/09-durable-mailbox-transport/09-RESEARCH.md section "Structural tests" — exact list of assertions required
  </read_first>

  <behavior>
    Structural test assertions (test_phase_09_shape.py):
    - test_message_ops_module_exists_and_has_required_functions: asserts src/em_proj/message/_ops.py exists; source contains "def mbox_write", "def mailbox_inbox", "def _build_mbox_key", "def _decode_entry"
    - test_message_init_exists: asserts src/em_proj/message/__init__.py exists
    - test_mbox_key_prefix_is_mbox_colon: asserts 'MBOX_KEY_PREFIX' in source AND ('"mbox:"' or "'mbox:'" in source)
    - test_message_ops_prohibits_forbidden_imports: import-line filter (lines starting "import " or "from "); none contain "typer", "multiprocessing", "threading"
    - test_message_app_wired_in_cli: cli.py contains "message_app" >= 2 times (import + add_typer)
    - test_message_init_has_inbox_command: message/__init__.py contains "@message_app.command" >= 1 time
    - test_uses_streams_not_list: source contains "xadd" and "xrange" (case-insensitive); does NOT contain consumer group markers "xreadgroup" or "xack" (per research locked decision — no consumer groups)
    - test_phase_09_summaries_exist: every 09-*-PLAN.md has a 09-*-SUMMARY.md sibling; pytest.skip() when PHASE_DIR absent

    Multiprocess test assertion (test_mailbox_durability.py):
    - test_mailbox_persists_for_offline_session: spawns one child to write a message via
      em-proj message send (or directly via Python API boundary using subprocess), then
      spawns a second child to run em-proj message inbox --json; asserts second child's
      stdout parses as JSON list with at least one message; asserts returncode == 0.
      NOTE: Because em-proj message send is a Phase 10 verb, the multiprocess test writes
      via direct Python subprocess call to a helper script OR by using the em-proj CLI once
      it exists. For Wave 0, stub the test with pytest.skip("Phase 10 send verb not yet
      available — test will pass once Phase 10 ships") inside the test body so it collects
      but does not fail on missing verb. This allows the RED phase to be about import errors
      in unit tests only, not blocked on Phase 10.
  </behavior>

  <action>
    Create tests/structural/test_phase_09_shape.py. Mirror test_phase_08_shape.py structure
    exactly:
    - REPO_ROOT = Path(__file__).resolve().parent.parent.parent
    - PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "09-durable-mailbox-transport"
    - MESSAGE_OPS = REPO_ROOT / "src" / "em_proj" / "message" / "_ops.py"
    - MESSAGE_INIT = REPO_ROOT / "src" / "em_proj" / "message" / "__init__.py"
    - CLI_PY = REPO_ROOT / "src" / "em_proj" / "cli.py"
    - All structural tests use Path.read_text() source inspection (no imports from message)
    - For test_uses_streams_not_list: read MESSAGE_OPS source; assert "xadd" in src.lower() and
      "xrange" in src.lower(); assert "xreadgroup" not in src.lower() and "xack" not in src.lower()
    - For test_phase_09_summaries_exist: pytest.skip() when PHASE_DIR absent (planning worktree
      may not be attached); use PHASE_DIR.glob("09-*-PLAN.md") and assert matching SUMMARY sibling

    Create tests/multiprocess/test_mailbox_durability.py:
    - Module docstring: "Phase 9 MBOX-01 durability test — proves messages persist for offline
      sessions across the CLI boundary. Skipped until Phase 10 message send verb ships."
    - Single test function test_mailbox_persists_for_offline_session(clean_db, redis_precheck):
      - Body: pytest.skip("Phase 10 'message send' verb not yet available — "
        "enable this test once Phase 10 ships and em-proj message send is on PATH")
      - Include a comment block describing what the full test will verify: spawn subprocess
        running em-proj message send --to <session_id> "hello", then spawn subprocess running
        em-proj message inbox --json, assert stdout parses as JSON list with 1 entry matching
        MBOX-04 fields.
    - This stub collects and skips (does not fail), keeping MBOX-01 in the Nyquist map
      without blocking the Phase 9 GREEN phase on a Phase 10 dependency.
  </action>

  <verify>
    <automated>bash /Users/emonical/projects/personal/ai-tools/em-proj/scripts/test.sh collect --tail 40</automated>
  </verify>

  <done>
    tests/structural/test_phase_09_shape.py exists with all 8 structural test functions
    (collected by pytest); tests/multiprocess/test_mailbox_durability.py exists with 1
    test function that skips cleanly; all three Wave 0 test files collected without syntax
    errors; bash scripts/test.sh structural exits with structural tests either failing
    (for missing files) or skipping (for PHASE_DIR absent).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → message._ops | Tests import from em_proj.message._ops; import errors are expected in Wave 0 (RED phase) |
| multiprocess test → Redis db=15 | clean_db fixture owns FLUSHDB; all tests target EM_PROJ_REDIS_DB=15 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | test_mailbox.py: tests run against prod Redis if EM_PROJ_REDIS_DB unset | mitigate | _point_session_at_test_db autouse fixture forces EM_PROJ_REDIS_DB=15; _reset_client_between_tests clears singleton between tests — same pattern as test_session.py |
| T-09-01-02 | Denial of Service | test_mbox_maxlen_bound writes 520+ messages to db=15 | accept | clean_db FLUSHDB before and after each test; db=15 is the isolated test database; no prod data at risk |
| T-09-01-03 | Information Disclosure | structural tests read source files via Path.read_text() | accept | read-only file inspection; no secrets in source files; matches Phase 8 precedent |
</threat_model>

<verification>
Full Wave 0 verification:
- bash /Users/emonical/projects/personal/ai-tools/em-proj/scripts/test.sh collect exits 0 (all three files collected)
- bash /Users/emonical/projects/personal/ai-tools/em-proj/scripts/test.sh unit -k "mailbox" exits non-zero (ImportError — RED phase confirmed)
- bash /Users/emonical/projects/personal/ai-tools/em-proj/scripts/test.sh structural exits 0 or exits with failures only for missing Phase 9 files (not syntax errors)
</verification>

<success_criteria>
- tests/unit/test_mailbox.py exists with all 10 test functions from the behavior block
- tests/structural/test_phase_09_shape.py exists with all 8 structural assertions
- tests/multiprocess/test_mailbox_durability.py exists and skips cleanly (no fail)
- pytest collects all test functions without SyntaxError or ImportError in the test files themselves
- Running tests/unit/test_mailbox.py fails with ImportError on em_proj.message._ops (RED confirmed)
- test_since_excludes_already_seen is present and asserts exclusive exclusion of already-seen msg_id
- No watch-mode flags in any test file; all verify commands route through bash scripts/test.sh
</success_criteria>

<output>
After completion, create .planning/phases/09-durable-mailbox-transport/09-01-SUMMARY.md
</output>
