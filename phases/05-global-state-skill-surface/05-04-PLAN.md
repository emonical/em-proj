---
phase: 05-global-state-skill-surface
plan: "04"
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - ~/.claude/skills/em-global-state/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03]
must_haves:
  truths:
    - "/em-global-state list shells out to em-proj state list --json and emits the result"
    - "/em-global-state get <key> shells out to em-proj state get <key> --json and emits the result"
    - "/em-global-state locks [--mine] [--stale] shells out to em-proj state lock-list with flags"
    - "/em-global-state claims [--mine] [--active] [--stale] shells out to em-proj state claim-list with flags"
    - "/em-global-state unlock <name> prompts via AskUserQuestion when holder is live; --force skips prompt"
    - "/em-global-state release <area> prompts via AskUserQuestion when holder is live; --force skips prompt"
    - "The skill NEVER shells out to em-proj state set, del, claim, or lock (only unlock and release as write verbs)"
  artifacts:
    - path: "~/.claude/skills/em-global-state/SKILL.md"
      provides: "em-global-state skill"
      contains: "em-proj state unlock"
  key_links:
    - from: "unlock action"
      to: "em-proj state check (via lock CLI) OR em-proj state lock-list --stale"
      via: "live-holder probe before confirmation"
    - from: "release action"
      to: "em-proj state check <area>"
      via: "live-holder probe before confirmation"
---

<objective>
Create `~/.claude/skills/em-global-state/SKILL.md` — the markdown skill file that
makes `/em-global-state` available in Claude Code sessions. The skill shells out to
the now-complete `em-proj state` CLI and handles confirmation for write operations.

Purpose: Deliver the SKILL-01/02/03 user-facing surface without any Python code —
pure markdown instructions for the orchestrator agent to follow.

Output: `~/.claude/skills/em-global-state/SKILL.md`
</objective>

<execution_context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
</context>

<interfaces>
From ~/.claude/skills/em-status/SKILL.md (shape to follow):
  YAML frontmatter: name, description, allowed-tools
  Sections: <objective>, <action>, <when_to_invoke>, <scope>, <related>

The em-global-state skill needs the same shape but with a richer <action> section
covering 6 verbs: list, get, locks, claims, unlock, release.

Confirmed CLI verbs available after Plans 05-01 through 05-03:
  em-proj state list --json                         (KV list)
  em-proj state get <key> --json                    (KV get)
  em-proj state lock-list [--mine] [--stale] --json (new — Plan 05-03)
  em-proj state claim-list [--mine] [--active] [--stale] --json (new — Plan 05-03)
  em-proj state unlock <name> --json               (escape hatch write — Phase 3)
  em-proj state release <area> --json              (escape hatch write — Phase 4)
  em-proj state check <area> --json                (read-only probe — Phase 4)

Note: the exact verb name (lock-list vs lock list) was finalized in Plan 05-03.
Read 05-03-SUMMARY.md before writing this skill to confirm the exact verb name.

Confirmation mechanism (locked decision D-04):
  When --force is absent AND a live holder exists, use AskUserQuestion tool:
    "Lock '<name>' is held by session <session_id> (expires_at: <expires_at>).
     Force unlock? This will displace the holder. (yes/no)"
  On "yes": proceed with shell-out to em-proj state unlock <name>
  On "no" or any other answer: abort with message "Unlock cancelled."
  Same pattern for release <area>.
  The live-holder check: run em-proj state check <area> --json for claims,
  or em-proj state lock-list --json and parse the items array for locks.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ~/.claude/skills/em-global-state/SKILL.md</name>
  <files>~/.claude/skills/em-global-state/SKILL.md</files>
  <read_first>
    - ~/.claude/skills/em-status/SKILL.md — canonical em-* skill shape (frontmatter + sections)
    - ~/.claude/skills/em-plan-management/SKILL.md — second analog with more complex action section
    - .planning/phases/05-global-state-skill-surface/05-03-SUMMARY.md — confirm exact verb names for lock-list and claim-list
    - src/em_proj/state/__init__.py — confirm final verb names from Task 1 of Plan 05-03
    - .planning/REQUIREMENTS.md — SKILL-01, SKILL-02, SKILL-03 definitions verbatim
  </read_first>
  <action>
    Create the directory ~/.claude/skills/em-global-state/ and write SKILL.md.

    YAML frontmatter:
      name: em-global-state
      description: "Read cross-session em-proj state (KV, locks, claims) and exercise escape hatches for stuck holders. Shells out to em-proj CLI — requires em-proj installed (uv tool install)."
      allowed-tools:
        - Bash
        - AskUserQuestion

    Sections:

    <objective>
    One paragraph: the skill provides a sub-agent-parseable view over em-proj's
    cross-session state and an escape hatch for stuck lock/claim holders.
    No Redis query hand-rolling needed; all output follows the schema_version envelope.
    </objective>

    <action>
    Six verb entries, each with the exact Bash invocation and what to do with the output.
    All read verbs: emit the CLI stdout directly (it is already the correct JSON envelope).
    Write verbs (unlock, release) require confirmation logic as described above.

    Verb table (write each as a subsection "### /em-global-state <verb>"):

    1. /em-global-state list
       Run: em-proj state list --json
       Emit: stdout verbatim. Output schema: {"schema_version":"1","status":"ok","data":{"keys":[...]}}

    2. /em-global-state get <key>
       Run: em-proj state get <key> --json
       Emit: stdout verbatim. Exit 0 = found; exit 2 = not found (status "not_found").

    3. /em-global-state locks [--mine] [--stale]
       Run: em-proj state <lock-list-verb> [--mine] [--stale] --json
       (Use the confirmed verb name from 05-03-SUMMARY.md)
       Emit: stdout verbatim. Output: {"status":"ok","data":{"items":[<lock_holder>...]}}
       Each lock_holder has: pid, session_id, project_hash, acquired_at, expires_at, reason.

    4. /em-global-state claims [--mine] [--active] [--stale]
       Run: em-proj state <claim-list-verb> [--mine] [--active] [--stale] --json
       (Use the confirmed verb name from 05-03-SUMMARY.md)
       Emit: stdout verbatim. Output: {"status":"ok","data":{"items":[<claim_holder>...]}}
       Each claim_holder has: session_id, project_hash, reason, claimed_at, expires_at.

    5. /em-global-state unlock <name> [--force]
       Without --force:
         a. Run: em-proj state lock-list --json (or confirmed verb)
            Parse items for entry where name matches. If found and holder is non-stale:
            Use AskUserQuestion: "Lock '<name>' is held by session <session_id>
            (expires at <expires_at>). Force unlock? This will displace the holder."
            On yes → proceed to step b.
            On no → output "Unlock cancelled." and stop.
         b. Run: em-proj state unlock <name> --json
            Emit result verbatim (exit 0 = released, exit 3 = displaced by another).
       With --force:
         Skip the AskUserQuestion. Directly run: em-proj state unlock <name> --json.
         Document that --force bypasses confirmation; use only when the holder is
         confirmed abandoned.

    6. /em-global-state release <area> [--force]
       Without --force:
         a. Run: em-proj state check <area> --json
            If exit 0 (held): Use AskUserQuestion:
            "Area '<area>' is claimed by session <session_id> (expires at <expires_at>).
             Force release? This will displace the holder."
            On yes → proceed to step b.
            On no → output "Release cancelled." and stop.
            If exit 2 (not held): output "Area '<area>' is not claimed." and stop.
         b. Run: em-proj state release <area> --json
            Emit result verbatim.
       With --force:
         Skip the AskUserQuestion. Directly run: em-proj state release <area> --json.

    CRITICAL: The skill MUST NOT shell out to any of these write verbs:
      em-proj state set, em-proj state del, em-proj state claim, em-proj state lock
    Only unlock and release are permitted write operations from this skill.
    Reason: all other writes must flow through code (ROADMAP SC#3).
    </action>

    <when_to_invoke>
    - User types /em-global-state followed by a verb.
    - Sub-agent needs to inspect cross-session state (locks, claims, KV) without
      hand-rolling Redis queries.
    - Sub-agent or user needs to unblock a stuck session by unlocking or releasing
      a held resource.
    - A session wants to check "is anyone else working on <area>?" before taking a claim.
    </when_to_invoke>

    <scope>
    READ surface (safe to call freely):
      /em-global-state list, get, locks, claims — all read-only; no side effects.

    WRITE surface (escape hatches — use with care):
      /em-global-state unlock, release — displaces the current holder.
      Without --force: always prompts for confirmation when a live holder exists.
      With --force: bypasses confirmation — use ONLY when the holder is confirmed abandoned.

    NEVER used for: em-proj state set, del, claim, lock.
    All writes except unlock/release must flow through application code, not debug commands.
    </scope>

    <related>
    - em-proj CLI (uv tool install --editable . from em-proj repo)
    - ~/.claude/CLAUDE.md §Cross-session and cross-repo coordination — explains em-proj state primitives
    - Phase 3 lock/unlock (em-proj state lock/unlock)
    - Phase 4 claim/release/check (em-proj state claim/release/check)
    </related>
  </action>
  <acceptance_criteria>
    - File exists at ~/.claude/skills/em-global-state/SKILL.md
    - Frontmatter has name: em-global-state, allowed-tools includes Bash and AskUserQuestion
    - All 6 verb subsections present in <action>
    - The phrase "em-proj state set" does NOT appear in the file (not a permitted write verb)
    - The phrase "em-proj state del" does NOT appear in the file
    - The phrase "em-proj state claim " (with trailing space — the create verb) does NOT appear
    - The phrase "em-proj state lock " (with trailing space — the acquire verb) does NOT appear
    - "em-proj state unlock" appears (escape hatch write verb — permitted)
    - "em-proj state release" appears (escape hatch write verb — permitted)
    - AskUserQuestion appears in the unlock and release sections
    - "--force" appears in the unlock and release sections
  </acceptance_criteria>
  <verify>
    <automated>bash -c "test -f $HOME/.claude/skills/em-global-state/SKILL.md && echo SKILL_EXISTS || echo MISSING"</automated>
  </verify>
  <done>
    SKILL.md exists with all 6 verb sections; only unlock and release as write verbs;
    AskUserQuestion confirmation flow documented for both; --force bypass documented
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill → CLI | Skill shells out to em-proj; any arg passed to Bash must be validated by the agent following the skill |
| Confirmation gate | AskUserQuestion is the only write gate; --force bypasses it; the skill must make this explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-04-01 | T (Tampering) | unlock --force without confirmation | accept | --force is an explicit operator escape hatch; skill documents that it displaces the holder unconditionally; calling agents are responsible for operator intent verification |
| T-5-04-02 | E (Elevation of Privilege) | skill write surface beyond unlock/release | mitigate | Skill body explicitly excludes set/del/claim/lock verbs; SC#3 structural test (Plan 05-05) audits the SKILL.md source text for forbidden write patterns |
| T-5-04-03 | I (Information Disclosure) | skill emits holder output verbatim | accept | CLI output already applies _HOLDER_DISCLOSURE_KEYS redaction at the verb layer; skill re-emitting verbatim cannot add back excluded fields (boot_id, proc_start_epoch) |
</threat_model>

<verification>
- `test -f ~/.claude/skills/em-global-state/SKILL.md` exits 0
- Frontmatter `name: em-global-state` present
- All 6 verb sections documented under <action>
- `grep "em-proj state set" ~/.claude/skills/em-global-state/SKILL.md` returns nothing
- `grep "em-proj state del" ~/.claude/skills/em-global-state/SKILL.md` returns nothing
- `grep "AskUserQuestion" ~/.claude/skills/em-global-state/SKILL.md` returns matches in unlock+release sections
</verification>

<success_criteria>
- SKILL.md created at the correct global path
- All 6 /em-global-state verbs documented with exact shell invocations
- Confirmation flow (AskUserQuestion + --force) documented for unlock and release
- Write-verb restriction (no set/del/claim/lock) stated explicitly in <scope>
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-state-skill-surface/05-04-SUMMARY.md`
</output>
