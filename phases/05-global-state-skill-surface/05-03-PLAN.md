---
phase: 05-global-state-skill-surface
plan: "03"
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/em_proj/state/__init__.py
  - tests/multiprocess/test_lock_list_race.py
  - tests/multiprocess/test_claim_list_race.py
autonomous: true
requirements: [SKILL-01, SKILL-03]
must_haves:
  truths:
    - "em-proj state lock list --json returns a JSON array of lock holder objects"
    - "em-proj state lock list --mine --json returns only locks held by the current session"
    - "em-proj state lock list --stale --json returns only stale locks"
    - "em-proj state claim list --json returns a JSON array of claim holder objects"
    - "em-proj state claim list --mine --json returns only claims held by the current session"
    - "em-proj state claim list --active --json and --stale --json filter correctly"
    - "Two concurrent sessions can each run lock list / claim list without corrupting Redis or each other's results"
  artifacts:
    - path: "src/em_proj/state/__init__.py"
      provides: "lock_list and claim_list verb wiring"
      contains: "def lock_list"
    - path: "tests/multiprocess/test_lock_list_race.py"
      provides: "multi-process list race test"
    - path: "tests/multiprocess/test_claim_list_race.py"
      provides: "multi-process claim list race test"
  key_links:
    - from: "lock_list verb"
      to: "lock_list_by_prefix"
      via: "import from em_proj.state.lock"
      pattern: "lock_list_by_prefix"
    - from: "claim_list verb"
      to: "claim_list_by_prefix"
      via: "import from em_proj.state.claim"
      pattern: "claim_list_by_prefix"
---

<objective>
Wire `em-proj state lock list [--mine] [--stale] [--json]` and
`em-proj state claim list [--mine] [--active] [--stale] [--json]` as new verbs
in `src/em_proj/state/__init__.py`, following the D-14/D-17 thin-verb discipline.
Add multi-process race tests confirming concurrent list calls behave correctly.

Purpose: Complete the CLI surface required by SKILL-01 and SKILL-03, making the
pure ops from Plans 05-01 and 05-02 accessible as stable, parseable CLI output.

Output: Two new verbs in `state/__init__.py` + multi-process tests.
</objective>

<execution_context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
</context>

<interfaces>
From src/em_proj/state/__init__.py (existing verb structure to follow):

```python
# Verb pattern (D-14 three-step template):
@state_app.command("verbname")
def verbname(
    arg: Annotated[str, typer.Argument(help="...")],
    flag: Annotated[bool, typer.Option("--flag/--no-flag", help="...")] = False,
    json_flag: Annotated[bool | None, typer.Option("--json/--no-json", help=_JSON_HELP)] = None,
) -> None:
    json_mode = resolve_json_mode(json_flag)
    client = get_client()
    die_if_redis_unreachable(client)
    result = pure_op(...)   # one call into state/lock.py or state/claim.py
    emit_ok({"items": result}, json_mode=json_mode)
```

New imports to add to state/__init__.py:
```python
from em_proj.state.lock import (
    ...existing...,
    lock_list_by_prefix,  # new — Plan 05-01
)
from em_proj.state.claim import (
    ...existing...,
    claim_list_by_prefix,  # new — Plan 05-02
)
```

Output schema for lock list (SKILL-01 / SKILL-03 stable schema):
  emit_ok({"items": [<holder_dict>, ...]}, json_mode=json_mode)
  Each holder dict has: pid, session_id, project_hash, acquired_at, expires_at, reason
  (subset filtered through _HOLDER_DISCLOSURE_KEYS from output.py — boot_id excluded)

Output schema for claim list (SKILL-01 / SKILL-03 stable schema):
  emit_ok({"items": [<holder_dict>, ...]}, json_mode=json_mode)
  Each holder dict has: session_id, project_hash, reason, claimed_at, expires_at
  (all 5 claim fields — claims have no boot_id or pid to redact)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire lock list and claim list verbs in state/__init__.py</name>
  <files>src/em_proj/state/__init__.py</files>
  <read_first>
    - src/em_proj/state/__init__.py — full file; understand existing verb pattern, imports section,
      _JSON_HELP constant, module docstring (update the D-14 import list in docstring)
    - src/em_proj/state/lock.py — lock_list_by_prefix signature (Plan 05-01)
    - src/em_proj/state/claim.py — claim_list_by_prefix signature (Plan 05-02)
    - src/em_proj/output.py — _HOLDER_DISCLOSURE_KEYS tuple; emit_ok; emit_error
    - .planning/phases/05-global-state-skill-surface/05-01-SUMMARY.md — confirm lock_list_by_prefix shape
    - .planning/phases/05-global-state-skill-surface/05-02-SUMMARY.md — confirm claim_list_by_prefix shape
  </read_first>
  <action>
    Add two new verbs to `src/em_proj/state/__init__.py`:

    1. Update the import from em_proj.state.lock to add lock_list_by_prefix.
    2. Update the import from em_proj.state.claim to add claim_list_by_prefix.
    3. Also import _HOLDER_DISCLOSURE_KEYS from em_proj.output (needed for lock list disclosure filter).

    Verb 1 — `lock list`:
    - Register as @state_app.command("lock-list") — note: typer uses hyphens in the command
      string, making it `em-proj state lock-list` on the CLI. However, to keep the UX
      consistent with Phase 5 plan (em-proj state lock list), register as a sub-group if
      feasible, OR register as `@state_app.command("lock-list")` which renders as
      `em-proj state lock-list`. Check how typer handles this before deciding — if typer
      natively supports `em-proj state lock list` as two-word subcommands via a nested Typer
      app, use that; otherwise use the hyphenated form and document it. Either form is
      acceptable as long as it matches what the skill will shell out to.
    - Parameters: mine (--mine/--no-mine, default False), stale (--stale/--no-stale, default False),
      json_flag (--json/--no-json, default None)
    - Body (D-14 three steps):
      a. json_mode = resolve_json_mode(json_flag)
      b. client = get_client(); die_if_redis_unreachable(client)
      c. holders = lock_list_by_prefix(mine=mine, stale=stale)
      d. Redact: apply _HOLDER_DISCLOSURE_KEYS filter to each holder before emitting
         (same as emit_held_by_another does — lock holders have boot_id and proc_start_epoch
         that must be excluded from output per _HOLDER_DISCLOSURE_KEYS)
      e. emit_ok({"items": redacted_holders}, json_mode=json_mode)

    Verb 2 — `claim list`:
    - Register as @state_app.command("claim-list") (or nested per typer capability check above)
    - Parameters: mine, active, stale (all --flag/--no-flag, default False), json_flag
    - Body (D-14 three steps):
      a. json_mode = resolve_json_mode(json_flag)
      b. client = get_client(); die_if_redis_unreachable(client)
      c. holders = claim_list_by_prefix(mine=mine, active=active, stale=stale)
      d. emit_ok({"items": holders}, json_mode=json_mode)
      (Claims have no boot_id/proc_start_epoch to redact — all 5 fields are safe to emit)

    Update the module docstring's D-14/D-17 import list to include lock_list_by_prefix
    and claim_list_by_prefix.
  </action>
  <verify>
    <automated>bash scripts/test.sh unit</automated>
  </verify>
  <done>
    Both new verbs appear in `em-proj state --help`; em-proj state lock-list --json (or
    em-proj state lock list --json) exits 0 when no locks held; em-proj state claim-list
    --json (or equivalent) exits 0 when no claims held; existing unit tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Multi-process race tests for lock list and claim list</name>
  <files>tests/multiprocess/test_lock_list_race.py, tests/multiprocess/test_claim_list_race.py</files>
  <read_first>
    - tests/multiprocess/test_claim_race.py — full file; observe harness fixture usage, RaceResult, assertion patterns
    - tests/multiprocess/test_lock_hold.py — full file; observe multiproc_race fixture and how EM_PROJ_BIN is invoked
    - tests/conftest.py — multiproc_race fixture, RaceResult, clean_db, redis_precheck
    - src/em_proj/state/__init__.py — confirmed verb name (lock-list or lock list) just wired in Task 1
  </read_first>
  <behavior>
    test_lock_list_race.py:
    - test_lock_list_concurrent: two children, one acquires a lock then calls lock-list --json,
      the other just calls lock-list --json; both should exit 0 and return valid JSON;
      the first child's output should contain the lock it holds (session_id present in items)
    - test_lock_list_empty_concurrent: two children both call lock-list --json with no locks held;
      both exit 0; both return {"status":"ok","data":{"items":[]}}

    test_claim_list_race.py:
    - test_claim_list_concurrent: two children, one claims an area then calls claim-list --json,
      the other just calls claim-list --json; both should exit 0 and return valid JSON;
      the first child's output should include the claimed area in items
    - test_claim_list_empty_concurrent: two children both call claim-list --json with no claims;
      both exit 0; output parses as {"status":"ok","data":{"items":[]}}
  </behavior>
  <action>
    Write tests/multiprocess/test_lock_list_race.py and tests/multiprocess/test_claim_list_race.py.
    Use the multiproc_race fixture and RaceResult from conftest.py.
    Use EM_PROJ_BIN for the binary name.

    For lock-list tests, set CLAUDE_CODE_SESSION_ID env via the multiproc_race fixture's
    env parameter if supported, or write a short child script that exports it before calling
    em-proj — check the conftest.py harness for how env variables are passed to children.

    The verb names (lock-list vs "lock list") should match exactly what was registered in Task 1
    (determined after reading state/__init__.py in Task 1).

    Keep tests < 10 seconds wall-time each; no sleep loops.
  </action>
  <verify>
    <automated>bash scripts/test.sh multiprocess tests/multiprocess/test_lock_list_race.py tests/multiprocess/test_claim_list_race.py -v</automated>
  </verify>
  <done>
    All 4 multi-process tests pass; both test files parse stdout as valid JSON; no test
    requires sleeping more than the lock acquisition 1s timeout
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → verb body | --mine, --stale, --active are boolean flags; no user-controlled string passed to Redis scan |
| verb → emit | lock list applies _HOLDER_DISCLOSURE_KEYS redaction before emit; claim list emits all 5 safe fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-03-01 | I (Information Disclosure) | lock-list output | mitigate | Apply _HOLDER_DISCLOSURE_KEYS dict comprehension to each lock holder before emit_ok — excludes boot_id and proc_start_epoch per output.py security rationale |
| T-5-03-02 | I (Information Disclosure) | claim-list output | accept | Claim holders have no boot_id/proc_start_epoch; all 5 fields (session_id, project_hash, reason, claimed_at, expires_at) are safe to emit per Phase 4 design |
| T-5-03-03 | D (Denial of Service) | concurrent list calls | accept | list operations are read-only SCAN + HGETALL; concurrent calls cannot corrupt state; Redis is single-threaded at command level |
</threat_model>

<verification>
- `em-proj state --help` shows new lock-list and claim-list subcommands
- `em-proj state lock-list --json` (no locks held) exits 0, outputs `{"schema_version":"1","status":"ok","data":{"items":[]}}`
- `em-proj state claim-list --json` (no claims held) exits 0, outputs same shape with empty items
- `bash scripts/test.sh multiprocess tests/multiprocess/test_lock_list_race.py -v` → all pass
- `bash scripts/test.sh multiprocess tests/multiprocess/test_claim_list_race.py -v` → all pass
- `bash scripts/test.sh all` → still passes (no regressions in existing suite)
</verification>

<success_criteria>
- Two new verbs wired; both appear in `em-proj state --help`
- lock-list verb redacts boot_id and proc_start_epoch via _HOLDER_DISCLOSURE_KEYS
- claim-list verb emits all 5 claim holder fields
- 4 multi-process tests pass (2 per verb)
- Full test suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-state-skill-surface/05-03-SUMMARY.md`
</output>
