---
phase: 05-global-state-skill-surface
plan: "05"
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - tests/structural/test_phase_05_shape.py
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03]
must_haves:
  truths:
    - "All Phase 5 structural invariants are encoded as pytest assertions and pass"
    - "The skill write-boundary audit (SC#3) is a machine-checkable test, not a manual review"
    - "bash scripts/verify-phase.sh 05 exits 0 — all deterministic phase checks pass"
  artifacts:
    - path: "tests/structural/test_phase_05_shape.py"
      provides: "AST-based structural invariants for Phase 5"
      contains: "def test_lock_list_by_prefix_defined"
    - path: "~/.claude/skills/em-global-state/SKILL.md"
      provides: "skill artifact (created in Plan 05-04)"
  key_links:
    - from: "test_skill_write_boundary"
      to: "~/.claude/skills/em-global-state/SKILL.md"
      via: "Path.read_text() source grep for forbidden write verbs"
      pattern: "em-proj state (set|del|claim |lock )"
    - from: "test_phase_05_summaries_present"
      to: ".planning/phases/05-global-state-skill-surface/"
      via: "glob 05-*-PLAN.md → assert 05-*-SUMMARY.md exists"
---

<objective>
Encode all Phase 5 acceptance criteria as pytest assertions in
`tests/structural/test_phase_05_shape.py` and run the final
`bash scripts/verify-phase.sh 05` phase gate.

Purpose: Make the structural invariants — including the SC#3 write-boundary audit —
machine-checkable so future phases cannot accidentally regress the skill surface.

Output: `tests/structural/test_phase_05_shape.py` + clean verify-phase.sh 05 run.
</objective>

<execution_context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
</context>

<interfaces>
From tests/structural/test_phase_04_shape.py (pattern to follow exactly):

```python
REPO_ROOT = Path(__file__).resolve().parent.parent.parent
SRC_EM_PROJ = REPO_ROOT / "src" / "em_proj"
STATE_PKG = SRC_EM_PROJ / "state"
PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "05-global-state-skill-surface"

def _parse_or_skip(path: Path, label: str) -> ast.Module: ...   # skip if absent
def _find_assign(tree, name): ...
def _find_funcdef(tree, name): ...
def _find_imports(tree): ...
```

SKILL.md path resolution (CI portability requirement):
  Primary:   Path.home() / ".claude" / "skills" / "em-global-state" / "SKILL.md"
  Fallback:  REPO_ROOT / ".claude" / "skills" / "em-global-state" / "SKILL.md"
  If neither exists: pytest.xfail("em-global-state SKILL.md not found at
    ~/.claude/skills/ or .claude/skills/ — skill may not be installed on this machine")
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/structural/test_phase_05_shape.py</name>
  <files>tests/structural/test_phase_05_shape.py</files>
  <read_first>
    - tests/structural/test_phase_04_shape.py — full file; copy helpers (_parse_or_skip, _find_assign, _find_funcdef, _find_imports, _find_all_names); do NOT import from it
    - src/em_proj/state/lock.py — confirm lock_list_by_prefix function exists
    - src/em_proj/state/claim.py — confirm claim_list_by_prefix function exists
    - src/em_proj/state/__init__.py — confirm lock-list and claim-list verbs are registered; count total @state_app.command() decorators (must be >= 11 after Phase 5)
    - ~/.claude/skills/em-global-state/SKILL.md — confirm write verbs present; confirm forbidden verbs absent
    - .planning/phases/05-global-state-skill-surface/ — list PLAN.md files to know how many SUMMARYs to expect
  </read_first>
  <action>
    Write tests/structural/test_phase_05_shape.py. Self-contained (no imports from other
    structural test files — copy helpers verbatim per Phase 1+2+3+4 precedent).

    Module-level path constants:
      REPO_ROOT, SRC_EM_PROJ, STATE_PKG, LOCK_PY, CLAIM_PY, STATE_INIT, PHASE_DIR
      SKILL_PATH = (resolve as: try ~/.claude/skills path first, fallback .claude/skills)

    Test functions (write all of these — aim for ~12 total):

    Group A — lock.py invariants:
    1. test_lock_list_by_prefix_defined
       AST: lock_list_by_prefix is a module-level function in lock.py
    2. test_lock_list_by_prefix_no_typer
       AST: lock.py still has no "typer" in imports (D-17 carry — no new typer after Phase 5 additions)
    3. test_lock_list_by_prefix_params
       AST: lock_list_by_prefix's arguments include "mine" and "stale"
       (Use node.args.args to get arg names from the FunctionDef)

    Group B — claim.py invariants:
    4. test_claim_list_by_prefix_defined
       AST: claim_list_by_prefix is a module-level function in claim.py
    5. test_claim_list_by_prefix_no_typer
       AST: claim.py still has no "typer" in imports (D-17 carry)
    6. test_claim_list_by_prefix_params
       AST: claim_list_by_prefix's arguments include "mine", "active", "stale"

    Group C — state/__init__.py verb count:
    7. test_state_init_registers_list_verbs
       AST: state/__init__.py has >= 11 @state_app.command() verbs
       (Phase 4 had 9; Phase 5 adds lock-list and claim-list = 11)

    Group D — SKILL.md write-boundary audit (SC#3):
    8. test_skill_file_exists
       Path check: SKILL_PATH exists; xfail with reason if neither path exists
    9. test_skill_write_boundary_no_forbidden_verbs
       Source grep: skill body does NOT contain any of:
         "em-proj state set"
         "em-proj state del"
         "em-proj state claim " (trailing space to distinguish from "em-proj state claim-list")
         "em-proj state lock "  (trailing space to distinguish from "em-proj state lock-list")
       Assert each is absent; error message if found must name the forbidden pattern.
    10. test_skill_write_boundary_has_permitted_verbs
        Source grep: skill body DOES contain "em-proj state unlock" and "em-proj state release"
        (SC#3: escape hatches must be present)
    11. test_skill_confirmation_mechanism
        Source grep: skill body contains "AskUserQuestion" (the confirmation mechanism per D-04)

    Group E — Phase 5 SUMMARY coverage:
    12. test_phase_05_summaries_present
        Same pattern as test_phase_04_summaries_present in test_phase_04_shape.py:
        glob 05-*-PLAN.md in PHASE_DIR, assert 05-*-SUMMARY.md exists for each.
        Skip if PHASE_DIR doesn't exist (planning worktree not attached).

    CRITICAL: For test 9 (write boundary audit), the grep patterns are:
      "em-proj state set"    — no trailing space needed (always followed by key arg)
      "em-proj state del"    — no trailing space needed
      "em-proj state claim " — trailing space required to avoid matching "em-proj state claim-list"
      "em-proj state lock "  — trailing space required to avoid matching "em-proj state lock-list"
    Use plain `in` string membership checks, not regex, for clarity.
  </action>
  <acceptance_criteria>
    - File tests/structural/test_phase_05_shape.py exists with >= 12 test functions
    - `bash scripts/test.sh structural tests/structural/test_phase_05_shape.py -v` exits 0
    - test_skill_write_boundary_no_forbidden_verbs passes (skill does not contain forbidden patterns)
    - test_skill_write_boundary_has_permitted_verbs passes (skill has unlock + release)
    - test_skill_confirmation_mechanism passes (skill has AskUserQuestion)
    - test_phase_05_summaries_present skips cleanly if PHASE_DIR absent (not a failure)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh structural tests/structural/test_phase_05_shape.py -v</automated>
  </verify>
  <done>
    All 12 tests pass (or xfail with reason for skill-absent case); no test imports from
    other structural files; helpers copied verbatim per precedent
  </done>
</task>

<task type="auto">
  <name>Task 2: Final phase gate — bash scripts/verify-phase.sh 05</name>
  <files></files>
  <read_first>
    - scripts/verify-phase.sh — understand what checks it runs (test suite, structural, redis, em-proj PATH, anti-pattern grep, SUMMARY coverage, commit traceability)
  </read_first>
  <action>
    Run `bash scripts/verify-phase.sh 05` and evaluate the result.

    The verifier checks:
    1. bash scripts/test.sh all → must be green
    2. bash scripts/test.sh structural → must be green (includes test_phase_05_shape.py)
    3. verify-redis-config.sh → PASS or SKIP (redis unreachable is not a Phase 5 failure)
    4. em-proj on PATH → must be found
    5. em-proj --version → must return X.Y.Z
    6. TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER grep → must be clean
    7. 05-01-PLAN.md through 05-05-PLAN.md each have a corresponding SUMMARY.md

    If any check fails: diagnose and fix the root cause before writing the SUMMARY.
    Common failure modes to watch for:
    - New TBD/TODO left in lock.py, claim.py, or __init__.py → remove them
    - Missing SUMMARY for one of the 5 plans → create the SUMMARY before running again
    - structural test failure → fix the test or the source

    After verify-phase.sh 05 exits 0: write the SUMMARY for this plan.
  </action>
  <acceptance_criteria>
    - `bash scripts/verify-phase.sh 05` exits 0
    - Report shows all checks PASS (or SKIP for redis-unreachable)
    - Phase 5 SUMMARY files: 05-01 through 05-05 all exist
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-phase.sh 05</automated>
  </verify>
  <done>
    verify-phase.sh 05 exits 0; all deterministic checks pass; 05-05-SUMMARY.md created
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structural test → SKILL.md source | AST-less source grep on markdown; cannot be fooled by comments since grep targets the shell invocations themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-05-01 | T (Tampering) | structural test bypassed by comment | mitigate | Write-boundary test greps the skill body for "em-proj state set", etc. as plain strings — they cannot appear even in comments without triggering the check; this is intentional (comments ARE the skill body for a markdown skill) |
| T-5-05-02 | T (Tampering) | SKILL.md path mismatch (not at expected location) | mitigate | test_skill_file_exists uses xfail (not skip) so the absence is VISIBLE in CI output; the test does not silently pass when the file is missing |
</threat_model>

<verification>
- `bash scripts/test.sh structural tests/structural/test_phase_05_shape.py -v` → all tests pass
- `bash scripts/test.sh all` → full suite green
- `bash scripts/verify-phase.sh 05` → exits 0
</verification>

<success_criteria>
- 12 structural tests pass encoding all Phase 5 acceptance criteria
- SC#3 write-boundary audit is machine-checkable (not manual)
- verify-phase.sh 05 exits 0 with all checks passing
- Phase 5 is complete: SKILL-01, SKILL-02, SKILL-03 fully delivered
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-state-skill-surface/05-05-SUMMARY.md`
</output>
