---
phase: 08-session-registry-hybrid
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/em_proj/session/__init__.py
  - src/em_proj/cli.py
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04

must_haves:
  truths:
    - "em-proj session register exits 0 and records the session in Redis"
    - "em-proj session heartbeat exits 0 on a registered session; exits 1 with not_found on an unregistered session"
    - "em-proj session list exits 0 and emits JSON with schema_version=1, status=ok, data containing a list of session records with held counts"
    - "em-proj session show <session_id> exits 0 and emits JSON with the full session record + full held-resource dicts"
    - "em-proj session show <nonexistent_id> exits 2 (not_found exit code)"
    - "em-proj session (no subcommand) prints help and exits 0"
  artifacts:
    - path: "src/em_proj/session/__init__.py"
      provides: "Typer session_app with register/heartbeat/list/show verb commands"
      contains: "session_app = typer.Typer"
    - path: "src/em_proj/cli.py"
      provides: "session subcommand mounted on the top-level em-proj app"
      contains: "session_app"
  key_links:
    - from: "src/em_proj/cli.py"
      to: "src/em_proj/session/__init__.py"
      via: "app.add_typer(session_app, name='session')"
      pattern: "session_app"
    - from: "session register verb"
      to: "session.session_register()"
      via: "thin verb shell: resolve_json_mode → die_if_redis_unreachable → call → emit_ok"
      pattern: "session_register"
---

<objective>
Wire the session subcommand into the em-proj CLI. Create src/em_proj/session/__init__.py
with the typer session_app and four verb commands (register, heartbeat, list, show).
Mount session_app on cli.py. Follow the thin-verb-shell pattern from state/__init__.py:
resolve_json_mode → die_if_redis_unreachable → call one session.py function → emit_*.

Purpose: After this plan, `em-proj session <verb>` is runnable at the CLI. Plan 08-03's
multi-process harness invokes these verbs fork+exec style to prove TEST-03.

Output: src/em_proj/session/__init__.py (session_app + 4 verb wrappers), updated cli.py.
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/08-session-registry-hybrid/08-CONTEXT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/08-session-registry-hybrid/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor wires against. Extracted from v1.0 sources. -->

From src/em_proj/session.py (Plan 08-01 output):
```python
KEY_PREFIX: str = "state:session:"
TTL_DEFAULT: int = 300

class SessionNotFound(Exception):
    code: str = "not_found"

def session_register() -> dict: ...          # returns 9-field session dict
def session_heartbeat() -> dict: ...         # returns refreshed session dict; raises SessionNotFound
def session_list() -> list[dict]: ...        # returns [{session: {...}, held: {claims:N, locks:N, reserves:N}}, ...]
def session_show(session_id: str) -> dict: ...  # returns {session:{...9 fields}, held:{claims:[...], locks:[...], reserves:[...]}}; raises SessionNotFound
```

From src/em_proj/output.py:
```python
SCHEMA_VERSION = "1"
def resolve_json_mode(json_flag: bool | None) -> bool: ...
def emit_ok(data=None, *, json_mode=None) -> NoReturn: ...  # exit 0
def emit_error(code, message, *, json_mode=None) -> NoReturn: ...  # exit 1
def emit_not_found(message, *, json_mode=None) -> NoReturn: ...  # exit 2
```

From src/em_proj/redis_client.py:
```python
def get_client(db=None): ...
def die_if_redis_unreachable(client): ...
```

From src/em_proj/cli.py (current shape — to be updated):
```python
from em_proj.state import state_app
app = typer.Typer(name="em-proj", ...)
app.add_typer(state_app, name="state", help="KV / lock / claim primitives")
```

From src/em_proj/state/__init__.py (verb pattern to replicate):
- Typer @app.command() → resolve_json_mode → get_client → die_if_redis_unreachable → call one op → emit_*
- --json/--no-json flag as Optional[bool] = typer.Option(None, "--json/--no-json")
- Each verb: 3 lines of body maximum (D-14 thin-verb-shell discipline)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/em_proj/session/__init__.py with session_app and four verb commands</name>
  <files>src/em_proj/session/__init__.py</files>

  <read_first>
    - src/em_proj/state/__init__.py lines 1-100 — module docstring, import pattern, state_app Typer construction, verb decoration pattern, --json option shape
    - src/em_proj/state/__init__.py (scan for a list verb) — list verb emit_ok(data=results) pattern
    - src/em_proj/session.py — public API (session_register, session_heartbeat, session_list, session_show, SessionNotFound)
    - src/em_proj/output.py lines 94-170 — emit_ok, emit_not_found, emit_error signatures
    - src/em_proj/redis_client.py — die_if_redis_unreachable usage
    - .planning/phases/08-session-registry-hybrid/08-CONTEXT.md — D1 output shape (list→counts, show→full dicts)
  </read_first>

  <action>
    Create src/em_proj/session/__init__.py. This is a PACKAGE __init__.py (session is a
    package, not a flat module) because cli.py mounts it as a subcommand family and a
    package allows future expansion (session listen in P11 adds more verbs without moving
    files).

    Module docstring: covers D-14 thin-verb-shell discipline, no business logic here,
    session.py owns all ops.

    session_app = typer.Typer(
        name="session",
        help="Session registry — register, heartbeat, list, show.",
        no_args_is_help=True,
        add_completion=False,
    )

    Four verb commands (mirror state/__init__.py discipline — thin shells only):

    @session_app.command("register")
    def session_register_cmd(json_flag: Optional[bool] = typer.Option(None, "--json/--no-json")):
        json_mode = resolve_json_mode(json_flag)
        client = get_client()
        die_if_redis_unreachable(client)
        record = session_register()
        emit_ok(data=record, json_mode=json_mode)

    @session_app.command("heartbeat")
    def session_heartbeat_cmd(json_flag: Optional[bool] = typer.Option(None, "--json/--no-json")):
        json_mode = resolve_json_mode(json_flag)
        client = get_client()
        die_if_redis_unreachable(client)
        try:
            record = session_heartbeat()
            emit_ok(data=record, json_mode=json_mode)
        except SessionNotFound as exc:
            emit_not_found(str(exc), json_mode=json_mode)

    @session_app.command("list")
    def session_list_cmd(json_flag: Optional[bool] = typer.Option(None, "--json/--no-json")):
        json_mode = resolve_json_mode(json_flag)
        client = get_client()
        die_if_redis_unreachable(client)
        results = session_list()
        emit_ok(data=results, json_mode=json_mode)

    @session_app.command("show")
    def session_show_cmd(
        session_id: str = typer.Argument(..., help="Session ID to inspect"),
        json_flag: Optional[bool] = typer.Option(None, "--json/--no-json"),
    ):
        json_mode = resolve_json_mode(json_flag)
        client = get_client()
        die_if_redis_unreachable(client)
        try:
            record = session_show(session_id)
            emit_ok(data=record, json_mode=json_mode)
        except SessionNotFound as exc:
            emit_not_found(str(exc), json_mode=json_mode)

    Imports needed:
    - from typing import Optional (or Annotated pattern from existing verbs)
    - import typer
    - from em_proj.session import session_register, session_heartbeat, session_list, session_show, SessionNotFound
      NOTE: self-reference risk — this IS em_proj.session. Import the ops from the parent
      module level: since session/__init__.py IS the session package, it should import from
      the session.py that was renamed. Wait — Plan 08-01 created session.py as a flat file;
      Plan 08-02 converts session to a package by creating session/__init__.py.

    CRITICAL MODULE RESTRUCTURING:
    Plan 08-01 created src/em_proj/session.py (flat module).
    Plan 08-02 must convert it to a package: src/em_proj/session/__init__.py.

    Step 1: Rename src/em_proj/session.py → src/em_proj/session/_ops.py (move the ops module)
    Step 2: Create src/em_proj/session/__init__.py (this file — typer verbs + re-export ops)

    In session/__init__.py, import ops from session._ops:
      from em_proj.session._ops import (
          session_register, session_heartbeat, session_list, session_show,
          SessionNotFound, KEY_PREFIX, TTL_DEFAULT,
      )

    Then define session_app and the verb commands using those imports.

    Also re-export the public API so external callers (tests) can do:
      from em_proj.session import session_register
    via __all__ = [...] or direct import re-export.

    If rename approach creates git friction, alternatively keep session.py as-is and import
    from it in session/__init__.py — but this creates an em_proj/session.py file AND an
    em_proj/session/ directory, which Python does NOT allow (one wins). Therefore the rename
    to _ops.py is required.

    Verify: after this task, `python -c "from em_proj.session import session_register"` exits 0.
  </action>

  <verify>
    <automated>bash /Users/emonical/projects/personal/ai-tools/em-proj/scripts/test.sh unit --tail 40</automated>
  </verify>

  <done>
    src/em_proj/session/__init__.py exists with session_app Typer; session/_ops.py contains
    the business logic (or equivalent restructuring); four @session_app.command decorators
    present; from em_proj.session import session_register succeeds; existing unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount session_app on cli.py and smoke-test all four verbs</name>
  <files>src/em_proj/cli.py</files>

  <read_first>
    - src/em_proj/cli.py — current contents (the app.add_typer line for state_app is the pattern to replicate)
    - src/em_proj/session/__init__.py (Task 1 output — confirm session_app is exported)
  </read_first>

  <action>
    Edit src/em_proj/cli.py to add:

    1. Import: from em_proj.session import session_app
    2. Mount: app.add_typer(session_app, name="session", help="Session registry — register, heartbeat, list, show.")

    Place the import after the existing state_app import. Place the add_typer call after the
    existing state add_typer call. Follow the exact same pattern — no other changes to cli.py.

    After editing cli.py, run smoke tests to confirm the verb routes work end-to-end:
    - em-proj session --help exits 0 (verifies mount + typer routing)
    - em-proj session register --json exits 0 (verifies register reaches Redis)
    - em-proj session list --json exits 0 (verifies list works; may return empty list)
    - em-proj session heartbeat --json exits with code 0 or 2 (0 if registered above, 2 if TTL expired)

    The executor runs these smoke checks using the binary at the path from conftest.EM_PROJ_BIN
    (typically the uv-managed entry point). Use: python -m em_proj for smoke checks if the
    installed binary path is unclear, or check pyproject.toml for the script entry point.
  </action>

  <verify>
    <automated>bash /Users/emonical/projects/personal/ai-tools/em-proj/scripts/test.sh unit --tail 30</automated>
  </verify>

  <done>
    cli.py imports session_app and mounts it as "session"; `em-proj session --help` shows
    register/heartbeat/list/show subcommands; `em-proj session register --json` exits 0 and
    emits {"schema_version":"1","status":"ok","data":{...9 fields...}}.
    All prior unit tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → session_show(session_id) | session_id is an untrusted string from argv; passed to Redis as HASH key suffix |
| verb layer → output.py emit_* | data dict from session ops; no secrets or paths that should be redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | session_id argv in session show | mitigate | session_id is used only as a Redis key suffix (KEY_PREFIX + session_id); no shell interpolation; Redis key lookup is read-only; malformed IDs return not_found, not errors |
| T-08-02-02 | Information Disclosure | emit_ok(data=record) for session list/show | accept | session records contain pid, cwd, project_hash — all locally-owned metadata; no secrets; same disclosure level as existing lock-list/claim-list verbs |
| T-08-02-03 | Denial of Service | session list scans all state:session:* | accept | bounded by active session count; stale reaping keeps keyspace compact; same risk profile as lock-list |
</threat_model>

<verification>
After both tasks:
- grep -c "session_app" /Users/emonical/projects/personal/ai-tools/em-proj/src/em_proj/cli.py outputs 2 (import + add_typer)
- bash scripts/test.sh unit passes (no regressions)
- em-proj session --help exits 0
- em-proj session register --json exits 0 and stdout contains "schema_version"
- em-proj session list --json exits 0 and stdout contains "schema_version"
- em-proj session show nonexistent-xyz --json exits 2
</verification>

<success_criteria>
- src/em_proj/session/__init__.py exists with session_app Typer and four @session_app.command verbs
- cli.py mounts session_app as "session" subcommand
- em-proj session register / heartbeat / list / show all route correctly
- session show for a nonexistent session_id exits 2 (emit_not_found)
- All existing unit tests continue to pass (no import breakage from module restructuring)
</success_criteria>

<output>
After completion, create .planning/phases/08-session-registry-hybrid/08-02-SUMMARY.md
</output>
