---
phase: 10-messaging-send-patterns
plan: "02"
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - src/em_proj/message/_ops.py
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04, MSG-05]

must_haves:
  truths:
    - "send_directed writes to the recipient's mbox: stream and returns a flat delivery metadata dict"
    - "send_broadcast fans out to all live sessions in the selected scope, excluding the sender"
    - "send_topic fans out to subscribers in the selected scope, intersecting with live sessions and excluding the sender"
    - "subscribe_topic and unsubscribe_topic manage Redis SET membership for topic:<scope_key>:<topic>"
    - "_validate_topic rejects topic names outside [a-zA-Z0-9_.-]{1,128}"
    - "enumerate_scope_recipients returns live session_ids filtered by scope, excluding the given session_id"
    - "exit code 4 = partial fan-out failure (some recipients written, some failed)"
    - "pub/sub PUBLISH is fire-and-forget to msg:<session_id> per recipient after mbox_write"
  artifacts:
    - path: src/em_proj/message/_ops.py
      provides: "Extended ops module with all Phase 10 send/subscribe primitives"
      contains: "def send_directed"
      exports:
        - send_directed
        - send_broadcast
        - send_topic
        - subscribe_topic
        - unsubscribe_topic
        - enumerate_scope_recipients
        - TOPIC_KEY_PREFIX
  key_links:
    - from: send_directed
      to: session._ops.session_show
      via: "validates recipient exists before mbox_write; re-raises SessionNotFound"
      pattern: "session_show\\(to_session_id\\)"
    - from: send_broadcast / send_topic
      to: session._ops.session_list
      via: "enumerate_scope_recipients calls session_list() then filters by scope field"
      pattern: "session_list\\(\\)"
    - from: send_directed / send_broadcast / send_topic
      to: mbox_write
      via: "one mbox_write call per recipient inside fan-out loop"
      pattern: "mbox_write\\("
    - from: send_directed / send_broadcast / send_topic
      to: redis PUBLISH
      via: "fire-and-forget client.publish(f'msg:{session_id}', ...) after each mbox_write"
      pattern: "client\\.publish\\(f.msg:"
---

<objective>
Wave 1: Implement all Phase 10 backend ops in src/em_proj/message/_ops.py — turning the RED Wave 0 unit tests GREEN.

Purpose: This is the core business logic layer. All send patterns, topic management, scope enumeration, validation, and delivery metadata live here. No CLI code is written in this wave.
Output: Extended _ops.py with 7 new public functions, 3 new private helpers, 1 new constant, and new cross-module imports.

Locked design decisions (NON-NEGOTIABLE — implement exactly as specified):
- D-PubSub: pub/sub channel = msg:<session_id> (per-recipient, fire-and-forget)
- D-ExitCode4: exit code 4 = partial delivery; ops functions return dict with recipients_failed > 0 to signal this to verb layer
- D-SelfExclude: sender's session_id excluded from broadcast and topic recipients (not from directed)
- D-TopicKey: Redis SET key = topic:<scope_key>:<topic>; TOPIC_KEY_PREFIX = "topic:"
- D-ValidateTopic: _validate_topic enforces [a-zA-Z0-9_.-]{1,128} via _TOPIC_RE; mirrors _validate_body pattern
</objective>

<execution_context>
@/Users/emonical/projects/personal/ai-tools/em-proj/CLAUDE.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/10-messaging-send-patterns/10-RESEARCH.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/10-messaging-send-patterns/10-PATTERNS.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/10-messaging-send-patterns/10-01-SUMMARY.md

<interfaces>
<!-- Key interfaces from existing codebase the executor extends in this wave. -->

From src/em_proj/message/_ops.py (current — extend in place):
  Existing constants: MBOX_KEY_PREFIX="mbox:", MBOX_MAXLEN=500, MBOX_TTL_SECONDS=3600, MAX_BODY_CHARS=4096
  Existing private: _STREAM_ID_RE, _build_mbox_key(session_id), _decode_entry(entry_id, fields), _validate_body(body), _validate_since(since)
  Existing re import on line 40 — re is already present; reuse for _TOPIC_RE
  Existing imports: json (39), re (40), time (41), get_client (43), ValidationError (44)
  Existing public: mbox_write(session_id, msg) -> str, mailbox_inbox(...), mbox_blocking_read(...)

From src/em_proj/session/_ops.py (import in _ops.py — DO NOT re-export from __init__.py yet):
  session_list() -> list[dict]   # [{"session": {...}, "held": {...}}]
  session_show(session_id) -> dict   # raises SessionNotFound
  class SessionNotFound
  KEY_PREFIX = "state:session:"

From src/em_proj/identity.py (new cross-module import):
  resolve_session_id() -> str   # current caller's session ID
  resolve_project_hash() -> str # project scope key
  resolve_upstream_identity() -> str # upstream scope key

New constant to add (after MBOX_TTL_SECONDS, line ~65):
  TOPIC_KEY_PREFIX: str = "topic:"

New regex to add (after _STREAM_ID_RE):
  _TOPIC_RE = re.compile(r"^[a-zA-Z0-9_.\-]{1,128}$")

New private helpers to add:
  _validate_topic(topic: str) -> None — mirrors _validate_body exactly; uses _TOPIC_RE.fullmatch
  _build_topic_key(scope_key: str, topic: str) -> str — f"{TOPIC_KEY_PREFIX}{scope_key}:{topic}"
  _resolve_scope_key(scope: str) -> str — maps scope string to scope_key:
    "machine" -> "machine"
    "project" -> resolve_project_hash()
    "upstream" -> resolve_upstream_identity()
    anything else -> raise ValidationError(code="validation_error", message=f"unknown scope: {scope!r}; must be 'project', 'upstream', or 'machine'")

New public functions to add (in this order, all after existing public functions):
  enumerate_scope_recipients(scope: str, exclude_session_id: str | None = None) -> list[str]
  subscribe_topic(session_id: str, topic: str, scope: str) -> None
  unsubscribe_topic(session_id: str, topic: str, scope: str) -> None
  get_topic_subscribers(scope: str, topic: str) -> set[str]
  send_directed(to_session_id: str, body: str, scope: str) -> dict
  send_broadcast(body: str, scope: str) -> dict
  send_topic(topic: str, scope: str, body: str) -> dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add constants, regex, private helpers to _ops.py</name>
  <files>src/em_proj/message/_ops.py</files>

  <read_first>
    - src/em_proj/message/_ops.py lines 1–160 — read full existing module header, imports, constants block, private helpers section. Extract exact line positions of MBOX_TTL_SECONDS and _STREAM_ID_RE before inserting new symbols.
    - src/em_proj/identity.py — confirm resolve_session_id, resolve_project_hash, resolve_upstream_identity are exported with those exact names
    - src/em_proj/session/_ops.py — confirm class SessionNotFound exists at module level; confirm session_list() and session_show() signatures
    - 10-PATTERNS.md section "_validate_body pattern (lines 124–134)" — copy _validate_topic pattern exactly from there
  </read_first>

  <behavior>
    - TOPIC_KEY_PREFIX = "topic:" — string equality test passes
    - _validate_topic("valid-topic") → no exception
    - _validate_topic("valid.topic_name") → no exception
    - _validate_topic("invalid topic") → raises ValidationError (space)
    - _validate_topic("") → raises ValidationError (empty string)
    - _validate_topic("a" * 129) → raises ValidationError (too long)
    - _validate_topic("a" * 128) → no exception (boundary case)
    - _build_topic_key("machine", "alerts") → "topic:machine:alerts"
    - _build_topic_key("proj-hash", "myalerts") → "topic:proj-hash:myalerts"
    - _resolve_scope_key("machine") → "machine" (no Redis call)
    - _resolve_scope_key("invalid") → raises ValidationError
  </behavior>

  <action>
    Modify src/em_proj/message/_ops.py.

    Step 1 — Add new imports after the existing import block (line ~44). Add:
      from em_proj.identity import resolve_project_hash, resolve_session_id, resolve_upstream_identity
      from em_proj.session._ops import SessionNotFound, session_list, session_show
    These are the ONLY new top-level imports needed. Do NOT import redis directly here — get_client() returns a connected client and redis.ConnectionError / redis.TimeoutError are accessed via the redis module already available through get_client()'s return type. Import redis at the top level: "import redis as _redis" to access _redis.ConnectionError and _redis.TimeoutError in the fan-out loop.

    Step 2 — Add TOPIC_KEY_PREFIX constant after MBOX_TTL_SECONDS. Add docstring: "Key prefix for topic membership sets. Full key: 'topic:<scope_key>:<topic>'. Distinct from state:*, mbox: namespaces."

    Step 3 — Add _TOPIC_RE after _STREAM_ID_RE: _TOPIC_RE = re.compile(r"^[a-zA-Z0-9_.\-]{1,128}$")

    Step 4 — Update the module docstring to add Phase 10 additions block (per 10-PATTERNS.md module docstring additions section). Also add to the Prohibited imports line: "pipeline (redis.client.Pipeline) — use plain loop per 10-RESEARCH Pitfall 4".

    Step 5 — Add private helpers in the Private helpers section (after _validate_since):
      _validate_topic(topic: str) -> None — copies _validate_body pattern exactly; uses _TOPIC_RE.fullmatch; ValidationError code="validation_error", message="invalid topic: must match [a-zA-Z0-9_.-]{1..128}"
      _build_topic_key(scope_key: str, topic: str) -> str — f"{TOPIC_KEY_PREFIX}{scope_key}:{topic}"
      _resolve_scope_key(scope: str) -> str — maps scope to scope_key using resolve_project_hash() / resolve_upstream_identity() / "machine"; raises ValidationError for unknown scope.

    Commit message: "feat(10-02): add TOPIC_KEY_PREFIX, _validate_topic, _build_topic_key, _resolve_scope_key to _ops.py"
  </action>

  <verify>
    <automated>scripts/test.sh unit -k test_message_send --tail 30</automated>
    After this task: ImportError should be partially resolved. Tests for _validate_topic and TOPIC_KEY_PREFIX should pass or fail on assertion (not ImportError). Tests importing send_directed/send_broadcast/send_topic still fail with ImportError = still correct partial RED.
  </verify>

  <done>
    src/em_proj/message/_ops.py contains "TOPIC_KEY_PREFIX", "_TOPIC_RE", "def _validate_topic", "def _build_topic_key", "def _resolve_scope_key"; scripts/test.sh unit passes all prior Phase 9 tests (no regressions from import additions); scripts/test.sh structural -k phase_10 Test B (TOPIC_KEY_PREFIX) now passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement enumerate_scope_recipients, subscribe_topic, unsubscribe_topic, get_topic_subscribers</name>
  <files>src/em_proj/message/_ops.py</files>

  <read_first>
    - src/em_proj/message/_ops.py current state (after Task 1) — read the full public functions section to locate the correct insertion point after mbox_blocking_read
    - src/em_proj/session/_ops.py lines 475–527 — session_list return shape, field names "project_hash" and "upstream_identity" as plain strings, list comprehension filter pattern
    - 10-PATTERNS.md section "session_list filter pattern" — copy the filter comprehension shape exactly
    - 10-RESEARCH.md Design Decision 2 — topic SET key TTL rationale (no TTL on topic keys; stale session_ids are harmless; send_topic intersects with live sessions)
  </read_first>

  <behavior>
    - enumerate_scope_recipients("machine", exclude_session_id=None) → returns all live session_ids from session_list()
    - enumerate_scope_recipients("machine", exclude_session_id="sender-id") → returns all live session_ids except "sender-id"
    - enumerate_scope_recipients("project", exclude_session_id=None) → returns only sessions with project_hash == resolve_project_hash()
    - enumerate_scope_recipients("upstream", exclude_session_id=None) → returns only sessions with upstream_identity == resolve_upstream_identity()
    - enumerate_scope_recipients("invalid") → raises ValidationError
    - subscribe_topic(session_id, "alerts", "machine") → SADD "topic:machine:alerts" session_id
    - subscribe_topic(session_id, "bad topic", "machine") → raises ValidationError (_validate_topic fires)
    - unsubscribe_topic(session_id, "alerts", "machine") → SREM "topic:machine:alerts" session_id
    - get_topic_subscribers("machine", "alerts") → set[str] of subscriber session_ids from SMEMBERS
  </behavior>

  <action>
    Extend src/em_proj/message/_ops.py Public functions section with 4 new functions.

    enumerate_scope_recipients(scope: str, exclude_session_id: str | None = None) -> list[str]:
      Docstring: "Return session_ids for all live sessions in the given scope, excluding exclude_session_id. Calls session_list() which applies is_holder_stale filtering automatically."
      Call _resolve_scope_key(scope) first (raises ValidationError on bad scope — no need to validate scope separately).
      Then session_list() → filter by scope using list comprehension.
      If scope == "machine": include all. If scope == "project": filter e["session"]["project_hash"] == scope_key. If scope == "upstream": filter e["session"]["upstream_identity"] == scope_key.
      Always exclude exclude_session_id from the result (if not None).
      Note: _resolve_scope_key is called for validation even for "machine" scope (it returns "machine" harmlessly).

    subscribe_topic(session_id: str, topic: str, scope: str) -> None:
      _validate_topic(topic). _resolve_scope_key(scope) to get scope_key. _build_topic_key(scope_key, topic). client.sadd(key, session_id).

    unsubscribe_topic(session_id: str, topic: str, scope: str) -> None:
      _validate_topic(topic). _resolve_scope_key(scope). _build_topic_key(...). client.srem(key, session_id).

    get_topic_subscribers(scope: str, topic: str) -> set[str]:
      _validate_topic(topic). _resolve_scope_key(scope). _build_topic_key(...). return client.smembers(key).
      smembers returns set of str because get_client() uses decode_responses=True (confirmed in Phase 9).

    Each function calls get_client() inside its body — same pattern as mbox_write (not a module-level client).

    Commit message: "feat(10-02): add enumerate_scope_recipients, subscribe_topic, unsubscribe_topic, get_topic_subscribers"
  </action>

  <verify>
    <automated>scripts/test.sh unit -k test_message_send --tail 30</automated>
    Tests for subscribe/unsubscribe/enumerate scope should now pass. Tests for send_directed/broadcast/topic still fail with ImportError (functions not yet written).
    <automated>scripts/test.sh unit --tail 20</automated>
    All prior Phase 9 unit tests still pass.
  </verify>

  <done>
    _ops.py contains "def enumerate_scope_recipients", "def subscribe_topic", "def unsubscribe_topic", "def get_topic_subscribers"; scripts/test.sh unit -k topic passes tests for subscribe/unsubscribe; scripts/test.sh unit -k scope passes scope-related tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement send_directed, send_broadcast, send_topic</name>
  <files>src/em_proj/message/_ops.py</files>

  <read_first>
    - src/em_proj/message/_ops.py current state (after Task 2) — read the function list to find insertion point and confirm imports are in place
    - 10-PATTERNS.md section "Fan-out loop with partial failure tracking" — copy the succeeded/failed loop skeleton exactly
    - 10-PATTERNS.md section "mbox_write call pattern" — copy the msg dict structure exactly; note field names pattern/scope/topic/body/sent_at/ttl
    - 10-RESEARCH.md Design Decision 4 — pub/sub PUBLISH behavior; fire-and-forget; return value ignored; channel = f"msg:{session_id}"
    - 10-RESEARCH.md Design Decision 5 — exit code semantics: recipients_failed > 0 → verb layer must raise SystemExit(4); ops layer returns dict (does NOT itself call sys.exit)
    - 10-RESEARCH.md Pitfall 6 — mid-loop ConnectionError; wrap loop in try/except (redis.ConnectionError, redis.TimeoutError)
    - 10-RESEARCH.md Design Decision 3 — fan-out semantics; plain loop (NOT pipeline); partial failure counting
  </read_first>

  <behavior>
    - send_directed("recipient-id", "hello", "machine") → calls session_show("recipient-id"), calls mbox_write with pattern="direct", calls client.publish("msg:recipient-id", ...), returns {"recipients_written": 1, "recipients_failed": 0, "pub_published": 0, "pattern": "direct", "scope": "machine"}
    - send_directed("nonexistent-id", "hi", "machine") → raises SessionNotFound (from session_show)
    - send_broadcast("hello", "machine") → calls enumerate_scope_recipients("machine", exclude_session_id=resolve_session_id()), calls mbox_write for each, returns {"recipients_written": N, "recipients_failed": 0, "pub_published": 0, "pattern": "broadcast", "scope": "machine"}
    - send_broadcast excludes sender's session_id (per D-SelfExclude)
    - send_broadcast with mid-loop ConnectionError → recipients_failed > 0 in returned dict (no exception raised)
    - send_topic("alerts", "machine", "hello") → calls get_topic_subscribers, intersects with live session_ids from enumerate_scope_recipients (no exclude_session_id for intersection — exclusion is applied by enumerating live sessions then intersecting; sender excluded via enumerate_scope_recipients with exclude_session_id=resolve_session_id())
    - send_topic with topic="invalid!" → raises ValidationError (_validate_topic called first)
    - All three send functions return a flat dict: recipients_written (int), recipients_failed (int), pub_published (int), pattern (str), scope (str) — all scalars
    - pub_published reflects the return value of client.publish() per recipient (integer count of live subscribers); summed across all recipients
  </behavior>

  <action>
    Add three public functions to src/em_proj/message/_ops.py.

    send_directed(to_session_id: str, body: str, scope: str) -> dict:
      Docstring notes: validates recipient via session_show (raises SessionNotFound if absent/stale per 09-VERIFICATION deferred item); mbox_write is the durable path; PUBLISH is fire-and-forget live path; scope is informational for directed (recorded in MBOX-04 record, no scope filtering needed).
      Body: session_show(to_session_id) — let SessionNotFound propagate. Build msg dict with pattern="direct", scope=scope, topic=None, from_session=resolve_session_id(), body=body, sent_at=time.time(), ttl=MBOX_TTL_SECONDS. Call mbox_write(to_session_id, msg). Call client.publish(f"msg:{to_session_id}", json.dumps({"pattern": "direct", "scope": scope, "body": body})) — assign return to pub_count. Return flat dict: recipients_written=1, recipients_failed=0, pub_published=pub_count, pattern="direct", scope=scope.

    send_broadcast(body: str, scope: str) -> dict:
      Docstring notes: fan-out to all live sessions in scope, excluding sender (D-SelfExclude); partial failure tracked; exit code 4 signaled via recipients_failed > 0 in return dict (verb layer calls raise SystemExit(4)).
      Body: sender_id = resolve_session_id(). recipients = enumerate_scope_recipients(scope, exclude_session_id=sender_id). succeeded = 0; failed = 0; pub_published = 0. Fan-out loop: try: mbox_write(r, msg); pub_published += client.publish(f"msg:{r}", json.dumps({...})); succeeded += 1. except (_redis.ConnectionError, _redis.TimeoutError): failed += 1. Return dict.

    send_topic(topic: str, scope: str, body: str) -> dict:
      Docstring notes: _validate_topic first; get_topic_subscribers; intersect with live sessions (exclude sender); fan-out loop same as broadcast; stale session_ids in SET are harmless — intersect filters them.
      Body: _validate_topic(topic). sender_id = resolve_session_id(). raw_subscribers = get_topic_subscribers(scope, topic). live_recipients = enumerate_scope_recipients(scope, exclude_session_id=sender_id). recipients = list(raw_subscribers & set(live_recipients)). Fan-out loop: same pattern as send_broadcast with pattern="topic", topic=topic. Return dict.

    For the msg dict in send_broadcast and send_topic: include pattern, scope, topic (None or topic_name), body, from_session=sender_id, sent_at=time.time(), ttl=MBOX_TTL_SECONDS.

    Important: client = get_client() inside each function body (not module-level). Import redis at top of module as "import redis as _redis" to access _redis.ConnectionError and _redis.TimeoutError.

    The PUBLISH payload is a minimal JSON dict sufficient for Phase 11 daemon to identify the message. The full message is always in the mailbox.

    Commit message: "feat(10-02): implement send_directed, send_broadcast, send_topic in message/_ops.py"
  </action>

  <verify>
    <automated>scripts/test.sh unit -k test_message_send --tail 50</automated>
    All unit tests in test_message_send.py should now pass (GREEN). This includes directed send, broadcast self-exclusion, topic subscribe/send/unsubscribe, validation errors, partial delivery, and metadata dict structure.
    <automated>scripts/test.sh unit --tail 20</automated>
    Full unit suite still passes.
    <automated>scripts/test.sh structural -k phase_10 --tail 20</automated>
    Test A (all 7 function names present) and Test B (TOPIC_KEY_PREFIX) now pass. Test E (CLI verbs) still fails = correct.
  </verify>

  <done>
    _ops.py contains "def send_directed", "def send_broadcast", "def send_topic"; scripts/test.sh unit -k test_message_send exits 0 with all tests passing; scripts/test.sh structural -k phase_10 shows tests A and B passing; the module prohibits pipeline (test D passes); no typer/multiprocessing/threading imports (test C passes).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI caller → _ops.py | topic and scope arguments cross from unvalidated CLI input to Redis key construction |
| sender → recipient mailbox | sender controls message body; recipient cannot control what arrives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | _validate_topic | mitigate | _TOPIC_RE = re.compile(r"^[a-zA-Z0-9_.\-]{1,128}$"); raises ValidationError before Redis key is built; prevents "topic:machine:injected:key" injection via topic name containing ":" |
| T-10-02-02 | Tampering | _resolve_scope_key scope parameter | mitigate | Explicit allowlist ("machine", "project", "upstream"); any other value raises ValidationError before scope_key is used |
| T-10-02-03 | DoS | mbox_write body length | accept | _validate_body (already in mbox_write from Phase 9) enforces MAX_BODY_CHARS=4096; send functions pass body through mbox_write which raises ValidationError before XADD |
| T-10-02-04 | Information Disclosure | send_broadcast scope=project | mitigate | scope filtering is correct-by-design: enumerate_scope_recipients filters session_list() by project_hash == resolve_project_hash(); no cross-project sessions receive messages |
| T-10-02-05 | Spoofing | send_directed to dead session | mitigate | session_show(to_session_id) validates recipient liveness (applies is_holder_stale, reaps dead sessions); dead session → SessionNotFound → exit 2 (not a silent write to orphaned mailbox) |
| T-10-02-06 | DoS | mid-loop ConnectionError during fan-out | accept | Caught by except (_redis.ConnectionError, _redis.TimeoutError); counted as failed; returned as recipients_failed > 0; no traceback; verb layer signals exit 4 |
</threat_model>

<verification>
After completing all three tasks:
- scripts/test.sh unit -k test_message_send → exits 0, all tests GREEN
- scripts/test.sh structural -k phase_10 → tests A (functions), B (TOPIC_KEY_PREFIX), C (no forbidden imports), D (no pipeline) pass; test E (CLI verbs) still fails = correct
- scripts/test.sh unit → all 453+ prior tests still pass, plus new test_message_send tests
- grep in _ops.py: "def send_directed", "def send_broadcast", "def send_topic", "def subscribe_topic", "def unsubscribe_topic", "def enumerate_scope_recipients", "def _validate_topic" all present
- grep in _ops.py: "pipeline" absent from all method calls (ast check passes)
- grep in _ops.py: no "import typer", "import multiprocessing", "import threading"
</verification>

<success_criteria>
- src/em_proj/message/_ops.py has all 7 new public functions and TOPIC_KEY_PREFIX constant
- scripts/test.sh unit exits 0 (all tests GREEN including new test_message_send tests)
- Delivery metadata dict shape: {recipients_written: int, recipients_failed: int, pub_published: int, pattern: str, scope: str} — all scalar values
- Sender self-exclusion: send_broadcast and send_topic exclude resolve_session_id() from recipients; send_directed does NOT exclude self (explicit directed-to-self is allowed)
- Pub/sub channel naming locked as msg:<session_id> (Phase 10↔11 contract)
- No pipeline import anywhere in _ops.py
</success_criteria>

<output>
After completion, create .planning/phases/10-messaging-send-patterns/10-02-SUMMARY.md
</output>
