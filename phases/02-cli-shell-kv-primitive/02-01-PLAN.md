---
phase: 02-cli-shell-kv-primitive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/em_proj/cli.py
  - src/em_proj/state/__init__.py
  - tests/unit/test_state_mount.py
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
tags: [carry-forward, scaffold, typer, mount, cli-help]

must_haves:
  truths:
    - "Per Phase 1 D-04 (carry-forward): `uv tool install --editable .` from the repo root installs `em-proj` and `command -v em-proj` resolves to it"
    - "Per Phase 1 D-05 (carry-forward): `em-proj --version` exits 0 and prints `em-proj 0.1.0`; `em-proj --help` exits 0 and renders typer auto-help"
    - "Per Phase 1 D-06 (carry-forward): all existing `tests/unit/test_cli.py` and `tests/unit/test_redis_client.py` tests still pass via `bash scripts/test.sh unit`"
    - "Per D-14: `em_proj/state/__init__.py` defines `state_app = typer.Typer(no_args_is_help=True)` and `em_proj/cli.py` calls `app.add_typer(state_app, name=\"state\", help=...)`"
    - "Per D-14: `em-proj state` (no verb) exits 0 and prints typer help listing the `state` Typer app's available verbs (initially empty — placeholder commands not required)"
    - "Per CLI-03 partial: `em-proj state --help` renders typer auto-help mentioning the `state` subcommand description"
  artifacts:
    - path: "src/em_proj/state/__init__.py"
      provides: "state_app Typer instance + add_typer mount target"
      contains: "state_app = typer.Typer"
      min_lines: 10
    - path: "src/em_proj/cli.py"
      provides: "app.add_typer(state_app, name=\"state\", ...) wiring"
      contains: "add_typer"
    - path: "tests/unit/test_state_mount.py"
      provides: "CliRunner-based assertions for `em-proj state --help`"
  key_links:
    - from: "src/em_proj/cli.py"
      to: "src/em_proj/state/__init__.py"
      via: "from em_proj.state import state_app + app.add_typer"
      pattern: "from em_proj\\.state import state_app"
---

<objective>
Verify the Phase 1 carry-forward (CLI-01, CLI-02) still holds after editable reinstall from the repo root, and land the empty `state` subcommand mount so Plans 03 and 04 can attach verbs without further touching `cli.py`. This plan does NOT re-implement CLI-01/CLI-02 — it proves the prior delivery, and adds the smallest possible nested-typer scaffold per D-14.

Purpose: Satisfy the Requirements Coverage Gate (CLI-01, CLI-02, CLI-03) without functional re-implementation, and stake out the mounting structure that D-14 locks in.

Output:
- Editable install verified from the repo root (addresses Phase 1 VERIFICATION.md carry-forward).
- `src/em_proj/state/__init__.py` exists with an empty `state_app` Typer instance (`no_args_is_help=True`).
- `src/em_proj/cli.py` mounts `state_app` via `app.add_typer(state_app, name="state", help="KV / lock / claim primitives")`.
- `tests/unit/test_state_mount.py` asserts that `em-proj state --help` exits 0 and includes the mount help string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cli-shell-kv-primitive/02-CONTEXT.md
@.planning/phases/01-test-harness-redis-foundation/01-CONTEXT.md
@.planning/phases/01-test-harness-redis-foundation/01-03-SUMMARY.md
@.planning/phases/01-test-harness-redis-foundation/01-04-SUMMARY.md
@CLAUDE.md
@src/em_proj/cli.py
@src/em_proj/__init__.py
@tests/unit/test_cli.py

<interfaces>
<!-- Existing surface this plan extends. Executor uses these directly; no exploration required. -->

From src/em_proj/cli.py:
- `app: typer.Typer` — the root Typer app, named "em-proj", no_args_is_help=True, add_completion=False
- Already exposes `--version` (eager) + `--help` via typer auto-help
- Pre-marked mount comment at lines 38-41 reserves the Phase 2 attachment point

From src/em_proj/__init__.py:
- `__version__ = "0.1.0"`

From the Phase 1 VERIFICATION.md carry-forward (per 02-CONTEXT.md canonical_refs):
- The previous editable install came from a worktree path. Re-running `uv tool install --editable .` from the repo root is required so `which em-proj` resolves to a binary that imports the live `src/em_proj/` tree on `main`.

From typer (>=0.16,<1.0):
- `typer.Typer(name=, help=, no_args_is_help=True)` — sub-app constructor
- `parent_app.add_typer(sub_app, name=, help=)` — mount nested sub-app
- `typer.testing.CliRunner().invoke(app, [...])` — in-process invocation for unit tests
</interfaces>

<deferred>
NOTE: `.planning/REQUIREMENTS.md` Traceability table still maps CLI-01 and CLI-02 to Phase 2 (stale — they delivered in Phase 1 per 01-CONTEXT.md D-04..D-06). This is informational and NOT blocking — the roadmapper will re-run after Phase 2 completes. Do not edit REQUIREMENTS.md from this plan.
</deferred>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify Phase 1 carry-forward (CLI-01, CLI-02) from repo root</name>
  <files>(no source files modified; verification-only)</files>
  <read_first>
    - .planning/phases/01-test-harness-redis-foundation/01-03-SUMMARY.md (Phase 1 D-04..D-06 — what was delivered)
    - .planning/phases/01-test-harness-redis-foundation/01-04-SUMMARY.md (Phase 1 completion checklist)
    - CLAUDE.md (test dispatcher convention — `bash scripts/test.sh unit` only)
    - pyproject.toml (confirm `[project.scripts] em-proj = "em_proj.cli:app"` still present)
  </read_first>
  <action>
    From the repo root (`/Users/emonical/projects/personal/ai-tools/em-proj`), run each command as a SEPARATE Bash invocation — do NOT chain them with `&&`, `;`, or `|` (project-wide no-pipe rule per repo CLAUDE.md + 02-CONTEXT.md). Each numbered step below is ONE allowlisted Bash call.

    1. `uv tool install --editable .` — reinstall the `em-proj` binary against the main checkout (addresses Phase 1 VERIFICATION.md carry-forward — the prior install was rooted at a worktree path). Expected: exit 0.
    2. `command -v em-proj` — confirms the binary is on PATH; the resolved path should be under `~/.local/bin/` or the active uv tool install location (not a worktree). Expected: exit 0, stdout = absolute path.
    3. `em-proj --version` — confirms typer dispatch + `__version__` callback. Expected: exit 0, stdout = `em-proj 0.1.0`.
    4. `em-proj --help` — confirms typer auto-help renders. Expected: exit 0, stdout contains `--version`.
    5. `bash scripts/test.sh unit` — confirms all existing unit tests pass (CLI-02 + redis-client tests inherited from Phase 1). Expected: exit 0, pytest summary shows all green.

    NOTE: Do NOT modify any source file in this task. Verification only. If any step fails, surface the failure to the orchestrator — do not "fix" Phase 1 deliverables from this plan.
  </action>
  <verify>
    <automated>bash scripts/test.sh unit</automated>
  </verify>
  <acceptance_criteria>
    Each line below is one allowlisted Bash invocation — no chains, no pipes.
    - `uv tool install --editable .` exits 0 (idempotent reinstall from repo root)
    - `command -v em-proj` exits 0 and prints a resolvable path
    - `em-proj --version` exits 0 with stdout containing the literal `em-proj 0.1.0`
    - `em-proj --help` exits 0 with stdout containing `--version`
    - `bash scripts/test.sh unit` exits 0 (all of tests/unit/test_cli.py + tests/unit/test_redis_client.py pass)
  </acceptance_criteria>
  <done>Phase 1 carry-forward verified: CLI-01 (installable + on PATH) and CLI-02 (typer dispatch + --version + --help) both still hold against the repo-root editable install. No source files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Create empty state_app subcommand mount (D-14)</name>
  <files>src/em_proj/state/__init__.py, src/em_proj/cli.py</files>
  <read_first>
    - src/em_proj/cli.py (current root app + the existing Phase 2 mount-point comment at lines 38-41)
    - .planning/phases/02-cli-shell-kv-primitive/02-CONTEXT.md (D-14 — nested typer app pattern + naming)
  </read_first>
  <action>
    1. Create `src/em_proj/state/__init__.py` defining `state_app = typer.Typer(name="state", help="KV / lock / claim primitives", no_args_is_help=True, add_completion=False)`. Module docstring should state: "state subcommand family — per D-14, nested typer app mounted from cli.py. Plans 03/04 attach the get/set/del/list verbs by importing kv_get/kv_set/kv_del/kv_list from em_proj.state.kv and decorating thin command wrappers with @state_app.command(). Phases 3/4 add lock.py and claim.py as siblings using the same pattern."
    2. Modify `src/em_proj/cli.py`: replace the placeholder comment block (lines 38-41) with `from em_proj.state import state_app` near the other imports + `app.add_typer(state_app, name="state", help="KV / lock / claim primitives")` after the `main` callback definition. Keep the existing `--version` callback and the `if __name__ == "__main__": app()` guard untouched.
    3. Do NOT add any `@state_app.command()` verbs in this plan — that is Plan 04's territory. The mount is intentionally empty; `em-proj state --help` should render typer's empty-app help listing (which still includes the parent help string).
  </action>
  <verify>
    <automated>uv run python -c "from em_proj.cli import app; from em_proj.state import state_app; assert state_app.info.name == 'state'; print('mount OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/em_proj/state/__init__.py` exists, &gt;=10 lines, contains the literal `state_app = typer.Typer`
    - `src/em_proj/cli.py` contains the literals `from em_proj.state import state_app` and `add_typer(state_app, name="state"`
    - `uv run python -c "from em_proj.state import state_app; assert state_app.info.name == 'state'"` exits 0
    - The empty-app convention holds: `state_app.registered_commands == []` (no verbs yet — Plan 04 adds them)
  </acceptance_criteria>
  <done>Empty `state_app` Typer instance lives at `em_proj.state`, mounted under the root `app` as `em-proj state`. Plans 03/04 can attach verbs without touching `cli.py` again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CliRunner unit test for the state mount (CLI-03 partial)</name>
  <files>tests/unit/test_state_mount.py</files>
  <read_first>
    - tests/unit/test_cli.py (CliRunner pattern to mirror — invoke(app, [...]) + assert exit_code + assert substring in stdout)
    - src/em_proj/cli.py (the freshly-mounted state_app)
    - src/em_proj/state/__init__.py (the empty state_app)
  </read_first>
  <behavior>
    - Test 1 (`test_state_mount_help_exits_zero`): Invoking the root app with `["state", "--help"]` exits 0
    - Test 2 (`test_state_help_mentions_mount_string`): The `state --help` stdout contains the mount help text "KV / lock / claim primitives" (the literal D-14 mount string)
    - Test 3 (`test_state_no_args_shows_help`): Invoking `["state"]` with no verb does NOT raise an unhandled exception (no_args_is_help=True semantics — typer prints help and exits with a non-error code; assert exit_code in (0, 2) since typer's no_args_is_help can return 2 in some versions)
  </behavior>
  <action>
    Create `tests/unit/test_state_mount.py` mirroring the `tests/unit/test_cli.py` shape: module docstring "CLI-03 partial coverage — `em-proj state --help` renders typer auto-help (D-14 mount verification).", import `CliRunner` from `typer.testing` and `app` from `em_proj.cli`, instantiate `runner = CliRunner()`, then write the three named test functions per the &lt;behavior&gt; block. Use clear assertion messages so the structural test in Plan 05 can introspect named tests later if needed. Tests must NOT require a running Redis (no `clean_db` fixture, no `redis_precheck`). Tests must NOT depend on Plans 03/04 wiring — they verify only the empty-mount behavior.
  </action>
  <verify>
    <automated>bash scripts/test.sh unit -k test_state_mount</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_state_mount.py` exists, contains the three named test functions verbatim
    - `bash scripts/test.sh unit -k test_state_mount` exits 0 with all three tests passing
    - No Redis connection attempted (tests run in &lt;0.1s)
  </acceptance_criteria>
  <done>State mount surface is verified via three deterministic CliRunner tests. CLI-03 is partially satisfied (`--help` works at the `state` subcommand level); Plan 04's per-verb `--help` checks finish the requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-shell → typer parsing | argv flows through typer's parser; no shell evaluation (Popen-style invocation only in tests) |
| editable install path → import resolution | `uv tool install --editable .` makes the binary import the live source tree; a stale install pointing at a worktree silently uses old code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01-01 | Tampering | editable install root | mitigate | Task 1 re-runs `uv tool install --editable .` from the repo root explicitly; subsequent plans assume `which em-proj` points at this install |
| T-2-01-02 | Information disclosure | `--version` output | accept | Version string is the package literal `0.1.0`; no env or path leakage |
| T-2-01-03 | Denial of service | empty state_app | accept | `state_app.registered_commands == []` is intentional; typer's no_args_is_help handles the empty-invocation case |
</threat_model>

<verification>
Each bullet is one allowlisted Bash invocation — no chains, no pipes.
- `command -v em-proj` resolves (Task 1)
- `em-proj --version` prints `em-proj 0.1.0` (Task 1)
- `em-proj --help` exits 0 (Task 1)
- `bash scripts/test.sh unit -k test_state_mount` exits 0 and stdout contains "KV / lock / claim primitives" rendered by `em-proj state --help` (Task 3 — assertion lives inside the pytest test, not in a shell pipeline)
- `bash scripts/test.sh unit` exits 0 (Task 1 + Task 3 — all unit tests green including the new test_state_mount.py)
- `grep -c "add_typer(state_app" src/em_proj/cli.py` returns 1 (Task 2 — single grep call, no pipe)
</verification>

<success_criteria>
1. Phase 1 carry-forward verified: `em-proj` binary on PATH, `--version` and `--help` work, all prior unit tests still pass.
2. `state` subcommand mount lands per D-14: `em_proj.state.state_app` exists and is mounted under the root `app`.
3. `em-proj state --help` renders typer auto-help (CLI-03 partial — per-verb help completes in Plan 04).
4. No Redis dependency introduced (tests run offline).
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-shell-kv-primitive/02-01-SUMMARY.md` documenting:
- Confirmation that `uv tool install --editable .` was re-run from the repo root and `which em-proj` resolves correctly.
- The new files (`src/em_proj/state/__init__.py`, `tests/unit/test_state_mount.py`) and the modified file (`src/em_proj/cli.py`).
- That `state_app.registered_commands` is intentionally empty pending Plan 04.
- Cited D-IDs: D-04, D-05, D-06 (Phase 1 carry-forward) + D-14 (mount style).
- Three commits in order: `chore(02-01): re-install em-proj editable from repo root` (if a commit makes sense for the install side-effect; if not, fold into the doc commit), `feat(02-01): mount empty state_app under root typer app (D-14)`, `test(02-01): add CliRunner tests for state mount help (CLI-03 partial)`.
</output>
</content>
