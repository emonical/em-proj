---
phase: 11-listener-daemon
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/em_proj/session/_daemon.py
  - src/em_proj/session/__init__.py
  - tests/structural/test_phase_11_shape.py
  - tests/multiprocess/test_daemon_lifecycle.py
autonomous: true
requirements: [DAEMON-01, DAEMON-02, DAEMON-03]

must_haves:
  truths:
    - "em-proj session listen starts a detached subprocess (start_new_session=True), records the daemon pid in daemon:<session_id> HASH, and exits 0 immediately"
    - "em-proj session listen --foreground runs the daemon poll loop in the foreground until SIGTERM — used by all tests"
    - "The daemon subscribes to msg:<session_id> via pubsub.get_message(timeout=5) and logs/no-ops on receipt — no mbox_write call"
    - "While alive, the daemon calls session_heartbeat() every DAEMON_HEARTBEAT_INTERVAL seconds (default 60, overridable via EM_PROJ_DAEMON_HEARTBEAT_INTERVAL)"
    - "DAEMON-02 proven at system level: message sent while daemon alive appears in message inbox (send-time mbox_write guarantees durability; daemon does not double-write)"
    - "session/_ops.py contains no subprocess/signal/shutil imports — structural invariant holds"
  artifacts:
    - path: src/em_proj/session/_daemon.py
      provides: "Daemon ops module: _daemon_start, _daemon_foreground_run, _daemon_record_write, _daemon_record_del, _daemon_record_read, DAEMON_KEY_PREFIX"
      contains: "def _daemon_foreground_run"
      exports:
        - _daemon_start
        - _daemon_foreground_run
        - _daemon_record_read
        - _daemon_record_write
        - _daemon_record_del
        - DAEMON_KEY_PREFIX
        - DAEMON_HEARTBEAT_INTERVAL
    - path: src/em_proj/session/__init__.py
      provides: "session listen verb (start + --foreground mode) mounted on session_app"
      contains: '@session_app.command("listen")'
    - path: tests/structural/test_phase_11_shape.py
      provides: "Structural invariant assertions for Phase 11 (file presence, prohibited imports, no mbox_write, verb presence, key prefix)"
    - path: tests/multiprocess/test_daemon_lifecycle.py
      provides: "TEST-05 multiprocess tests: start/stop basic flow, heartbeat liveness, message-liveness (DAEMON-02 system-level proof)"
  key_links:
    - from: "session listen (start mode)"
      to: "_daemon_start in _daemon.py"
      via: "D-14 verb shell: one ops call"
      pattern: "_daemon_start\\("
    - from: "session listen --foreground"
      to: "_daemon_foreground_run in _daemon.py"
      via: "D-14 verb shell: --foreground flag routes to foreground body"
      pattern: "_daemon_foreground_run\\("
    - from: "_daemon_foreground_run poll loop"
      to: "session_heartbeat() in session/_ops.py"
      via: "monotonic tick comparison; cmd_client is get_client(); ps is cmd_client.pubsub()"
      pattern: "session_heartbeat\\("
    - from: "_daemon_start Popen"
      to: "em-proj binary (shutil.which fallback)"
      via: "subprocess.Popen([binary, 'session', 'listen', '--foreground'], start_new_session=True)"
      pattern: "start_new_session"
---

<objective>
Wave 1: Implement the daemon body module (_daemon.py), wire session listen into session_app, and drive the start + foreground + heartbeat + message-liveness tests green.

Purpose: This is the daemon's core existence — the foreground polling loop, heartbeat integration, Redis pub/sub subscription, and daemon HASH record primitives that all subsequent lifecycle features build on.

Output:
- src/em_proj/session/_daemon.py — all daemon ops and the foreground loop body
- session listen verb with --foreground flag in session/__init__.py
- tests/structural/test_phase_11_shape.py — structural invariants (green after this plan)
- tests/multiprocess/test_daemon_lifecycle.py — start, --foreground, heartbeat, and liveness tests (green after this plan)

Locked decisions implemented here (NON-NEGOTIABLE):
- Daemon role = liveness + lifecycle only. No mbox_write anywhere in _daemon.py (per D-02 system-level interpretation locked in CONTEXT.md).
- Detach = subprocess.Popen(start_new_session=True) re-invoking em-proj session listen --foreground (per CONTEXT.md D-02 and RESEARCH Pattern 1).
- Two Redis clients: cmd_client = get_client() for commands/heartbeat; ps = cmd_client.pubsub() for subscription. Never call EVAL/HSET on the subscribed pubsub object (per RESEARCH Pitfall 2, Pattern 4).
- DAEMON_KEY_PREFIX = "daemon:" for the daemon HASH key; full key is daemon:<session_id> (per CONTEXT.md D-03 locked decision).
- DAEMON_HEARTBEAT_INTERVAL = int(os.environ.get("EM_PROJ_DAEMON_HEARTBEAT_INTERVAL", "60")) — env override for tests (per RESEARCH Open Q2 recommendation).
- session/_ops.py MUST remain free of subprocess/signal/shutil imports (structural test enforces this).
</objective>

<execution_context>
@/Users/emonical/projects/personal/ai-tools/em-proj/CLAUDE.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/11-listener-daemon/11-RESEARCH.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/11-listener-daemon/11-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/em_proj/session/__init__.py (extend in place):
  session_app = typer.Typer(name="session", ...)   # add listen + stop commands here
  Existing commands: "register", "heartbeat", "list", "show"
  Import pattern: from em_proj.session._ops import ...  — extend similarly for _daemon imports
  D-14 verb shell: json_mode = resolve_json_mode(json_flag); client = get_client(); die_if_redis_unreachable(client); call one ops fn; call one emit_*

From src/em_proj/session/_ops.py (import from, DO NOT modify):
  session_heartbeat() -> dict   # calls LUA_SESSION_HEARTBEAT; raises SessionNotFound if session absent
  class SessionNotFound          # propagated from session_heartbeat when session key absent or TTL'd
  TTL_DEFAULT: int = 300         # heartbeat re-arms this TTL; DAEMON_HEARTBEAT_INTERVAL must be < 300

From src/em_proj/identity.py (import in _daemon.py):
  resolve_session_id() -> str         # reads CLAUDE_CODE_SESSION_ID env or pid-<pid> fallback
  current_process_composite() -> dict  # {pid: int, proc_start_epoch: float, boot_id: str, ...}
  is_holder_stale(holder: dict) -> bool  # requires {pid: int, proc_start_epoch: float, boot_id: str}

From src/em_proj/redis_client.py (import in _daemon.py):
  get_client() -> redis.Redis   # module-level singleton; decode_responses=True
  die_if_redis_unreachable(client) -> None  # used in verb layer (not in daemon body)

From src/em_proj/output.py (used in session/__init__.py verb):
  emit_ok(data, json_mode) -> None
  emit_error(message, json_mode) -> None
  resolve_json_mode(json_flag) -> bool

From src/em_proj/message/_ops.py (for message-liveness multiprocess test):
  send_directed(to_session_id: str, body: str, scope: str) -> dict
  # used via CLI: em-proj message send --to <sid> <body>

From tests/conftest.py:
  EM_PROJ_BIN: str = "em-proj"
  TEST_DB: int = 15
  RaceResult dataclass (returncode, stdout, stderr, duration_ms)
  clean_db fixture — FLUSHDB on db=15 before/after each test
  multiproc_race fixture — spawn N subprocesses with EM_PROJ_REDIS_DB=15

Lua atomicity pattern (from src/em_proj/session/_ops.py LUA_SESSION_UPSERT, lines 80-118):
  LUA_DAEMON_WRITE_OR_DETECT mirrors this pattern exactly:
    EXISTS KEYS[1] -> 0 -> HSET all 4 fields -> return 'written'
    EXISTS KEYS[1] -> 1 -> HMGET [pid, proc_start_epoch, boot_id, started_at] -> return list
  Caller converts returned list to dict for is_holder_stale probe.

SIGTERM handler pattern (from src/em_proj/state/lock.py lines 727-741):
  _shutdown = False
  def _on_sigterm(*_): nonlocal _shutdown; _shutdown = True
  signal.signal(signal.SIGTERM, _on_sigterm)
  Loop checks `while not _shutdown`.

subprocess.Popen pattern (from tests/conftest.py multiproc_race fixture, line 127):
  child_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB)}
  For daemon spawn: pass env=None (inherit parent env — EM_PROJ_REDIS_DB already in os.environ).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/em_proj/session/_daemon.py with daemon HASH ops and foreground loop body</name>
  <files>src/em_proj/session/_daemon.py</files>

  <read_first>
    - src/em_proj/session/_ops.py lines 1-70 — module docstring (prohibited-import pattern), constants block (TTL_DEFAULT=300), KEY_PREFIX; confirm session_heartbeat() and SessionNotFound are importable from here
    - src/em_proj/session/_ops.py lines 80-145 — LUA_SESSION_UPSERT and LUA_SESSION_HEARTBEAT for Lua write-or-detect pattern to mirror
    - src/em_proj/identity.py lines 1-55 — module docstring, public API (resolve_session_id, current_process_composite, is_holder_stale signatures)
    - src/em_proj/identity.py lines 262-315 — is_holder_stale full implementation; confirm it requires {pid: int, proc_start_epoch: float, boot_id: str} as plain Python types (not strings)
    - src/em_proj/state/lock.py lines 727-741 — SIGTERM handler pattern to mirror exactly
    - src/em_proj/redis_client.py — get_client() signature; confirm decode_responses=True
    - 11-RESEARCH.md Pattern 1, Pattern 2, Pattern 3, Pattern 4 — primary implementation guide
    - 11-RESEARCH.md Pitfall 2 (PubSub/EVAL conflict), Pitfall 4 (EXPIRE on daemon record), Pitfall 6 (orphaned daemon after FLUSHDB)
  </read_first>

  <behavior>
    - DAEMON_KEY_PREFIX constant equals "daemon:" — string equality
    - DAEMON_HEARTBEAT_INTERVAL reads EM_PROJ_DAEMON_HEARTBEAT_INTERVAL env var; defaults to 60; test with env var "1" returns 1
    - _daemon_record_key(session_id) returns "daemon:" + session_id
    - _daemon_record_write(session_id) uses Lua write-or-detect; returns "written" if key was absent; returns existing record dict {pid, proc_start_epoch, boot_id, started_at} if key already existed
    - _daemon_record_read(session_id) returns dict with {pid: int, proc_start_epoch: float, boot_id: str, started_at: float} when key exists; returns None when key absent
    - _daemon_record_del(session_id) issues DEL on the daemon HASH key; returns None
    - _daemon_start(session_id) with no existing record: calls Popen, returns {"status": "started", "pid": <child_pid>}
    - is_holder_stale requires pid as int; _daemon_record_read must convert raw Redis str to int/float
    - Module docstring lists subprocess, signal, shutil, os as allowed imports (distinguishes _daemon.py from _ops.py's prohibited list)
  </behavior>

  <action>
    Create src/em_proj/session/_daemon.py as a new module.

    Module docstring: Explain this module owns all daemon process/signal/subprocess code for the Phase 11 listener daemon. State explicitly: "subprocess, signal, shutil, os are legitimately imported here — these are PROHIBITED in session/_ops.py but intentionally present in _daemon.py." List the four public entry points (_daemon_start, _daemon_foreground_run, _daemon_record_read, _daemon_record_del). Reference DAEMON_KEY_PREFIX = "daemon:" namespace.

    Imports block:
      import os
      import shutil
      import signal
      import subprocess
      import sys
      import time
      from em_proj.identity import current_process_composite, is_holder_stale, resolve_session_id
      from em_proj.redis_client import get_client
      from em_proj.session._ops import SessionNotFound, session_heartbeat
      Do NOT import typer. Do NOT import threading or multiprocessing.

    Constants:
      DAEMON_KEY_PREFIX: str = "daemon:"
      DAEMON_HEARTBEAT_INTERVAL: int = int(os.environ.get("EM_PROJ_DAEMON_HEARTBEAT_INTERVAL", "60"))

    Lua script:
      LUA_DAEMON_WRITE_OR_DETECT: str — mirrors LUA_SESSION_UPSERT from _ops.py. KEYS[1] = daemon HASH key. EXISTS KEYS[1]: if 0 HSET all 4 fields {pid, proc_start_epoch, boot_id, started_at} from ARGV[1..4] and return 'written'. If 1 HMGET KEYS[1] pid proc_start_epoch boot_id started_at and return the list.

    Private helpers:
      _daemon_record_key(session_id: str) -> str: return f"{DAEMON_KEY_PREFIX}{session_id}"

      _daemon_record_write(session_id: str) -> str | dict: Calls Lua LUA_DAEMON_WRITE_OR_DETECT with KEYS=[key], ARGV=[str(composite["pid"]), str(composite["proc_start_epoch"]), composite["boot_id"], str(time.time())] where composite = current_process_composite(). Returns "written" if the Lua script returned the string "written". If it returned a list, converts [pid_str, start_str, boot_id, started_at_str] to dict {pid: int(pid_str), proc_start_epoch: float(start_str), boot_id: boot_id, started_at: float(started_at_str)} and returns that dict.

      _daemon_record_read(session_id: str) -> dict | None: client.hgetall(_daemon_record_key(session_id)). If empty dict or falsy, return None. Else convert strings: {pid: int(raw["pid"]), proc_start_epoch: float(raw["proc_start_epoch"]), boot_id: raw["boot_id"], started_at: float(raw["started_at"])}. Return dict. Note: is_holder_stale requires pid as plain int — do NOT pass a string.

      _daemon_record_del(session_id: str) -> None: client.delete(_daemon_record_key(session_id)).

      _daemon_clear_if_stale(session_id: str) -> bool: record = _daemon_record_read(session_id). If None return False (nothing to clear). If is_holder_stale(record) DEL record key, return True. Else return False (live daemon exists).

    Public functions:
      _daemon_start(session_id: str) -> dict: Implements single-instance start. Step 1: _daemon_record_write(session_id). If it returns a dict (existing record), call is_holder_stale on it. If NOT stale: return {"status": "already_running", "pid": existing["pid"]}. If stale: _daemon_record_del(session_id) and retry Popen. Step 2 (Popen): binary = shutil.which("em-proj") or "em-proj". Popen([binary, "session", "listen", "--foreground"], stdin=DEVNULL, stdout=DEVNULL, stderr=DEVNULL, start_new_session=True, close_fds=True). Return {"status": "started", "pid": proc.pid}. Note: env=None (inherit parent env — EM_PROJ_REDIS_DB propagates automatically per RESEARCH Runtime State Inventory).

      _daemon_foreground_run(session_id: str) -> None: The long-lived daemon body. Invoked by session listen --foreground. Step 1: Call _daemon_record_write(session_id). If result is a dict (existing record) AND not is_holder_stale(result): sys.exit(0) (another daemon already registered — race condition guard per RESEARCH Pattern 3). If result is dict AND stale: _daemon_record_del(session_id) then re-call _daemon_record_write(session_id) (proceed). Step 2: cmd_client = get_client(). ps = cmd_client.pubsub(ignore_subscribe_messages=True). ps.subscribe(f"msg:{session_id}"). Step 3: SIGTERM handler — _shutdown = False; def _on_sigterm(*_): nonlocal _shutdown; _shutdown = True; signal.signal(signal.SIGTERM, _on_sigterm). Step 4: last_heartbeat = time.monotonic(). interval = int(os.environ.get("EM_PROJ_DAEMON_HEARTBEAT_INTERVAL", "60")). Poll loop while not _shutdown: msg = ps.get_message(timeout=5.0). If msg is not None: pass (log/no-op — DAEMON-02 system-level: message is already in mailbox). now = time.monotonic(); if now - last_heartbeat >= interval: try session_heartbeat(); last_heartbeat = now. except SessionNotFound: break (session reaped externally — clean exit per RESEARCH Pitfall 6). Step 5: Clean shutdown after loop: ps.unsubscribe(); ps.close(); _daemon_record_del(session_id). sys.exit(0).

    The function _daemon_foreground_run must NOT call mbox_write under any circumstance. The phrase "mbox_write" must not appear anywhere in this file.
  </action>

  <verify>
    <automated>scripts/test.sh unit -k daemon --tail 30</automated>
    No unit tests exist yet for daemon (test file is created in Task 3). This command should collect 0 daemon tests and exit 0 (no failures). Structural tests targeting _daemon.py pass once the file exists.
  </verify>

  <done>
    src/em_proj/session/_daemon.py exists with DAEMON_KEY_PREFIX, DAEMON_HEARTBEAT_INTERVAL, LUA_DAEMON_WRITE_OR_DETECT, _daemon_start, _daemon_foreground_run, _daemon_record_write, _daemon_record_read, _daemon_record_del, _daemon_clear_if_stale; no mbox_write call anywhere in the file; subprocess, signal, shutil imported; typer not imported.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire session listen verb (start + --foreground) into session/__init__.py + create structural shape test</name>
  <files>src/em_proj/session/__init__.py, tests/structural/test_phase_11_shape.py</files>

  <read_first>
    - src/em_proj/session/__init__.py full file — read existing verb patterns (register_cmd, heartbeat_cmd, list_cmd, show_cmd) to understand D-14 thin-wrapper shape; note existing imports from _ops and from output/redis_client; identify the exact insertion point for the new import from _daemon and the new command functions
    - src/em_proj/session/_ops.py lines 40-42 — the "Prohibited imports" docstring line that structural tests assert; confirm it lists subprocess, multiprocessing, threading
    - tests/structural/test_phase_10_shape.py — full file; use as direct template for test_phase_11_shape.py structure (REPO_ROOT, file paths, ast.parse pattern, pytest.skip for planning worktree)
    - 11-RESEARCH.md Prohibited-Import Test Update section — exact list of structural assertions for Phase 11
  </read_first>

  <behavior>
    - session_app.command("listen") exists in session/__init__.py
    - session listen with no flags: calls _daemon_start, emits ok result, exits 0
    - session listen --foreground: calls _daemon_foreground_run directly (no Popen), blocks until SIGTERM
    - session listen emits {"status": "already_running", "pid": N} when daemon already live (idempotent caller)
    - test_daemon_module_exists: asserts _daemon.py file present
    - test_daemon_module_imports_subprocess: asserts "import subprocess" in _daemon.py source
    - test_daemon_module_not_import_typer: asserts "typer" not in import lines of _daemon.py
    - test_daemon_module_not_call_mbox_write: AST check — "mbox_write" not in called names in _daemon.py
    - test_session_ops_prohibits_subprocess: asserts "subprocess" not in _ops.py import lines
    - test_session_init_has_listen_stop_commands: asserts @session_app.command("listen") and @session_app.command("stop") present in __init__.py
    - test_daemon_key_prefix_constant: asserts DAEMON_KEY_PREFIX or "daemon:" literal in _daemon.py
  </behavior>

  <action>
    Step A — Extend src/em_proj/session/__init__.py:

    Add import at top of file (after existing _ops imports): from em_proj.session._daemon import _daemon_start, _daemon_foreground_run.

    Add @session_app.command("listen") function session_listen_cmd with two parameters:
      foreground: Annotated[bool, typer.Option("--foreground/--no-foreground", help="Run daemon in foreground (for testing/debugging). Default: detach as background process.")] = False
      json_flag: Annotated[bool | None, typer.Option("--json/--no-json", help=_JSON_HELP)] = None

    Docstring for session_listen_cmd: "Start the per-session listener daemon (detached background process). Double-start is idempotent. Use --foreground to run in the calling process (testing only). Exit codes: 0 = started or already running, 1 = Redis unreachable."

    Body (D-14 thin wrapper): json_mode = resolve_json_mode(json_flag). client = get_client(). die_if_redis_unreachable(client). session_id = resolve_session_id() — import resolve_session_id from em_proj.identity (already in scope via existing imports or add to _daemon import; check __init__.py's current imports first). If foreground: _daemon_foreground_run(session_id) — this blocks; no emit (daemon runs until SIGTERM). Else: result = _daemon_start(session_id); emit_ok(data=result, json_mode=json_mode).

    Note: resolve_session_id is already imported in _ops.py. For __init__.py, import it from em_proj.identity directly (do not reach through _ops re-exports unless already present).

    Also add a placeholder @session_app.command("stop") — the full implementation is Plan 11-02, but the structural test expects both verbs to be registered. The placeholder stop command: def session_stop_cmd() -> None: emit_ok(data={"status": "not_implemented"}, json_mode=resolve_json_mode(None)). Add a TODO comment referencing Plan 11-02. This satisfies the structural test without coupling plans.

    Step B — Create tests/structural/test_phase_11_shape.py:

    Module docstring: "Phase 11 structural invariants — file-presence, symbol-presence, prohibited-import, and no-mbox_write assertions. Each test is independently GREEN after Plan 11-01 (except test_session_init_has_listen_stop_commands which requires the stop verb from Plan 11-02 — but a stub stop verb in __init__.py satisfies it here)."

    Import block mirrors test_phase_10_shape.py: ast, Path, pytest, REPO_ROOT, PHASE_DIR (= REPO_ROOT / ".planning" / "phases" / "11-listener-daemon"), SESSION_DAEMON = REPO_ROOT / "src" / "em_proj" / "session" / "_daemon.py", SESSION_OPS = REPO_ROOT / "src" / "em_proj" / "session" / "_ops.py", SESSION_INIT = REPO_ROOT / "src" / "em_proj" / "session" / "__init__.py".

    Tests:
      test_daemon_module_exists: assert SESSION_DAEMON.exists()
      test_daemon_module_imports_subprocess: src = SESSION_DAEMON.read_text(); assert "import subprocess" in src
      test_daemon_module_not_import_typer: src = SESSION_DAEMON.read_text(); import_lines = [l.strip() for l in src.splitlines() if l.strip().startswith("import ") or l.strip().startswith("from ")]; assert not any("typer" in line for line in import_lines)
      test_daemon_module_not_call_mbox_write: AST parse SESSION_DAEMON; walk for ast.Call with ast.Attribute func; collect all .attr names; assert "mbox_write" not in called_attrs. Message: "_daemon.py must not call mbox_write — DAEMON-02 is satisfied at system level by send-time write in message/_ops.py"
      test_session_ops_prohibits_subprocess: src = SESSION_OPS.read_text(); import_lines as above; assert not any("subprocess" in line for line in import_lines)
      test_session_init_has_listen_stop_commands: src = SESSION_INIT.read_text(); assert '@session_app.command("listen")' in src; assert '@session_app.command("stop")' in src
      test_daemon_key_prefix_constant: src = SESSION_DAEMON.read_text(); assert "DAEMON_KEY_PREFIX" in src or '"daemon:"' in src or "'daemon:'" in src
      test_phase_11_summaries_exist: same pattern as test_phase_10_shape.py — skip if PHASE_DIR absent; find 11-*-PLAN.md; assert sibling 11-*-SUMMARY.md exists for each

    Commit each file separately with messages: "feat(11-01): add session listen --foreground verb wiring" and "test(11-01): add structural shape invariants for Phase 11"
  </action>

  <verify>
    <automated>scripts/test.sh structural -k phase_11 --tail 30</automated>
    All structural tests in test_phase_11_shape.py must pass (except test_phase_11_summaries_exist which skips until SUMMARY files exist). All 7 presence/invariant tests should be green.
    <automated>scripts/test.sh unit --tail 20</automated>
    Prior test suite still green (no regressions from __init__.py changes).
  </verify>

  <done>
    @session_app.command("listen") in session/__init__.py; @session_app.command("stop") stub in session/__init__.py; tests/structural/test_phase_11_shape.py exists with 8 tests all green (test_phase_11_summaries_exist skips); em-proj session listen --help shows listen and --foreground flag.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write multiprocess daemon tests (start, --foreground, heartbeat, message-liveness) and confirm all green</name>
  <files>tests/multiprocess/test_daemon_lifecycle.py</files>

  <read_first>
    - tests/multiprocess/test_session_registry.py lines 1-55 — module docstring and TEST-03 rationale; confirms design invariants (subprocess.Popen, communicate, EM_PROJ_REDIS_DB=15, distinct session_id per test)
    - tests/multiprocess/test_message_delivery.py lines 1-60 — pattern for registering a session via Python API before running CLI tests (same problem as daemon test: register keeps the test runner's stable pid alive)
    - tests/conftest.py lines 86-145 — full clean_db and multiproc_race fixture bodies; child_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB)}
    - src/em_proj/session/_daemon.py (just created) — DAEMON_KEY_PREFIX, _daemon_record_key(session_id) shape; confirm key = "daemon:" + session_id
    - 11-RESEARCH.md TEST-05 Harness Design section — full test list and expected behaviors; Pitfall 6 (orphaned daemon after FLUSHDB); Pitfall 5 (--foreground for testability)
    - 11-RESEARCH.md Heartbeat test challenge section — EM_PROJ_DAEMON_HEARTBEAT_INTERVAL=1 approach
  </read_first>

  <behavior>
    - test_daemon_foreground_starts_and_records_pid: start em-proj session listen --foreground as Popen child with EM_PROJ_DAEMON_HEARTBEAT_INTERVAL=1; verify daemon:<session_id> HASH exists in Redis with pid field == child process pid; send SIGTERM; wait for child to exit; verify HASH is DELeted
    - test_daemon_start_detaches: call em-proj session listen (no --foreground) as a Popen child with short timeout; child exits 0 (verb returns immediately); verify daemon:<session_id> HASH exists in Redis with a pid that is a valid running process; clean up by sending SIGTERM to the recorded pid
    - test_daemon_heartbeat_refreshes_session: register a session via Python API (session_register()); start em-proj session listen --foreground as Popen; wait 3 seconds (EM_PROJ_DAEMON_HEARTBEAT_INTERVAL=1 triggers heartbeats rapidly); read session HASH TTL from Redis; assert TTL > 250 (was refreshed, not decayed to ~297); send SIGTERM; cleanup
    - test_daemon_message_liveness: register two sessions via Python API (sender + recipient); start daemon --foreground for recipient; send directed message via CLI (em-proj message send --to <recipient_sid> "hello"); read recipient inbox via CLI (em-proj message inbox --json); assert message present with body "hello"; send SIGTERM to daemon; cleanup. This is the system-level DAEMON-02 proof: message in inbox despite daemon not writing it — the send-time mbox_write did the work.
    - test_daemon_stop_when_not_running: call em-proj session stop (from Plan 11-02 stub is OK; if not yet implemented, skip this test body with pytest.mark.skip noting Plan 11-02); verify exit 0 or graceful behavior
  </behavior>

  <action>
    Create tests/multiprocess/test_daemon_lifecycle.py.

    Module docstring: Explain this is the TEST-05 multiprocess harness for Phase 11 daemon lifecycle. Note the same design invariants as test_session_registry.py (subprocess.Popen, communicate, EM_PROJ_REDIS_DB=15, not multiprocessing.Process). Explain the --foreground pattern: tests start the daemon via em-proj session listen --foreground as a Popen child so they can control its lifetime and verify SIGTERM cleanup.

    Imports: import json, os, signal, subprocess, time; import pytest; import redis as redis_module; from tests.conftest import EM_PROJ_BIN, TEST_DB. Also: from em_proj.session._ops import session_register, SessionNotFound (for in-process registration with stable pid).

    Module-level helper _run_cli(args, session_id=None, timeout=10.0, env_overrides=None) -> subprocess.CompletedProcess: Runs EM_PROJ_BIN with args via subprocess.run. Builds child_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB), **(env_overrides or {})}. If session_id: adds CLAUDE_CODE_SESSION_ID=session_id. Returns completed process (stdout/stderr captured as text). Use subprocess.run (not Popen) for short-lived CLI calls.

    Module-level helper _start_foreground_daemon(session_id, heartbeat_interval=1) -> subprocess.Popen: Starts em-proj session listen --foreground as a Popen child. Builds env with EM_PROJ_REDIS_DB=15, CLAUDE_CODE_SESSION_ID=session_id, EM_PROJ_DAEMON_HEARTBEAT_INTERVAL=str(heartbeat_interval). Returns the Popen object. Caller is responsible for cleanup (send SIGTERM + communicate).

    Module-level helper _stop_foreground_daemon(proc: subprocess.Popen, timeout=5.0) -> None: Send proc.send_signal(signal.SIGTERM); proc.communicate(timeout=timeout). Tolerates TimeoutExpired by calling proc.kill() + proc.communicate().

    Module-level helper _daemon_hash(client, session_id) -> dict | None: client.hgetall(f"daemon:{session_id}"). Return None if empty, else raw dict (str values from Redis).

    Tests (all use @pytest.mark.usefixtures or accept clean_db fixture):

    test_daemon_foreground_starts_and_records_pid(clean_db): session_id = "test-daemon-foreground". Register session in-process (session_register() after setting CLAUDE_CODE_SESSION_ID in os.environ temporarily — or set it via env only for the daemon child). Start _start_foreground_daemon(session_id). time.sleep(0.5) (allow daemon to write HASH). raw = _daemon_hash(clean_db, session_id); assert raw is not None; assert int(raw["pid"]) == proc.pid. _stop_foreground_daemon(proc). time.sleep(0.3). raw_after = _daemon_hash(clean_db, session_id); assert raw_after == {} or raw_after is None (SIGTERM handler DELed the record).

    test_daemon_heartbeat_refreshes_session(clean_db): session_id = "test-daemon-heartbeat". Register session via Python API (session_register — set CLAUDE_CODE_SESSION_ID env temporarily). Get initial TTL: clean_db.ttl(f"state:session:{session_id}"). Start _start_foreground_daemon(session_id, heartbeat_interval=1). time.sleep(3) (3 heartbeats at 1s interval). ttl_after = clean_db.ttl(f"state:session:{session_id}"). assert ttl_after > 250 (heartbeat refreshed it toward 300; would be ~297 if decaying normally, proves refresh happened). _stop_foreground_daemon(proc).

    test_daemon_message_liveness(clean_db): sender_id = "test-daemon-sender", recipient_id = "test-daemon-recipient". Register both sessions via Python API (set env vars appropriately for each). Start _start_foreground_daemon(recipient_id). time.sleep(0.3). Send message via CLI: _run_cli(["message", "send", "--to", recipient_id, "hello-daemon"], session_id=sender_id). Read inbox via CLI: result = _run_cli(["message", "inbox", "--json"], session_id=recipient_id); assert result.returncode == 0; inbox = json.loads(result.stdout.strip()); assert len(inbox) >= 1; assert any(m.get("body") == "hello-daemon" for m in inbox). _stop_foreground_daemon(proc). This proves DAEMON-02 at system level: daemon was alive (session heartbeat kept it valid), send-time mbox_write wrote the message, inbox reflects it.

    test_daemon_stop_when_not_running(clean_db): pytest.mark.skip note: "session stop full implementation in Plan 11-02; this placeholder ensures test collection succeeds". OR: if the stub stop command is already in __init__.py, run _run_cli(["session", "stop"]), assert returncode == 0.

    Env management note: To set CLAUDE_CODE_SESSION_ID for in-process session_register calls, save/restore os.environ["CLAUDE_CODE_SESSION_ID"] around the call. Use try/finally for cleanup to avoid leaking state.

    Commit message: "test(11-01): add multiprocess daemon lifecycle tests (foreground, heartbeat, message-liveness)"
  </action>

  <verify>
    <automated>scripts/test.sh multiprocess -k daemon --tail 50</automated>
    test_daemon_foreground_starts_and_records_pid, test_daemon_heartbeat_refreshes_session, and test_daemon_message_liveness should all pass. test_daemon_stop_when_not_running either skips or passes with stub.
    <automated>scripts/test.sh all --tail 30</automated>
    Full suite green (structural + unit + multiprocess).
  </verify>

  <done>
    tests/multiprocess/test_daemon_lifecycle.py exists with 4 tests; scripts/test.sh multiprocess -k daemon exits 0 with all tests green (test_daemon_stop_when_not_running skips or passes); scripts/test.sh all exits 0 — no regressions from new imports.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI caller → session listen | --foreground flag, session_id from env var cross from invocation context to daemon process |
| daemon HASH → os.kill | pid value read from Redis before SIGTERM — must be validated as int before use |
| daemon HASH → process table | pid could be recycled after crash; proc_start_epoch + boot_id triple prevents wrong-pid signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | _daemon_record_read — pid used in is_holder_stale | mitigate | _daemon_record_read converts raw Redis string to int(raw["pid"]) before returning; is_holder_stale raises ValueError if pid is not int — surfaces immediately in tests |
| T-11-01-02 | Tampering | PubSub/EVAL connection sharing | mitigate | cmd_client = get_client() for EVAL (session_heartbeat); ps = cmd_client.pubsub() allocates separate pool connection; never call EVAL on ps — enforced by two-client design in _daemon_foreground_run |
| T-11-01-03 | Denial of Service | Daemon forks without EM_PROJ_REDIS_DB propagation — writes to prod db=0 in tests | mitigate | Popen with env=None (inherit parent env); test harness sets EM_PROJ_REDIS_DB=15 in os.environ before spawning; child inherits it automatically per RESEARCH Runtime State Inventory |
| T-11-01-04 | Tampering | _daemon_foreground_run called twice (race) — two daemons both proceed past Lua write | mitigate | Lua LUA_DAEMON_WRITE_OR_DETECT is atomic; second caller receives existing record; if is_holder_stale(existing) is False → sys.exit(0) immediately; race window is one Lua round-trip |
| T-11-01-05 | Denial of Service | Daemon crash without SIGTERM — leaves daemon HASH forever (no TTL) | accept | No TTL on daemon HASH is correct-by-design (RESEARCH Pattern 2); is_holder_stale detects stale record on next session listen call; Plan 11-02 implements the clear-and-restart path |
</threat_model>

<verification>
After completing all three tasks:
- scripts/test.sh structural -k phase_11 → exits 0, all 7 presence/invariant tests pass (test_phase_11_summaries_exist skips)
- scripts/test.sh multiprocess -k daemon → exits 0, all daemon lifecycle tests pass
- scripts/test.sh all → exits 0, full suite green
- grep in _daemon.py: "import subprocess", "import signal", "import shutil", "DAEMON_KEY_PREFIX", "def _daemon_start", "def _daemon_foreground_run" all present
- grep in _daemon.py: "mbox_write" absent
- grep in _daemon.py: "import typer" absent
- grep in _daemon.py: "import threading" absent
- em-proj session listen --help: shows listen command with --foreground flag
- em-proj session listen --foreground invoked and SIGTERM'd exits 0
</verification>

<success_criteria>
- src/em_proj/session/_daemon.py ships with all daemon ops, foreground loop, and Lua write-or-detect script
- session/__init__.py registers "listen" and "stop" (stub) commands on session_app
- DAEMON_KEY_PREFIX = "daemon:" — namespace distinct from state:*, mbox:*, topic:*
- Heartbeat cadence respects EM_PROJ_DAEMON_HEARTBEAT_INTERVAL env override (default 60, tests use 1)
- No mbox_write call anywhere in _daemon.py — DAEMON-02 satisfied at system level
- session/_ops.py: still free of subprocess/signal/shutil/threading imports (structural test green)
- scripts/test.sh all exits 0
</success_criteria>

<output>
After completion, create .planning/phases/11-listener-daemon/11-01-SUMMARY.md
</output>
