---
phase: 07-project-scoped-reservation-registry
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/em_proj/state/__init__.py
  - tests/unit/test_reserve_verbs.py
  - tests/multiprocess/test_reserve_race.py
  - tests/multiprocess/test_reserve_three_clones_list.py
autonomous: true
requirements:
  - RESERVE-02
  - RESERVE-03
  - RESERVE-04
  - RESERVE-05

must_haves:
  truths:
    - "src/em_proj/state/__init__.py exposes new typer commands `reserve` and `reserve-list`, and the existing `check` command accepts a new `--upstream` flag"
    - "The `reserve` verb refuses anonymous callers (CLAUDE_CODE_SESSION_ID unset or empty) with exit 1 and code='anonymous_claim'"
    - "When --workstream <name> is passed, the verb uses that name verbatim and does NOT call claim_check or prompt"
    - "When --workstream is NOT passed AND claim_check('workstream.active') returns a holder, the verb proceeds to the TTY-prompt path because Phase 6 does NOT store the workstream name in the holder's reason field (Q-H finding from 07-01-SUMMARY) — workstream-existence alone is insufficient"
    - "When workstream is unresolvable AND both sys.stdin.isatty() AND sys.stdout.isatty() are True, the verb prompts on stderr and reads from stdin"
    - "When workstream is unresolvable AND either stdin or stdout is NOT a TTY, the verb exits 1 with code='workstream_unresolved' and message containing 'workstream unresolved — set it via' (locked actionable copy)"
    - "The `reserve` verb canonicalizes the workstream name via validate_key (Pitfall §security workstream-injection mitigation) before passing to reserve_take"
    - "The `reserve-list` verb auto-resolves upstream_identity from cwd unless --upstream is passed; --category filters post-scan by area prefix-before-first-dot"
    - "The `check --upstream` flag routes the query into the reserve namespace (via reserve_check) instead of the claim namespace"
    - "tests/multiprocess/test_reserve_race.py spawns TWO subprocess.Popen children with per-child `cwd=` pointing at distinct tmp clone dirs containing identical fake .git/config — exactly one exits 0 and one exits 3; the loser's stderr contains the winner's workstream name"
    - "tests/multiprocess/test_reserve_three_clones_list.py spawns THREE subprocess.Popen processes with per-child cwd; clone-a takes a reservation, clones b and c both see it via reserve-list (SC#3 demo)"
    - "All multi-clone tests inject EM_PROJ_REDIS_DB=15 + distinct CLAUDE_CODE_SESSION_ID + per-child cwd= (Pitfall #5 + Pitfall #4 + Pitfall #6)"
    - "All multi-clone tests pass `--workstream test-ws` explicitly to bypass the TTY-prompt path (Pitfall #5)"
    - "bash scripts/test.sh unit -k reserve_verbs exits 0; bash scripts/test.sh multiprocess -k reserve exits 0"
  artifacts:
    - path: "src/em_proj/state/__init__.py"
      provides: "Verb wiring for reserve, reserve-list, check --upstream — typer commands with anonymous-refusal, Redis pre-check, workstream-resolution chain, anti-Pitfall-#4 aliasing"
      contains: "@state_app.command(\"reserve\")"
    - path: "tests/unit/test_reserve_verbs.py"
      provides: "CliRunner-based verb tests: JSON envelope shape, anonymous refusal, TTY prompt monkeypatch, non-TTY exit 1, --workstream override, --category filter, --upstream override on check"
      contains: "test_reserve_exits_0_with_json_output"
    - path: "tests/multiprocess/test_reserve_race.py"
      provides: "Two-clone race with per-child cwd= and fake .git/config — proves cross-clone serialization"
      contains: "_make_fake_clone"
    - path: "tests/multiprocess/test_reserve_three_clones_list.py"
      provides: "Three-clone SC#3 demo — reserve-list visible from sibling clones of the same upstream"
      contains: "test_three_clones_see_shared_reservation"
  key_links:
    - from: "src/em_proj/state/__init__.py reserve verb"
      to: "em_proj.state.reserve.reserve_take"
      via: "from em_proj.state.reserve import reserve_take, HeldByAnother as ReserveHeldByAnother, ReserveNotHeld, …"
      pattern: "reserve_take"
    - from: "src/em_proj/state/__init__.py reserve verb workstream fallback"
      to: "em_proj.state.claim.claim_check aliased as workstream_check"
      via: "from em_proj.state.claim import claim_check as workstream_check (Pitfall #4 alias)"
      pattern: "workstream_check"
    - from: "src/em_proj/state/__init__.py reserve / reserve-list verbs"
      to: "em_proj.identity.resolve_upstream_identity"
      via: "from em_proj.identity import resolve_upstream_identity, _canonicalize_upstream_url"
      pattern: "resolve_upstream_identity"
    - from: "tests/multiprocess/test_reserve_race.py"
      to: "subprocess.Popen with per-child cwd= AND per-child env="
      via: "Popen(cmd, env=child_env, cwd=str(clone_dir), stdout=PIPE, stderr=PIPE, text=True)"
      pattern: "cwd=str\\(clone"
---

<objective>
Wire the Plan 07-01 substrate into typer commands and prove cross-clone
correctness via multi-process subprocess.Popen tests with per-child `cwd=`
pointing at distinct fake clones of the same upstream.

Purpose:
  - RESERVE-02 + RESERVE-03 + RESERVE-04 + RESERVE-05 all live at the verb
    layer. The pure-ops module (Plan 07-01) is RESERVE-01..02 substrate;
    this plan is the user-facing surface.
  - Per RESEARCH §Architectural Responsibility Map (lines 84-92): the verb
    layer owns workstream resolution (TTY prompt, --workstream override,
    Phase 6 lookup), upstream_identity resolution from cwd, anonymous-
    refusal gating, Redis pre-check, and emit_*/emit_held_by_another
    output. reserve.py stays pure-ops and receives resolved values as
    parameters.
  - RESEARCH §Pitfall #4 mitigation: alias `claim_check as workstream_check`
    in the import block to prevent accidental shadowing with `reserve_check`.
  - Q-H finding (from 07-01-SUMMARY): Phase 6 does NOT pass `--reason
    <workstream-name>` to em-proj when claiming workstream.active. The
    workstream name is therefore NOT extractable from the claim holder's
    reason field. The verb MUST treat `claim_check('workstream.active')`
    as a presence-check only; workstream NAME resolution falls through to
    the TTY-prompt path even when Phase 6 has set a workstream. This is
    documented in RESEARCH Open Q-H "Risk if wrong" as graceful failure;
    not a bug.
  - Pitfall #6 mitigation: race tests use per-child `cwd=` pointing at
    distinct tmp clone dirs with identical fake .git/config files. A test
    that varies only env= (Phase 4-6 pattern) would produce a false-positive
    because both children would resolve the SAME upstream_identity from the
    SAME (test runner) cwd.

Output:
  - src/em_proj/state/__init__.py — extended with `reserve`, `reserve-list`
    verbs and a new `--upstream` flag on existing `check`.
  - tests/unit/test_reserve_verbs.py — CliRunner-based verb tests including
    monkeypatched TTY prompt + non-TTY exit-1 path.
  - tests/multiprocess/test_reserve_race.py — two-clone race with per-child
    cwd= proving Lua serialization across distinct cwds.
  - tests/multiprocess/test_reserve_three_clones_list.py — SC#3 demo:
    three clones, one reserves, the other two see the reservation.
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-RESEARCH.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-PATTERNS.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Plan 07-01 substrate (shipped before this plan executes) -->

From src/em_proj/state/reserve.py (Plan 07-01):
  TTL_DEFAULT: int = 1800
  MIN_TTL: int = 60
  MAX_TTL: int = 86400
  KEY_PREFIX: str = "state:reserve:"
  class HeldByAnother(Exception): code = "held_by_another"; holder: dict | None
  class ReserveNotHeld(Exception): code = "not_held"
  def reserve_take(area, *, upstream_identity, workstream, ttl=TTL_DEFAULT, reason=None) -> dict
  def reserve_release(area, *, upstream_identity) -> None
  def reserve_check(area, *, upstream_identity) -> dict
  def reserve_list_by_prefix(*, upstream_identity, mine=False, active=False, stale=False) -> list[dict]

From src/em_proj/identity.py (Plan 07-01):
  def resolve_upstream_identity(cwd: str | None = None) -> str
  def _canonicalize_upstream_url(raw: str) -> str | None

<!-- Existing verb-layer patterns (already shipped in state/__init__.py — read
     these first; do not re-implement) -->

From src/em_proj/state/__init__.py:
  resolve_json_mode(json_flag) -> bool   # CLI-05
  die_if_redis_unreachable(client)       # D-18
  emit_ok(data, json_mode)               # CLI-05
  emit_error(code, message, json_mode)   # CLI-05 — calls SystemExit
  emit_held_by_another(code, message, holder, json_mode)
  emit_not_found(message, json_mode)
  _JSON_HELP                             # --json/--no-json help string
  Anonymous refusal idiom (Phase 4 claim verb lines 475-476):
    if not os.environ.get("CLAUDE_CODE_SESSION_ID", "").strip():
        emit_error("anonymous_claim", "anonymous claims refused", json_mode=json_mode)
  Dual-isatty idiom (Phase 3 lock --warn lines 344-352):
    if not (sys.stdout.isatty() and sys.stdin.isatty()):
        emit_error("warn_requires_tty", "...", json_mode=json_mode)

<!-- Q-H FINDING — locked by 07-01-SUMMARY -->

Direct read of the patched sdk/dist/query/workstream.js (lines 220-229)
confirms Phase 6's claim invocation is:
  spawnSync('em-proj', ['state', 'claim', '--ttl', '1800', '--json',
                        'workstream.active'], { cwd: projectDir, ... })
No `--reason <name>` argument is passed. Therefore:
  claim_check("workstream.active")  → returns holder dict if a workstream
                                       is currently set in this clone
  holder["reason"]                  → ALWAYS None/"" (no name stored here)

The workstream NAME is stored ONLY in the legacy file
`<projectDir>/.planning/active-workstream` (gsd-sdk's setActiveWorkstream
file write — see workstream.js:263). Reading that file from em-proj would
be a Phase 6 boundary violation and is OUT OF SCOPE for Phase 7. The
Phase 7 design treats workstream-existence (the claim) as NOT-sufficient
for workstream-name resolution; the verb falls through to TTY prompt.

The TTY prompt has access to gsd-sdk; users who want zero-friction can
pass --workstream <name> explicitly. RESEARCH Open Q-H "Risk if wrong"
documents this as acceptable graceful failure.

<!-- Multi-clone test infrastructure (NEW for Phase 7) -->

Per RESEARCH §Pattern 5 lines 516-619, the multi-clone test pattern is:

  def _make_fake_clone(parent: Path, name: str, origin_url: str) -> Path:
      clone_dir = parent / name
      git_dir = clone_dir / ".git"
      git_dir.mkdir(parents=True)
      config = (
          '[remote "origin"]\n'
          f'\turl = {origin_url}\n'
          '\tfetch = +refs/heads/*:refs/remotes/origin/*\n'
      )
      (git_dir / "config").write_text(config)
      (git_dir / "HEAD").write_text("ref: refs/heads/main\n")
      return clone_dir

  proc = subprocess.Popen(
      cmd,
      stdout=subprocess.PIPE,
      stderr=subprocess.PIPE,
      text=True,
      env=child_env,
      cwd=str(clone_dir),   # ← Pitfall #6 mitigation — distinct per child
  )

Race test cmd MUST include `--workstream test-ws` to bypass TTY-prompt
(Pitfall #5 — subprocess stdin is a pipe, not a TTY, so the verb would
exit 1 otherwise).

<!-- Existing race-test invariants (Phase 4 carry-forward) -->

From tests/conftest.py:
  EM_PROJ_BIN: str = "em-proj"  (resolved on PATH)
  TEST_DB: int = 15

From tests/multiprocess/test_claim_race.py:
  - subprocess.Popen (NOT multiprocessing.Process — fork+exec macOS safety)
  - .communicate(timeout=) (NOT .wait() — pipe-buffer deadlock)
  - EM_PROJ_REDIS_DB=15 in every child env (never write prod db=0)
  - distinct CLAUDE_CODE_SESSION_ID per child (otherwise Lua refresh masks race)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire reserve / reserve-list / check --upstream verbs in src/em_proj/state/__init__.py + unit-test them</name>
  <files>src/em_proj/state/__init__.py, tests/unit/test_reserve_verbs.py</files>

  <read_first>
    - src/em_proj/state/__init__.py — full file (~700 lines). Note: imports block (lines 105-115) where claim.py exports are aliased; the `claim` verb (lines 439-505) as the structural template for the new `reserve` verb; the `check` verb (lines 552-584) as the template for the --upstream flag extension; the `claim-list` verb (lines 629-669) as the template for `reserve-list`; the dual-isatty pattern in lock --warn (lines 344-352).
    - src/em_proj/state/reserve.py — confirm exports from Plan 07-01 (TTL_DEFAULT, MIN_TTL, MAX_TTL, HeldByAnother, ReserveNotHeld, reserve_take, reserve_release, reserve_check, reserve_list_by_prefix).
    - src/em_proj/state/claim.py — confirm `claim_check` is exported (used for the workstream.active presence-check via the workstream_check alias).
    - src/em_proj/identity.py — confirm `resolve_upstream_identity` and `_canonicalize_upstream_url` are exported (Plan 07-01).
    - src/em_proj/state/kv.py — confirm `validate_key` and `ValidationError` are exported (used for workstream-name sanitization per RESEARCH §Security workstream-injection mitigation).
    - src/em_proj/output.py — confirm emit_ok / emit_error / emit_held_by_another / emit_not_found signatures (already in use).
    - tests/unit/test_claim_verbs.py — full file (~330 lines). Use as structural template for test_reserve_verbs.py. Note: CliRunner setup (lines 18-55), the anonymous-refusal monkeypatch pattern (lines 76-99), the JSON-envelope assertion pattern (lines 61-69).
    - 07-01-SUMMARY.md — Q-H finding (workstream name NOT in claim holder; presence-check insufficient).
    - 07-RESEARCH.md §"Example 3" (lines 745-839) for the reserve verb shape; §"Example 4" (lines 841-890) for reserve-list; §"Pattern 4" (lines 437-495) for _resolve_workstream; §"Pitfall 4" (lines 692-699) for the workstream_check aliasing.
  </read_first>

  <behavior>
    Verb shape — `em-proj state reserve <area> [--ttl N] [--reason TEXT] [--workstream NAME] [--json/--no-json]`:
      - Anonymous refusal: exit 1 / code "anonymous_claim" / message "anonymous reservations refused" when CLAUDE_CODE_SESSION_ID is unset or whitespace-only.
      - Redis pre-check: die_if_redis_unreachable before any reserve.py call.
      - Workstream resolution order (locked):
          (a) --workstream <name>: use verbatim after validate_key sanitization.
          (b) claim_check("workstream.active") via workstream_check alias — if it RAISES ClaimNotHeld, workstream is unset → fall to (c). If it RETURNS a holder, workstream is SET in this clone but the name is unknown (Q-H finding) → also fall to (c) because we cannot extract the name. (The presence check is documented in code via an explanatory comment but does NOT skip the prompt.)
          (c) TTY prompt path: if sys.stdin.isatty() AND sys.stdout.isatty() → write prompt to stderr ("Workstream is unset (or unresolvable) for this clone. Enter a workstream name (or press Enter to abort): "), readline from stdin, strip, validate via validate_key. Empty input → emit_error("workstream_unresolved", "empty workstream name; aborting reservation").
          (d) Non-TTY: emit_error("workstream_unresolved", "workstream unresolved — set it via `gsd-sdk query workstream.set <name>` or pass `--workstream <name>`").
      - Upstream identity: call resolve_upstream_identity() with no args (uses os.getcwd()).
      - reserve_take call: `reserve_take(area, upstream_identity=upstream, workstream=resolved_workstream, ttl=effective_ttl, reason=reason)`.
      - Success envelope (RESEARCH §Example 3 lines 828-838):
          emit_ok({"area": area, "upstream_identity": upstream, "workstream": resolved_workstream,
                   "ttl": effective_ttl, "claimed_at": holder["claimed_at"],
                   "expires_at": holder["expires_at"]}, json_mode=json_mode)
      - Conflict path (HeldByAnother): emit_held_by_another with holder dict; the message includes the winner's workstream field so a sibling clone learns "who has it AND in what workstream".

    Verb shape — `em-proj state reserve-list [--category NAME] [--upstream URL_OR_ID] [--json/--no-json]`:
      - Redis pre-check.
      - Resolve upstream: if --upstream given, canonicalize via _canonicalize_upstream_url; if that returns None (input is already canonical or invalid), fall back to the raw input. If --upstream NOT given, call resolve_upstream_identity().
      - Call reserve_list_by_prefix(upstream_identity=canonical).
      - If --category given, filter: keep holders where holder["area"].split(".", 1)[0] == category.
      - emit_ok({"upstream_identity": canonical, "items": holders}, json_mode=json_mode).
      - Exit 0 always (empty list is still 0).

    `check` verb extension — `em-proj state check <area> [--upstream URL_OR_ID] [--json/--no-json]`:
      - If --upstream NOT given: existing behavior unchanged (call claim_check; on ClaimNotHeld emit_not_found; on success emit_ok with holder).
      - If --upstream IS given: canonicalize via _canonicalize_upstream_url (fall back to raw on None). Call reserve_check(area, upstream_identity=canonical). On ReserveNotHeld emit_not_found. On success emit_ok({"area": area, "holder": holder}).

    Verb-test cases for tests/unit/test_reserve_verbs.py (mirror test_claim_verbs.py structure):
      - test_reserve_exits_0_with_json_output: pass --workstream test-ws --json, area "migrations.v200"; assert exit 0 + payload.data.area == "migrations.v200" + payload.data.workstream == "test-ws" + payload.data.upstream_identity is a string.
      - test_reserve_anonymous_refusal_exit_1: monkeypatch.delenv CLAUDE_CODE_SESSION_ID; assert exit 1; "anonymous reservations refused" substring.
      - test_reserve_workstream_flag_bypasses_resolution: even with no workstream.active claim, --workstream X succeeds; holder.workstream == "X".
      - test_reserve_tty_prompt_path: monkeypatch sys.stdin.isatty + sys.stdout.isatty to True; monkeypatch sys.stdin.readline to return "prompted-ws\n"; call reserve WITHOUT --workstream; assert exit 0 + payload.data.workstream == "prompted-ws".
      - test_reserve_nontty_exits_1_when_workstream_unset: ensure no workstream.active claim is held; monkeypatch sys.stdin.isatty → False; call WITHOUT --workstream; assert exit 1 + "workstream unresolved" substring.
      - test_reserve_phase_6_claim_set_but_name_unknown_still_prompts: take a claim on "workstream.active" (no --reason) via direct claim_take to simulate Phase 6 having set a workstream; monkeypatch TTY True + readline → "name-from-prompt\n"; call reserve WITHOUT --workstream; assert exit 0 + payload.data.workstream == "name-from-prompt" (the Phase 6 presence-check does NOT short-circuit the prompt — Q-H validation at the verb layer).
      - test_reserve_empty_tty_input_exits_1: TTY True; readline → "\n"; assert exit 1 + "empty workstream name" substring.
      - test_reserve_held_by_another_exit_3: take a reservation as session A, then monkeypatch CLAUDE_CODE_SESSION_ID to session B (re-instantiate CliRunner with fresh env) and attempt the same area+upstream; assert exit 3 + payload.data.holder.session_id == "A" AND payload.data.holder.workstream == "A's workstream".
      - test_reserve_list_returns_items_under_current_upstream: take 2 reservations under canonical "github.com:o/r" (via --upstream override on reserve verb if supported, OR via direct reserve_take in a setup step + monkeypatch resolve_upstream_identity in the verb); call reserve-list --upstream "git@github.com:o/r.git" --json; assert items length 2.
      - test_reserve_list_category_filter: take "migrations.v100" + "migrations.v200" + "db.5432" all under the same upstream; reserve-list --category migrations → items length 2; reserve-list --category db → items length 1.
      - test_reserve_list_upstream_override_canonicalizes: pass --upstream "git@github.com:o/r.git" (raw URL); the verb should canonicalize internally and return data.upstream_identity == "github.com:o/r".
      - test_check_with_upstream_routes_to_reserve_namespace: reserve_take a known area under upstream X; em-proj state check <area> --upstream X --json → exit 0 + holder fields visible. Then em-proj state check <area> --json (no --upstream) → exit 2 / not_held (claim namespace, distinct).
  </behavior>

  <action>
    Step 1 — Extend src/em_proj/state/__init__.py.

    Add to the import block at the top of the file:
      import sys  (may already be imported — verify)
      from em_proj.identity import resolve_upstream_identity, _canonicalize_upstream_url
      from em_proj.state.claim import claim_check as workstream_check  # Pitfall #4 alias
      from em_proj.state.reserve import (
          TTL_DEFAULT as RESERVE_TTL_DEFAULT,
          MIN_TTL as RESERVE_MIN_TTL,
          MAX_TTL as RESERVE_MAX_TTL,
          HeldByAnother as ReserveHeldByAnother,
          ReserveNotHeld,
          reserve_check,
          reserve_list_by_prefix,
          reserve_release,
          reserve_take,
      )

    (Note: `claim_check` is already imported under its own name for the existing `check` verb. The new `workstream_check` is an alias of the SAME function — Python allows multiple bindings. Pitfall #4 mitigation: code that LOOKS like a workstream lookup is unmistakably named.)

    Add a module-private helper `_resolve_workstream` per RESEARCH §Pattern 4 lines 445-493, with the Q-H adjustment (workstream-presence does NOT yield a name):

      def _resolve_workstream(workstream_arg: str | None, json_mode: bool) -> str:
          if workstream_arg:
              # Validate via validate_key (RESEARCH §Security workstream-injection mitigation).
              # If validation fails, ValidationError propagates → caller catches and emits.
              try:
                  validate_key(workstream_arg)
              except ValidationError as e:
                  emit_error(e.code, f"invalid --workstream value: {e.message}",
                             json_mode=json_mode)
              return workstream_arg

          # Q-H: Phase 6 (gsd-sdk patched workstream.js lines 220-229) claims
          # workstream.active WITHOUT passing --reason, so the holder's
          # `reason` field is ALWAYS empty even when Phase 6 has set a
          # workstream. Presence-check (workstream_check succeeds vs raises
          # ClaimNotHeld) tells us "a workstream is set" but NOT what its
          # name is. We fall through to the prompt regardless.
          # (Future Phase 7.1 could read .planning/active-workstream as a
          # fallback, but that is a cross-tool boundary violation deferred
          # by Phase 7's scope.)
          try:
              workstream_check("workstream.active")
              # If we reach here, a workstream IS set. We still cannot recover
              # the name — fall through to the prompt path. The user will be
              # prompted; they can either type the name or press ctrl-C.
          except ClaimNotHeld:
              pass
          except Exception:
              # Defensive: any other failure in the presence-check should
              # NOT block the user from reserving. Fall through to prompt.
              pass

          # TTY prompt path (D-07 dual-isatty)
          if sys.stdin.isatty() and sys.stdout.isatty():
              sys.stderr.write(
                  "Workstream is unset (or its name is unrecoverable) for "
                  "this clone. Enter a workstream name (or press Enter to "
                  "abort): "
              )
              sys.stderr.flush()
              answer = sys.stdin.readline().strip()
              if answer:
                  try:
                      validate_key(answer)
                  except ValidationError as e:
                      emit_error(e.code,
                                 f"invalid workstream name from prompt: {e.message}",
                                 json_mode=json_mode)
                  return answer
              emit_error("workstream_unresolved",
                         "empty workstream name; aborting reservation",
                         json_mode=json_mode)

          # Non-TTY: actionable error (locked copy)
          emit_error(
              "workstream_unresolved",
              "workstream unresolved — set it via "
              "`gsd-sdk query workstream.set <name>` "
              "or pass `--workstream <name>`",
              json_mode=json_mode,
          )
          # emit_error calls SystemExit; satisfy type-checkers:
          raise SystemExit(1)

    Add `@state_app.command("reserve")` per RESEARCH §Example 3 (lines 745-839), using `_resolve_workstream` for step (3) and `resolve_upstream_identity()` for step (4). Anonymous-refusal gate FIRST, Redis pre-check SECOND, workstream resolution THIRD, upstream resolution FOURTH, reserve_take FIFTH. The error message on HeldByAnother MUST include the holder's `workstream` field so siblings learn the contextual identity (Success Criterion #2 from ROADMAP).

    Add `@state_app.command("reserve-list")` per RESEARCH §Example 4 (lines 841-890). The --upstream override path uses `_canonicalize_upstream_url(upstream) or upstream` (fall back to raw input when canonicalization returns None — handles the case where the user already passed a canonical identity).

    Extend the existing `check` verb (find at lines 552-584): add a typer Option `upstream: Annotated[str | None, typer.Option("--upstream", help="Query the reserve namespace under this upstream identity (URL or canonical form) instead of the claim namespace.")] = None`. When `upstream` is set, route to reserve_check (canonicalize the input first); otherwise leave existing claim_check path unchanged.

    DO NOT introduce a separate `check-reserve` verb — RESEARCH Open Q-D locked the `--upstream` flag.

    Step 2 — Create tests/unit/test_reserve_verbs.py.

    Module docstring summarizing the 12 behavior cases enumerated in <behavior>. Reference RESERVE-02..05 + Pitfall #4 + Pitfall #5 + Q-H finding.

    Imports + CliRunner setup: copy verbatim from tests/unit/test_claim_verbs.py lines 18-55 (the try/except CliRunner instantiation handles the mix_stderr-removed-in-newer-typer case) plus the three autouse fixtures (reset_client, point_at_test_db, set_session_id).

    For the test_reserve_phase_6_claim_set_but_name_unknown_still_prompts test, set up the Phase 6 simulation directly via reserve.py's sibling — call em_proj.state.claim.claim_take("workstream.active") (no reason argument, mirroring Phase 6's argv) inside the test BEFORE running the reserve verb. This avoids depending on gsd-sdk being installed at test time.

    For the TTY-prompt monkeypatch tests: typer's CliRunner runs the verb in-process, so monkeypatching `sys.stdin.isatty` and `sys.stdout.isatty` via monkeypatch.setattr works as expected. ALSO monkeypatch `sys.stdin.readline` to return a fixed string (mimics what stdin would yield in an interactive run).

    For test_reserve_held_by_another_exit_3: take the first reservation in a setup block by directly calling em_proj.state.reserve.reserve_take (no need to invoke the verb), so the test focuses on the conflict path. Then change CLAUDE_CODE_SESSION_ID via monkeypatch + reset the redis client (`rc._reset_for_tests()`) and invoke the reserve verb with --workstream Y --json; assert exit 3 + correct holder JSON.

    For test_reserve_list_returns_items_under_current_upstream and the category-filter test, either: (a) use direct reserve_take in setup with upstream_identity="github.com:o/r" and then call reserve-list --upstream "github.com:o/r" --json — most straightforward; OR (b) monkeypatch em_proj.state.__init__.resolve_upstream_identity to return a fixed canonical form during the verb call. (a) is preferred for shape clarity.

    test_check_with_upstream_routes_to_reserve_namespace requires TWO em-proj state check invocations against the same area name but distinct namespaces — one with --upstream and one without. Set up both a claim and a reserve in the test fixture (different namespaces; same area name allowed because the prefixes are disjoint).

    Document at the top of the test file in a comment block: "Q-H validation lives in test_reserve_phase_6_claim_set_but_name_unknown_still_prompts. If a future Phase 7.x stores the workstream name in the claim holder, that test SHOULD start failing (the workstream value would no longer come from the prompt) — that failure is the signal to re-litigate the design, not to skip the test."
  </action>

  <verify>
    <automated>bash scripts/test.sh unit -k reserve_verbs</automated>
  </verify>

  <done>
    - src/em_proj/state/__init__.py exports two new typer commands and a new flag; imports `claim_check as workstream_check` (Pitfall #4 mitigation)
    - `bash scripts/test.sh unit -k reserve_verbs` exits 0 (all 12 behavior cases pass)
    - The TTY-prompt test passes (monkeypatched stdin readline + dual-isatty)
    - The non-TTY exit-1 test passes with the exact "workstream unresolved" substring
    - The Q-H validation test passes: even with workstream.active CLAIMED, the verb prompts (Phase 6 presence-check does NOT short-circuit the prompt)
    - `bash scripts/test.sh unit` exits 0 (no regression to Phase 4/5 claim/check verb tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/multiprocess/test_reserve_race.py (two-clone race with per-child cwd=)</name>
  <files>tests/multiprocess/test_reserve_race.py</files>

  <read_first>
    - tests/multiprocess/test_claim_race.py — full file. Lines 1-39 (module docstring + invariants), lines 40-94 (imports + helpers _project_hash/_claim_key/_redis_client), lines 102-179 (the canonical two-session race pattern). This IS the structural template.
    - tests/multiprocess/test_workstream_consumer_race.py — lines 122-200 (the per-child env injection block AND the cwd= kwarg patterns — Phase 6 used cwd= but only for gsd-sdk's --cwd consumption, not as the upstream_identity discriminator).
    - tests/conftest.py — confirm EM_PROJ_BIN, TEST_DB, clean_db fixture, redis_precheck autouse fixture (already shipping).
    - 07-RESEARCH.md §"Pattern 5" (lines 497-619) — full canonical race-test code shape including _make_fake_clone helper and the post-race reserve-list readback assertion.
    - 07-RESEARCH.md §"Pitfall 5" (lines 700-708) — distinct CLAUDE_CODE_SESSION_ID per child; --workstream test-ws explicit on cmd.
    - 07-RESEARCH.md §"Pitfall 6" (lines 710-717) — per-child cwd= is REQUIRED; without it both children resolve the test-runner's cwd and produce false positives.
  </read_first>

  <behavior>
    Test 1 — test_two_clones_race_reserve_one_wins:
      Two fake clones (clone-a, clone-b) under tmp_path with IDENTICAL origin URL but DISTINCT cwds. Per-child env injects EM_PROJ_REDIS_DB=15 + distinct CLAUDE_CODE_SESSION_ID. cmd includes `--workstream test-ws` to bypass TTY prompt (Pitfall #5). Tight Popen launch loop; .communicate(timeout=15.0) each. Sorted exit codes == [0, 3]. The loser's stdout JSON contains data.holder.session_id == the WINNER's session_id AND data.holder.workstream == "test-ws" AND data.holder.upstream_identity == "github.com:emonical/roleplay-engine".

    Test 2 — test_reserve_list_visible_from_other_clone_after_race:
      After Test 1's race (this test is structured as a setup-then-readback within one test function, NOT a dependency on Test 1's state), invoke `em-proj state reserve-list --json` from clone-a's cwd (the OTHER child can be used too). Assert: items array has length 1; items[0]["area"] == "migrations.v200"; items[0]["upstream_identity"] == "github.com:emonical/roleplay-engine"; items[0]["session_id"] equals one of the two race-children's session IDs.

    Test 3 — test_two_clones_same_session_refresh_does_not_conflict:
      Two clones, SAME CLAUDE_CODE_SESSION_ID (refresh path); each takes the same area+workstream. Both exit 0 (one "taken", one "refreshed"). No held_by_another. Validates that the Lua refresh-or-take's compare-on-(session_id, upstream_identity) works ACROSS clones (the whole point of Phase 7's upstream-anchored compare).

    All three tests inject EM_PROJ_REDIS_DB=15 + per-child cwd= + the --workstream flag explicitly.
  </behavior>

  <action>
    Create tests/multiprocess/test_reserve_race.py mirroring test_claim_race.py.

    Module docstring per test_claim_race.py lines 1-39 with the Phase-7-specific addendum:

      """Multi-process race tests for `em-proj state reserve` (Plan 07-02).

      Uses real em-proj subprocess invocations against db=15 (via
      EM_PROJ_REDIS_DB injection) to prove cross-clone reserve serialization.

      Design invariants (Phase 1 RESEARCH Pitfalls, carried forward):
        - subprocess.Popen NOT multiprocessing.Process (#6 — macOS fork+exec)
        - .communicate(timeout=) NOT .wait() (#2 — pipe-buffer deadlock)
        - EM_PROJ_REDIS_DB=15 in child env (#4 — never writes prod db=0)

      Phase-7-specific invariants (07-RESEARCH §Pitfalls 5+6):
        - Per-child cwd= pointing at distinct tmp clones with IDENTICAL fake
          .git/config (Pitfall #6 — env-only variation produces false-positive
          passes because both children resolve the SAME upstream_identity
          from the SAME (test runner) cwd).
        - cmd includes `--workstream test-ws` explicitly to bypass the verb's
          TTY-prompt path (Pitfall #5 — subprocess stdin is a pipe, not a
          TTY; without --workstream the verb would exit 1).
        - Distinct CLAUDE_CODE_SESSION_ID per child for the race test
          (otherwise Lua refresh path masks the conflict — Phase 4 pitfall
          #4 carry).

      Import pattern (per Phase 1 Plan 04 SUMMARY):
        from tests.conftest import EM_PROJ_BIN, TEST_DB
      """

    Imports:
      from __future__ import annotations
      import json
      import os
      import subprocess
      from pathlib import Path

      import pytest
      import redis as redis_module

      from tests.conftest import EM_PROJ_BIN, TEST_DB

    Helper `_make_fake_clone` per RESEARCH §Pattern 5 lines 516-539 — copy verbatim. (This helper is duplicated in Task 3's test file per the project's "self-contained tests" convention.)

    Test 1 implementation per RESEARCH §Pattern 5 lines 542-618:
      origin = "git@github.com:emonical/roleplay-engine.git"
      clone_a = _make_fake_clone(tmp_path, "clone-a", origin)
      clone_b = _make_fake_clone(tmp_path, "clone-b", origin)

      child_a_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB),
                     "CLAUDE_CODE_SESSION_ID": "reserve-race-A"}
      child_b_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB),
                     "CLAUDE_CODE_SESSION_ID": "reserve-race-B"}

      cmd = [
          EM_PROJ_BIN, "state", "reserve",
          "--ttl", "60",
          "--workstream", "test-ws",
          "--reason", "race test",
          "--json",
          "migrations.v200",
      ]

      # Tight Popen launch loop — no sleep between spawns. This IS the race.
      proc_a = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.PIPE,
                                text=True, env=child_a_env, cwd=str(clone_a))
      proc_b = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.PIPE,
                                text=True, env=child_b_env, cwd=str(clone_b))

      out_a, err_a = proc_a.communicate(timeout=15.0)
      out_b, err_b = proc_b.communicate(timeout=15.0)

      exit_codes = sorted([proc_a.returncode, proc_b.returncode])
      assert exit_codes == [0, 3], (...)

      # Identify winner + loser
      winner_rc = 0
      losers_json = [out for (rc, out) in [(proc_a.returncode, out_a),
                                            (proc_b.returncode, out_b)]
                     if rc == 3]
      winners_json = [out for (rc, out) in [(proc_a.returncode, out_a),
                                             (proc_b.returncode, out_b)]
                      if rc == 0]
      assert len(winners_json) == 1 and len(losers_json) == 1

      winner_payload = json.loads(winners_json[0])
      loser_payload = json.loads(losers_json[0])

      assert winner_payload["data"]["upstream_identity"] == \
          "github.com:emonical/roleplay-engine"
      assert winner_payload["data"]["workstream"] == "test-ws"

      # The loser's holder dict surfaces the winner's workstream — this is
      # the ROADMAP Phase 7 Success Criterion #2 assertion.
      assert "holder" in loser_payload["data"]
      holder = loser_payload["data"]["holder"]
      assert holder["workstream"] == "test-ws"
      assert holder["upstream_identity"] == "github.com:emonical/roleplay-engine"
      assert holder["session_id"] in ("reserve-race-A", "reserve-race-B")

    Test 2 implementation — reserve-list readback after a successful take:
      Set up two fake clones; have ONE clone take a reservation via subprocess
      (cmd identical to Test 1 but only one Popen call). Then invoke
      `em-proj state reserve-list --json` from the OTHER clone's cwd. Assert
      items[0] reflects the winning clone's reservation, including the
      upstream_identity, workstream, and area fields.

      list_cmd = [EM_PROJ_BIN, "state", "reserve-list", "--json"]
      list_result = subprocess.run(list_cmd, capture_output=True, text=True,
                                   env=child_b_env, cwd=str(clone_b),
                                   timeout=10.0)
      assert list_result.returncode == 0
      list_payload = json.loads(list_result.stdout)
      items = list_payload["data"]["items"]
      assert len(items) == 1
      assert items[0]["area"] == "migrations.v200"
      assert items[0]["upstream_identity"] == "github.com:emonical/roleplay-engine"

    Test 3 implementation — same-session refresh across clones:
      Both children use the SAME CLAUDE_CODE_SESSION_ID. Both exit 0. No
      held_by_another. The reserve-list readback from either clone shows
      exactly ONE reservation (the same key, just refreshed by both).

    Use the clean_db fixture parameter for all three tests. No autouse
    fixtures beyond those in conftest.py.

    Defensive cleanup at the end of each test: invoke `em-proj state release
    migrations.v200 --upstream <origin>` once — wait, the release verb takes
    --upstream? It does NOT in the current scope (Plan 07-02 wires reserve /
    reserve-list / check --upstream, but NOT release --upstream — Phase 7
    scope decision). Cleanup falls to clean_db's FLUSHDB. Document this in
    a comment so future readers understand why no explicit release.
  </action>

  <verify>
    <automated>bash scripts/test.sh multiprocess -k test_reserve_race</automated>
  </verify>

  <done>
    - tests/multiprocess/test_reserve_race.py exists with 3 test functions
    - Each test injects EM_PROJ_REDIS_DB=15 in every child env (Pitfall #5)
    - Each test uses per-child cwd= pointing at distinct fake clones (Pitfall #6)
    - cmd always includes `--workstream test-ws` (Pitfall #5)
    - The race test asserts the loser's holder dict carries the winner's workstream field (ROADMAP SC#2)
    - bash scripts/test.sh multiprocess -k test_reserve_race exits 0
    - bash scripts/test.sh multiprocess exits 0 (no regression to Phases 1-6 multi-process tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/multiprocess/test_reserve_three_clones_list.py (SC#3 demo)</name>
  <files>tests/multiprocess/test_reserve_three_clones_list.py</files>

  <read_first>
    - tests/multiprocess/test_reserve_race.py — just-written file from Task 2. Copy the imports and the _make_fake_clone helper verbatim (self-contained convention).
    - tests/multiprocess/test_workstream_clobber_demo.py — the SC#3 demo pattern from Phase 6 (lines 1-60 in particular for the "this file IS the SC#3 demo" framing).
    - 07-RESEARCH.md §"Ready for Planning" reference to a three-clone test (test_reserve_three_clones_list.py is called out as "optional but recommended for SC#3 demo" in §Pattern 5 line 650; 07-VALIDATION.md task 07-NN-08 explicitly requires it — so this plan implements it).
  </read_first>

  <behavior>
    Test 1 — test_three_clones_see_shared_reservation (SC#3 demo):
      Setup: three fake clones (clone-a, clone-b, clone-c) under tmp_path, ALL with the same origin URL "git@github.com:emonical/roleplay-engine.git". Each gets the fake .git/config via _make_fake_clone.
      Step 1: clone-a invokes `em-proj state reserve migrations.v200 --workstream ws-a --json` via subprocess.run (NOT Popen — we want to wait for completion). cwd=str(clone_a), env injects CLAUDE_CODE_SESSION_ID="three-clones-A" + EM_PROJ_REDIS_DB=15. Assert exit 0.
      Step 2: clone-b invokes `em-proj state reserve-list --json` via subprocess.run. cwd=str(clone_b), env distinct session-id, EM_PROJ_REDIS_DB=15. Assert exit 0 and the items array has length 1 with area "migrations.v200" and upstream_identity "github.com:emonical/roleplay-engine".
      Step 3: clone-c does the SAME readback. Assert IDENTICAL items content as clone-b.
      The assertion that B and C see identical content IS the SC#3 proof — sibling clones of the same upstream coordinate via the shared upstream_identity namespace.

    Test 2 — test_three_clones_distinct_areas_grouped_correctly:
      Same three clones, same upstream URL. clone-a reserves "migrations.v200"; clone-b reserves "db.5432"; clone-c reserves "migrations.v201". Then from ANY clone, invoke reserve-list --json → items has length 3. Invoke reserve-list --category migrations --json → items has length 2. Invoke reserve-list --category db --json → items has length 1.

      This test exercises RESERVE-04 (category filter) under realistic multi-clone setup.
  </behavior>

  <action>
    Create tests/multiprocess/test_reserve_three_clones_list.py.

    Module docstring (mirror test_workstream_clobber_demo.py lines 1-27):

      """SC#3 demo: three sibling clones of the same upstream share a
      reservation namespace (Plan 07-02).

      This file IS the Phase 7 SC#3 human-runnable demo. Run via:

          bash scripts/test.sh multiprocess -k three_clones

      Three fake clones are set up under tmp_path, each with an IDENTICAL
      fake .git/config containing the same origin URL. One clone takes a
      reservation; the other two then invoke `em-proj state reserve-list`
      and BOTH see the reservation. The shared visibility IS the proof of
      ROADMAP Phase 7 Success Criterion #3.

      Design invariants:
        - subprocess.run with per-child cwd= (Pitfall #6 from 07-RESEARCH)
        - EM_PROJ_REDIS_DB=15 in every child env (Pitfall #5)
        - Distinct CLAUDE_CODE_SESSION_ID per child to avoid Lua-refresh
          masking ownership effects (Pitfall #4 carry)
        - --workstream <name> passed explicitly to every reserve invocation
          (Pitfall #5 — non-TTY subprocess would otherwise exit 1)

      Self-contained per project convention: _make_fake_clone is duplicated
      from test_reserve_race.py rather than imported from a shared module.

      Reference: ROADMAP Phase 7 Success Criteria #3.
      """

    Module-level skip pattern — NOT NEEDED for Phase 7's tests; em-proj is in-repo and on PATH for the test runner. (Phase 6 needed it because gsd-sdk is npm-installed; Phase 7 has no such cross-tool dependency.)

    Imports identical to test_reserve_race.py.

    Helper _make_fake_clone — DUPLICATED VERBATIM from test_reserve_race.py per the project's "structural tests are self-contained" convention (Plan 02-05 SUMMARY).

    Test 1 implementation:
      origin = "git@github.com:emonical/roleplay-engine.git"
      clone_a = _make_fake_clone(tmp_path, "clone-a", origin)
      clone_b = _make_fake_clone(tmp_path, "clone-b", origin)
      clone_c = _make_fake_clone(tmp_path, "clone-c", origin)

      def _env(session_id: str) -> dict:
          return {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB),
                  "CLAUDE_CODE_SESSION_ID": session_id}

      reserve_result = subprocess.run(
          [EM_PROJ_BIN, "state", "reserve", "--workstream", "ws-a",
           "--reason", "SC#3 demo", "--json", "migrations.v200"],
          capture_output=True, text=True, env=_env("three-clones-A"),
          cwd=str(clone_a), timeout=10.0,
      )
      assert reserve_result.returncode == 0, (
          f"clone-a's reserve should succeed; got rc={reserve_result.returncode}, "
          f"stderr={reserve_result.stderr[:200]!r}"
      )

      def _list(clone_dir: Path, session_id: str) -> list[dict]:
          r = subprocess.run(
              [EM_PROJ_BIN, "state", "reserve-list", "--json"],
              capture_output=True, text=True, env=_env(session_id),
              cwd=str(clone_dir), timeout=10.0,
          )
          assert r.returncode == 0, r.stderr
          return json.loads(r.stdout)["data"]["items"]

      items_b = _list(clone_b, "three-clones-B")
      items_c = _list(clone_c, "three-clones-C")

      assert len(items_b) == 1, items_b
      assert len(items_c) == 1, items_c
      assert items_b[0]["area"] == "migrations.v200"
      assert items_b[0]["upstream_identity"] == "github.com:emonical/roleplay-engine"
      assert items_b[0]["workstream"] == "ws-a"
      # Identical content from both sibling clones — THE proof of SC#3.
      assert items_b == items_c, (
          "Sibling clones must see identical reserve-list contents "
          "(shared upstream_identity namespace). Got "
          f"items_b={items_b}, items_c={items_c}"
      )

    Test 2 implementation: three reserves, three areas, then list assertions with --category filtering. Use subprocess.run with --category and parse data.items length.

    Use clean_db fixture for both tests. No multi-process race here (subprocess.run, not Popen) because the test sequences operations across clones rather than racing them — SC#3 is about visibility, not serialization.
  </action>

  <verify>
    <automated>bash scripts/test.sh multiprocess -k three_clones</automated>
  </verify>

  <done>
    - tests/multiprocess/test_reserve_three_clones_list.py exists with 2 test functions
    - All subprocess invocations use per-child cwd= AND EM_PROJ_REDIS_DB=15 AND --workstream <name>
    - test_three_clones_see_shared_reservation asserts items_b == items_c (SC#3 proof)
    - test_three_clones_distinct_areas_grouped_correctly validates --category filter under realistic multi-clone setup
    - bash scripts/test.sh multiprocess -k three_clones exits 0
    - bash scripts/test.sh multiprocess exits 0 (no regression)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → reserve verb | area, --workstream, --reason are user-controlled strings; validate_key gates them |
| stdin → _resolve_workstream (TTY path) | User-typed workstream name; validate_key on the readline result |
| --upstream argv → _canonicalize_upstream_url | User-supplied URL; canonicalization rejects unparseable inputs |
| test_reserve_race.py children → Redis db=15 | Per-child EM_PROJ_REDIS_DB=15 injection ensures prod db=0 is untouched |
| test_reserve_race.py children → fake .git/config | tmp_path-scoped; the fake config has no execution surface (just text fields read by `git remote get-url`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | T (Tampering) | Workstream auto-resolution race (workstream changes between resolve and stamp) | accept | RESEARCH §Q-H locks the design: the verb resolves workstream BEFORE the reserve_take Lua EVAL. A workstream change between resolve and EVAL would be reflected in a NEW reserve verb call, not a race within one call. Single-flight per-call semantics. |
| T-07-04 | T (Tampering) | TTY-prompt false positive in test envs | mitigate | The dual-isatty check (sys.stdin.isatty() AND sys.stdout.isatty()) is the SAME pattern Phase 3 D-07 uses. Test infrastructure that simulates TTY (monkeypatched isatty + monkeypatched readline) is explicit; subprocess.Popen with default pipe stdin reports False for stdin.isatty(), exercising the non-TTY exit-1 path correctly. |
| T-07-05 | T (Tampering) | Verb-level argv injection via reservation area names OR workstream values | mitigate | area is validated by validate_key inside reserve_take (Plan 07-01 substrate). workstream value (both --workstream argument and TTY readline result) is validated by validate_key inside _resolve_workstream before reserve_take is called. RESEARCH §Security workstream-injection-via-prompt-response mitigation lines 1147. |
| T-07-06 | T (Tampering) | Multi-clone fixture cwd-leakage produces false-pass | mitigate | Every Popen and subprocess.run call in test_reserve_race.py and test_reserve_three_clones_list.py passes cwd=str(clone_X) where clone_X is a tmp_path-derived directory containing the fake .git/config. The structural test in Plan 07-03 asserts cwd= appears at every subprocess.Popen site (AST/source-grep) so this invariant is machine-enforced. |
| T-4-01-01 carry | Spoofing | CLAUDE_CODE_SESSION_ID env-controlled | accept | Same posture as Phase 4. Single-user single-machine target. |
| T-07-13 | Information Disclosure | Loser's holder dict exposes winner's session_id + workstream | accept | The whole POINT of RESERVE-02 success criterion #2 is that the loser sees the winner's workstream so they know who has it. Documented; not a leak. |
| T-07-14 | DoS | Reserve TTL infinite refresh by holder | mitigate | MAX_TTL = 86400 cap inherited from Plan 07-01 substrate. |
</threat_model>

<verification>
- bash scripts/test.sh unit -k reserve_verbs exits 0 (Task 1 — 12 test cases)
- bash scripts/test.sh multiprocess -k test_reserve_race exits 0 (Task 2 — 3 test functions)
- bash scripts/test.sh multiprocess -k three_clones exits 0 (Task 3 — 2 test functions)
- bash scripts/test.sh unit exits 0 (no regression to Phases 1-6 unit tests)
- bash scripts/test.sh multiprocess exits 0 (no regression to Phases 1-6 multi-process tests)
- Every multi-process test file source contains `cwd=str(clone` substring (Pitfall #6 — verified by structural test in Plan 07-03)
- Phase 6 read handlers (workstream.get, workstream.complete) are UNAFFECTED (Plan 07-02 only touches em-proj source, not gsd-sdk)
</verification>

<success_criteria>
- src/em_proj/state/__init__.py exposes `reserve` and `reserve-list` typer commands with the full RESERVE-02..05 contract
- check verb accepts `--upstream` and routes correctly between the two namespaces
- Workstream resolution chain matches Q-H finding (presence-check insufficient → fall through to prompt; the test test_reserve_phase_6_claim_set_but_name_unknown_still_prompts validates this)
- Two-clone race produces deterministic [0, 3] outcome with loser carrying winner's workstream field (ROADMAP SC#2)
- Three-clone SC#3 demo passes — sibling clones see identical reserve-list contents (ROADMAP SC#3)
- All race-test invariants from Phases 1-6 are preserved AND extended with per-child cwd= (Pitfall #6)
- RESERVE-02 (verb-level take), RESERVE-03 (reserve-list), RESERVE-04 (--category + --upstream), RESERVE-05 (TTY prompt + non-TTY exit 1) are delivered
</success_criteria>

<output>
After completion, create `.planning/phases/07-project-scoped-reservation-registry/07-02-SUMMARY.md`.

The SUMMARY must include:
  - Q-H validation status: the verb implementation confirms the 07-01-SUMMARY finding that Phase 6 does NOT store workstream name in the claim holder. The Q-H presence-check is documented in code via an explanatory comment block in `_resolve_workstream`. The test test_reserve_phase_6_claim_set_but_name_unknown_still_prompts pins this behavior; any future Phase 7.x that wants to extract the name (e.g., by reading `.planning/active-workstream`) should re-litigate via /gsd-discuss-phase.
  - The locked actionable error copy: "workstream unresolved — set it via `gsd-sdk query workstream.set <name>` or pass `--workstream <name>`". Plan 07-03's structural test should grep for this string in state/__init__.py to lock the wording.
  - Multi-clone test infrastructure: the _make_fake_clone helper is DUPLICATED across test_reserve_race.py and test_reserve_three_clones_list.py per project convention. If a third file needs it, the cost-of-extraction calculus should be re-litigated then, not now.
  - Plan 07-03 will write tests/structural/test_phase_07_shape.py + the SKILL.md extension + run bash scripts/verify-phase.sh 07. The structural test should assert: KEY_PREFIX disjointness, presence of the three Lua scripts in reserve.py, the @state_app.command decorators for reserve / reserve-list / --upstream, and per-child cwd= in every Popen call in tests/multiprocess/test_reserve_*.py.
</output>
