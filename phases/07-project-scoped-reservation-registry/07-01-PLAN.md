---
phase: 07-project-scoped-reservation-registry
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/em_proj/identity.py
  - src/em_proj/state/reserve.py
  - tests/unit/test_upstream_identity.py
  - tests/unit/test_reserve.py
autonomous: true
requirements:
  - RESERVE-01
  - RESERVE-02

must_haves:
  truths:
    - "src/em_proj/identity.py exports resolve_upstream_identity() and _canonicalize_upstream_url() (module-private)"
    - "resolve_upstream_identity() returns the canonical host:owner/repo string when `git -C <cwd> remote get-url origin` succeeds AND the output parses"
    - "resolve_upstream_identity() falls back to resolve_project_hash() on: git missing (FileNotFoundError), git timeout (5.0s), non-zero returncode, empty stdout, OR canonicalization returning None"
    - "_canonicalize_upstream_url handles all 13 rows of the RESEARCH §Pattern 1 input/output table (SSH, HTTPS, .git suffix, trailing slash, port, user-info, host case-insensitivity, owner/repo case-preservation, subgroup paths, empty, malformed)"
    - "src/em_proj/identity.py contains NO `import redis` and NO `import typer` (Redis-free invariant preserved)"
    - "src/em_proj/state/reserve.py exists with KEY_PREFIX = 'state:reserve:' and 7-field holder shape {session_id, project_hash, upstream_identity, workstream, reason, claimed_at, expires_at}"
    - "reserve.py exports reserve_take(area, upstream_identity, workstream, ttl, reason) with upstream_identity AND workstream as REQUIRED keyword arguments (verb resolves them; pure-op receives them)"
    - "reserve.py exports reserve_release(area, upstream_identity), reserve_check(area, upstream_identity), reserve_list_by_prefix(upstream_identity, mine, active, stale)"
    - "reserve.py defines LUA_RESERVE_REFRESH_OR_TAKE, LUA_RESERVE_COMPARE_AND_DELETE, LUA_RESERVE_CHECK as module-level string constants"
    - "Both Lua refresh and Lua compare-and-delete scripts authorize on (session_id, upstream_identity) — NOT (session_id, project_hash)"
    - "reserve_take returns a 7-key dict; reserve_check on a held reservation returns a 7-key dict; reserve.py exports HeldByAnother and ReserveNotHeld exceptions"
    - "reserve.py contains NO `import typer`, NO `import multiprocessing`, NO `import threading`, NO `import subprocess`"
    - "reserve.py source-text does NOT contain the literal 'state:claim:' and claim.py is NOT touched (two-namespace invariant)"
    - "bash scripts/test.sh unit -k upstream_identity exits 0"
    - "bash scripts/test.sh unit -k test_reserve exits 0"
  artifacts:
    - path: "src/em_proj/identity.py"
      provides: "Identity resolver extension — adds resolve_upstream_identity + _canonicalize_upstream_url"
      exports:
        - "resolve_upstream_identity"
        - "_canonicalize_upstream_url"
    - path: "src/em_proj/state/reserve.py"
      provides: "Pure-ops reservation module mirroring claim.py with upstream_identity namespace"
      exports:
        - "TTL_DEFAULT"
        - "MIN_TTL"
        - "MAX_TTL"
        - "KEY_PREFIX"
        - "HeldByAnother"
        - "ReserveNotHeld"
        - "reserve_take"
        - "reserve_release"
        - "reserve_check"
        - "reserve_list_by_prefix"
        - "LUA_RESERVE_REFRESH_OR_TAKE"
        - "LUA_RESERVE_COMPARE_AND_DELETE"
        - "LUA_RESERVE_CHECK"
    - path: "tests/unit/test_upstream_identity.py"
      provides: "Table-driven canonicalizer + resolver tests (Redis-free)"
      contains: "_canonicalize_upstream_url"
    - path: "tests/unit/test_reserve.py"
      provides: "Pure-ops reserve tests against Redis db=15 (mirror of test_claim.py)"
      contains: "test_reserve_take_fresh_area"
  key_links:
    - from: "src/em_proj/state/reserve.py"
      to: "src/em_proj/identity.py"
      via: "from em_proj.identity import resolve_session_id, resolve_project_hash, resolve_upstream_identity"
      pattern: "resolve_upstream_identity"
    - from: "src/em_proj/state/reserve.py"
      to: "src/em_proj/redis_client.py"
      via: "from em_proj.redis_client import get_client"
      pattern: "get_client"
    - from: "src/em_proj/identity.py:_canonicalize_upstream_url"
      to: "13-row test vector in tests/unit/test_upstream_identity.py"
      via: "@pytest.mark.parametrize"
      pattern: "parametrize"
    - from: "LUA_RESERVE_REFRESH_OR_TAKE"
      to: "Redis HSET (7 fields) + EXPIRE"
      via: "client.eval in reserve_take"
      pattern: "LUA_RESERVE_REFRESH_OR_TAKE"
    - from: "LUA_RESERVE_COMPARE_AND_DELETE"
      to: "Redis DEL guarded by session_id + upstream_identity match"
      via: "client.eval in reserve_release"
      pattern: "LUA_RESERVE_COMPARE_AND_DELETE"
---

<objective>
Land the two pure-code surfaces of Phase 7 — the upstream-identity resolver
and the reserve.py pure-ops module — plus their unit-test files. This plan
ships the SUBSTRATE that the verb layer (Plan 07-02) wires into typer
commands.

Purpose: RESEARCH §"Key insight" — Phase 7 is Phase 4 plus a new identity
namespace. The temptation to consolidate claim.py and reserve.py into one
parameterized module is the dangerous failure mode. This plan executes the
opposite: a structural mirror of claim.py with three named deltas
(KEY_PREFIX, holder shape, refresh-compare term) and a sibling identity
resolver that lives alongside the existing two.

Output:
  - src/em_proj/identity.py — extended with `resolve_upstream_identity()`
    and module-private `_canonicalize_upstream_url()` helper.
  - src/em_proj/state/reserve.py — NEW pure-ops module with KEY_PREFIX =
    "state:reserve:", 7-field holder, three Lua scripts, HeldByAnother and
    ReserveNotHeld exceptions, reserve_take/release/check/list_by_prefix
    public functions.
  - tests/unit/test_upstream_identity.py — table-driven canonicalizer tests
    + resolver fallback behavior (Redis-free).
  - tests/unit/test_reserve.py — pure-ops behavior tests against real Redis
    on db=15 (mirror of tests/unit/test_claim.py).
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-RESEARCH.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-PATTERNS.md
@/Users/emonical/projects/personal/ai-tools/em-proj/CLAUDE.md
</context>

<interfaces>
<!-- claim.py is the structural mirror for reserve.py. Reading claim.py once
     in full at task start is REQUIRED; do not re-read. Copy structure
     verbatim and apply the three named deltas. -->

From src/em_proj/state/claim.py (commit 0e6e319):
  KEY_PREFIX: str = "state:claim:"   # → reserve.py changes to "state:reserve:"
  TTL_DEFAULT: int = 1800            # → keep
  MIN_TTL: int = 60                  # → keep
  MAX_TTL: int = 86400               # → keep
  MAX_REASON_CHARS: int = 256        # → keep
  LUA_CLAIM_REFRESH_OR_TAKE (str)    # → LUA_RESERVE_REFRESH_OR_TAKE — see "Lua deltas" below
  LUA_CLAIM_COMPARE_AND_DELETE (str) # → LUA_RESERVE_COMPARE_AND_DELETE — ARGV[2] becomes upstream_identity
  LUA_CLAIM_CHECK (str)              # → LUA_RESERVE_CHECK — verbatim copy (read-only EXISTS+HGETALL)
  class HeldByAnother(Exception)     # → keep name; only default message changes ("claim" → "reservation")
  class ClaimNotHeld(Exception)      # → rename to ReserveNotHeld
  _build_redis_key(project_hash, area) -> str   # → (upstream_identity, area)
  _make_holder(area, reason, ttl) -> dict       # → (area, upstream_identity, workstream, reason, ttl) — 7 fields
  _hgetall_to_holder(raw) -> dict               # → 7 fields (add upstream_identity, workstream)
  _validate_reason(reason)                      # → verbatim copy
  _validate_ttl(ttl)                            # → verbatim copy
  claim_take(area, ttl, reason) -> dict         # → reserve_take(area, upstream_identity, workstream, ttl, reason)
  claim_release(area) -> None                   # → reserve_release(area, upstream_identity)
  claim_check(area) -> dict                     # → reserve_check(area, upstream_identity)
  claim_list_by_prefix(mine, active, stale) -> list[dict]
                                                # → reserve_list_by_prefix(upstream_identity, mine, active, stale)

From src/em_proj/identity.py (commit 81c094db):
  def resolve_session_id() -> str               # → existing, no change
  def resolve_project_hash() -> str             # → existing, no change — reserve_upstream_identity falls back to this

<!-- Lua deltas (RESEARCH §Pattern 3 lines 394-435) -->

LUA_RESERVE_REFRESH_OR_TAKE differs from LUA_CLAIM_REFRESH_OR_TAKE on three
axes:
  1. The HSET in the "take" branch writes 7 fields (insert upstream_identity
     at ARGV[3] and workstream at ARGV[4]; shift remaining 5 fields right).
  2. ARGV grows from [1..6] to [1..8].
  3. The refresh-guard reads (session_id, upstream_identity) — NOT
     (session_id, project_hash).

ARGV ordering for reserve_take (locked — Pitfall #3 mitigation):
  ARGV[1] = session_id          (caller's)
  ARGV[2] = project_hash        (caller's local cwd-derived value)
  ARGV[3] = upstream_identity   (caller's resolved canonical)
  ARGV[4] = workstream          (caller's resolved name)
  ARGV[5] = reason              (string or "")
  ARGV[6] = claimed_at          (float as string)
  ARGV[7] = expires_at          (float as string)
  ARGV[8] = ttl                 (int as string, for EXPIRE)

LUA_RESERVE_COMPARE_AND_DELETE: ARGV[2] is upstream_identity (not project_hash).
  ARGV[1] = session_id
  ARGV[2] = upstream_identity

<!-- Canonicalization input/output table (locked — RESEARCH §Pattern 1
     lines 320-333). This is the test vector. -->

| Input | Expected output |
|-------|-----------------|
| `git@github.com:emonical/roleplay-engine.git` | `github.com:emonical/roleplay-engine` |
| `git@github.com:emonical/roleplay-engine` | `github.com:emonical/roleplay-engine` |
| `https://github.com/emonical/roleplay-engine.git` | `github.com:emonical/roleplay-engine` |
| `https://github.com/emonical/roleplay-engine` | `github.com:emonical/roleplay-engine` |
| `https://github.com/emonical/roleplay-engine/` | `github.com:emonical/roleplay-engine` |
| `ssh://git@github.com/emonical/roleplay-engine.git` | `github.com:emonical/roleplay-engine` |
| `ssh://git@github.com:22/emonical/roleplay-engine.git` | `github.com:emonical/roleplay-engine` |
| `https://user:token@github.com/emonical/roleplay-engine.git` | `github.com:emonical/roleplay-engine` |
| `https://GitHub.COM/emonical/roleplay-engine` | `github.com:emonical/roleplay-engine` |
| `https://github.com/EMonical/RolePlay-Engine` | `github.com:EMonical/RolePlay-Engine` |
| `git@gitlab.example.com:org/sub/repo.git` | `gitlab.example.com:org/sub/repo` |
| `` (empty) | `None` |
| `not-a-url` | `None` |

<!-- Fake .git/config helper (NEW for Phase 7) -->

Minimal INI that satisfies `git -C <dir> remote get-url origin`:
  [remote "origin"]
  \turl = <url>
  \tfetch = +refs/heads/*:refs/remotes/origin/*

Plus a `.git/HEAD` file containing `ref: refs/heads/main\n` for safety.
Verified locally with git 2.43; documented in RESEARCH §Pattern 5 lines 516-539.

<!-- Test infrastructure (already shipped — do NOT extend conftest.py) -->

From tests/conftest.py (verbatim):
  EM_PROJ_BIN: str = "em-proj"
  TEST_DB: int = 15
  clean_db fixture — function-scoped FLUSHDB before+after each test
  redis_precheck fixture — autouse, skips if Redis unreachable

Test-local autouse fixtures (copy from tests/unit/test_claim.py:37-48):
  _reset_client_between_tests — rc._reset_for_tests() before/after each
  _point_at_test_db — monkeypatch EM_PROJ_REDIS_DB=15
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend identity.py with resolve_upstream_identity() + write tests/unit/test_upstream_identity.py</name>
  <files>src/em_proj/identity.py, tests/unit/test_upstream_identity.py</files>

  <read_first>
    - src/em_proj/identity.py — full file (single Read; ~310 lines). Note the module docstring conventions, the existing `resolve_session_id` (lines 106-122) and `resolve_project_hash` (lines 125-147) shapes, the T-3-01-03 security-rationale block (lines 39-46) that documents WHY shell-out for project_hash was rejected — Phase 7 explicitly addresses that surface for the new git-remote-get-url-origin path. Also note the "NO `import typer` ... NO redis_client import — identity is Redis-free" invariant (lines 5-13).
    - tests/unit/test_identity.py — full file (~150 lines). The Redis-free posture is the template — no clean_db fixture, no redis_precheck. Uses monkeypatch + tmp_path + plain assertions. Note the `monkeypatch.chdir(tmp_path)` idiom (line 72) — same pattern needed for upstream-identity tests.
    - .planning/phases/07-project-scoped-reservation-registry/07-RESEARCH.md §"Pattern 1" (lines 251-336) for the canonicalization regex shape AND the 13-row test vector; §"Pattern 2" (lines 338-392) for the resolver shape; §"Pitfall 2" (empty-stdout fallback); §"Pitfall 7" (no .git/ → returncode != 0 → fallback).
    - CLAUDE.md — test dispatcher conventions (`bash scripts/test.sh unit`).
  </read_first>

  <behavior>
    `_canonicalize_upstream_url(raw)` test cases — all 13 rows from the interfaces table above MUST be covered via `@pytest.mark.parametrize` with one row per id. Confidence test: the literal `github.com:EMonical/RolePlay-Engine` row PRESERVES owner/repo case while the `https://GitHub.COM/emonical/roleplay-engine` row LOWERCASES host — two independent properties verified by two adjacent rows.

    `resolve_upstream_identity()` test cases (Redis-free, uses monkeypatch.chdir + tmp_path + a fake .git/config writer helper):
      - test_resolve_upstream_identity_with_origin_returns_canonical:
          tmp_path/.git/config contains `[remote "origin"]\n\turl = git@github.com:emonical/repo.git\n` → returns "github.com:emonical/repo"
      - test_resolve_upstream_identity_with_https_origin_canonicalizes:
          fake config with https URL → returns the canonical form
      - test_resolve_upstream_identity_no_git_dir_falls_back_to_project_hash:
          tmp_path has NO .git/ → returncode != 0 → falls back to resolve_project_hash() (the cwd-as-dashes form)
      - test_resolve_upstream_identity_empty_origin_falls_back_to_project_hash:
          fake config with `url = ` (empty) → git returns 0 with empty stdout → falls back (Pitfall #2)
      - test_resolve_upstream_identity_unparseable_origin_falls_back:
          fake config with `url = not-a-url\n` → canonicalization returns None → falls back
      - test_resolve_upstream_identity_explicit_cwd_argument:
          call resolve_upstream_identity(cwd=str(tmp_path)) without monkeypatch.chdir — confirm the cwd kwarg is honored
      - test_resolve_upstream_identity_git_missing_falls_back:
          monkeypatch a fake PATH (containing no git binary) so subprocess.run raises FileNotFoundError → falls back. (If hard to simulate cleanly, ACCEPTABLE alternative: monkeypatch subprocess.run to raise FileNotFoundError directly via monkeypatch.setattr — document the choice in a comment.)
  </behavior>

  <action>
    Step 1 — Extend src/em_proj/identity.py.

    Add `import subprocess` at the top of the imports block (alongside `import os`). This is the FIRST subprocess import in identity.py; the T-3-01-03 docstring block already documents the rationale for why earlier resolvers REJECTED shell-out — extend that block to document why the upstream-identity resolver opts INTO subprocess.run with shell=False + argv list + timeout (per RESEARCH §Pattern 2 lines 354-391 + §Anti-Patterns line 623).

    Extend the module docstring with a new subsection titled "Phase 7 — upstream identity (Plan 07-01 additions — RESERVE-01):" mirroring the format of the existing "Stale-detection probe API (Plan 03-02 additions — IDENT-02):" subsection. Enumerate `resolve_upstream_identity` and `_canonicalize_upstream_url` as the two new public-ish surfaces (the underscore-prefixed helper is module-private but the docstring should mention both for grep traceability).

    Add the two compiled regexes per RESEARCH §Pattern 1 lines 271-285:
      `_SCP_FORM` for SCP-style (git@host:owner/repo[.git])
      `_URL_FORM` for URL-style (https/ssh/git protocols with optional port, optional user-info)
    Use the verbatim regex bodies in the RESEARCH (do not invent variants — they were debugged against the 13-row test vector).

    Add `_canonicalize_upstream_url(raw: str) -> str | None`:
      - Strip whitespace; if empty → return None.
      - Try _URL_FORM.match first (has explicit protocol); if no match try _SCP_FORM.match.
      - If no match: return None.
      - Extract groups "host" (lowercase via .lower()) and "path" (.strip("/") belt-and-braces).
      - Return `f"{host}:{path}"`.

    Add `resolve_upstream_identity(cwd: str | None = None) -> str` per RESEARCH §Pattern 2:
      - target_cwd = cwd if cwd is not None else os.getcwd().
      - Wrap subprocess.run in try/except (FileNotFoundError, subprocess.TimeoutExpired) → return resolve_project_hash().
      - Argv: ["git", "-C", target_cwd, "remote", "get-url", "origin"] (Anti-Patterns §line 623 mandates `git -C <cwd>` — do NOT use cwd= kwarg on subprocess instead).
      - subprocess.run kwargs: capture_output=True, text=True, timeout=5.0, check=False, shell=False (shell=False is the default but be explicit in code-review terms via a comment).
      - If result.returncode != 0: return resolve_project_hash().
      - raw = result.stdout.strip(); if not raw: return resolve_project_hash() (Pitfall #2).
      - canonical = _canonicalize_upstream_url(raw); if canonical is None: return resolve_project_hash().
      - Return canonical.

    Docstrings for both new functions follow the same shape as `resolve_project_hash` (lines 125-147 of the analog): "Strategy:" enumerated bullets, "Design choice:" block citing T-3-01-03 and explaining why subprocess is acceptable here (argv list + shell=False + timeout) where it was rejected for project_hash (no functional need at the time).

    DO NOT touch resolve_session_id, resolve_project_hash, or the stale-detection probes (Plan 03-02 surface). Only ADD the two new functions and update the module docstring.

    Step 2 — Create tests/unit/test_upstream_identity.py.

    Module-level docstring per the test_identity.py shape (lines 1-20 of analog). Enumerate the test inventory inline.

    Imports:
      from __future__ import annotations
      import subprocess
      from pathlib import Path
      import pytest
      from em_proj.identity import (
          resolve_upstream_identity,
          _canonicalize_upstream_url,
          resolve_project_hash,
      )

    Helper:
      def _make_fake_git_config(tmp_path: Path, origin_url: str) -> None:
          git_dir = tmp_path / ".git"
          git_dir.mkdir()
          config = (
              '[remote "origin"]\n'
              f'\turl = {origin_url}\n'
              '\tfetch = +refs/heads/*:refs/remotes/origin/*\n'
          )
          (git_dir / "config").write_text(config)
          (git_dir / "HEAD").write_text("ref: refs/heads/main\n")

    Parametrized canonicalizer test — use the 13-row table from <interfaces> verbatim:
      @pytest.mark.parametrize("raw,expected", [
          ("git@github.com:emonical/roleplay-engine.git", "github.com:emonical/roleplay-engine"),
          ... (12 more rows — one row per table entry)
      ], ids=["scp_with_git_suffix", "scp_no_suffix", "https_with_git", "https_no_suffix", "https_trailing_slash", "ssh_protocol", "ssh_with_port", "user_token_https", "host_lowercased", "owner_repo_case_preserved", "subgroup_path", "empty", "malformed"])
      def test_canonicalize_upstream_url_table(raw, expected):
          assert _canonicalize_upstream_url(raw) == expected

    Resolver tests — one function per <behavior> case. Use `monkeypatch.chdir(tmp_path)` to point os.getcwd() at the fake clone for the implicit-cwd tests; the explicit-cwd test passes `cwd=str(tmp_path)` and does NOT chdir.

    For the git-missing test: monkeypatch.setenv("PATH", "/nonexistent") at the START of the test so subprocess.run("git", ...) raises FileNotFoundError. If that proves flaky on dev machines where /usr/bin/git would still be on PATH via /etc/paths or shell init, ALTERNATIVELY use `monkeypatch.setattr("em_proj.identity.subprocess.run", lambda *a, **kw: (_ for _ in ()).throw(FileNotFoundError()))` — document the choice via an inline comment naming why.

    No conftest.py edits. No fixture additions. Module-level only.
  </action>

  <verify>
    <automated>bash scripts/test.sh unit -k upstream_identity</automated>
  </verify>

  <done>
    - src/em_proj/identity.py imports cleanly (no `import typer`, no `import redis`); contains `import subprocess`, `resolve_upstream_identity`, `_canonicalize_upstream_url`, `_SCP_FORM`, `_URL_FORM`
    - The module docstring's new "Phase 7 — upstream identity" subsection enumerates both new surfaces
    - tests/unit/test_upstream_identity.py exists; bash scripts/test.sh unit -k upstream_identity exits 0
    - All 13 parametrized canonicalizer rows pass
    - All 7 resolver behavior cases pass
    - No edits to resolve_session_id or resolve_project_hash (additive change only)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/em_proj/state/reserve.py + tests/unit/test_reserve.py</name>
  <files>src/em_proj/state/reserve.py, tests/unit/test_reserve.py</files>

  <read_first>
    - src/em_proj/state/claim.py — full file (~520 lines). Read once. This IS the structural mirror. Note: KEY_PREFIX line 66, TTL constants lines 70-79, the three Lua scripts lines 103-165, the two exception classes lines 173-216, the four private helpers lines 224-301, the four public functions lines 309-526.
    - tests/unit/test_claim.py — full file (~300 lines). This IS the structural mirror for tests/unit/test_reserve.py. Note the autouse fixtures lines 37-48, the module-constant tests lines 56-69, the 8 behavior cases starting line 77.
    - src/em_proj/identity.py — confirm `resolve_session_id` and `resolve_project_hash` exports (Task 1 added `resolve_upstream_identity` but reserve.py uses session_id + project_hash directly; upstream_identity is a parameter, not a resolver call inside reserve.py).
    - src/em_proj/redis_client.py — `get_client` pattern (verbatim re-use).
    - src/em_proj/state/kv.py — `validate_key` and `ValidationError` exports (re-used identically to claim.py).
    - 07-RESEARCH.md §Pattern 3 (lines 394-435) for the Lua deltas; §"Common Pitfalls" Pitfall #3 lines 683-690 (ARGV-index drift mitigation via _RESERVE_ARGV_ORDER constant + 7-field HGETALL assertion); §"Anti-Patterns" lines 621-634; §"Security Domain" lines 1133-1147 (which mitigations live in which Lua script).
    - 07-PATTERNS.md §"reserve.py" section (lines 84-261) for the line-by-line copy-and-delta map.
  </read_first>

  <behavior>
    `reserve_take(area, upstream_identity, workstream, ttl, reason)` test cases (mirror test_claim.py's 8 cases, expanded to verify the 7-field holder):
      - test_reserve_take_fresh_area_returns_holder_with_7_fields:
          reserve_take("foo", upstream_identity="github.com:o/r", workstream="ws-x") returns dict; set(holder.keys()) == {"session_id", "project_hash", "upstream_identity", "workstream", "reason", "claimed_at", "expires_at"}.
      - test_reserve_take_area_key_uses_upstream_identity_prefix (Pitfall #3 mitigation):
          After reserve_take("myarea", upstream_identity="x:y/z", workstream="ws"), confirm exact Redis key is `state:reserve:x:y/z:myarea` and hgetall on that key returns exactly 7 fields with the expected values. THIS test catches ARGV-index drift.
      - test_reserve_take_same_session_refreshes:
          same session_id + same upstream_identity → result == "refreshed" branch returns the holder (no exception); TTL > previous TTL.
      - test_reserve_take_different_session_raises_held_by_another:
          set session_id A, take; set session_id B (via monkeypatch CLAUDE_CODE_SESSION_ID), attempt take same area+upstream → raises HeldByAnother with holder.session_id == "A".
      - test_reserve_take_different_upstream_does_not_conflict:
          take("foo", upstream_identity="x:a/b"); take("foo", upstream_identity="x:c/d") → second call SUCCEEDS (different namespace, different key). Confirms the upstream_identity component of the key isolates reservations across upstreams.
      - test_reserve_release_by_holder_deletes_key:
          take, then release(area, upstream_identity=same) → Redis EXISTS == 0.
      - test_reserve_release_by_non_holder_raises_held_by_another:
          A takes; B attempts release(area, upstream_identity=same) → raises HeldByAnother.
      - test_reserve_release_absent_raises_held_by_another_with_holder_none:
          release on never-taken area → raises HeldByAnother(holder=None).
      - test_reserve_check_held_returns_7_field_holder:
          take, then check(area, upstream_identity=same) → holder dict with all 7 keys.
      - test_reserve_check_absent_raises_reserve_not_held:
          check on never-taken area → raises ReserveNotHeld.
      - test_reserve_list_by_prefix_returns_holders_for_upstream:
          take two areas under same upstream, take one under different upstream; reserve_list_by_prefix(upstream_identity="x:a/b") returns ONLY the two from upstream a/b. Each holder dict includes an injected "area" field.

    Module-constant tests (mirror test_claim.py lines 56-69):
      - test_ttl_default_is_1800 → assert TTL_DEFAULT == 1800
      - test_key_prefix_is_state_reserve → assert KEY_PREFIX == "state:reserve:"
      - test_ttl_bounds → assert MIN_TTL == 60 and MAX_TTL == 86400

    Two-namespace disjointness sanity test (NEW for Phase 7 — Pitfall #8 mitigation, structural-test analog at unit level):
      - test_reserve_does_not_collide_with_claim_for_same_area:
          run claim_take("foo") and reserve_take("foo", upstream_identity=..., workstream=...); BOTH succeed because their Redis keys are in disjoint prefixes. Validates the structural test's assertion at runtime.
  </behavior>

  <action>
    Step 1 — Create src/em_proj/state/reserve.py.

    Use claim.py as the line-by-line template. The patch is:
      - Module docstring: copy lines 1-51 of claim.py and replace "claim"→"reservation" prose. Document the upstream_identity namespacing (lines 14-17 of claim.py) and the 7-field holder shape (line 19-33 of claim.py extended). Reference Plan 07-01 (not 04-01). Document the two-namespace invariant (Pitfall #8): "this module's Redis keys are state:reserve:<upstream_identity>:<area>; the disjoint claim namespace lives in claim.py at state:claim:<project_hash>:<area>".

      - Imports (copy from claim.py lines 52-58 + add upstream_identity resolver):
          from __future__ import annotations
          import time
          from em_proj.identity import (
              resolve_session_id,
              resolve_project_hash,
              resolve_upstream_identity,  # NEW — Plan 07-01 Task 1
          )
          from em_proj.redis_client import get_client
          from em_proj.state.kv import validate_key, ValidationError  # noqa: F401

        NOTE: reserve.py imports `resolve_upstream_identity` for completeness/grep traceability, but does NOT call it internally — the verb layer (Plan 07-02) calls it and passes the result as the `upstream_identity` parameter. RESEARCH §Architectural Responsibility Map lines 84-86 locks this responsibility split.

      - Module constants — copy claim.py lines 60-80 verbatim EXCEPT change `KEY_PREFIX` to `"state:reserve:"`. Update the comment on line 64-65 to read "Phase 7 D-XX analog — two-segment prefix + upstream_identity + area. Full key: state:reserve:<upstream_identity>:<area>". Add a `_RESERVE_ARGV_ORDER` tuple constant immediately after KEY_PREFIX (Pitfall #3 mitigation):

          # Pitfall #3 (07-RESEARCH): ARGV-index drift between claim.py and reserve.py
          # is silent and damaging. This tuple documents the ARGV positions used by
          # LUA_RESERVE_REFRESH_OR_TAKE; the unit test in test_reserve.py asserts that
          # after a take, client.hgetall(key) returns EXACTLY these 7 fields.
          _RESERVE_ARGV_ORDER: tuple[str, ...] = (
              "session_id",          # ARGV[1]
              "project_hash",        # ARGV[2]
              "upstream_identity",   # ARGV[3]
              "workstream",          # ARGV[4]
              "reason",              # ARGV[5]
              "claimed_at",          # ARGV[6]
              "expires_at",          # ARGV[7]
              # ARGV[8] is the integer TTL passed to EXPIRE — not a holder field.
          )

      - Lua scripts — three module-level string constants:

          LUA_RESERVE_REFRESH_OR_TAKE: copy claim.py LUA_CLAIM_REFRESH_OR_TAKE (lines 103-124), apply Pattern 3 deltas:
            (a) HSET writes 7 fields in the order documented in _RESERVE_ARGV_ORDER (insert upstream_identity, workstream between project_hash and reason).
            (b) Refresh-guard: read HGET KEYS[1] "session_id" AND HGET KEYS[1] "upstream_identity" (NOT project_hash); compare ARGV[1] AND ARGV[3] (NOT ARGV[2]).
            (c) Refresh path: HSET KEYS[1] "expires_at" ARGV[7] + EXPIRE KEYS[1] ARGV[8] → return "refreshed".

          LUA_RESERVE_COMPARE_AND_DELETE: copy claim.py LUA_CLAIM_COMPARE_AND_DELETE (lines 139-149). Delta: the second compare term is `upstream` (HGET KEYS[1] "upstream_identity"), and ARGV[2] is upstream_identity. ARGV stays at 2 elements.

          LUA_RESERVE_CHECK: copy claim.py LUA_CLAIM_CHECK (lines 161-165) VERBATIM — no changes (it's a read-only EXISTS + HGETALL).

      - Exceptions: copy claim.py lines 173-216. KEEP the name `HeldByAnother` (same exception class semantic, different module). RENAME `ClaimNotHeld` → `ReserveNotHeld`. Default messages: "reservation held by session …" / "area is not reserved". The `code` strings stay "held_by_another" / "not_held" (the OUTPUT envelope uses the same codes; the user-facing wording differs only in the python message).

      - Private helpers:
          _build_redis_key(upstream_identity, area) → KEY_PREFIX + upstream_identity + ":" + area
          _make_holder(area, upstream_identity, workstream, reason, ttl) → 7-field dict; session_id and project_hash resolved via the identity helpers; upstream_identity and workstream taken from parameters; reason/claimed_at/expires_at computed as in claim.py.
          _hgetall_to_holder(raw) → 7-field dict; the two new fields (upstream_identity, workstream) are pass-through strings (no coercion, like project_hash already).
          _validate_reason and _validate_ttl → copy verbatim from claim.py lines 278-301.

      - Public functions:
          reserve_take(area, *, upstream_identity, workstream, ttl=TTL_DEFAULT, reason=None) → dict.
            Per claim.py lines 309-372 with the deltas: validate area+reason+ttl; build holder via _make_holder with the new fields; build redis_key from upstream_identity (not project_hash); EVAL LUA_RESERVE_REFRESH_OR_TAKE with the 8 ARGV positions per _RESERVE_ARGV_ORDER; on "taken" or "refreshed" return holder; on "conflict" HGETALL existing key (empty-guard from claim.py:365-372 verbatim) and raise HeldByAnother.

          reserve_release(area, *, upstream_identity) → None.
            Per claim.py lines 375-410. Build redis_key from upstream_identity. EVAL LUA_RESERVE_COMPARE_AND_DELETE with ARGV=[session_id, upstream_identity]. result == 1 → return; result == -1 → raise HeldByAnother(holder=None); result == 0 → HGETALL → raise HeldByAnother(holder=current).

          reserve_check(area, *, upstream_identity) → dict.
            Per claim.py lines 490-525. Build redis_key from upstream_identity. EVAL LUA_RESERVE_CHECK. If false/nil → raise ReserveNotHeld; else convert HGETALL pairs to dict and _hgetall_to_holder.

          reserve_list_by_prefix(*, upstream_identity, mine=False, active=False, stale=False) → list[dict].
            Per claim.py lines 413-487. SCAN MATCH `state:reserve:<upstream_identity>:*`. For each key: HGETALL, _hgetall_to_holder, inject the `area` field (everything after the upstream-identity segment of the key). Apply mine/active/stale filters identically to claim.py. CRITICAL: copy the CR-01 ttl=-2 guard from claim.py lines 472-475 (the "ttl returned -2 because the key vanished between SCAN and HGETALL" stale-filter dropout — VERBATIM).

      Use keyword-only `*,` syntax (per the function signatures above) so call sites in the verb layer (Plan 07-02) cannot positionally swap upstream_identity and workstream — a defensive measure against Pitfall #3-adjacent argument-order bugs.

      reserve.py must NOT import: typer, multiprocessing, threading, subprocess. (subprocess is allowed in identity.py but not in pure-ops modules — same discipline as claim.py.)

    Step 2 — Create tests/unit/test_reserve.py.

    Use tests/unit/test_claim.py as the line-by-line template. The patch:

      - Module docstring per test_claim.py lines 1-13; reference Plan 07-01, RESERVE-01..02, validates "the 11 behavior cases from 07-01-PLAN.md".

      - Imports + autouse fixtures: copy verbatim from test_claim.py lines 15-48 (the _reset_client_between_tests and _point_at_test_db autouse fixtures). Replace `from em_proj.state.claim import …` with `from em_proj.state.reserve import …`.

      - Module-constant tests (3 functions): copy test_claim.py lines 56-69 with KEY_PREFIX changed to "state:reserve:".

      - Behavior tests: implement the 11 cases listed in <behavior>. For each case, use the `clean_db` fixture as the first parameter (the fixture is autouse already via redis_precheck but explicit is better). For multi-session tests, monkeypatch the CLAUDE_CODE_SESSION_ID env var BEFORE each subsequent operation (the env var is read inside resolve_session_id at every call site — no caching).

      - For test_reserve_take_area_key_uses_upstream_identity_prefix (Pitfall #3 mitigation), use the direct-redis-client pattern from test_claim.py lines 92-102:
          import redis as redis_module
          test_client = redis_module.Redis(host="127.0.0.1", port=6379, db=15, decode_responses=True)
          key = f"{KEY_PREFIX}{upstream_identity}:{area}"
          raw = test_client.hgetall(key)
          assert set(raw.keys()) == {"session_id", "project_hash", "upstream_identity", "workstream", "reason", "claimed_at", "expires_at"}
          assert raw["upstream_identity"] == upstream_identity
          assert raw["workstream"] == workstream

      - For test_reserve_does_not_collide_with_claim_for_same_area, import claim_take/HeldByAnother from em_proj.state.claim AND reserve_take from em_proj.state.reserve. Take one of each on the same area name; assert both succeed and that Redis has TWO distinct keys (one with state:claim: prefix, one with state:reserve: prefix). This is the runtime mirror of the structural-test assertion in Plan 07-03.

      Test file ordering: module-constant tests first (fast), then take/refresh/conflict/release/check/list (slower because each does a Lua EVAL).
  </action>

  <verify>
    <automated>bash scripts/test.sh unit -k test_reserve</automated>
  </verify>

  <done>
    - src/em_proj/state/reserve.py exists; `from em_proj.state.reserve import reserve_take, reserve_release, reserve_check, reserve_list_by_prefix, HeldByAnother, ReserveNotHeld, TTL_DEFAULT, MIN_TTL, MAX_TTL, KEY_PREFIX` succeeds
    - KEY_PREFIX == "state:reserve:"
    - TTL_DEFAULT == 1800
    - Three Lua scripts defined as module-level string constants
    - reserve.py contains NO `import typer`, NO `import multiprocessing`, NO `import threading`, NO `import subprocess`
    - reserve.py source-text does NOT contain the literal "state:claim:"
    - tests/unit/test_reserve.py exists; bash scripts/test.sh unit -k test_reserve exits 0 (11 behavior cases + 3 constant tests pass)
    - The cross-namespace coexistence test passes (claim + reserve on same area both succeed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → subprocess.run([git, -C, cwd, …]) | An adversarial cwd or PATH could redirect to a malicious git binary; argv form + shell=False mitigates injection of meta-shell syntax |
| caller → _canonicalize_upstream_url(raw) | raw comes from git's stdout — untrusted user-controlled .git/config content can produce arbitrary URL strings |
| caller → reserve_take(upstream_identity=…) | upstream_identity is an argument; the VERB layer canonicalizes it (or resolves via the resolver), but reserve.py trusts it as a Redis-key segment |
| Lua scripts → Redis HASH | Lua compares (session_id, upstream_identity) before refresh/release; string-equality, not cryptographic proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | _canonicalize_upstream_url — adversarial git URL producing weird identity | mitigate | Two-regex canonicalizer rejects anything that doesn't match SCP or URL form → returns None → resolver falls back to project_hash (per-clone scope). Adversary cannot inject a colon-containing string that splits the Redis key namespace because the canonical form's only structural colon is between host and path; both halves are constrained by `[a-zA-Z0-9._\-/]+` charsets that exclude shell/glob metacharacters. |
| T-07-02 | T (Tampering) | Two-namespace cross-contamination — accidental claim write under reserve prefix | mitigate | reserve.py has its own KEY_PREFIX constant; the structural test in Plan 07-03 asserts neither file contains the other's prefix literal. test_reserve.py's `test_reserve_does_not_collide_with_claim_for_same_area` validates this at runtime. |
| T-07-04 | T (Tampering) | resolve_upstream_identity falsely succeeds when fake .git/config in unrelated dir | accept | The resolver READS the .git/config in target_cwd. If the user `cd`s into a directory with a fake config, they get whatever upstream_identity that config encodes. This is identical to git's own trust model (`git remote get-url origin` trusts the .git/config it finds). No additional mitigation in scope. |
| T-07-09 | DoS | git binary hangs (credential prompt, network drama) | mitigate | subprocess.run timeout=5.0; on TimeoutExpired we fall back to project_hash. The user notices stale per-clone scope rather than a hanging CLI. |
| T-4-01-05 carry | DoS | Reservation TTL infinite refresh by holder | mitigate | MAX_TTL = 86400 caps the EXPIRE argument (same as Phase 4 claim.py). |
| T-07-12 | T (Tampering) | ARGV-index drift between Lua and Python (Pitfall #3) | mitigate | _RESERVE_ARGV_ORDER constant documents the contract in source; test_reserve_take_area_key_uses_upstream_identity_prefix asserts the exact 7-field HGETALL at runtime. A drift bug would fail that test immediately. |
</threat_model>

<verification>
- bash scripts/test.sh unit -k upstream_identity exits 0 (Task 1 tests)
- bash scripts/test.sh unit -k test_reserve exits 0 (Task 2 tests)
- bash scripts/test.sh unit exits 0 (full unit suite — no regression to Phases 1-6)
- src/em_proj/state/claim.py is UNTOUCHED by this plan (additive only; Phase 7 claim semantics unchanged)
</verification>

<success_criteria>
- src/em_proj/identity.py exports `resolve_upstream_identity` and `_canonicalize_upstream_url`; no Redis imports
- src/em_proj/state/reserve.py exists with the 3 Lua scripts, 7-field holder, two exceptions, 4 public ops; no typer/multiprocessing/threading/subprocess imports; source contains no `state:claim:` literal
- 13-row canonicalizer table passes (RESEARCH §Pattern 1)
- All 11 reserve behavior cases pass against db=15
- Cross-namespace coexistence test passes (claim + reserve on same area)
- RESERVE-01 (resolver + canonicalizer) and RESERVE-02 (7-field holder + pure ops) substrate are delivered; verb wiring is Plan 07-02 territory
</success_criteria>

<output>
After completion, create `.planning/phases/07-project-scoped-reservation-registry/07-01-SUMMARY.md`.

The SUMMARY must note:
  - Q-H IS RESOLVED via direct read of the patched sdk/dist/query/workstream.js: Phase 6 does NOT pass `--reason <name>` when claiming `workstream.active`. The em-proj claim's `reason` field is therefore EMPTY when Phase 6 sets a workstream. This means Plan 07-02's `_resolve_workstream` helper CANNOT extract the workstream name from `claim_check("workstream.active").reason` — that field will be None even when Phase 6 has set a workstream. The TTY-prompt path will fire whenever `--workstream` is not explicitly passed (graceful failure per RESEARCH Open Q-H "Risk if wrong" — UX is worse than expected, but correct).
  - The SUMMARY MUST hand Plan 07-02 this finding so the verb implementation knows: `claim_check("workstream.active")` proves a workstream is set BUT does not tell us its name. The TTY prompt must fire in that case (workstream-existence ≠ workstream-name).
  - The 13-row canonicalization table and the 7-field holder shape are now locked at the source-code level; Plan 07-02's verb tests should consume the same canonical forms.
</output>
