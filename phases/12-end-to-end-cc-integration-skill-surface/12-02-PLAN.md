---
phase: 12-end-to-end-cc-integration-skill-surface
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - tests/multiprocess/test_hook_e2e_delivery.py
  - docs/em-sessions-skill.md
  - tests/structural/test_em_sessions_skill_boundaries.py
autonomous: true
requirements: [HOOK-03, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "A directed message sent from session A (`em-proj message send --to B`) is surfaced by session B's UserPromptSubmit hook script — proving the HOOK-03 path end-to-end for the directed pattern"
    - "A broadcast message sent from session A within a scope B belongs to is surfaced by session B's UserPromptSubmit hook script — proving the HOOK-03 path for the broadcast pattern"
    - "A topic message sent from session A to a topic B has subscribed to is surfaced by session B's UserPromptSubmit hook script — proving the HOOK-03 path for the topic pattern"
    - "docs/em-sessions-skill.md fully specifies the /em-sessions skill: session list, session show <id>, message inbox --peek (read surface); message send --to, message broadcast --scope (send surface) — and its NEVER section names state set/del, lock/claim acquire, and session register/listen/stop as excluded"
    - "None of the fenced example commands in docs/em-sessions-skill.md ever invoke state set, state del, a lock/claim acquire verb, or session register/listen/stop — the skill's write boundary holds at the command-invocation level, not just in prose"
  artifacts:
    - path: tests/multiprocess/test_hook_e2e_delivery.py
      provides: "TEST for HOOK-03: A-to-B delivery across directed/broadcast/topic patterns via the UserPromptSubmit hook script (not a synthetic single-session seed — a real two-session send-then-surface pipeline)"
    - path: docs/em-sessions-skill.md
      provides: "Full /em-sessions SKILL.md content, staged in-repo for the orchestrator to copy verbatim to ~/.claude/skills/em-sessions/SKILL.md (executor cannot write there directly)"
      contains: "session list"
    - path: tests/structural/test_em_sessions_skill_boundaries.py
      provides: "Durable invariant: the skill doc's fenced command examples never invoke a forbidden write verb"
  key_links:
    - from: "session A message send --to B / message broadcast / message send --topic"
      to: "session B's mbox:<B> Redis stream"
      via: "durable write at send time (Phase 10 message/_ops.py — send_directed / send_broadcast / send_topic)"
      pattern: "send_directed|send_broadcast|send_topic"
    - from: "session B's mbox:<B>"
      to: "scripts/hooks/user_prompt_submit.py stdout"
      via: "em-proj message inbox --json consumed and formatted by the Plan 12-01 hook script"
      pattern: "inbox"
    - from: "docs/em-sessions-skill.md"
      to: "~/.claude/skills/em-sessions/SKILL.md"
      via: "orchestrator-applied copy after phase execution (executor permission scope denies writes under ~/.claude/skills/)"
      pattern: "orchestrator-applied"
---

<objective>
Wave 2: Close Phase 12 (and the v1.1 milestone) with the full A-to-B end-to-end proof across all three send patterns (HOOK-03), and stage the `/em-sessions` skill content (SKILL-04, SKILL-05) for the orchestrator to apply.

Purpose: Plan 12-01 shipped the two hook scripts in isolation (synthetic single-session tests). This plan proves the REAL pipeline — a message sent by one session's CLI genuinely reaches another session's hook-surfaced context — and delivers the read+send skill surface that lets sub-agents interact with the registry and mailbox without hand-rolling CLI calls.

Output:
- tests/multiprocess/test_hook_e2e_delivery.py — HOOK-03 proof: directed, broadcast, and topic patterns, each session-A-sends → session-B's-hook-surfaces
- docs/em-sessions-skill.md — the full `/em-sessions` skill content, staged in-repo (ORCHESTRATOR-APPLIED to `~/.claude/skills/em-sessions/SKILL.md` — the executor does not and cannot write outside this repo)
- tests/structural/test_em_sessions_skill_boundaries.py — durable invariant guarding the skill's write boundary

Locked decisions implemented here (NON-NEGOTIABLE, per 12-CONTEXT.md):
- "Skill surface = NEW /em-sessions skill (LOCKED)": a new skill, sibling to `/em-global-state`, not an extension of it. Read surface = `session list`, `session show <id>`, `message inbox --peek` (peek only — the skill is a viewer, never the mailbox's consumer). Send surface = `message send --to`, `message broadcast --scope`. NEVER: `state set/del`, lock/claim acquire, or `session register/listen/stop`.
- "`/em-sessions` SKILL.md is ORCHESTRATOR-APPLIED, not executor-applied (LOCKED)": per the project memory on cross-repo SKILL.md edits (the gsd-executor permission scope denies writes under `~/.claude/skills/`), this plan does NOT attempt to write `~/.claude/skills/em-sessions/SKILL.md` at all. Instead it produces `docs/em-sessions-skill.md` — a fully-formed, in-repo, reviewable staging artifact containing the exact target content — for the orchestrator (or a manual post-step) to copy verbatim after this phase executes. Acceptance criteria for the skill task are verified against actual CLI behavior (the commands the doc references), never against a file under `~/.claude/skills/`.
- "HOOK-03 proof is two-layered": Plan 12-01 already delivered layer (1), the deterministic per-hook-script tests with a synthetic single-session seed. This plan delivers layer (2): the full A-to-B path in the multiprocess harness — session A sends via the real `message send`/`broadcast`/`send --topic` CLI, and session B's actual `user_prompt_submit.py` hook script (from Plan 12-01, unmodified) surfaces it. The genuinely-live two-Claude-Code-session demo remains a documented MANUAL validation step (see `<verification>` below) — not gated, not automated (a live CC session cannot be scripted), but the underlying mechanism is fully automated and proven here.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces (full Phase 12 — both plans; this plan creates the last three)

| Artifact | Created by | Notes |
|----------|-----------|-------|
| `scripts/hooks/session_start.py` | Plan 12-01, Task 1 | SessionStart hook (HOOK-01) |
| `scripts/hooks/user_prompt_submit.py` | Plan 12-01, Task 2 | UserPromptSubmit hook (HOOK-02) |
| `.claude/settings.json` — `"hooks"."SessionStart"` + `"hooks"."UserPromptSubmit"` entries | Plan 12-01, Tasks 1+2 | Repo-scoped, `$CLAUDE_PROJECT_DIR`-relative |
| `EM_SESSIONS_AUTOSTART` (env var, not a file) | Plan 12-01 | The opt-in gate both hook scripts check |
| `tests/multiprocess/test_em_sessions_hooks.py` | Plan 12-01 | Deterministic per-hook harness (7 tests) |
| `tests/structural/test_hook_script_boundaries.py` | Plan 12-01 | Durable hook-script invariants (3 tests) |
| `tests/multiprocess/test_hook_e2e_delivery.py` | This plan (12-02), Task 1 | HOOK-03 A-to-B proof (directed/broadcast/topic) — 3 tests |
| `docs/em-sessions-skill.md` | This plan (12-02), Task 2 | Staged content for `~/.claude/skills/em-sessions/SKILL.md` — **orchestrator-applied**, executor does not write outside the repo |
| `tests/structural/test_em_sessions_skill_boundaries.py` | This plan (12-02), Task 2 | Durable skill write-boundary invariant (3 tests) |
| `~/.claude/skills/em-sessions/SKILL.md` (outside this repo) | **Orchestrator**, after this plan executes | Copied verbatim from `docs/em-sessions-skill.md`; the gsd-executor permission scope denies this write |
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/CLAUDE.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/REQUIREMENTS.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/12-end-to-end-cc-integration-skill-surface/12-CONTEXT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/12-end-to-end-cc-integration-skill-surface/12-01-SUMMARY.md

<interfaces>
<!-- Key interfaces established in Plan 12-01 that this plan extends, plus the skill exemplar. -->

From tests/multiprocess/test_em_sessions_hooks.py (Plan 12-01 output — reuse, do not duplicate):
  `_run_hook(hook_path, payload, gate_on=True, env_overrides=None, timeout=10.0) -> subprocess.CompletedProcess`
  `_unique_session_id() -> str`
  `REPO_ROOT`, `SESSION_START_HOOK`, `USER_PROMPT_SUBMIT_HOOK` module constants
  Import these via `from tests.multiprocess.test_em_sessions_hooks import _run_hook, _unique_session_id, USER_PROMPT_SUBMIT_HOOK` at the top of the new test file — do not copy-paste the helpers.

From tests/multiprocess/test_message_delivery.py (full file — the TEST-04 3x3 exemplar to mirror for this plan's session-registration and send-via-CLI helpers):
  `_register_session_for_test(session_id, client) -> dict` — writes the session HASH directly against the passed `client` (the `clean_db` fixture), bypassing the em_proj Redis singleton entirely (same DB-mismatch rationale as Plan 12-01).
  `_send_via_cli(args, sender_session_id) -> (proc, stdout, stderr)` pattern — subprocess.Popen + communicate(timeout=15) with `CLAUDE_CODE_SESSION_ID=<sender>` and `EM_PROJ_REDIS_DB=15` in the child env.

From src/em_proj/message/__init__.py (read-only):
  `em-proj message send --to <session_id> <body>` — directed.
  `em-proj message broadcast <body> --scope <project|upstream|machine>` — broadcast (default scope machine).
  `em-proj message subscribe <topic> [--scope ...]` / `em-proj message send --topic <topic> <body> --scope <...>` — topic.

From src/em_proj/session/__init__.py and src/em_proj/message/__init__.py (read-only — the exact verbs the skill doc wraps):
  `em-proj session list --json`, `em-proj session show <session_id> --json`, `em-proj message inbox --peek --json`, `em-proj message send --to <session_id> <body> --json`, `em-proj message broadcast <body> --scope <scope> --json`.

From ~/.claude/skills/em-global-state/SKILL.md (full file, already read during planning — the exact structural exemplar to mirror in docs/em-sessions-skill.md): frontmatter (`name`, `description`, `allowed-tools: [Bash]`), `<objective>`, `<when_to_invoke>`, `<action>` with one `###` subsection per verb (each showing the shell command in a fenced bash block and the output schema in a fenced json block), `<scope>` with READ surface / WRITE surface / NEVER used for, `<related>` linking back to phases and sibling skills.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HOOK-03 — full A-to-B end-to-end proof across directed, broadcast, and topic patterns via the real hook script</name>
  <files>tests/multiprocess/test_hook_e2e_delivery.py</files>

  <read_first>
    - tests/multiprocess/test_message_delivery.py — full file; the 3x3 TEST-04 harness this plan's helpers mirror (`_unique_session_id`, `_register_session_for_test`, the send-via-CLI subprocess pattern)
    - tests/multiprocess/test_em_sessions_hooks.py (Plan 12-01 output) — full file; reuse `_run_hook`, `_unique_session_id`, `USER_PROMPT_SUBMIT_HOOK` via import, do not redefine them
    - src/em_proj/message/_ops.py send_directed, send_broadcast, send_topic, subscribe_topic — confirm exact CLI-level argument shapes (--to, --scope, --topic) and that a directed send validates the recipient exists via `session_show` (so B must be registered in Redis before A can send to it)
    - src/em_proj/session/_ops.py KEY_PREFIX ("state:session:") — needed to register two sessions with a shared or distinct project_hash/upstream_identity for the broadcast/topic scope tests
  </read_first>

  <behavior>
    - test_hook_e2e_directed_delivery: session A `message send --to B "<body>"`; B's user_prompt_submit.py hook (gate-on, payload session_id=B) surfaces stdout containing the body and A's session_id
    - test_hook_e2e_broadcast_delivery: session A `message broadcast --scope machine "<body>"` with B registered and live; B's hook surfaces stdout containing the body
    - test_hook_e2e_topic_delivery: B `message subscribe <topic> --scope machine`; A `message send --topic <topic> --scope machine "<body>"`; B's hook surfaces stdout containing the body and the literal substring `[topic:<topic>]`
    - Each test leaves Redis clean (no orphaned session/mailbox/topic-membership keys) at the end via the `clean_db` fixture's own teardown plus any explicit cleanup this test performs
  </behavior>

  <action>
    Create tests/multiprocess/test_hook_e2e_delivery.py.

    Module docstring: state this is the HOOK-03 multiprocess proof — a REAL two-session pipeline (session A sends via the actual `em-proj message` CLI, session B's actual `user_prompt_submit.py` hook script surfaces it), distinct from Plan 12-01's synthetic single-session seed tests. Note the genuinely-live two-Claude-Code-session demo is a separate, manual, non-automatable validation step documented in this plan's `<verification>` section — this file automates everything that CAN be automated (the send-to-mailbox-to-hook-surface mechanism).

    Imports: `from __future__ import annotations`; json, os, subprocess, time; pytest; `from tests.conftest import EM_PROJ_BIN, TEST_DB`; `from tests.multiprocess.test_em_sessions_hooks import _run_hook, _unique_session_id, USER_PROMPT_SUBMIT_HOOK`.

    Helper `_register_session_for_test(session_id, client)`: copy the exact body from tests/multiprocess/test_message_delivery.py's function of the same name (writes the 9-field session HASH directly via `client.hset(...)` using `current_process_composite()` + `resolve_upstream_identity()`, then `client.expire(redis_key, ops.TTL_DEFAULT)`) — import `em_proj.session._ops as ops` and `from em_proj.identity import current_process_composite, resolve_upstream_identity` at module level for this.

    Helper `_send_via_cli(args, sender_session_id, timeout=15.0)`: build `child_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB), "CLAUDE_CODE_SESSION_ID": sender_session_id}`; `return subprocess.run([EM_PROJ_BIN, "message"] + args, capture_output=True, text=True, env=child_env, timeout=timeout)`.

    Test `test_hook_e2e_directed_delivery(clean_db)`: `a_id, b_id = _unique_session_id(), _unique_session_id()`; register both via `_register_session_for_test` (both get the same project_hash/upstream_identity naturally, since both come from the same test-runner process — fine for a directed send, which ignores scope for recipient resolution). `body = "e2e-directed-hello"`; `send_result = _send_via_cli(["send", "--to", b_id, body], a_id)`; assert `send_result.returncode == 0`. `hook_result = _run_hook(USER_PROMPT_SUBMIT_HOOK, {"session_id": b_id}, gate_on=True)`; assert `hook_result.returncode == 0`; assert `body in hook_result.stdout`; assert `a_id in hook_result.stdout`.

    Test `test_hook_e2e_broadcast_delivery(clean_db)`: `a_id, b_id = _unique_session_id(), _unique_session_id()`; register both (same scope by construction — both resolve to the same project_hash/upstream_identity in-process). `body = "e2e-broadcast-hello"`; `send_result = _send_via_cli(["broadcast", "--scope", "machine", body], a_id)`; assert `send_result.returncode == 0`. `hook_result = _run_hook(USER_PROMPT_SUBMIT_HOOK, {"session_id": b_id}, gate_on=True)`; assert `hook_result.returncode == 0`; assert `body in hook_result.stdout`.

    Test `test_hook_e2e_topic_delivery(clean_db)`: `a_id, b_id = _unique_session_id(), _unique_session_id()`; register both. `topic = "e2e-topic-" + b_id[-8:]` (unique per test run, avoids cross-test topic-membership collisions). `subscribe_result = _send_via_cli(["subscribe", topic, "--scope", "machine"], b_id)`; assert `subscribe_result.returncode == 0`. `body = "e2e-topic-hello"`; `send_result = _send_via_cli(["send", "--topic", topic, "--scope", "machine", body], a_id)`; assert `send_result.returncode == 0`. `hook_result = _run_hook(USER_PROMPT_SUBMIT_HOOK, {"session_id": b_id}, gate_on=True)`; assert `hook_result.returncode == 0`; assert `body in hook_result.stdout`; assert `f"[topic:{topic}]" in hook_result.stdout`.

    Commit message: "test(12-02): add HOOK-03 A-to-B end-to-end delivery proof (directed/broadcast/topic)".
  </action>

  <verify>
    <automated>scripts/test.sh multiprocess -k hook_e2e --tail 60</automated>
    All 3 tests pass: test_hook_e2e_directed_delivery, test_hook_e2e_broadcast_delivery, test_hook_e2e_topic_delivery.
    <automated>scripts/test.sh all --tail 40</automated>
    Full suite green.
  </verify>

  <done>
    tests/multiprocess/test_hook_e2e_delivery.py has 3 passing tests proving HOOK-03 across all 3 send patterns via the real hook script; scripts/test.sh all exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stage docs/em-sessions-skill.md (SKILL-04, SKILL-05, orchestrator-applied) + write-boundary structural test</name>
  <files>docs/em-sessions-skill.md, tests/structural/test_em_sessions_skill_boundaries.py</files>

  <read_first>
    - ~/.claude/skills/em-global-state/SKILL.md — full file (already read during planning); the exact structural exemplar for docs/em-sessions-skill.md's frontmatter, `<objective>`, `<when_to_invoke>`, per-verb `###` subsections (fenced bash command + fenced json schema), `<scope>` READ/WRITE/NEVER split, `<related>` section
    - src/em_proj/session/__init__.py — session_list_cmd, session_show_cmd exact `--json` output shapes and exit codes
    - src/em_proj/message/__init__.py — inbox_cmd (`--peek`), send_cmd (`--to`), broadcast_cmd (`--scope`) exact `--json` output shapes and exit codes
    - .planning/phases/12-end-to-end-cc-integration-skill-surface/12-CONTEXT.md `<decisions>` section, "Skill surface = NEW /em-sessions skill (LOCKED)" — the exact READ/SEND/NEVER boundary this file must encode
    - tests/structural/test_session_daemon_boundaries.py — style exemplar for writing a self-contained, invariant-named structural test module
  </read_first>

  <behavior>
    - docs/em-sessions-skill.md exists with valid YAML frontmatter (name: em-sessions, description, allowed-tools: [Bash]) and a verb reference covering session list, session show <id>, message inbox --peek, message send --to, message broadcast --scope
    - The doc's `<scope>` section explicitly lists state set/del, lock/claim acquire, and session register/listen/stop under NEVER
    - None of the doc's FENCED command examples ever contain the literal invocation shapes `state set`, `state del`, `session register`, `session listen`, or `session stop` as an actual command to run — those verb names may appear in prose (the NEVER section explaining what is excluded) but never inside a fenced code block
  </behavior>

  <action>
    Create docs/em-sessions-skill.md containing the full content intended for `~/.claude/skills/em-sessions/SKILL.md`. This file is a STAGING ARTIFACT — the gsd-executor's permission scope denies writes under `~/.claude/skills/`, so this task does NOT attempt to write there. The orchestrator (or a manual post-execution step) copies this file's content verbatim to `~/.claude/skills/em-sessions/SKILL.md` after the phase completes. Mirror `~/.claude/skills/em-global-state/SKILL.md`'s exact structural shape (frontmatter block, `<objective>`, `<when_to_invoke>`, `<action>` with one `###` subsection per verb showing a fenced bash command and a fenced json output schema, `<scope>` with READ surface / WRITE surface / NEVER used for, `<related>`).

    Frontmatter: `name: em-sessions`; `description:` a one-line summary — "Read the live em-proj session registry + inbox, and send messages to other sessions. Shells out to em-proj CLI — requires em-proj installed (uv tool install)."; `allowed-tools:` list containing only `Bash`.

    `<objective>`: explain this wraps `em-proj session` (registry) and `em-proj message` (inbox/send/broadcast) behind the same `schema_version` envelope as `/em-global-state`; explicitly state it NEVER writes KV/locks/claims and NEVER manages session/daemon lifecycle (register/listen/stop) — those are the SessionStart hook's and daemon's job (Phase 12), not a skill write.

    `<when_to_invoke>`: bullet list — user types `/em-sessions <verb>`; a sub-agent wants to see who else is live before starting overlapping work; a sub-agent wants to peek at an inbox without consuming it; a sub-agent wants to notify another session or broadcast to a scope.

    `<action>` verb reference, one `###` subsection each, each with a fenced `bash` block for the exact command and a fenced `json` block for the output schema (mirror em-global-state's exact fencing style):
      - `### /em-sessions list` — command `em-proj session list --json`; schema `{"schema_version":"1","status":"ok","data":[{"session":{...9 fields...},"claims":N,"locks":N,"reservations":N}, ...]}`; exit 0 (empty list still exit 0).
      - `### /em-sessions show <session_id>` — command `em-proj session show <session_id> --json`; exit 0 found, exit 2 not found.
      - `### /em-sessions inbox [--session <session_id>]` — ALWAYS uses `--peek` (the skill is a viewer, never the mailbox's consumer — the UserPromptSubmit hook is the consumer); command `em-proj message inbox --peek --json`; note that passing `--session <session_id>` means running the command with `CLAUDE_CODE_SESSION_ID=<session_id>` set for that invocation only (peeking a session other than the caller's own), e.g. a second fenced example `CLAUDE_CODE_SESSION_ID=<session_id> em-proj message inbox --peek --json`; schema is a list of MBOX-04 records (msg_id, from_session, pattern, scope, topic, body, sent_at, ttl); exit 0 (empty mailbox still exit 0).
      - `### /em-sessions send <session_id> <body>` — command `em-proj message send --to <session_id> <body> --json`; exit 0 delivered, exit 2 recipient does not exist, exit 4 partial fan-out.
      - `### /em-sessions broadcast <body> [--scope project|upstream|machine]` — command `em-proj message broadcast <body> --scope <scope> --json` (default scope machine); exit 0 delivered-or-empty-scope, exit 4 partial fan-out.

    `<scope>`:
      READ surface (safe at any time): `/em-sessions list`, `/em-sessions show <session_id>`, `/em-sessions inbox [--session <session_id>]` (always peek, never consumes).
      WRITE surface (explicit message ops, first-class CLI operations, not ad-hoc state writes): `/em-sessions send <session_id> <body>`, `/em-sessions broadcast <body> [--scope ...]`.
      NEVER used for: state set / state del (KV writes — `/em-global-state`'s domain); lock/claim acquire; `session register` / `session listen` / `session stop` (lifecycle is the SessionStart hook's and daemon's job — Phase 12 HOOK-01 — never a skill-invoked write).

    `<related>`: link to `em-proj` CLI install instructions, `/em-global-state` (sibling skill, state/locks/claims/reservations only), and Phases 8-12 of the em-proj repo (registry, mailbox, send patterns, daemon, this integration).

    Step B — Create tests/structural/test_em_sessions_skill_boundaries.py, self-contained, named for the invariant it protects (the skill's write boundary), not the phase.

    Imports: `from __future__ import annotations`; `re`; `from pathlib import Path`.

    Constants: `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`; `SKILL_DOC = REPO_ROOT / "docs" / "em-sessions-skill.md"`; `_FORBIDDEN_INVOCATIONS = ("state set", "state del", "session register", "session listen", "session stop")`. Define `_FENCE_RE` as a compiled regex matching a markdown fenced code block — opened by three backtick characters optionally followed by `bash` or `json`, a newline, then a captured group of any characters (non-greedy) up to the next occurrence of three backtick characters — using `re.DOTALL` so the capture spans newlines.

    Test `test_em_sessions_skill_doc_exists_and_has_frontmatter()`: `src = SKILL_DOC.read_text()`; assert `src.startswith("---")`; assert `"name: em-sessions" in src`.

    Test `test_em_sessions_skill_fenced_commands_never_invoke_forbidden_verbs()`: `src = SKILL_DOC.read_text()`; `fenced_blocks = _FENCE_RE.findall(src)`; `fenced_text = "\n".join(fenced_blocks)`; for each `forbidden` in `_FORBIDDEN_INVOCATIONS`: assert `forbidden not in fenced_text`, failure message referencing SKILL-04/05's locked write boundary (the skill may DESCRIBE these verbs as excluded in prose, but must never show them as an actual command to run).

    Test `test_em_sessions_skill_documents_the_never_boundary()`: `src = SKILL_DOC.read_text()`; assert `"NEVER" in src`; assert `"state set" in src` (the prose IS allowed and expected to name the excluded verb — only the fenced-block check in the previous test restricts where it may NOT appear).

    Commit message: "docs(12-02): stage /em-sessions skill content + write-boundary structural test (SKILL-04, SKILL-05)".
  </action>

  <verify>
    <automated>scripts/test.sh structural -k em_sessions_skill --tail 30</automated>
    All 3 tests in tests/structural/test_em_sessions_skill_boundaries.py pass.
    <automated>em-proj session list --json</automated>
    Exits 0, prints a schema_version envelope — confirms the first verb docs/em-sessions-skill.md references behaves as documented.
    <automated>em-proj message inbox --peek --json</automated>
    Exits 0, prints a schema_version envelope — confirms the inbox --peek verb behaves as documented.
    <automated>scripts/test.sh all --tail 40</automated>
    Full suite green.
  </verify>

  <done>
    docs/em-sessions-skill.md exists with the full skill content described above; tests/structural/test_em_sessions_skill_boundaries.py has 3 passing tests; the referenced CLI verbs run successfully; scripts/test.sh all exits 0. The SUMMARY for this plan explicitly flags docs/em-sessions-skill.md as ORCHESTRATOR-APPLIED to ~/.claude/skills/em-sessions/SKILL.md — not yet applied by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session A's CLI send → session B's mailbox → session B's hook stdout | The full HOOK-03 pipeline this plan proves end-to-end |
| docs/em-sessions-skill.md (in-repo) → ~/.claude/skills/em-sessions/SKILL.md (out-of-repo, orchestrator-applied) | Content crosses a permission boundary the executor cannot cross itself |
| /em-sessions send/broadcast → sender identity | A skill invocation always sends as the CALLING session — no spoofing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Spoofing | `/em-sessions send`/`broadcast` letting a caller impersonate a different `from_session` | accept | Sender identity is always `resolve_session_id()` of the CALLING session (Phase 10's existing `send_directed`/`send_broadcast`/`send_topic` behavior, unchanged here) — a skill invocation cannot spoof a different sender; this is the same trust boundary the underlying CLI already has. |
| T-12-02-02 | Information Disclosure | `/em-sessions inbox --session <id>` lets the caller peek ANY session's mailbox, not just its own | accept | Single-user, single-machine trust model (PROJECT.md) — every em-proj session belongs to the same user; peeking another of your own sessions' inbox is the intentional SKILL-04 read capability, not a privilege-boundary crossing. |
| T-12-02-03 | Tampering | `docs/em-sessions-skill.md`'s content is only reviewed in-repo; the actual `~/.claude/skills/em-sessions/SKILL.md` copy step happens outside this repo's CI/tests | accept | This is the documented orchestrator-applied boundary (project memory: cross-repo SKILL.md edits need the orchestrator path). The CLI behavior the skill wraps is fully tested in-repo (Phases 8-12); the skill file itself is a thin, hand-reviewed wrapper mirroring the already-shipped `/em-global-state` pattern. |
| T-12-02-04 | Denial of Service | test_hook_e2e_delivery.py's three tests each register sessions and send real messages — a failed test could leave orphaned Redis keys (session HASH, mailbox stream, topic membership SET) | mitigate | Every test runs under the `clean_db` fixture, which FLUSHDBs on db=15 before AND after every test (function-scoped) — orphaned keys from a failed assertion mid-test are still wiped by the fixture's teardown FLUSHDB. |
</threat_model>

<verification>
After completing both tasks:
- `scripts/test.sh multiprocess -k hook_e2e` → exits 0, all 3 HOOK-03 pattern tests green
- `scripts/test.sh structural -k em_sessions_skill` → exits 0, all 3 tests green
- `scripts/test.sh all` → exits 0, full suite green (Phase 12 complete, v1.1 milestone requirements all satisfied)
- `docs/em-sessions-skill.md` exists and its fenced command examples never invoke a forbidden write verb

**Manual validation (documented, non-blocking — per 12-CONTEXT.md's two-layer HOOK-03 proof):**
The automated tests above prove the send-to-mailbox-to-hook-surface mechanism deterministically. The genuinely-live demonstration — two real Claude Code sessions, both opted in via `EM_SESSIONS_AUTOSTART=1`, session A sending `em-proj message send --to <B's session_id> "hello"` and observing the greeting appear as additional context in session B's next turn — cannot be scripted (a live CC session is not a test fixture) and is NOT a gating step for this plan. To perform it by hand: open two Claude Code sessions in this repo with `EM_SESSIONS_AUTOSTART=1` exported in both shells before launching; find each session's `session_id` via `em-proj session list --json`; from session A run `em-proj message send --to <B_session_id> "hello from A"`; on session B's next prompt, confirm the `[em-proj inbox]` block appears as additional context.

**Orchestrator follow-up (not part of this plan's automated scope):** copy `docs/em-sessions-skill.md`'s content verbatim to `~/.claude/skills/em-sessions/SKILL.md` (create the `em-sessions` directory if absent).
</verification>

<success_criteria>
- HOOK-03 proven automatically across all 3 send patterns (directed/broadcast/topic) via the real hook script, plus documented as a manual two-session validation step
- docs/em-sessions-skill.md fully specifies `/em-sessions` (SKILL-04 read surface, SKILL-05 send surface), ready for orchestrator copy to `~/.claude/skills/em-sessions/SKILL.md`
- The skill's write boundary (never state set/del, lock/claim acquire, or session register/listen/stop) holds at the fenced-command level, not just in prose
- `scripts/test.sh all` exits 0 — Phase 12 complete, v1.1 milestone (Phases 8-12) fully shipped
</success_criteria>

<output>
After completion, create .planning/phases/12-end-to-end-cc-integration-skill-surface/12-02-SUMMARY.md
</output>
