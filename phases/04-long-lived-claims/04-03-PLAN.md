---
phase: 04-long-lived-claims
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - tests/multiprocess/test_claim_race.py
autonomous: true
requirements:
  - CLAIM-01
  - CLAIM-02
  - CLAIM-03

must_haves:
  truths:
    - "Two processes racing claim <area> simultaneously: exactly one exits 0, the other exits 3"
    - "Holder refreshing own claim repeatedly keeps the key alive past initial TTL"
    - "Non-holder attempting release <area> exits 3, claim key still present after"
    - "Holder releasing own claim: exits 0, subsequent check exits 2"
    - "Anonymous claim attempt (CLAUDE_CODE_SESSION_ID unset) exits 1 with 'anonymous claims refused'"
  artifacts:
    - path: "tests/multiprocess/test_claim_race.py"
      provides: "Multi-process race coverage for all 4 Phase 4 ROADMAP success criteria at CLI boundary"
  key_links:
    - from: "test_claim_race.py"
      to: "multiproc_race fixture"
      via: "from tests.conftest import EM_PROJ_BIN, TEST_DB, RaceResult"
      pattern: "multiproc_race"
    - from: "test_claim_race.py"
      to: "em-proj state claim/release/check verbs"
      via: "subprocess.Popen([EM_PROJ_BIN, 'state', 'claim', ...])"
      pattern: "EM_PROJ_BIN.*state.*claim"
---

<objective>
Write tests/multiprocess/test_claim_race.py — multi-process race tests for the claim
primitive at the CLI boundary.

Purpose: Claims are a cross-session coordination primitive; correctness under concurrent
access must be proven at the process level, not just unit-tested. This plan mirrors the
Phase 3 test_lock_hold.py pattern: real em-proj processes, db=15, multiproc_race fixture.

Output: test_claim_race.py with 5 test functions covering all Phase 4 success criteria
at the race/integration level. All tests must pass against a live Redis on db=15.
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/04-long-lived-claims/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Test harness contracts from tests/conftest.py -->

from tests.conftest import EM_PROJ_BIN, TEST_DB, RaceResult

EM_PROJ_BIN: str = "em-proj"
TEST_DB: int = 15

RaceResult (frozen dataclass):
  returncode: int
  stdout: str    # decoded text
  stderr: str    # decoded text
  duration_ms: float

multiproc_race fixture: (commands: list[list[str]], timeout: float = 10.0) -> list[RaceResult]
  - Spawns all commands in parallel (tight launch loop)
  - Joins via .communicate(timeout=)
  - Children inherit EM_PROJ_REDIS_DB=15 from fixture-injected env
  - Returns results in launch order

clean_db fixture: function-scoped FLUSHDB on db=15 before+after each test
  (implicit through multiproc_race's fixture dependency chain)

<!-- How to run a single em-proj command and capture output -->
Direct subprocess (for sequential steps within one test):
  import subprocess, os
  env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB)}
  result = subprocess.run(
      [EM_PROJ_BIN, "state", "claim", "foo", "--json"],
      capture_output=True, text=True, env=env
  )

<!-- How to read a claim key directly from db=15 for post-race assertions -->
  import redis
  client = redis.Redis(host="127.0.0.1", port=6379, db=TEST_DB, decode_responses=True)
  client.exists("state:claim:<project_hash>:foo")  # 1 if present, 0 if absent

<!-- Key shape from claim.py -->
  claim key = "state:claim:<project_hash>:<area>"
  where project_hash = os.getcwd().replace("/", "-")
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write multi-process race tests for claim/release/check</name>

  <read_first>
    - tests/multiprocess/test_lock_hold.py — race test structure, multiproc_race usage, RaceResult assertions, timing relative bounds (Warning #6: no absolute upper bounds). Mirror the style closely.
    - tests/conftest.py — EM_PROJ_BIN, TEST_DB, RaceResult, multiproc_race fixture, clean_db fixture. Read the import pattern: "from tests.conftest import EM_PROJ_BIN, TEST_DB, RaceResult".
    - src/em_proj/state/claim.py — KEY_PREFIX and key shape, so tests can inspect Redis state directly after a race for post-hoc assertions.
    - CLAUDE.md — test dispatcher (bash scripts/test.sh multiprocess -k claim), no Co-Authored-By.
  </read_first>

  <behavior>
    Test 1 — ROADMAP SC#1 race: two simultaneous claim attempts on same area
      - Two children race [EM_PROJ_BIN, "state", "claim", "--ttl", "30", "claim-race-area"]
      - Expected: exactly one exits 0, the other exits 3
      - Post-race: Redis key "state:claim:<project_hash>:claim-race-area" has TTL > 0
      - NOTE: both children will have the SAME session_id (same CLAUDE_CODE_SESSION_ID env)
        which would trigger the refresh path. To force a race, the test must inject DIFFERENT
        session_id values for each child. Use per-child env injection:
          child1_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB), "CLAUDE_CODE_SESSION_ID": "session-A"}
          child2_env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB), "CLAUDE_CODE_SESSION_ID": "session-B"}
        Then launch via subprocess.Popen with env= per child. The multiproc_race fixture
        injects a SHARED env, so for this test use direct subprocess.Popen calls outside
        the fixture (or pass a custom_env list to a modified fixture call — the simplest
        approach is direct Popen for this test only).

    Test 2 — ROADMAP SC#1 refresh: same holder repeating claim refreshes TTL
      - Set claim with ttl=5 (5 seconds) as session-X
      - Sleep 3s
      - Repeat claim as session-X with ttl=30 — should exit 0 (refresh)
      - Check that Redis TTL is > 25 (was refreshed, not expired)
      - Verify claimed_at stays the same, expires_at increased
      - All done sequentially (single subprocess.run calls), not multiproc_race

    Test 3 — ROADMAP SC#3: non-holder release attempt exits 3, claim survives
      - Claim as session-A
      - Attempt release as session-B → exit 3
      - Verify claim key still present in Redis (exists == 1)
      - Sequential subprocess.run calls with per-call env injection

    Test 4 — ROADMAP SC#3: holder release exits 0, claim gone
      - Claim as session-C
      - Release as session-C → exit 0
      - check area → exit 2 (not held)
      - Sequential subprocess.run calls

    Test 5 — ROADMAP SC#4: anonymous claim refused
      - subprocess.run with CLAUDE_CODE_SESSION_ID="" in env (empty = absent per verb check)
      - Exit code must be 1
      - stderr must contain "anonymous claims refused"
      - No Redis key created (optional post-check)
      - NOTE: also test with CLAUDE_CODE_SESSION_ID not present at all (pop it from env dict)
        to cover both the empty-string and unset cases.
  </behavior>

  <action>
    Create tests/multiprocess/test_claim_race.py with the following structure:

    Module docstring: Mirror test_lock_hold.py's style. Document what each test covers
    (ROADMAP SC references), the design invariants (subprocess.Popen not multiprocessing.Process,
    .communicate not .wait, EM_PROJ_REDIS_DB=15, per-child session-id injection for race tests).

    Imports:
      from __future__ import annotations
      import json, os, subprocess, time
      import pytest, redis
      from tests.conftest import EM_PROJ_BIN, TEST_DB, RaceResult

    Helper: _project_hash() → os.getcwd().replace("/", "-") for key introspection.
    Helper: _claim_key(area) → f"state:claim:{_project_hash()}:{area}"
    Helper: _redis_client() → redis.Redis(host="127.0.0.1", port=6379, db=TEST_DB, decode_responses=True)
    Helper: _run(argv, extra_env=None) → subprocess.run result with capture_output=True, text=True,
      env={**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB), **(extra_env or {})}

    Five test functions named:
      test_two_sessions_race_claim_one_wins
      test_same_holder_refresh_extends_ttl
      test_non_holder_release_exits_3_claim_survives
      test_holder_release_exits_0_claim_gone
      test_anonymous_claim_refused_exit_1

    For test_two_sessions_race_claim_one_wins: use direct Popen (not multiproc_race fixture)
    because per-child env injection is needed. Use the tight-launch-then-communicate pattern
    from conftest.py _run() internals. inject CLAUDE_CODE_SESSION_ID separately for each child.
    Both children: [EM_PROJ_BIN, "state", "claim", "--ttl", "60", "race-area"]
    After both complete: assert sorted(exit_codes) == [0, 3]; assert Redis key TTL > 0.

    For test_same_holder_refresh_extends_ttl: use _run() helper.
    Step 1: _run([EM_PROJ_BIN, "state", "claim", "--ttl", "5", "refresh-area", "--json"],
                  extra_env={"CLAUDE_CODE_SESSION_ID": "session-refresh"})
    Step 2: time.sleep(3)
    Step 3: _run([EM_PROJ_BIN, "state", "claim", "--ttl", "30", "refresh-area", "--json"],
                  extra_env={"CLAUDE_CODE_SESSION_ID": "session-refresh"})
    Assert step3.returncode == 0 (refresh, not conflict)
    Assert _redis_client().ttl(_claim_key("refresh-area")) > 25  (refreshed to ~30s)

    For test_non_holder_release_exits_3_claim_survives:
    Step 1: claim as "session-holder" → exit 0
    Step 2: release as "session-intruder" → exit 3
    Step 3: assert key still exists in Redis

    For test_holder_release_exits_0_claim_gone:
    Step 1: claim as "session-owner"
    Step 2: release as "session-owner" → exit 0
    Step 3: check → exit 2

    For test_anonymous_claim_refused_exit_1:
    Variant A: extra_env={"CLAUDE_CODE_SESSION_ID": ""} → exit 1
    Variant B: pop CLAUDE_CODE_SESSION_ID from env entirely → exit 1
    Both: stderr contains "anonymous claims refused"

    TIMING INVARIANTS (Warning #6 carry):
      - test_two_sessions_race: no absolute upper bound on duration. Only assert relative: one
        exits 0, the other exits 3. Do NOT assert "loser took < 500ms" or similar.
      - test_same_holder_refresh: use time.sleep(3) with ttl=5 so the key would expire at ~5s.
        After refresh at t=3, TTL > 25 is the assertion. Avoid hard timing on refresh response time.

    Every test requires the clean_db fixture (Redis flush). Tests 2-5 use @pytest.fixture(autouse)
    via the function-scoped clean_db; for test 1 (uses raw Popen, not multiproc_race), add
    clean_db as an explicit parameter to the test function signature.
  </action>

  <verify>
    <automated>bash scripts/test.sh multiprocess -k claim</automated>
  </verify>

  <done>
    - tests/multiprocess/test_claim_race.py exists with 5 test functions
    - bash scripts/test.sh multiprocess -k claim exits 0
    - All 5 tests pass: race-winner/loser, refresh, non-holder-release-exit3, holder-release-gone, anonymous-refused
    - No absolute timing upper bounds in assertions
    - No multiprocessing.Process imports (subprocess.Popen only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → em-proj subprocess | Tests inject CLAUDE_CODE_SESSION_ID to simulate different sessions; this is test-only behavior, not a production threat |
| db=15 | All tests use TEST_DB=15; clean_db fixture ensures isolation; production db=0 never touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-03-01 | Tampering | Test contamination of prod Redis | mitigate | clean_db fixture + EM_PROJ_REDIS_DB=15 in child env prevents any test from touching db=0 |
| T-4-03-02 | Denial of Service | Orphaned claim key from failed test | mitigate | clean_db FLUSHDB after each test; test keys on db=15 never survive test session |
| T-4-03-03 | Spoofing | Session-id injection in race tests | accept | CLAUDE_CODE_SESSION_ID injection is the test mechanism itself; in production the env var is set by Claude Code, not injectable by untrusted callers at the claim layer |
</threat_model>

<verification>
bash scripts/test.sh multiprocess -k claim
bash scripts/test.sh all --tail 60
</verification>

<success_criteria>
- tests/multiprocess/test_claim_race.py exists with 5 test functions
- bash scripts/test.sh multiprocess -k claim exits 0 (all 5 pass)
- bash scripts/test.sh all exits 0 (no regressions in other test suites)
- No "multiprocessing" import in test_claim_race.py
- No absolute upper-bound timing assertions (no "< N ms" that would be CI-flaky)
</success_criteria>

<output>
After completion, create .planning/phases/04-long-lived-claims/04-03-SUMMARY.md
</output>
