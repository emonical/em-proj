---
phase: 04-long-lived-claims
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - tests/structural/test_phase_04_shape.py
autonomous: true
requirements:
  - CLAIM-01
  - CLAIM-02
  - CLAIM-03

must_haves:
  truths:
    - "tests/structural/test_phase_04_shape.py exists and passes via bash scripts/test.sh structural"
    - "AST checks verify claim.py has TTL_DEFAULT, KEY_PREFIX, all 3 Lua scripts, no typer/multiprocessing import"
    - "AST checks verify state/__init__.py registers claim + release + check verbs"
    - "bash scripts/verify-phase.sh 04 exits 0 (all deterministic checks pass)"
    - "No TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER tokens in src/, tests/, scripts/ (verify-phase.sh check)"
    - "Every PLAN.md in phases/04-long-lived-claims/ has a corresponding SUMMARY.md"
  artifacts:
    - path: "tests/structural/test_phase_04_shape.py"
      provides: "AST-based structural invariants for Phase 4 code"
  key_links:
    - from: "test_phase_04_shape.py"
      to: "src/em_proj/state/claim.py"
      via: "CLAIM_PY path constant + ast.parse"
      pattern: "CLAIM_PY"
    - from: "test_phase_04_shape.py"
      to: "src/em_proj/state/__init__.py"
      via: "STATE_INIT path constant + ast.parse"
      pattern: "STATE_INIT"
---

<objective>
Add tests/structural/test_phase_04_shape.py and run bash scripts/verify-phase.sh 04
as the final acceptance gate for Phase 4.

Purpose: Encode Phase 4's plan acceptance criteria as runtime assertions in the
structural test suite (one allowlisted call covers what would otherwise be many grep
invocations). verify-phase.sh 04 is the deterministic end-to-end check a Phase 5
planner or verifier can run to confirm Phase 4 is truly complete.

Output: test_phase_04_shape.py passing via structural runner + clean verify-phase.sh 04 run.
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/04-long-lived-claims/04-03-SUMMARY.md
</context>

<interfaces>
<!-- Structural test pattern from tests/structural/test_phase_03_shape.py -->

Key AST helpers (self-contained — copy/adapt, do not import from other structural files):
  _parse_or_skip(path, label) -> ast.Module   (skip if file absent)
  _find_assign(tree, name) -> ast.AST | None  (find module-level assignment by name)
  _find_funcdef(tree, name) -> ast.FunctionDef | None
  _find_imports(tree) -> set[str]             (top-level module names imported)
  _find_all_names(tree) -> set[str]           (all Name nodes in the tree — for "no X import" checks)

Path constants pattern (from test_phase_03_shape.py):
  REPO_ROOT = Path(__file__).resolve().parent.parent.parent
  SRC_EM_PROJ = REPO_ROOT / "src" / "em_proj"
  STATE_PKG = SRC_EM_PROJ / "state"
  CLAIM_PY = STATE_PKG / "claim.py"
  STATE_INIT = STATE_PKG / "__init__.py"
  PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "04-long-lived-claims"

<!-- verify-phase.sh contract -->
bash scripts/verify-phase.sh 04
  Exit 0: all checks pass
  Exit 1: one or more checks fail (report shows which)
  Checks run: test suite (all + structural), anti-pattern grep on src/tests/scripts,
              SUMMARY.md presence per PLAN.md, recent commit traceability.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write test_phase_04_shape.py structural invariants</name>

  <read_first>
    - tests/structural/test_phase_03_shape.py — the full Phase 3 structural test. Read the self-contained helpers at the top (_parse_or_skip, _find_assign, etc.), the path constants block, and the first 5 test functions for style. The Phase 4 file follows exactly the same pattern.
    - tests/structural/test_phase_02_shape.py — supplement if needed for emit_ helper assertion patterns.
    - src/em_proj/state/claim.py — the actual symbols the structural tests will assert. Must read the final file to write assertions that match reality, not assumptions.
    - src/em_proj/state/__init__.py — the actual verb registrations. Count existing @state_app.command() calls and assert the new count includes claim/release/check.
    - CLAUDE.md — test dispatcher, self-contained file rule ("each structural file is self-contained").
  </read_first>

  <action>
    Create tests/structural/test_phase_04_shape.py. Structure:

    Module docstring: List what each assertion covers (decision ID or plan criterion reference).
    Include:
      - D-17 carry — claim.py is pure: no typer import, no multiprocessing import
      - D-18 carry — claim.py has no redis.Redis() constructor call (uses get_client() only)
      - CLAIM-01 constants — TTL_DEFAULT = 1800, KEY_PREFIX = "state:claim:" present
      - CLAIM-01 Lua — all 3 Lua scripts present as module-level string constants
          (LUA_CLAIM_REFRESH_OR_TAKE, LUA_CLAIM_COMPARE_AND_DELETE, LUA_CLAIM_CHECK)
      - CLAIM-01 public ops — claim_take, claim_release, claim_check exported from claim.py
      - CLAIM-02 exceptions — HeldByAnother and ClaimNotHeld exported from claim.py
      - CLAIM-03 anonymous gate — state/__init__.py claim verb body contains
          the string "anonymous claims refused" (literal grep on AST or source)
      - D-14 extended — state/__init__.py registers >= 9 verbs total
          (4 kv + lock + unlock + claim + release + check = 9)
      - Phase 4 PLAN presence — every 04-NN-PLAN.md in phases/04-long-lived-claims/ has a
          corresponding 04-NN-SUMMARY.md (SUMMARY inventory check)

    Self-contained helpers (copy relevant ones from test_phase_03_shape.py, do not import):
      _parse_or_skip, _find_assign, _find_funcdef, _find_imports, _find_all_names

    Path constants block:
      REPO_ROOT, SRC_EM_PROJ, STATE_PKG, CLAIM_PY = STATE_PKG / "claim.py",
      STATE_INIT, PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "04-long-lived-claims"

    Test functions (each prefixed test_):

      test_claim_py_no_typer_import:
        tree = _parse_or_skip(CLAIM_PY, "claim.py")
        imports = _find_imports(tree)
        assert "typer" not in imports

      test_claim_py_no_multiprocessing_import:
        assert "multiprocessing" not in _find_imports(_parse_or_skip(CLAIM_PY, "claim.py"))

      test_claim_py_no_redis_constructor:
        source = CLAIM_PY.read_text()
        assert "redis.Redis(" not in source, "claim.py must use get_client() not redis.Redis()"

      test_claim_py_ttl_default_is_1800:
        tree = _parse_or_skip(CLAIM_PY, "claim.py")
        node = _find_assign(tree, "TTL_DEFAULT")
        assert node is not None, "TTL_DEFAULT missing from claim.py"
        assert isinstance(node, ast.Constant) and node.value == 1800

      test_claim_py_key_prefix:
        tree = _parse_or_skip(CLAIM_PY, "claim.py")
        node = _find_assign(tree, "KEY_PREFIX")
        assert node is not None
        assert isinstance(node, ast.Constant) and node.value == "state:claim:"

      test_claim_py_lua_scripts_present:
        tree = _parse_or_skip(CLAIM_PY, "claim.py")
        for name in ("LUA_CLAIM_REFRESH_OR_TAKE", "LUA_CLAIM_COMPARE_AND_DELETE", "LUA_CLAIM_CHECK"):
            assert _find_assign(tree, name) is not None, f"{name} missing from claim.py"

      test_claim_py_public_ops_exported:
        tree = _parse_or_skip(CLAIM_PY, "claim.py")
        for name in ("claim_take", "claim_release", "claim_check"):
            assert _find_funcdef(tree, name) is not None, f"{name} not defined in claim.py"

      test_claim_py_exceptions_exported:
        tree = _parse_or_skip(CLAIM_PY, "claim.py")
        # HeldByAnother and ClaimNotHeld should be class definitions
        class_names = {
            node.name for node in ast.walk(tree) if isinstance(node, ast.ClassDef)
        }
        assert "HeldByAnother" in class_names, "HeldByAnother not defined in claim.py"
        assert "ClaimNotHeld" in class_names, "ClaimNotHeld not defined in claim.py"

      test_state_init_registers_claim_verbs:
        tree = _parse_or_skip(STATE_INIT, "state/__init__.py")
        # Count @state_app.command() decorated functions
        # A decorator with attribute access: state_app.command("claim")
        decorators = []
        for node in ast.walk(tree):
            if isinstance(node, ast.FunctionDef):
                for dec in node.decorator_list:
                    decorators.append(ast.unparse(dec))
        command_calls = [d for d in decorators if "state_app.command" in d]
        assert len(command_calls) >= 9, (
            f"Expected >= 9 @state_app.command() verbs, found {len(command_calls)}: {command_calls}"
        )

      test_state_init_anonymous_gate_present:
        source = STATE_INIT.read_text()
        assert "anonymous claims refused" in source, (
            "CLAIM-03 anonymous-refusal message not found in state/__init__.py"
        )

      test_phase_04_summaries_present:
        if not PHASE_DIR.exists():
            pytest.skip("Phase 04 directory not present yet")
        plans = sorted(PHASE_DIR.glob("04-*-PLAN.md"))
        for plan in plans:
            # e.g. 04-01-PLAN.md → 04-01-SUMMARY.md
            summary = plan.parent / plan.name.replace("-PLAN.md", "-SUMMARY.md")
            assert summary.exists(), f"Missing SUMMARY for {plan.name}: expected {summary.name}"

    IMPORTANT — self-contained rule: all helper functions are defined inline in this file.
    No imports from other structural test files. No shared conftest in tests/structural/.
    This mirrors the explicit "each structural file is self-contained" rule from Plan 02-05 SUMMARY.
  </action>

  <verify>
    <automated>bash scripts/test.sh structural</automated>
  </verify>

  <done>
    - tests/structural/test_phase_04_shape.py exists with >= 11 test functions
    - bash scripts/test.sh structural exits 0 (all Phase 2+3+4 structural tests pass)
    - No imports from other structural files
  </done>
</task>

<task type="auto">
  <name>Task 2: Run verify-phase.sh 04 and resolve any failures</name>

  <read_first>
    - scripts/verify-phase.sh — full dispatcher surface (run bash scripts/verify-phase.sh help to see checks). Understand what each check does before interpreting failures.
    - CLAUDE.md — verify-phase.sh section: "run bash scripts/verify-phase.sh 04, read the output, apply judgment about whether the phase goal is delivered".
    - .planning/phases/04-long-lived-claims/ — all four PLAN.md files. Every PLAN.md must have a corresponding SUMMARY.md before this task passes.
  </read_first>

  <action>
    Run bash scripts/verify-phase.sh 04 and read the structured markdown report.

    Checks to pass:
      1. Test suite: bash scripts/test.sh all → 0 failures, 0 errors
      2. Anti-pattern grep: grep for TBD|FIXME|XXX|HACK|TODO|PLACEHOLDER in src/, tests/, scripts/ → 0 matches
         If matches found: fix each one (replace placeholder comment with real implementation or remove)
      3. em-proj on PATH + --version: confirm the installed binary is current
      4. SUMMARY.md presence: every 04-NN-PLAN.md has a 04-NN-SUMMARY.md
         If any SUMMARY is missing: create a minimal one (title + "Complete — no deviations")
      5. Commit traceability: recent commits reference phase 04 plans
         If no commits yet for this phase: ensure feat/test commits from Plans 01-03 are present

    For any check that fails: fix the root cause, not the check. Do NOT comment out assertions
    or modify verify-phase.sh to skip checks. Every check must pass for the phase to be done.

    After verify-phase.sh 04 exits 0, write the final commit for Plan 04-04:
      git add tests/structural/test_phase_04_shape.py
      git commit -m "test(04-04): structural shape assertions + verify-phase.sh 04 clean"
    (No Co-Authored-By trailer.)
  </action>

  <verify>
    <automated>bash scripts/verify-phase.sh 04</automated>
  </verify>

  <done>
    - bash scripts/verify-phase.sh 04 exits 0
    - bash scripts/test.sh all exits 0
    - bash scripts/test.sh structural exits 0
    - bash scripts/test.sh multiprocess exits 0
    - No TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER in src/, tests/, scripts/
    - All four 04-NN-SUMMARY.md files exist in phases/04-long-lived-claims/
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| structural test → source files | Tests read source files via Path.read_text() and ast.parse(); read-only, no execution |
| verify-phase.sh → test suite | Runs the full test suite; test isolation (db=15, clean_db) prevents production side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04-01 | Tampering | Structural test bypass via ast comment | mitigate | Structural tests use ast.parse() (not grep) for code-property assertions; comments are not AST nodes and cannot fool AST-based checks |
| T-4-04-02 | Information Disclosure | Anti-pattern grep exposing internal comments | accept | TBD/TODO grep is internal to the dev workflow; output goes to the developer, not a public surface |
</threat_model>

<verification>
bash scripts/test.sh structural
bash scripts/verify-phase.sh 04
</verification>

<success_criteria>
- bash scripts/test.sh structural exits 0 (all Phase 2+3+4 structural tests pass)
- bash scripts/verify-phase.sh 04 exits 0 (all deterministic checks pass)
- tests/structural/test_phase_04_shape.py has >= 11 test functions
- All four 04-NN-SUMMARY.md files present in .planning/phases/04-long-lived-claims/
</success_criteria>

<output>
After completion, create .planning/phases/04-long-lived-claims/04-04-SUMMARY.md
</output>
