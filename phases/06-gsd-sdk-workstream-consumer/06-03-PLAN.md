---
phase: 06-gsd-sdk-workstream-consumer
plan: "03"
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - tests/structural/test_phase_06_shape.py
autonomous: true
requirements: [CONSUMER-01, CONSUMER-02]

must_haves:
  truths:
    - "tests/structural/test_phase_06_shape.py exists and passes via `bash scripts/test.sh structural`"
    - "The structural test resolves the npm-installed gsd-sdk workstream.js via shutil.which('gsd-sdk') + walk to lib/node_modules/get-shit-done-cc/sdk/dist/query/workstream.js (Q-D portable resolver)"
    - "The structural test xfail-fails (not silently skips) when the resolved workstream.js is missing — reversion is VISIBLE in CI output (Pitfall #6 + Pattern D)"
    - "The structural test asserts BOTH sdk/dist/query/workstream.js AND sdk/src/query/workstream.ts contain the literal 'em-proj' (Q-C lockstep)"
    - "The structural test asserts the em-proj shellout appears BEFORE setActiveWorkstream within the workstreamSet function body (gate-precedes-write ordering)"
    - "Every 06-NN-PLAN.md in the Phase 6 directory has a corresponding 06-NN-SUMMARY.md (SUMMARY coverage check, same pattern as Phase 4/5)"
    - "bash scripts/verify-phase.sh 06 exits 0 (all deterministic checks pass: tests + anti-pattern grep + SUMMARY presence + commit traceability)"
  artifacts:
    - path: "tests/structural/test_phase_06_shape.py"
      provides: "AST/source-grep structural invariants for Phase 6 — auditing the cross-repo gsd-sdk patch + SUMMARY coverage"
      contains: "test_gsd_sdk_workstream_js_contains_em_proj_shellout"
  key_links:
    - from: "test_phase_06_shape.py"
      to: "npm-installed sdk/dist/query/workstream.js"
      via: "_resolve_workstream_artifact helper (shutil.which + walk up to lib/node_modules/get-shit-done-cc/)"
      pattern: "_resolve_workstream_artifact"
    - from: "test_gsd_sdk_workstream_js_contains_em_proj_shellout"
      to: "literal 'em-proj' substring + 'setActiveWorkstream' ordering check"
      via: "re.search for workstreamSet body + .find() index comparison"
      pattern: "em_proj_idx > 0.*set_active_idx > em_proj_idx"
    - from: "test_phase_06_summaries_present"
      to: "Phase 6 PLAN/SUMMARY pairing"
      via: "glob 06-*-PLAN.md → assert 06-*-SUMMARY.md exists"
      pattern: "PLAN.md.*SUMMARY.md"
---

<objective>
Encode Phase 6 acceptance criteria as runtime pytest assertions in
`tests/structural/test_phase_06_shape.py`, then run
`bash scripts/verify-phase.sh 06` as the final acceptance gate.

Purpose:
  - CONSUMER-01 is structurally checkable only via cross-repo audit — the
    npm-installed sdk/dist/query/workstream.js must contain the em-proj
    shellout, OR the patch was never landed / was reverted by an `npm
    install -g get-shit-done-cc` upgrade. The structural test exists to
    catch that reversion VISIBLY (Pitfall #6 / Pattern D).
  - Pattern D from 06-PATTERNS.md: use pytest.xfail (NOT pytest.skip) when
    the cross-repo artifact is missing, so absence is reported in CI
    output instead of silently passing.
  - Q-C lockstep: assert BOTH .js (runtime-loaded) AND .ts (source-of-truth)
    contain the em-proj shellout.
  - Q-D portable resolver: do NOT hardcode `/Users/emonical/.nvm/.../v22.13.1/...`
    in the test. Resolve via `shutil.which("gsd-sdk")` + walk to the
    package root (mirrors Phase 5's primary+fallback SKILL.md resolver).
  - SUMMARY coverage: every PLAN.md gets a matching SUMMARY.md (same shape
    as Phase 4/5).
  - verify-phase.sh 06 is the deterministic end-to-end gate the human
    (or `/gsd-verify-work`) runs to confirm the phase is done.

Output:
  - tests/structural/test_phase_06_shape.py with >= 6 test functions
  - bash scripts/verify-phase.sh 06 exits 0
  - Plans 06-01, 06-02, 06-03 each have a SUMMARY.md in the phase directory
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/06-gsd-sdk-workstream-consumer/06-RESEARCH.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/06-gsd-sdk-workstream-consumer/06-PATTERNS.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/06-gsd-sdk-workstream-consumer/06-01-SUMMARY.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/06-gsd-sdk-workstream-consumer/06-02-SUMMARY.md
</context>

<interfaces>
<!-- Self-contained helper convention (Phase 1+2+3+4+5 precedent — locked) -->

Every structural test file copies the helpers it needs verbatim from a
sibling phase rather than importing from a shared module. Per Plan 02-05
SUMMARY and reinforced through Phases 3-5.

Helpers Phase 6 needs (subset of test_phase_04_shape.py / test_phase_05_shape.py):
  _resolve_workstream_artifact(filename: str) -> Path | None   (NEW in Phase 6)
  (No AST helpers needed — Phase 6 audits JS/TS files via source grep + regex,
   not Python AST.)

<!-- Resolver pattern (Q-D — locked) -->

def _resolve_workstream_artifact(rel_path: str) -> Path | None:
    """Resolve <gsd-sdk-package-root>/<rel_path> portably.

    Strategy:
      1. shutil.which("gsd-sdk") → bin shim symlink
         (e.g. /Users/<u>/.nvm/versions/node/<v>/bin/gsd-sdk)
      2. Resolve symlinks: Path(...).resolve()
         (resolves to .../lib/node_modules/get-shit-done-cc/bin/gsd-sdk.js)
      3. Walk up two dirs: parent.parent → .../get-shit-done-cc/
      4. Descend into rel_path: pkg_root / rel_path

    Returns None if gsd-sdk is not on PATH OR the candidate does not exist.
    The CALLER is responsible for pytest.xfail when None is returned.
    """
    sdk_bin = shutil.which("gsd-sdk")
    if not sdk_bin:
        return None
    bin_path = Path(sdk_bin).resolve()
    pkg_root = bin_path.parent.parent
    candidate = pkg_root / rel_path
    return candidate if candidate.exists() else None

<!-- xfail-on-missing-cross-repo pattern (Pattern D — locked) -->

Why xfail (not skip):
  - skip: test reports as SKIPPED, treated as green; reversion invisible
  - xfail: test reports as XFAIL with reason, visible in CI summary
  - The whole POINT of the structural test is to catch the npm-upgrade
    reversion scenario (Pitfall #6). Silent skip defeats the purpose.

Phase 5 precedent (test_phase_05_shape.py:250-254):
  if not SKILL_PATH.exists():
      pytest.xfail("em-global-state SKILL.md not found at ~/.claude/skills/...")
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/structural/test_phase_06_shape.py</name>
  <files>tests/structural/test_phase_06_shape.py</files>
  <read_first>
    - tests/structural/test_phase_05_shape.py — full file; the most-recent structural test. Copy idioms: REPO_ROOT/PHASE_DIR constants, self-contained-helper preamble comment, xfail-on-missing pattern (specifically the SKILL_PATH resolver block at lines 56-59 + xfail call), test_phase_NN_summaries_present pattern at lines 340-361.
    - tests/structural/test_phase_04_shape.py — supplement for the SUMMARY-coverage pattern (lines 192-199 of test_phase_04_shape.py).
    - .planning/phases/06-gsd-sdk-workstream-consumer/06-RESEARCH.md §"Code Examples Example 4" (lines 482-526) — canonical code shape for the workstream.js content + ordering assertions.
    - .planning/phases/06-gsd-sdk-workstream-consumer/06-PATTERNS.md §"File 4" — file-by-file analog mapping and the new `_resolve_workstream_artifact` helper.
    - /Users/emonical/.nvm/versions/node/v22.13.1/lib/node_modules/get-shit-done-cc/sdk/dist/query/workstream.js (patched by Plan 06-01) — confirm the patched content exists before this plan's tests reach for it. If absent: Plan 06-01 either didn't ship or was reverted; flag back to the orchestrator.
  </read_first>
  <action>
    Create tests/structural/test_phase_06_shape.py. Self-contained (no imports
    from sibling structural test files — copy helpers verbatim per the
    "each structural file is self-contained" rule from Plan 02-05 SUMMARY,
    reinforced through Phases 3+4+5).

    Module structure (top-to-bottom):

    1. Future-imports + docstring:
       from __future__ import annotations
       """Module docstring listing which decisions each test encodes.

       Phase 6 invariants:
         - CONSUMER-01: gsd-sdk's workstreamSet shells out to em-proj state
           claim before setActiveWorkstream() — asserted via source-text grep
           + ordering check on the npm-installed sdk/dist/query/workstream.js
         - Q-C lockstep: BOTH .js (runtime-loaded) and .ts (source-of-truth)
           contain the shellout
         - Q-D portable resolver: shutil.which('gsd-sdk') + walk to pkg root
           (no hardcoded nvm path)
         - Pattern D xfail-on-missing: cross-repo absence is visible in CI
         - SUMMARY coverage: every 06-NN-PLAN.md has a 06-NN-SUMMARY.md
       """

    2. Imports (minimal):
       import re
       import shutil
       from pathlib import Path
       import pytest

       # Self-contained — no imports from sibling test_phase_*_shape.py files.

    3. Path constants:
       REPO_ROOT = Path(__file__).resolve().parent.parent.parent
       PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "06-gsd-sdk-workstream-consumer"

    4. Resolver helper (NEW for Phase 6 — Q-D):
       def _resolve_workstream_artifact(rel_path: str) -> Path | None:
           """See <interfaces> block above for full contract."""
           sdk_bin = shutil.which("gsd-sdk")
           if not sdk_bin:
               return None
           bin_path = Path(sdk_bin).resolve()
           pkg_root = bin_path.parent.parent
           candidate = pkg_root / rel_path
           return candidate if candidate.exists() else None

       WORKSTREAM_JS = _resolve_workstream_artifact("sdk/dist/query/workstream.js")
       WORKSTREAM_TS = _resolve_workstream_artifact("sdk/src/query/workstream.ts")

    5. Test functions (6 minimum):

       Test A — test_gsd_sdk_workstream_js_contains_em_proj_shellout:
         if WORKSTREAM_JS is None:
             pytest.xfail(
                 "gsd-sdk not installed (or workstream.js not found via "
                 "shutil.which('gsd-sdk') + walk to "
                 "lib/node_modules/get-shit-done-cc/sdk/dist/query/) — "
                 "cannot audit consumer patch. If gsd-sdk IS installed, the "
                 "patch may have been reverted by `npm install -g "
                 "get-shit-done-cc` upgrade — re-apply Plan 06-01."
             )
         source = WORKSTREAM_JS.read_text()
         assert "'em-proj'" in source or '"em-proj"' in source, (
             f"{WORKSTREAM_JS} does not reference 'em-proj' — Phase 6 consumer "
             "patch either never landed or was reverted by an `npm install -g` "
             "upgrade. Re-apply Plan 06-01 to restore."
         )

       Test B — test_gsd_sdk_workstream_js_shellout_precedes_set_active:
         """Ordering invariant: the em-proj gate MUST appear before
         setActiveWorkstream within workstreamSet. If the gate is added
         AFTER the write, it provides no concurrency protection.
         """
         if WORKSTREAM_JS is None:
             pytest.xfail("see test_gsd_sdk_workstream_js_contains_em_proj_shellout")
         source = WORKSTREAM_JS.read_text()
         # Match the workstreamSet handler body. The .js may use either
         # `export const workstreamSet = async` or `exports.workstreamSet =
         # async` depending on the compiler output. Match both.
         m = re.search(
             r"workstreamSet\s*=\s*async[\s\S]+?\n\}\s*;?",
             source,
         )
         assert m, (
             "Could not locate workstreamSet handler in workstream.js. "
             "File format may have changed in an upstream release."
         )
         body = m.group(0)
         em_proj_idx = body.find("em-proj")
         set_active_idx = body.find("setActiveWorkstream")
         assert em_proj_idx > 0, (
             f"em-proj shellout missing from workstreamSet body in {WORKSTREAM_JS}"
         )
         assert set_active_idx > em_proj_idx, (
             "em-proj shellout must appear BEFORE setActiveWorkstream call "
             "(gate-precedes-write invariant). Found em-proj at idx "
             f"{em_proj_idx}, setActiveWorkstream at idx {set_active_idx}."
         )

       Test C — test_gsd_sdk_workstream_js_contains_held_by_another_branch:
         """Assert the status-3 branch is wired (loser receives structured
         envelope, not silent fall-through)."""
         if WORKSTREAM_JS is None:
             pytest.xfail("see test_gsd_sdk_workstream_js_contains_em_proj_shellout")
         source = WORKSTREAM_JS.read_text()
         assert "held_by_another" in source, (
             "workstream.js missing 'held_by_another' branch — the loser of a "
             "race would not receive the structured envelope CONSUMER-02 demands."
         )

       Test D — test_gsd_sdk_workstream_js_contains_enoent_fallback:
         """Assert the Q-B silent fallback branch is wired (em-proj missing
         from PATH → stderr warning + legacy write, not hard error)."""
         if WORKSTREAM_JS is None:
             pytest.xfail("see test_gsd_sdk_workstream_js_contains_em_proj_shellout")
         source = WORKSTREAM_JS.read_text()
         assert "ENOENT" in source, (
             "workstream.js missing 'ENOENT' branch — em-proj-missing-from-PATH "
             "case will not fall through to the legacy unguarded write (Q-B)."
         )

       Test E — test_gsd_sdk_workstream_ts_contains_em_proj_shellout:
         """Q-C lockstep: the TS source-of-truth must also contain the
         shellout, even though only the .js is runtime-loaded. Symmetry
         keeps future upstream-PR work trivial and makes the patch visible
         to readers of the .ts source.
         """
         if WORKSTREAM_TS is None:
             pytest.xfail(
                 "gsd-sdk TS source not found via "
                 "lib/node_modules/get-shit-done-cc/sdk/src/query/workstream.ts — "
                 "Q-C lockstep cannot be verified"
             )
         ts_source = WORKSTREAM_TS.read_text()
         assert "em-proj" in ts_source, (
             f"{WORKSTREAM_TS} (TS source-of-truth) lacks 'em-proj' shellout — "
             "Q-C symmetry contract broken; .ts and .js must be in lockstep."
         )

       Test F — test_phase_06_summaries_present:
         """SUMMARY coverage check, identical pattern to Phase 4/5."""
         if not PHASE_DIR.exists():
             pytest.skip(
                 f"{PHASE_DIR.relative_to(REPO_ROOT)} not present — planning "
                 "worktree may not be attached on this checkout"
             )
         plans = sorted(PHASE_DIR.glob("06-*-PLAN.md"))
         if not plans:
             pytest.skip(f"no 06-*-PLAN.md files yet under "
                         f"{PHASE_DIR.relative_to(REPO_ROOT)}")
         for plan in plans:
             summary = plan.parent / plan.name.replace("-PLAN.md", "-SUMMARY.md")
             assert summary.exists(), (
                 f"Missing SUMMARY for {plan.name}: expected {summary.name} "
                 f"in {PHASE_DIR.relative_to(REPO_ROOT)}"
             )

    NOTE on xfail-vs-skip discipline:
      - Tests A-E use xfail when the cross-repo artifact is missing (the
        whole point — npm reversion must be visible).
      - Test F uses skip for PHASE_DIR absence because PHASE_DIR is em-proj-
        internal, not cross-repo; absence indicates "this checkout has no
        planning worktree attached" which is a legitimate developer setup
        (not a regression).

    NOTE on regex resilience: the workstreamSet regex in Test B is loose
      (`[\s\S]+?` is non-greedy any-char). If gsd-sdk's compiler emits a
      different handler shape in a future release (e.g. arrow function
      with single-expression body), the regex may need adjustment. The
      test failure mode in that case is "Could not locate workstreamSet
      handler" — actionable, not silent.
  </action>
  <acceptance_criteria>
    - File tests/structural/test_phase_06_shape.py exists with >= 6 test functions
    - `bash scripts/test.sh structural -k test_phase_06_shape` exits 0 (all tests pass OR xfail with reason; no test FAILS)
    - `bash scripts/test.sh structural` exits 0 (full structural suite green — Phase 2/3/4/5 invariants still hold)
    - Tests A, B, C, D, E all PASS when WORKSTREAM_JS and WORKSTREAM_TS resolve (Plan 06-01 patched both files); they xfail (NOT skip, NOT fail) if either file is missing
    - Test F PASSES when 06-01-SUMMARY.md and 06-02-SUMMARY.md exist (Plan 06-03's own SUMMARY is written AFTER this test passes, so during Task 1 the test may report a 06-03 SUMMARY missing — that is expected and addressed in Task 2)
    - No imports from any other tests/structural/*.py file
    - The hardcoded path `/Users/emonical/.nvm/versions/node/v22.13.1` does NOT appear in the test file (Q-D portable resolver only)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh structural -k test_phase_06_shape -v</automated>
  </verify>
  <done>
    test_phase_06_shape.py exists with all 6 tests; tests A-E pass when both
    workstream.{js,ts} contain 'em-proj'; test F passes when all expected
    SUMMARY files exist (06-01, 06-02 — 06-03's SUMMARY is created by
    Task 2 of this plan); xfail messages name the specific re-apply step
    on cross-repo absence
  </done>
</task>

<task type="auto">
  <name>Task 2: Run bash scripts/verify-phase.sh 06 as the final acceptance gate</name>
  <files></files>
  <read_first>
    - scripts/verify-phase.sh — full dispatcher surface (run `bash scripts/verify-phase.sh help`). Understand which checks fire for phase 06: test suite (all + structural), Redis backend, em-proj PATH + --version, anti-pattern grep on src/tests/scripts, SUMMARY presence for every PLAN.md, recent commit traceability.
    - .planning/phases/06-gsd-sdk-workstream-consumer/ — full file listing. Confirm 06-01-SUMMARY.md and 06-02-SUMMARY.md exist (created by Plans 06-01 and 06-02). This plan will create 06-03-SUMMARY.md as the FINAL action after verify-phase exits 0.
    - CLAUDE.md §"verify-phase.sh — phase verification dispatcher" — operating instructions for the gate.
  </read_first>
  <action>
    Run the deterministic phase-verification dispatcher:

      bash scripts/verify-phase.sh 06

    Read the structured markdown report. Checks that must pass:

      1. bash scripts/test.sh all → 0 failures, 0 errors (full pytest suite)
      2. bash scripts/test.sh structural → green (test_phase_06_shape.py
         from Task 1 inclusive)
      3. verify-redis-config.sh → PASS or SKIP (redis-unreachable is not a
         Phase 6 failure; production redis-config is a Phase 1 concern)
      4. em-proj on PATH → must be found
      5. em-proj --version → must return a version string
      6. Anti-pattern grep: no TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER tokens
         in src/, tests/, scripts/
      7. SUMMARY.md presence: 06-01-PLAN.md → 06-01-SUMMARY.md (exists from
         Plan 06-01); 06-02-PLAN.md → 06-02-SUMMARY.md (exists from
         Plan 06-02); 06-03-PLAN.md → 06-03-SUMMARY.md (created at end of
         THIS task)
      8. Commit traceability: recent commits reference Phase 06 plans
         (feat/test commits from Plans 06-01 [JS/TS patch — NOTE: those
         edits are OUTSIDE em-proj's repo, so commit traceability for 06-01
         is via the SUMMARY.md alone, not a code commit in em-proj's main
         branch; the planning-branch SUMMARY commit IS the traceability]
         and 06-02 [test files] and 06-03 [structural test])

    Special handling for check #8 (Phase-6-specific):

      Plan 06-01 edits gsd-sdk's npm-installed files OUTSIDE em-proj's
      repository. Those edits produce NO commit in em-proj's main branch.
      The ONLY em-proj-side artifact from Plan 06-01 is 06-01-SUMMARY.md on
      the planning branch. If verify-phase.sh's commit-traceability check
      flags 06-01 as having no main-branch commit, that is EXPECTED and
      not a failure. Document this in 06-03-SUMMARY.md so future archaeology
      knows why feat(06-01): commits aren't on main.

      Plans 06-02 and 06-03 DO produce em-proj-side commits (test files +
      structural test), so feat(06-02): and test(06-03): commits should
      be visible on main and the traceability check should pass for them.

    Order of operations:

      1. Run `bash scripts/verify-phase.sh 06`. Expect failure on check #7
         (06-03-SUMMARY.md doesn't exist yet — that's about to be created).
      2. Inspect the report. ALL OTHER CHECKS (1-6, 8) must pass.
         If any non-SUMMARY check fails: diagnose root cause and fix
         (never silence the check).
      3. Create 06-03-SUMMARY.md per the standard summary template (see
         <output> block below).
      4. Re-run `bash scripts/verify-phase.sh 06`. All checks must pass
         this time (exit 0).

    Common failure modes to watch for:
      - A new TBD/TODO accidentally introduced in the new test files →
        remove before re-running
      - Plan 06-01's smoke-test cleanup left a stale claim in db=15 →
        run `EM_PROJ_REDIS_DB=15 em-proj state release workstream.active`
        to clean up, then re-run
      - test_phase_06_shape.py's regex (Test B) fails to match because
        gsd-sdk's compiled output uses a different handler shape than
        anticipated → adjust the regex in Task 1 to match the actual
        compiled form (this is a real risk; verify by inspecting the
        patched workstream.js if Test B fails)
      - Plan 06-01's smoke-test created a `/tmp/em-proj-smoke-06` dir that
        survived → already auto-cleaned by Plan 06-01's verify gate, but
        check `ls /tmp/em-proj-smoke-06 2>&1 | head -1` and remove if present

    After verify-phase.sh 06 exits 0:
      - Commit test_phase_06_shape.py (em-proj-side, on main):
          git add tests/structural/test_phase_06_shape.py
          git commit -m "test(06-03): structural shape assertions + verify-phase.sh 06 clean"
        (NO Co-Authored-By trailer, per global rule + project CLAUDE.md)
      - Then write 06-03-SUMMARY.md on the planning-branch worktree.
  </action>
  <acceptance_criteria>
    - `bash scripts/verify-phase.sh 06` exits 0
    - Report shows all checks PASS (or SKIP for redis-unreachable, which is allowed)
    - test_phase_06_shape.py committed to main as `test(06-03): ...` (single commit; no Co-Authored-By trailer)
    - 06-03-SUMMARY.md exists on the planning branch (created AFTER verify-phase exits 0)
    - 06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-03-SUMMARY.md all present in `.planning/phases/06-gsd-sdk-workstream-consumer/`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-phase.sh 06</automated>
  </verify>
  <done>
    bash scripts/verify-phase.sh 06 exits 0; all three SUMMARY.md files
    exist; test_phase_06_shape.py committed to main; Phase 6 is structurally
    complete and ready for /gsd-verify-work
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structural test → npm-installed gsd-sdk artifacts | Tests read .js/.ts via Path.read_text(); read-only, no execution. Cross-repo boundary means the test cannot prevent reversion, only DETECT it. |
| Structural test → PHASE_DIR | Tests glob PLAN.md/SUMMARY.md; read-only. |
| verify-phase.sh → full test suite | Runs all tests including Phase 6's multi-process tests against Redis db=15; clean_db fixture isolates from prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T (Tampering) | npm install reversion of Plan 06-01's patch | mitigate | Test A (test_gsd_sdk_workstream_js_contains_em_proj_shellout) greps the runtime-loaded workstream.js for the 'em-proj' literal. On `npm install -g get-shit-done-cc@latest`, the file is overwritten upstream; the test then xfails with an actionable message ("re-apply Plan 06-01"). xfail (not skip) ensures the regression is VISIBLE in CI output (Pattern D). |
| T-06-07 | T (Tampering) | Structural test bypassed by comment in workstream.js | mitigate | The 'em-proj' literal grep is a plain substring check. The literal CANNOT appear in a JS comment without also being present in the source — comments DO count as source content. (The reverse — appearing only in a comment without functional use — would be a false positive; but Test B's ordering check requires 'em-proj' to appear BEFORE setActiveWorkstream within the workstreamSet body, which a stray comment cannot satisfy.) |
| T-06-08 | T (Tampering) | Hardcoded nvm path in test breaks portability | mitigate | Q-D portable resolver (`shutil.which("gsd-sdk")` + walk) is the ONLY path resolution mechanism; the test file does NOT contain the literal `/Users/emonical/.nvm/`. Acceptance criterion enforces this via grep. |
</threat_model>

<verification>
- bash scripts/test.sh structural -k test_phase_06_shape -v → all tests pass (or xfail with reason on cross-repo absence)
- bash scripts/test.sh structural → green (Phase 2/3/4/5/6 invariants together)
- bash scripts/test.sh all → green (full suite, no regression)
- bash scripts/verify-phase.sh 06 → exits 0
- grep `/Users/emonical/.nvm` tests/structural/test_phase_06_shape.py → no matches (portable resolver only)
- All three Phase 6 SUMMARY.md files exist
</verification>

<success_criteria>
- tests/structural/test_phase_06_shape.py exists with 6+ test functions encoding CONSUMER-01 + Q-C lockstep + Q-D portable resolver + Pattern D xfail + SUMMARY coverage
- bash scripts/verify-phase.sh 06 exits 0 (all deterministic checks pass)
- Phase 6 deliverables complete:
  - CONSUMER-01 satisfied: gsd-sdk's workstream.set shells out to em-proj state claim (Plan 06-01 + structural assertion in this plan)
  - CONSUMER-02 satisfied: two-session race produces deterministic outcome with structured loser envelope (Plan 06-02 race test + this plan's structural xfail-on-reversion gate)
  - SC#3 satisfied: clobber-vs-resolution demo runnable via `bash scripts/test.sh multiprocess -k clobber_demo` (Plan 06-02 demo file)
- Phase 6 is ready for /gsd-verify-work to confirm DELIVERY (vs. just check-passing)
</success_criteria>

<output>
After completion of Task 2 (verify-phase.sh 06 exits 0), create
`.planning/phases/06-gsd-sdk-workstream-consumer/06-03-SUMMARY.md` on the
planning branch worktree.

The SUMMARY must include:

  1. Standard summary preamble (link to PLAN, completion date, no deviations).

  2. Phase 6 closeout notes:
     - CONSUMER-01 delivered (per Plan 06-01 cross-repo patch + this plan's
       structural xfail gate that catches reversion).
     - CONSUMER-02 delivered (per Plan 06-02 race test).
     - SC#3 delivered (per Plan 06-02 clobber-demo file).

  3. CRITICAL cross-repo recovery notes:
     - Plan 06-01's patched files live at `/Users/emonical/.nvm/versions/
       node/v22.13.1/lib/node_modules/get-shit-done-cc/sdk/{dist,src}/query/
       workstream.{js,ts}`. These files are NOT in em-proj's git repository.
     - Any future `npm install -g get-shit-done-cc` will overwrite both
       files. The structural test `test_gsd_sdk_workstream_js_contains_em_proj_shellout`
       will then XFAIL in CI (visible, not silent).
     - Recovery: re-run Plan 06-01's Tasks 1 and 2 (idempotent file edits).
     - Long-term: upstream PR to gsd-sdk is OUT OF M1 SCOPE per Q-G.

  4. Commit traceability note: Plan 06-01 produces NO main-branch commit
     in em-proj because the edited files are outside the repo. Plans 06-02
     and 06-03 produce main-branch test commits as normal. All three plans
     have planning-branch SUMMARY.md commits.

  5. Hand-off to /gsd-verify-work: verify-phase.sh 06 has already validated
     all deterministic checks; /gsd-verify-work spawns a verifier that
     applies judgment about whether the phase GOAL (end-to-end clobber
     prevention) is DELIVERED, not just whether checks pass.
</output>
