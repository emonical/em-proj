---
phase: 02-cli-shell-kv-primitive
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - tests/unit/test_redis_unreachable_verbs.py
  - tests/structural/test_phase_02_shape.py
  - scripts/test.sh
autonomous: true
requirements: [REDIS-02, CLI-03, CLI-04, CLI-05]
tags: [redis-02, structural, single-chokepoint, monkeypatch, dispatcher, end-to-end-verification]

must_haves:
  truths:
    - "Per D-19: REDIS-02 is proved by a unit test that monkey-patches `em_proj.redis_client.get_client` (or its returned client) to make `ping()` raise `redis.ConnectionError`, invokes a state verb via typer.testing.CliRunner, and asserts exit code 1 + the locked stderr line containing `Redis unreachable at 127.0.0.1:6379` and `brew services start redis`"
    - "Per D-19 (structural carry-forward from Phase 1 D-19): a structural test in `tests/structural/test_phase_02_shape.py` enforces tree-wide that no source file outside `src/em_proj/redis_client.py` constructs `redis.Redis(...)` directly — preserves D-18 single-chokepoint invariant"
    - "Per D-17: structural test enforces the state package layout — `src/em_proj/state/__init__.py` and `src/em_proj/state/kv.py` both exist; `kv.py` does not import typer; `state/__init__.py` does not catch `redis.ConnectionError` or `redis.TimeoutError`"
    - "Per D-15: structural test enforces that `src/em_proj/output.py` exports the locked names `SCHEMA_VERSION`, `resolve_json_mode`, `emit_ok`, `emit_not_found`, `emit_error` and that `SCHEMA_VERSION` is the literal string `\"1\"`"
    - "Per Decision Coverage Gate: a structural test reads all `.planning/phases/02-cli-shell-kv-primitive/02-*-PLAN.md` files in Python (no shell pipes), extracts every `D-NN` reference, asserts the union covers D-01..D-19 — so a single allowlisted `bash scripts/test.sh structural` call replaces the previous pipe-chained `grep | sort -u | wc -l` recipe"
    - "All five ROADMAP Phase 2 success criteria verifiable: install + per-subcommand --help (criterion 1), KV ops with semantic exit codes (criterion 2), --ttl eviction (criterion 3 — verified in-process by Plan 04 `test_set_ttl_then_get_evicts_after_expiry` and manually via CLI in Task 4 of this plan), JSON when non-TTY + schema_version + errors to stderr (criterion 4), one-line Redis-unreachable error with exit 1 (criterion 5 — Task 1 of this plan)"
    - "CLI-03 fully satisfied: `em-proj --help`, `em-proj state --help`, and each of `em-proj state {get,set,del,list} --help` all render typer auto-help (Plan 01 + Plan 04 + Task 4 of this plan)"
    - "Optional: a `state-unit` (or similarly named) dispatcher subcommand in `scripts/test.sh` makes running just `tests/unit/test_state_*.py` ergonomic; landed in a separate `chore:` commit BEFORE tests use it, per CLAUDE.md project policy"
  artifacts:
    - path: "tests/unit/test_redis_unreachable_verbs.py"
      provides: "REDIS-02 monkey-patched ConnectionError unit test per D-19"
      min_lines: 40
    - path: "tests/structural/test_phase_02_shape.py"
      provides: "AST-based shape tests enforcing D-15, D-17, D-18, D-19 invariants + a decision-coverage test that scans all 02-*-PLAN.md files in Python"
      min_lines: 120
    - path: "scripts/test.sh"
      provides: "(optional) `state-unit` subcommand for the Phase 2 unit test subset"
  key_links:
    - from: "tests/unit/test_redis_unreachable_verbs.py"
      to: "em_proj.state (each verb)"
      via: "CliRunner.invoke(app, ['state', VERB, ...]) with monkeypatched get_client"
      pattern: "monkeypatch.*get_client"
    - from: "tests/structural/test_phase_02_shape.py"
      to: "src/em_proj/ source tree"
      via: "ast.parse + walk + assert"
      pattern: "ast\\.parse"
    - from: "tests/structural/test_phase_02_shape.py"
      to: ".planning/phases/02-cli-shell-kv-primitive/02-*-PLAN.md"
      via: "Path.read_text + re.findall('D-[0-9]+')"
      pattern: "re\\.findall.*D-"
---

<objective>
Close out Phase 2 with the verification that proves the user-facing REDIS-02 UX works and the structural invariants (D-15, D-17, D-18, D-19) survive future edits. This plan adds no production code — it adds tests and (optionally) a test-dispatcher subcommand. After this plan, the Phase 2 verifier can run `bash scripts/verify-phase.sh 02` and get a clean report.

Purpose:
1. Prove REDIS-02 at the user-facing surface (every state verb) per D-19: monkey-patch `get_client`'s return value so `ping()` raises `ConnectionError`, invoke each verb via CliRunner, assert the locked stderr line + exit code 1 + no traceback.
2. Lock the structural invariants tree-wide so a future plan cannot accidentally regress D-18 single-chokepoint, D-17 package layout, or D-15 helper exports.
3. Encode the Decision Coverage Gate as a pytest test (NOT a shell `grep | sort | wc` pipeline) so it lives inside the structural suite and runs under one allowlisted dispatcher call.
4. (Optional but recommended) Add a `state-unit` subcommand to `scripts/test.sh` if the developer ergonomics warrant it — separate `chore:` commit, per CLAUDE.md.

Output:
- `tests/unit/test_redis_unreachable_verbs.py` — one parametrized REDIS-02 test per verb (4 cases) + assertions on stderr content, exit code, and traceback absence.
- `tests/structural/test_phase_02_shape.py` — ~13 AST-based structural assertions + 1 decision-coverage test following the Phase 1 `test_conftest_shape.py` precedent.
- (Optional) `scripts/test.sh` adds `state-unit` (or similar) subcommand in a separate commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cli-shell-kv-primitive/02-CONTEXT.md
@.planning/phases/01-test-harness-redis-foundation/01-CONTEXT.md
@.planning/phases/02-cli-shell-kv-primitive/02-04-PLAN.md
@CLAUDE.md
@src/em_proj/redis_client.py
@src/em_proj/state/__init__.py
@src/em_proj/state/kv.py
@src/em_proj/output.py
@tests/structural/test_conftest_shape.py
@tests/unit/test_redis_client.py
@scripts/test.sh
@scripts/verify-phase.sh

<interfaces>
<!-- Patterns to mirror and surfaces to assert. -->

From tests/structural/test_conftest_shape.py (Phase 1 precedent):
- Helper functions: `_find_assign(tree, name)`, `_find_funcdef(tree, name)`, `_fixture_scope(func)`, `_iter_imports(tree)`, `_iter_attribute_chains(tree)`, `_iter_method_calls(tree, method_name)`, `_class_has_decorator(cls, name)`
- Pattern: per-target `@pytest.fixture(scope="module")` to read+parse source once, skip if file absent
- Pattern: AST checks for code-level invariants; source-text grep only for non-Python checks
- Pattern: `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`

From tests/unit/test_redis_client.py (Phase 1 monkeypatch pattern):
- `def _raise_connerr(*args, **kwargs): raise redis.ConnectionError("Connection refused")`
- `monkeypatch.setattr(client, "ping", _raise_connerr)`
- `with pytest.raises(SystemExit) as exc_info: ...`
- `assert exc_info.value.code == 1`
- `assert "Redis unreachable at 127.0.0.1:6379" in captured.err`
- `assert "brew services start redis" in captured.err`
- `assert "Traceback" not in captured.err`

From em_proj.redis_client (Phase 1):
- `get_client()` is the chokepoint; module-level `_client` singleton; `_reset_for_tests()` clears it between tests
- `die_if_redis_unreachable(client)` is called by Plan 04 verbs BEFORE any kv op
- Strategy for Task 1: monkeypatch `em_proj.redis_client._client.ping` (or use `_reset_for_tests()` + monkeypatch the freshly-created singleton's `ping`) so that when verbs call `die_if_redis_unreachable(client)` the ping raises ConnectionError, which the wrapper catches → prints the D-17 line → raises `_RedisUnreachable(SystemExit(1))`

From scripts/test.sh (current dispatcher):
- Existing subcommands: unit, multiprocess, harness, structural, all, conftest-check, collect, help
- Pattern for adding: case branch in the `case "$cmd" in ... esac` block + entry in the `# Subcommands:` doc comment + entry in CLAUDE.md table
- IMPORTANT: per CLAUDE.md, if Phase 2 adds a subcommand, it MUST land in a separate `chore:` commit BEFORE tests that use it (so the dispatcher allowlist stays in sync)

From scripts/verify-phase.sh (existing — already covers most Plan 05 verification):
- `bash scripts/verify-phase.sh 02` runs `test.sh all` + `test.sh structural` + `verify-redis-config.sh` + checks `em-proj` on PATH + `em-proj --version` + anti-pattern grep + SUMMARY presence per PLAN — all in ONE allowlisted Bash call
- Exit code 0 = all checks pass; 1 = at least one failed
- This is the canonical single-command for end-to-end phase verification — Task 4 uses it directly to avoid composing acceptance criteria from chained ad-hoc Bash commands
</interfaces>
</context>

<deferred>
- A user-facing `em-proj health` verb is explicitly out of scope for Phase 2 (per Phase 1 D-18 — no current REQ-ID). REDIS-02 is exercised through the regular state verbs, not a dedicated health check.
- REQUIREMENTS.md traceability table still maps CLI-01 and CLI-02 to Phase 2 (stale per Phase 1 D-04..D-06). The roadmapper will re-run after Phase 2 close; do not edit REQUIREMENTS.md from this plan.
- Adding `tests/structural/__init__.py` (if not already present per Phase 1) — leave to the executor to discover and add if pytest's package mode requires it; this is a known Phase 1 carry-forward consideration.
</deferred>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REDIS-02 monkey-patched ConnectionError unit test per D-19</name>
  <files>tests/unit/test_redis_unreachable_verbs.py</files>
  <read_first>
    - tests/unit/test_redis_client.py (the existing Phase 1 monkeypatch + capsys pattern for `die_if_redis_unreachable`)
    - src/em_proj/redis_client.py (the `_RedisUnreachable(SystemExit)` machinery — exit 1, no traceback)
    - src/em_proj/state/__init__.py (confirm each verb calls `die_if_redis_unreachable(client)` early)
    - .planning/phases/02-cli-shell-kv-primitive/02-CONTEXT.md (D-18, D-19)
  </read_first>
  <behavior>
    Use `typer.testing.CliRunner(mix_stderr=False)` to invoke each state verb with a monkey-patched Redis client whose `ping()` raises `redis.ConnectionError`. Assert exit code 1, the D-17 stderr line, and absence of a Python traceback. Parametrize over the four verb invocations:

    - `["state", "get", "anykey"]`
    - `["state", "set", "anykey", "anyvalue"]`
    - `["state", "del", "anykey"]`
    - `["state", "list"]`

    Required tests:
    - `test_each_verb_surfaces_redis_unreachable_message` (parametrize over the four invocations above):
      1. Reset the Redis client singleton: `em_proj.redis_client._reset_for_tests()` (autouse fixture or per-test)
      2. Construct the client lazily: `client = em_proj.redis_client.get_client()` (just builds the singleton; no socket)
      3. Monkeypatch `client.ping` to raise `redis.ConnectionError("Connection refused")`
      4. `result = runner.invoke(app, argv)` (using the parametrized argv)
      5. Assert `result.exit_code == 1`
      6. Assert `"Redis unreachable at 127.0.0.1:6379" in result.stderr`
      7. Assert `"brew services start redis" in result.stderr`
      8. Assert `"Traceback" not in result.stderr` (D-17 no-traceback contract)
      9. Assert `result.stdout == ""` (PROJECT.md: errors to stderr)

    - `test_verb_does_not_swallow_connection_error`: same setup but assert that the test goes through the wrapper path (i.e., the verb did NOT catch `redis.ConnectionError` itself). If the verb caught it, we'd see no `"Redis unreachable"` message and either exit 0 (silent swallow) or a different message. The assertion in `test_each_verb_surfaces_redis_unreachable_message` already covers this implicitly, but include this as a named regression gate so a future reviewer scanning test names sees the invariant called out.

    - `test_redis_unreachable_also_handles_timeout_via_verbs` (one verb, not parametrized): same setup but raise `redis.TimeoutError("timed out")` instead of `ConnectionError`. Assert the same exit 1 + stderr line. Confirms the wrapper handles both exception types end-to-end through the verb path.
  </behavior>
  <action>
    Create `tests/unit/test_redis_unreachable_verbs.py`. Module docstring: "REDIS-02 + D-19 coverage at the verb level. Monkey-patches the Redis client's ping() to raise ConnectionError/TimeoutError, invokes each state verb via CliRunner, asserts exit 1 + the D-17 actionable stderr line + no traceback. Confirms D-18 single-chokepoint holds at the user-facing surface — no verb catches the exception itself." Imports: `CliRunner`, `from em_proj.cli import app`, `from em_proj import redis_client as rc`, `redis`, `pytest`. Use an `autouse` fixture for `rc._reset_for_tests()` between tests (mirrors the Phase 1 pattern in `tests/unit/test_redis_client.py`). Instantiate `runner = CliRunner(mix_stderr=False)` so stdout and stderr are asserted independently. For `monkeypatch.setattr(client, "ping", ...)`: note that the client returned by `get_client()` IS the module singleton; CliRunner runs the verb in the same Python process, so the monkeypatch will be seen by the verb's call to `client.ping()` inside `die_if_redis_unreachable`. Do NOT use `clean_db` here — these tests must run without Redis being up (the whole point is to simulate Redis being unreachable).
  </action>
  <verify>
    <automated>bash scripts/test.sh unit -k test_redis_unreachable_verbs</automated>
  </verify>
  <acceptance_criteria>
    Each line below is one allowlisted Bash invocation — no shell chains, no pipes.
    - `tests/unit/test_redis_unreachable_verbs.py` exists with the three named test functions (parametrize counted as one)
    - `bash scripts/test.sh unit -k test_redis_unreachable_verbs` exits 0 with all parametrize cases passing (4 parametrize + the timeout variant = 5 effective passes minimum)
    - `grep -cE "Connection refused|ConnectionError" tests/unit/test_redis_unreachable_verbs.py` returns 1 or more (regex alternation INSIDE one grep, not a shell pipe)
    - `grep -c "TimeoutError" tests/unit/test_redis_unreachable_verbs.py` returns 1 or more
    - `grep -c "Traceback" tests/unit/test_redis_unreachable_verbs.py` returns 1 or more (asserting Traceback NOT in stderr)
    - `grep -c "_reset_for_tests" tests/unit/test_redis_unreachable_verbs.py` returns 1 or more (singleton hygiene)
  </acceptance_criteria>
  <done>Every state verb is proven to surface the REDIS-02 actionable error when Redis is down. Both ConnectionError and TimeoutError paths are covered. No verb catches the exception itself (D-18 user-facing invariant). The Phase 1 D-17 no-traceback contract is asserted end-to-end through the verb path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural test enforcing D-15, D-17, D-18, D-19 + decision-coverage gate</name>
  <files>tests/structural/test_phase_02_shape.py</files>
  <read_first>
    - tests/structural/test_conftest_shape.py (Phase 1 precedent — AST helpers + pytest.fixture(scope="module") + skip-if-absent pattern)
    - src/em_proj/state/__init__.py (the verb module — D-14 mount + D-18 no-redis-error-catch invariant)
    - src/em_proj/state/kv.py (D-17 no-typer-import + D-18 no-direct-redis.Redis-construction invariants)
    - src/em_proj/output.py (D-15 exports — SCHEMA_VERSION, resolve_json_mode, emit_ok, emit_not_found, emit_error)
    - .planning/phases/02-cli-shell-kv-primitive/02-CONTEXT.md (D-14..D-19 — the invariants being locked; D-01..D-19 are the IDs the decision-coverage test enumerates)
  </read_first>
  <behavior>
    Mirror the Phase 1 structural-test shape (`@pytest.fixture(scope="module")` to read+parse source once, AST checks for code invariants, `pytest.skip` if a target file is absent so the test file is safe to land before its targets). Required tests:

    **D-15 output.py exports** (parse `src/em_proj/output.py`):
    - `test_output_py_exists_and_parseable`
    - `test_output_py_exports_schema_version_constant`: top-level `SCHEMA_VERSION = "1"` assignment
    - `test_output_py_exports_resolve_json_mode`: top-level `def resolve_json_mode(...)` FunctionDef
    - `test_output_py_exports_three_emit_helpers`: top-level FunctionDefs `emit_ok`, `emit_not_found`, `emit_error`
    - `test_output_py_no_typer_or_redis_imports` (D-15 dependency-free): walk imports; assert no `typer`, `redis`, or `em_proj.redis_client`

    **D-17 state package layout** (parse `src/em_proj/state/__init__.py` and `src/em_proj/state/kv.py`):
    - `test_state_package_has_init_and_kv_files`: both files exist
    - `test_state_init_defines_state_app_typer`: top-level `state_app = typer.Typer(...)` call
    - `test_state_init_registers_four_verbs`: AST-walk for `@state_app.command(...)` decorators; count == 4; the four name strings include `"get"`, `"set"`, `"del"`, `"list"` (either as the first positional arg or as the `name=` keyword)
    - `test_kv_py_does_not_import_typer` (D-17): walk imports; assert no `typer` import in `state/kv.py`
    - `test_kv_py_uses_redis_client_chokepoint` (D-18): walk imports; assert `em_proj.redis_client.get_client` is imported (either `from em_proj.redis_client import get_client` or `import em_proj.redis_client`)

    **D-18 single chokepoint tree-wide** (parse all `*.py` under `src/em_proj/` except `redis_client.py`):
    - `test_no_direct_redis_redis_construction_outside_chokepoint`: walk every src/em_proj/**/*.py except redis_client.py; for each `ast.Call` node where `func` is the attribute chain `redis.Redis`, fail with the offending file+line. The Phase 1 SUMMARY explicitly documents this as the canonical regression gate.

    **D-19 verb-level no-catch invariant** (parse `src/em_proj/state/__init__.py`):
    - `test_state_init_does_not_catch_redis_errors`: walk all `ast.ExceptHandler` nodes; for each, inspect the exception type expression; fail if any handler catches `redis.ConnectionError`, `redis.TimeoutError`, or bare `redis.*Error`. Verbs must let the wrapper handle Redis errors (D-18 single chokepoint).

    **D-14 mount in cli.py** (parse `src/em_proj/cli.py`):
    - `test_cli_py_mounts_state_app`: walk Call nodes; assert at least one call matches `app.add_typer(state_app, name="state", ...)` (positional first arg can be `state_app` Name; second/third kwargs are flexible; check that `name="state"` keyword exists)

    **Decision Coverage Gate** (read all Phase 2 PLAN.md files in Python; NO shell pipes):
    - `test_every_decision_id_d01_to_d19_cited_in_at_least_one_plan`:
      1. `phase_dir = REPO_ROOT / ".planning" / "phases" / "02-cli-shell-kv-primitive"`
      2. If `phase_dir` does not exist: `pytest.skip("planning worktree not attached on main branch")`
      3. `plan_files = sorted(phase_dir.glob("02-*-PLAN.md"))`; skip if empty
      4. `cited: set[str] = set()`; for each plan path: `for match in re.findall(r"D-(\\d+)", plan.read_text()): cited.add(f"D-{int(match):02d}")`
      5. `expected = {f"D-{i:02d}" for i in range(1, 20)}` (D-01 through D-19)
      6. `missing = expected - cited`; assert `not missing`, f"Decision coverage gate failed — D-IDs missing from all Phase 2 plans: {sorted(missing)}"
      7. Note: the regex `D-(\\d+)` matches both `D-1` and `D-01`; we normalize to two-digit form so `D-1` and `D-01` count as the same ID.

    Each test uses `@pytest.fixture(scope="module")` for source loading; skips cleanly if the file is absent (so this test file can land before Plan 04 if waves are reordered — but in practice Plan 05 follows Plan 04, so all files exist).
  </behavior>
  <action>
    Create `tests/structural/test_phase_02_shape.py`. Module docstring: "Phase 2 structural invariants. AST-based assertions encoding plan acceptance criteria for D-14 (mount), D-15 (output.py exports), D-17 (state package layout — no typer in kv.py), D-18 (single redis_client chokepoint tree-wide), and D-19 (verb-level no-catch). Includes the Decision Coverage Gate test that scans every 02-*-PLAN.md in pure Python (replacing the previous pipe-chained shell recipe so one allowlisted `bash scripts/test.sh structural` call covers it). Mirrors the Phase 1 precedent in tests/structural/test_conftest_shape.py. Skips cleanly if a target file is absent so this module is safe to land before its dependencies." Reuse the helpers from `test_conftest_shape.py` by copy-paste (do NOT import them — keep each structural file self-contained per the Phase 1 precedent) OR by relative import if there's a precedent (default to copy-paste for self-containment). Pattern: `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`. For the tree-walk test, use `Path(REPO_ROOT, "src", "em_proj").rglob("*.py")` and filter out `redis_client.py`. For attribute-chain inspection, reuse `_iter_attribute_chains(tree)` from Phase 1. For the decision-coverage test, use `import re` + `Path.read_text()` — do NOT shell out, do NOT use pipes.
  </action>
  <verify>
    <automated>bash scripts/test.sh structural -k test_phase_02_shape</automated>
  </verify>
  <acceptance_criteria>
    Each line below is one allowlisted Bash invocation — no shell chains, no pipes.
    - `tests/structural/test_phase_02_shape.py` exists, &gt;=120 lines, contains all 13 named test functions per the &lt;behavior&gt; block (12 invariants + 1 decision-coverage)
    - `bash scripts/test.sh structural -k test_phase_02_shape` exits 0 (all 13 tests pass)
    - `bash scripts/test.sh structural` (full structural suite incl. Phase 1) exits 0 (no regressions)
    - `bash scripts/test.sh structural -k test_every_decision_id_d01_to_d19_cited_in_at_least_one_plan` exits 0 (decision-coverage gate green — all of D-01..D-19 cited across the 5 Phase 2 plans)
    - `grep -cE "ast.parse|ast.walk" tests/structural/test_phase_02_shape.py` returns 2 or more (AST-based checks dominate; source-text grep is reserved for non-Python checks per project convention; regex alternation INSIDE one grep, not a shell pipe)
    - `grep -c "pytest.skip" tests/structural/test_phase_02_shape.py` returns 3 or more (per-target skip-if-absent pattern, matching Phase 1's `test_conftest_shape.py`; +1 for the decision-coverage test's "planning worktree not attached" skip)
    - `grep -c "re.findall" tests/structural/test_phase_02_shape.py` returns 1 or more (decision-coverage Python scan replaces the previous shell `grep | sort | wc -l` recipe)
  </acceptance_criteria>
  <done>D-14, D-15, D-17, D-18, D-19 invariants are encoded as AST-based runtime assertions. The Decision Coverage Gate is encoded as a Python scan over all 02-*-PLAN.md files (no shell pipes). A future plan that accidentally constructs `redis.Redis()` outside the chokepoint, imports typer in kv.py, catches `redis.ConnectionError` in a verb, removes an output.py public export, or fails to cite a Phase 2 D-ID, will fail the structural suite — caught at `bash scripts/test.sh all`.</done>
</task>

<task type="auto">
  <name>Task 3: (Optional) Add `state-unit` subcommand to scripts/test.sh — separate chore commit BEFORE use</name>
  <files>scripts/test.sh, CLAUDE.md</files>
  <read_first>
    - scripts/test.sh (existing dispatcher pattern — case branch + doc comment)
    - CLAUDE.md (the subcommand table at the top — keep in sync)
    - .planning/phases/02-cli-shell-kv-primitive/02-CONTEXT.md ("If Phase 2 needs a new test subset … ADD a subcommand … BEFORE the tests use it")
  </read_first>
  <action>
    DECISION (executor judgement): only add this subcommand if running just the Phase 2 state-unit subset would meaningfully improve dev ergonomics (rough threshold: when `bash scripts/test.sh unit` exceeds 5s but `tests/unit/test_state_*.py` alone is under 2s, the subset shortcut is worth landing). If the full unit suite stays fast (under 3s total after all Phase 2 plans land), SKIP this task entirely — record `## Task 3 Skipped` in the plan SUMMARY with the timing rationale, and proceed to Task 4 directly.

    If landing:
    1. Add a `state-unit` case branch to `scripts/test.sh` after the existing `unit` case. The line is: `run_or_tail uv run pytest tests/unit/test_state_kv.py tests/unit/test_state_verbs.py tests/unit/test_state_mount.py tests/unit/test_output.py tests/unit/test_redis_unreachable_verbs.py -v "$@"` (or similar — match the actual filenames landed by Plans 01-04 and Task 1 above).
    2. Add a `state-unit` row to the `# Subcommands:` doc block at the top of `scripts/test.sh`.
    3. Add a `state-unit` row to the table in `CLAUDE.md` under "Subcommands".
    4. Commit as `chore: add state-unit test dispatcher subcommand for Phase 2 unit subset` — separate commit, NOT folded into the structural-test commit (per CLAUDE.md project policy: dispatcher extensions land BEFORE tests use them so the allowlist stays in sync).

    Do NOT call the new subcommand from any test or from this plan's `<verify>` blocks until the chore commit has landed. The Phase 2 verifier in `bash scripts/verify-phase.sh 02` will pick up the new subcommand automatically since `test.sh all` covers everything.
  </action>
  <verify>
    <automated>bash scripts/test.sh help</automated>
  </verify>
  <acceptance_criteria>
    Each line below is one allowlisted Bash invocation — no shell chains, no pipes. Either branch (landed or skipped) is acceptable; the SUMMARY records which.

    Landed branch (only assert these if Task 3 lands the subcommand):
    - `bash scripts/test.sh state-unit` exits 0 (the new subcommand runs and the Phase 2 unit tests pass)
    - `grep -c "state-unit" scripts/test.sh` returns 2 or more (case branch + doc comment row)
    - `grep -c "state-unit" CLAUDE.md` returns 1 or more (subcommand table row)

    Skipped branch (only assert these if Task 3 is skipped):
    - `bash scripts/test.sh help` exits 0 (dispatcher still usable; unchanged)
    - The plan SUMMARY contains a `## Task 3 Skipped` section explaining the timing rationale (full unit suite under 3s)
  </acceptance_criteria>
  <done>Dispatcher ergonomics improved (or explicitly deferred with rationale). If landed, allowlist stays in sync with the new subcommand because the chore commit precedes any test that uses it.</done>
</task>

<task type="auto">
  <name>Task 4: End-to-end phase verification via verify-phase.sh + manual TTL + REDIS-02 proofs</name>
  <files>(no source files modified; verification-only)</files>
  <read_first>
    - scripts/verify-phase.sh (the dispatcher's exit-code semantics + check inventory — `bash scripts/verify-phase.sh 02` already runs `test.sh all`, `test.sh structural`, `verify-redis-config.sh`, `em-proj --version`, anti-pattern grep, SUMMARY presence)
    - .planning/ROADMAP.md (Phase 2 success criteria — all 5 must be PROVABLE post-Plan-05)
    - .planning/phases/02-cli-shell-kv-primitive/02-CONTEXT.md (all 19 D-IDs to confirm at least one plan cites each — the Decision Coverage Gate test in Task 2 enforces this)
  </read_first>
  <action>
    Final end-to-end verification before declaring Phase 2 complete. Run each command as a SEPARATE Bash invocation — no chains, no pipes, no `;` bundling. Each numbered step below is ONE allowlisted Bash call. Record every outcome in the plan SUMMARY verbatim.

    **Deterministic checks (single dispatcher call):**

    1. `bash scripts/verify-phase.sh 02` — single call that internally runs `test.sh all` + `test.sh structural` + `verify-redis-config.sh` + `em-proj` PATH/`--version` + anti-pattern grep + SUMMARY-per-PLAN inventory + recent-commits log. Expected: exit 0; the markdown report's footer reads "all deterministic checks PASS". Paste the footer into the plan SUMMARY.

    **CLI-03 per-verb help (six separate steps):**

    2. `em-proj state --help` — exit 0; stdout mentions "KV / lock / claim primitives"
    3. `em-proj state get --help` — exit 0; stdout mentions `--json` and `--no-json`
    4. `em-proj state set --help` — exit 0; stdout mentions `--json`, `--no-json`, and `--ttl`
    5. `em-proj state del --help` — exit 0; stdout mentions `--json` and `--no-json`
    6. `em-proj state list --help` — exit 0; stdout mentions `--json` and `--no-json`

    **KV-02 manual TTL eviction proof (ROADMAP criterion #3 — four separate steps):**

    7. `em-proj state set ephemeral_test_key x --ttl 2 --json` — exit 0; parsed stdout has `data.key == "ephemeral_test_key"`, `data.ttl == 2`
    8. `em-proj state get ephemeral_test_key --json` — exit 0; parsed stdout has `data.value == "x"`
    9. `sleep 3` — wait past the 2-second TTL (allow 1s jitter so we don't race the eviction)
    10. `em-proj state get ephemeral_test_key --json` — exit 2; parsed stdout has `status == "not_found"`, `error.code == "not_found"`

    **REDIS-02 manual unreachable proof (ROADMAP criterion #5 — four separate steps):**

    11. `brew services stop redis` — exit 0; Redis now down
    12. `em-proj state list` — exit 1; stderr contains both `Redis unreachable at 127.0.0.1:6379` and `brew services start redis`; stderr does NOT contain `Traceback`
    13. `brew services start redis` — exit 0; Redis restored
    14. `bash scripts/verify-redis-config.sh` — exit 0; AOF + persistence settings survived the stop/start cycle

    **Decision Coverage Gate (encoded as a pytest test in Task 2 — single allowlisted call):**

    15. `bash scripts/test.sh structural -k test_every_decision_id_d01_to_d19_cited_in_at_least_one_plan` — exit 0; the test internally reads every 02-*-PLAN.md in Python and asserts D-01..D-19 are all cited.

    If ANY step fails, surface it to the orchestrator BEFORE the SUMMARY commit — gap-closure or revision may be needed. The plan SUMMARY records pass/fail per numbered step.
  </action>
  <verify>
    <automated>bash scripts/verify-phase.sh 02</automated>
  </verify>
  <acceptance_criteria>
    Each line below is one allowlisted Bash invocation — no shell chains, no pipes.
    - `bash scripts/verify-phase.sh 02` exits 0 (single dispatcher call covers test.sh all + structural + verify-redis-config + em-proj PATH/version + anti-pattern grep + SUMMARY inventory)
    - `bash scripts/test.sh structural -k test_every_decision_id_d01_to_d19_cited_in_at_least_one_plan` exits 0 (Decision Coverage Gate green)
    - The plan SUMMARY contains a numbered pass/fail line for each of steps 1-15 in the &lt;action&gt; block
    - The plan SUMMARY contains a per-requirement table covering CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, KV-01, KV-02, REDIS-02 with PASS/FAIL + the specific step number that proves each
    - The plan SUMMARY contains a per-ROADMAP-criterion table covering the 5 Phase 2 success criteria with PASS/FAIL + the proof step
    - After step 13, `redis-cli ping` returns `PONG` (Redis restored — explicit verification that step 11's stop was reversed)
  </acceptance_criteria>
  <done>Phase 2 is verifiable as complete: `verify-phase.sh 02` green, manual TTL eviction observed end-to-end via CLI in steps 7-10, manual REDIS-02 error observed in steps 11-14, Decision Coverage Gate green via the structural test, all 5 ROADMAP success criteria pass, all 8 Phase 2 REQ-IDs satisfied, all 19 D-IDs cited in plans. The Phase 2 verifier (`/gsd-verify-phase 02`) has everything it needs to write VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Redis singleton | `_reset_for_tests()` and monkeypatch must run BETWEEN tests; leaked state between tests could make REDIS-02 tests pass spuriously |
| structural test → source tree | AST-walks consume the on-disk source; if a test file lives outside `src/em_proj/`, the single-chokepoint walk must not flag it (tests are allowed to use redis.Redis directly for assertion purposes) |
| structural test → planning artifacts | Decision-coverage test reads `.planning/phases/02-cli-shell-kv-primitive/02-*-PLAN.md` from disk; if the planning worktree is not attached (e.g., on a fresh main checkout) the test SKIPs cleanly rather than failing |
| dispatcher subcommand → allowlist | A new `state-unit` subcommand expands the Bash allowlist surface; landing it in a separate commit BEFORE tests use it keeps the allowlist in sync (per CLAUDE.md) |
| manual REDIS-02 proof → developer's local Redis | Stopping `brew services stop redis` during verification temporarily disables the developer's local Redis; verification MUST restore via `brew services start redis` at the end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05-01 (Tampering — monkeypatch leakage) | Tampering | REDIS-02 unit test isolation | mitigate | Autouse `_reset_for_tests()` fixture mirrors `tests/unit/test_redis_client.py` pattern; monkeypatch is per-test (pytest's monkeypatch fixture auto-reverts on test teardown) |
| T-2-05-02 (Tampering — structural test false-positive on test code) | Tampering | tree-walk for `redis.Redis(...)` | mitigate | Walk scope is `src/em_proj/**/*.py` only; `tests/**` and `scripts/**` excluded. Tests legitimately construct redis clients directly (e.g., for asserting post-race state). |
| T-2-05-03 (Information disclosure — verify-phase output leaks paths) | Information disclosure | `verify-phase.sh` report | accept | The report goes to stdout for the user; paths shown are repo-internal and already visible to the user. No mitigation needed. |
| T-2-05-04 (DoS — verify run leaves Redis stopped) | Denial of service | manual REDIS-02 proof (steps 11-14) | mitigate | Task 4 sequence explicitly numbers `brew services start redis` (step 13) and `verify-redis-config.sh` (step 14) AFTER the stop step; SUMMARY records the post-restore verification per acceptance criterion ("after step 13, redis-cli ping returns PONG") |
| T-2-05-05 (Tampering — dispatcher subcommand allowlist drift) | Tampering | scripts/test.sh + allowlist | mitigate | Task 3 lands the dispatcher entry in a separate `chore:` commit BEFORE any test uses it (per CLAUDE.md project policy); CLAUDE.md table updated in the same commit so the subcommand is discoverable |
| T-2-05-06 (Tampering — Decision Coverage Gate bypass via stale plan files) | Tampering | decision-coverage test | mitigate | The test reads ALL `.planning/phases/02-cli-shell-kv-primitive/02-*-PLAN.md` files at runtime; the assertion is union-over-all-plans, so a plan can drop a D-ID only if another plan picks it up. The test runs every `bash scripts/test.sh all` invocation, catching drift at commit time. |
</threat_model>

<verification>
Each bullet below is ONE allowlisted Bash invocation — no shell chains, no pipes, no `;` bundling. The decision-coverage check is folded into the structural suite (Task 2) so a single `bash scripts/test.sh structural` covers it.

- `bash scripts/verify-phase.sh 02` exits 0 (single dispatcher call covers `test.sh all` + `test.sh structural` + `verify-redis-config.sh` + em-proj PATH/version + anti-pattern grep + SUMMARY inventory)
- `bash scripts/test.sh unit -k test_redis_unreachable_verbs` exits 0 (REDIS-02 verb-level proof passes — Task 1)
- `bash scripts/test.sh structural -k test_phase_02_shape` exits 0 (D-14..D-19 invariants + Decision Coverage Gate all green — Task 2)
- `bash scripts/test.sh structural -k test_every_decision_id_d01_to_d19_cited_in_at_least_one_plan` exits 0 (Decision Coverage Gate isolated — same single test, called by name for forensic clarity in CI logs)
- All 5 ROADMAP Phase 2 success criteria have a documented PASS in the plan SUMMARY (Task 4 records the proof step per criterion)
- All 8 Phase 2 REQ-IDs (CLI-01..05, KV-01, KV-02, REDIS-02) have a documented PASS in the plan SUMMARY
</verification>

<success_criteria>
1. REDIS-02 is user-facing-proved: every state verb surfaces the locked one-line stderr message with exit 1 when Redis is down (both ConnectionError and TimeoutError paths).
2. Structural invariants are encoded in `tests/structural/test_phase_02_shape.py`: D-14 mount, D-15 output exports, D-17 package layout (no typer in kv.py), D-18 single redis chokepoint tree-wide, D-19 no-catch in verbs.
3. Decision Coverage Gate encoded as a pytest test — single allowlisted dispatcher call replaces the previous pipe-chained shell recipe.
4. (Optional) `state-unit` dispatcher subcommand lands in a separate `chore:` commit OR is explicitly skipped with rationale.
5. End-to-end Phase 2 verification: `bash scripts/verify-phase.sh 02` green; manual TTL-eviction proof (steps 7-10) and manual REDIS-02-down proof (steps 11-14) both pass.
6. All 19 D-IDs from `02-CONTEXT.md` are cited in at least one Phase 2 plan's `must_haves.truths` (Decision Coverage Gate satisfied via the pytest test, NOT via shell pipes).
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-shell-kv-primitive/02-05-SUMMARY.md` documenting:
- The REDIS-02 test outcomes: which 4 verbs were exercised, ConnectionError + TimeoutError coverage, exit codes + stderr lines observed.
- The 13 structural test names and what each enforces (D-ID mapping for the 12 invariants + the decision-coverage test).
- Task 3 outcome: landed (with commit hash + the subcommand surface added) OR skipped (with the "unit suite is fast enough" rationale).
- Task 4 verification log: paste the `bash scripts/verify-phase.sh 02` final footer line, plus the per-step transcript for steps 2-15 (six --help calls, four TTL eviction steps, four REDIS-02 stop/start steps, the decision-coverage test outcome). Each step gets its own line so the executor's Bash invocations stay one-per-step.
- Per-requirement PASS table: CLI-01 (carry-forward verified — Task 4 step 1 covers `em-proj --version`), CLI-02 (carry-forward verified — same), CLI-03 (per-verb --help works — Task 4 steps 2-6), CLI-04 (exit codes 0/1/2 observed — Plan 04 tests + Task 4 steps 10, 12), CLI-05 (JSON envelope shape verified end-to-end — Plan 04 tests + Task 4 step 7-8), KV-01 (atomic SET/GET/DEL/SCAN proved by multiproc — Plan 04 Task 3), KV-02 (TTL eviction observed end-to-end — Plan 04 `test_set_ttl_then_get_evicts_after_expiry` + Task 4 steps 7-10), REDIS-02 (one-line error message observed when Redis stopped — Task 1 unit test + Task 4 step 12).
- Cited D-IDs: D-15, D-17, D-18, D-19 (this plan's direct invariants — plus D-01..D-19 listed as the Decision Coverage Gate target).
- Commits: `test(02-05): cover REDIS-02 verb-level surface via monkeypatched ConnectionError (D-19)`, `test(02-05): structural shape tests for D-14..D-19 invariants + Decision Coverage Gate`, (optional) `chore: add state-unit test dispatcher subcommand for Phase 2 unit subset`.
- "Phase 2 complete — ready for /gsd-verify-phase 02" recommendation OR the explicit list of gaps if any verification failed (so the verifier can act).
</output>
</content>
