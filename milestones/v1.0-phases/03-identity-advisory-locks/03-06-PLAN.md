---
phase: 03-identity-advisory-locks
plan: 06
type: execute
wave: 6
depends_on: [03-03, 03-04, 03-05]
files_modified:
  - tests/structural/test_phase_03_shape.py
  - tests/multiprocess/test_lock_stale.py
autonomous: true
requirements: [IDENT-02, LOCK-01, LOCK-02, LOCK-03]
tags: [structural, ast, stale-takeover, verify-phase, decision-coverage-gate]

must_haves:
  truths:
    - "tests/structural/test_phase_03_shape.py codifies every Phase 3 D-* and load-bearing invariant as a runtime AST assertion"
    - "D-18 carry-forward is tree-wide: no direct redis.Redis() construction outside src/em_proj/redis_client.py (including the new lock.py and identity.py)"
    - "Lock JSON record schema (D-02) — the encode/decode round-trip carries all 8 fields"
    - "lock_force_displace is exported from em_proj.state.lock (Blocker #1 resolution: --warn override uses this public op, not private _encode_holder)"
    - "Verb code in state/__init__.py does NOT import private (underscore-prefixed) symbols from em_proj.state.lock (D-14/D-17 thin-shell discipline pinned)"
    - "RefresherThread.run() in lock.py catches (redis.ConnectionError, redis.TimeoutError) (Blocker #3 resolution: refresher recovery posture pinned)"
    - "An end-to-end multi-process stale-takeover test demonstrates that a crashed --hold leaves a stale lock and the next acquire opportunistically takes it over (D-10)"
    - "Decision Coverage Gate: every D-01..D-12 cited in at least one Phase 3 plan"
    - "bash scripts/verify-phase.sh 03 exits 0 (deterministic phase verification: test suite + anti-pattern grep + SUMMARY inventory + commit traceability)"
  artifacts:
    - path: "tests/structural/test_phase_03_shape.py"
      provides: "AST + source-text assertions for Phase 3 D-01..D-12 and inherited invariants, including lock_force_displace export, no-private-import in verb code, and the refresher recovery-posture handler"
      min_lines: 280
    - path: "tests/multiprocess/test_lock_stale.py"
      provides: "End-to-end stale-takeover proof: SIGKILL leaves stale lock; next acquire takes over via Lua compare-and-swap"
      min_lines: 100
  key_links:
    - from: "tests/structural/test_phase_03_shape.py"
      to: "src/em_proj/state/lock.py, src/em_proj/identity.py, src/em_proj/output.py, src/em_proj/state/__init__.py"
      via: "ast.parse + source-text grep encoding plan acceptance criteria"
      pattern: "ast\\.parse|\\.read_text\\(\\)"
    - from: "tests/multiprocess/test_lock_stale.py"
      to: "subprocess.Popen + os.kill(SIGKILL) + clean_db raw client"
      via: "kill -9 a holder child, then prove next acquire takes over"
      pattern: "SIGKILL|signal\\.SIGKILL"
---

<objective>
Land the Phase 3 verification substrate: a structural AST test file that pins every Phase 3 D-* and inherited invariant (including the Blocker #1 + Blocker #3 resolution pins from this revision iteration); an end-to-end stale-takeover test that closes ROADMAP success criterion #2 (the IDENT-02 "stale-detection composite correctly identifies abandoned locks across PID reuse and reboot"); and final phase-level verification via `bash scripts/verify-phase.sh 03`.

Purpose: structural tests are the regression gate that catches anti-patterns the next time the codebase is touched (e.g., a Phase 4 claim verb that accidentally `import redis` for direct construction; a refactor that removes the validate_key call from lock_acquire; a future plan that re-introduces `_encode_holder` imports into verb code; a refactor that drops the refresher's narrow Redis-transient catch). The stale-takeover multi-process test is the only end-to-end proof that D-10's opportunistic-takeover-on-acquire flow ACTUALLY works against real Redis with a real dead process. `bash scripts/verify-phase.sh 03` is the one-allowlisted-call phase verifier that downstream `gsd-verify-phase 3` will rely on.

Output:
  - `tests/structural/test_phase_03_shape.py` — Phase 3 D-* invariants + Blocker #1/#3 pins + Decision Coverage Gate
  - `tests/multiprocess/test_lock_stale.py` — SIGKILL → stale lock → opportunistic takeover proof
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-identity-advisory-locks/03-CONTEXT.md
@.planning/phases/02-cli-shell-kv-primitive/02-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The analog for Phase 3's structural shape test is the Phase 2 file, which is ~340 lines. -->

From tests/structural/test_phase_02_shape.py (analog — read full file and mirror the helper functions and fixture pattern):
```python
REPO_ROOT = Path(__file__).resolve().parent.parent.parent
SRC_EM_PROJ = REPO_ROOT / "src" / "em_proj"
# Per-file Path constants...

def _parse_or_skip(path: Path, label: str) -> ast.Module: ...
def _find_assign(tree: ast.Module, name: str) -> ast.AST | None: ...
def _find_funcdef(tree: ast.Module, name: str) -> ast.FunctionDef | None: ...
def _iter_imports(tree: ast.Module) -> list[str]: ...

@pytest.fixture(scope="module")
def output_tree() -> ast.Module: ...
# ... per-file fixtures
```

Required new structural assertions for Phase 3:

1. **D-12: identity.py at top level** — `src/em_proj/identity.py` exists; lives at top-level, not under `state/`.
2. **D-17 carry: identity.py is pure** — no `import typer`; no `from em_proj.redis_client import` (identity is stateless).
3. **D-11: psutil is importable** — `import psutil` succeeds; `psutil` referenced in identity.py imports.
4. **D-18 tree-wide: no `redis.Redis(...)` construction outside `redis_client.py`** — extend Phase 2 test to also scan `lock.py` and `identity.py`. NOTE: lock.py is now allowed to `import redis` (the module) for exception-class references in the refresher (Blocker #3 resolution), but MUST NOT call `redis.Redis(...)`. The assertion uses the regex `redis\.Redis\(` (construction call), not `import redis` (the module reference).
5. **D-02 record shape: lock.py defines an 8-field holder** — round-trip a fake holder dict through `_make_holder` (if testable directly) or via `lock_acquire` round-trip; assert exactly the 8 keys.
6. **D-06 compare-and-delete script exists** — `LUA_COMPARE_AND_DELETE` string is defined in `lock.py`; contains `cjson.decode` and `redis.call('DEL', KEYS[1])`.
7. **D-10 compare-and-swap-if-stale script exists** — `LUA_COMPARE_AND_SWAP_IF_STALE` string is defined.
8. **D-07 --warn checks BOTH isatty calls** — `state/__init__.py` source contains BOTH `sys.stdout.isatty()` AND `sys.stdin.isatty()` (T-3-XX-05 mitigation pin).
9. **D-08 --warn + --hold mutex check in state/__init__.py** — verify there's an `if warn and hold:` (or equivalent) gate that runs before any `get_client` call. AST walk: find the lock function; assert the first `if`-statement body checks warn+hold and emits an error.
10. **emit_held_by_another exported from output.py** — analogous to the Phase 2 `test_output_py_exports_three_emit_helpers`; assert `_find_funcdef(output_tree, "emit_held_by_another")` is not None.
11. **D-15 carry: output.py still dependency-free** — Phase 2 test already pins this; add a named assertion specifically that `emit_held_by_another` does not introduce a forbidden import.
12. **lock.py uses validate_key from kv.py (T-3-XX-01 carry)** — `_iter_imports` on lock.py includes `em_proj.state.kv` (or specifically the `validate_key` symbol).
13. **lock.py does not catch redis errors in primitive ops (D-19 carry, narrowed)** — same shape as Phase 2's `test_state_init_does_not_catch_redis_errors` but applied to `lock.py`. EXCEPTION: the refresher thread's `run()` IS allowed to catch `(redis.ConnectionError, redis.TimeoutError)` (Blocker #3 — recovery posture). The assertion must permit the refresher's narrow handler while rejecting broad catches elsewhere. Implementation: grep for `except (redis.ConnectionError, redis.TimeoutError)` (expected: >= 1, in RefresherThread.run) AND grep for broader patterns like `except redis.RedisError` or `except Exception` inside lock_acquire/lock_release/lock_force_displace (expected: 0 — narrow check by walking those function bodies via AST).
14. **lock.py does not import typer (D-17 carry)** — same shape as Phase 2's `test_kv_py_does_not_import_typer`.
15. **lock.py does not import multiprocessing (Phase 1 pitfall #6 carry)** — assert "multiprocessing" not in `_iter_imports(lock_tree)`.
16. **state/__init__.py registers 6 verbs total (4 kv + 2 new)** — extend the Phase 2 `test_state_init_registers_four_verbs` to a Phase-3-specific version asserting >=6 verbs including `lock` and `unlock`.
17. **Decision Coverage Gate for Phase 3** — scan `03-*-PLAN.md` files; assert D-01..D-12 are all cited.
18. **(NEW — Blocker #1 pin a) lock_force_displace is exported from em_proj.state.lock** — `_find_funcdef(lock_tree, "lock_force_displace")` is not None; ALSO assert importability via `from em_proj.state.lock import lock_force_displace` succeeds at test time (sanity check that the symbol is module-level, not nested).
19. **(NEW — Blocker #1 pin b) state/__init__.py does NOT import private symbols from em_proj.state.lock** — walk imports from `em_proj.state.lock`; assert no imported name begins with `_` (underscore). Specifically pin: `_encode_holder`, `_decode_holder`, `_make_holder`, `_validate_reason` MUST NOT appear in state/__init__.py's imports. This enforces the D-14/D-17 thin-shell discipline.
20. **(NEW — Blocker #3 pin) RefresherThread.run catches (redis.ConnectionError, redis.TimeoutError)** — source-text grep on lock.py: `re.search(r"except \(redis\.ConnectionError,\s*redis\.TimeoutError\)", source) is not None`. This pins the refresher recovery-posture handler; a future refactor that drops the narrow catch (e.g., changes it to bare `except Exception` or removes it entirely) fails this test.

Required new multi-process test for stale-takeover:

```python
def test_stale_takeover_after_sigkill(clean_db):
    """Spawn `em-proj state lock --hold stale-foo --ttl 60 -- sleep 30`, SIGKILL
    the parent at t=0.5s (no cleanup runs), then immediately invoke
    `em-proj state lock --hold stale-foo --ttl 60 -- echo took-over` and assert
    it succeeds via the opportunistic Lua compare-and-swap on a dead-PID holder.
    """
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/structural/test_phase_03_shape.py</name>
  <files>tests/structural/test_phase_03_shape.py</files>
  <read_first>
    - tests/structural/test_phase_02_shape.py (FULL file — exact analog; copy the helper functions verbatim per Phase 1+2 precedent of self-contained structural files)
    - src/em_proj/identity.py (final state after Plan 03-01 + 03-02)
    - src/em_proj/state/lock.py (final state after Plan 03-03 + 03-05 — note `lock_force_displace` is the Blocker #1 export; refresher exception handler is the Blocker #3 pin)
    - src/em_proj/state/__init__.py (final state after Plan 03-04 + 03-05 — note verb code must NOT import underscore-prefixed symbols from lock.py)
    - src/em_proj/output.py (final state after Plan 03-03 — `_HOLDER_DISCLOSURE_KEYS` constant is the source of truth for what emit_held_by_another echoes)
    - .planning/phases/03-identity-advisory-locks/03-CONTEXT.md (the source of every D-* and invariant being pinned)
  </read_first>
  <behavior>
    - 20 named tests (17 original + 3 new pins from this revision: Blocker #1 a/b + Blocker #3), one per assertion bullet in the <interfaces> block above.
    - All tests are AST-based or source-text grep based; none require live Redis.
    - Tests use `_parse_or_skip` to gracefully skip if a target file is absent (so the structural file can land before the production files in some interleaving; in practice all files exist by Plan 03-06).
    - The Decision Coverage Gate test scans `03-*-PLAN.md` under `.planning/phases/03-identity-advisory-locks/`; if the directory doesn't exist (planning worktree not attached), the test skips with a clear message.
    - The gate asserts every D-01..D-12 (Phase 3 has 12 decisions per CONTEXT.md) is cited in at least one plan body.
    - Helper functions (`_parse_or_skip`, `_find_assign`, `_find_funcdef`, `_iter_imports`, `_iter_attribute_chains`) are copy-pasted from `test_phase_02_shape.py` per the Phase 1 precedent ("each structural file is self-contained" — Plan 02-05 SUMMARY documents this).
  </behavior>
  <action>
    Create `tests/structural/test_phase_03_shape.py`. Copy the helper-function block (`_parse_or_skip`, `_find_assign`, `_find_funcdef`, `_iter_imports`, `_iter_attribute_chains`) verbatim from `test_phase_02_shape.py` — DO NOT factor into a shared module (Phase 1 precedent locked self-contained files).

    Module-level Path constants:
      - `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`
      - `SRC_EM_PROJ = REPO_ROOT / "src" / "em_proj"`
      - `IDENTITY_PY = SRC_EM_PROJ / "identity.py"`
      - `OUTPUT_PY = SRC_EM_PROJ / "output.py"`
      - `STATE_PKG = SRC_EM_PROJ / "state"`
      - `STATE_INIT = STATE_PKG / "__init__.py"`
      - `STATE_LOCK = STATE_PKG / "lock.py"`
      - `REDIS_CLIENT = SRC_EM_PROJ / "redis_client.py"`
      - `PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "03-identity-advisory-locks"`

    Per-module fixtures (scope="module"):
      - `identity_tree`, `lock_tree`, `state_init_tree`, `output_tree`.

    Write the 20 tests per the behavior bullets. Each test is short (5-15 lines). Group related tests under section comments (`# --- D-12: identity placement ---`, `# --- D-06: Lua compare-and-delete script ---`, `# --- Blocker #1: thin-shell discipline ---`, `# --- Blocker #3: refresher recovery posture ---`, etc.) for navigability.

    For Test 5 (D-02 8-field holder shape): build a fake composite dict (8 fields per D-02), encode via `lock._encode_holder`, decode via `lock._decode_holder` (or `json.loads`), assert the round-trip preserves all 8 keys with the expected types. Do NOT call `lock_acquire` (would need live Redis). **NOTE: structural tests are allowed to reach into module-private symbols for testing purposes — the rule only forbids VERB code (state/__init__.py) from importing them.**

    For Test 6 + 7 (Lua scripts): use `_find_assign` to locate `LUA_COMPARE_AND_DELETE` and `LUA_COMPARE_AND_SWAP_IF_STALE` constants. Assert they're string constants; grep their values for `cjson.decode` and `redis.call('DEL'`/`redis.call('SET'` markers respectively. Optionally also locate `LUA_FORCE_DISPLACE` and assert its body contains `redis.call('DEL'` (the Blocker #1 server-side displacement script).

    For Test 8 (--warn double-isatty check): grep `state/__init__.py` source text (NOT AST — simpler) for both literals: `assert "sys.stdout.isatty()" in source AND "sys.stdin.isatty()" in source`.

    For Test 9 (mutex check): AST-walk the `lock` function; assert the first statement is a check that involves both `warn` and `hold` identifiers and is followed by an `emit_error` call. If AST navigation is brittle, fall back to source-text grep: `assert "warn and hold" in source OR "hold and warn" in source` (cover both orderings).

    For Test 13 (D-19 narrowed): use AST to walk the bodies of `lock_acquire`, `lock_release`, `lock_force_displace`; assert no `ExceptHandler` node in those function bodies catches anything from the `redis` module. Then a separate assertion: source-text grep for `except (redis.ConnectionError, redis.TimeoutError)` in the full lock.py source — must be present at least once (this is the refresher's allowed handler). The two checks together pin: "primitives don't catch Redis errors; the refresher's narrow transient catch is the only exception and is required."

    For Test 16 (six verbs): mirror the Phase 2 test pattern; expected = `{"get", "set", "del", "list", "lock", "unlock"}`.

    For the Decision Coverage Gate (Test 17): copy the `test_every_decision_id_d01_to_d19_cited_in_at_least_one_plan` from `test_phase_02_shape.py`, change the PHASE_DIR + glob pattern (`"03-*-PLAN.md"`) + expected range (`range(1, 13)` for D-01..D-12). Name the test `test_every_phase_3_decision_cited_in_at_least_one_plan`.

    **For Test 18 (Blocker #1 pin a — lock_force_displace exported):**
    ```python
    def test_lock_force_displace_is_exported(lock_tree):
        """Blocker #1: --warn override uses the public op lock_force_displace,
        not private _encode_holder. This pins the public surface."""
        assert _find_funcdef(lock_tree, "lock_force_displace") is not None, (
            "lock_force_displace must be a module-level function in em_proj.state.lock"
        )
        # Also assert importability — catches accidental indentation under a class
        from em_proj.state import lock as lock_module
        assert hasattr(lock_module, "lock_force_displace")
        assert callable(lock_module.lock_force_displace)
    ```

    **For Test 19 (Blocker #1 pin b — verb code imports only public symbols):**
    ```python
    def test_state_init_imports_no_private_symbols_from_lock(state_init_tree):
        """Blocker #1: state/__init__.py (verb code) must not import private
        (underscore-prefixed) symbols from em_proj.state.lock. The thin-verb-shell
        discipline (D-14/D-17) requires displacement Lua to live in
        lock_force_displace (server-side), not in the verb body via _encode_holder."""
        private_names = []
        for node in ast.walk(state_init_tree):
            if isinstance(node, ast.ImportFrom) and node.module == "em_proj.state.lock":
                for alias in node.names:
                    if alias.name.startswith("_"):
                        private_names.append(alias.name)
        assert not private_names, (
            f"state/__init__.py must not import private symbols from em_proj.state.lock; "
            f"found: {private_names}. Use the public lock_force_displace op instead."
        )
        # Explicit name-checks for the four specific symbols that would be the
        # likely backslide candidates:
        source = STATE_INIT.read_text()
        for forbidden in ("_encode_holder", "_decode_holder", "_make_holder", "_validate_reason"):
            assert forbidden not in source, (
                f"state/__init__.py references {forbidden!r} — private symbol from lock.py. "
                f"Use the lock_force_displace public op (D-14/D-17 thin-shell)."
            )
    ```

    **For Test 20 (Blocker #3 pin — refresher exception handler):**
    ```python
    def test_refresher_catches_redis_transients(lock_tree):
        """Blocker #3: RefresherThread.run() must catch (redis.ConnectionError,
        redis.TimeoutError) — the documented recovery posture. A refactor that
        drops the narrow catch (or changes it to bare `except Exception`) fails
        this test."""
        source = STATE_LOCK.read_text()
        import re
        # Accept whitespace flexibility but pin the exact exception tuple shape.
        pattern = re.compile(r"except\s*\(\s*redis\.ConnectionError\s*,\s*redis\.TimeoutError\s*\)")
        assert pattern.search(source) is not None, (
            "lock.py must contain `except (redis.ConnectionError, redis.TimeoutError)` "
            "in RefresherThread.run() — Blocker #3 recovery posture (log + keep-looping). "
            "If this test fails, either the handler was removed or its shape changed."
        )
    ```

    Confirm by reading the structural file: every test function has a docstring naming the D-* or invariant it pins. The three new tests (18, 19, 20) reference Blocker #1 / #3 explicitly so future readers know why they exist.
  </action>
  <verify>
    <automated>bash scripts/test.sh structural -k test_phase_03_shape</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/structural/test_phase_03_shape.py` exists
    - `bash scripts/test.sh structural -k test_phase_03_shape` reports >= 20 tests collected, 0 failed
    - `grep -c '^def test_' tests/structural/test_phase_03_shape.py` returns >= 20
    - `grep -c 'IDENTITY_PY\|STATE_LOCK\|STATE_INIT' tests/structural/test_phase_03_shape.py` returns >= 4 (Path constants used)
    - `grep -c '_parse_or_skip\|_find_funcdef\|_find_assign' tests/structural/test_phase_03_shape.py` returns >= 6 (helper functions copied + used)
    - `grep -c 'D-01\|D-02\|D-03\|D-04\|D-05\|D-06\|D-07\|D-08\|D-09\|D-10\|D-11\|D-12' tests/structural/test_phase_03_shape.py` returns >= 12 (every D-ID referenced in at least one test docstring or comment — feeds the Decision Coverage Gate that 20 tests fundamentally validate)
    - `grep -c 'emit_held_by_another' tests/structural/test_phase_03_shape.py` returns >= 1
    - `grep -c 'sys.stdout.isatty\|sys.stdin.isatty' tests/structural/test_phase_03_shape.py` returns >= 2 (the --warn double-check assertion)
    - `grep -c 'multiprocessing' tests/structural/test_phase_03_shape.py` returns >= 1 (the assertion that lock.py does NOT import multiprocessing)
    - `grep -c 'lock_force_displace' tests/structural/test_phase_03_shape.py` returns >= 2 (Blocker #1 pin a: export check + importability)
    - `grep -c '_encode_holder' tests/structural/test_phase_03_shape.py` returns >= 1 (Blocker #1 pin b: forbidden import name check)
    - `grep -c 'redis\.ConnectionError.*redis\.TimeoutError\|ConnectionError, redis\.TimeoutError' tests/structural/test_phase_03_shape.py` returns >= 1 (Blocker #3 pin: refresher handler regex)
    - `bash scripts/test.sh structural` passes overall (Phase 1 + Phase 2 structural tests still green; no regression)
  </acceptance_criteria>
  <done>Every Phase 3 D-* + inherited invariant + Decision Coverage Gate + Blocker #1 / #3 revision pins codified as a passing structural test; the file is self-contained per the Phase 1+2 precedent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-process stale-takeover end-to-end test</name>
  <files>tests/multiprocess/test_lock_stale.py</files>
  <read_first>
    - tests/multiprocess/test_lock_hold.py (Plan 03-05 final state — analog for spawning real em-proj children and asserting Redis state after)
    - src/em_proj/state/lock.py (specifically `LUA_COMPARE_AND_SWAP_IF_STALE` — the script being exercised end-to-end)
    - tests/conftest.py (multiproc_race + clean_db fixtures; EM_PROJ_REDIS_DB injection requirement)
    - .planning/phases/03-identity-advisory-locks/03-CONTEXT.md (D-10 opportunistic stale-takeover on acquire — the exact flow being proven)
  </read_first>
  <behavior>
    - Test 1 — Stale-takeover on dead-PID holder: spawn `em-proj state lock --ttl 60 --hold stale-foo -- sleep 30` as a `subprocess.Popen` child with the EM_PROJ_REDIS_DB=15 env. **CLI option order per Warning #5 pin: `--ttl 60 --hold stale-foo -- sleep 30`** — options BEFORE positional name BEFORE `--`. Wait 0.5s for the lock to land. Confirm `clean_db.get("state:lock:stale-foo")` is non-None and contains the child's pid in the JSON. SIGKILL the python parent (`os.kill(proc.pid, signal.SIGKILL)`) — no cleanup runs. Wait briefly for OS reaping (`proc.wait(timeout=2)`); the child's exit code is -9 (SIGKILL on POSIX). Confirm the lock is STILL present in Redis (TTL backstop is 60s; SIGKILL bypasses atexit + signal handlers). Then invoke a fresh `em-proj state lock --ttl 60 --hold stale-foo -- echo took-over` via `subprocess.run` — assert exit 0 + stdout contains "took-over". Read `clean_db.get("state:lock:stale-foo")` — assert the holder JSON's pid is no longer the dead child's pid (it's been replaced by the new acquirer or is now None because the second `--hold echo` already finished and released).
    - Test 2 — Stale-takeover does NOT displace live holders: spawn child A `em-proj state lock --hold live-foo -- sleep 3` (live, with cleanup). Wait 0.3s. Invoke a SECOND `em-proj state lock --hold live-foo -- echo nope` via subprocess.run with timeout=2s — assert exit 3 (held_by_another, not took-over). Confirm `clean_db.get("state:lock:live-foo")` still contains child A's pid. Wait for A to finish. (This test proves the Lua compare-and-swap doesn't fire when the holder is live — the inverse of Test 1.)
    - Test 3 — Stale-takeover preserves the new holder's identity: in Test 1's second-invocation flow, the new lock JSON's session_id field reflects the NEW process (the second em-proj's resolve_session_id output, not the dead child's). Add an assertion: read the lock value AFTER the new --hold's echo has finished AND released — actually that's None. Better: spawn `em-proj state lock --ttl 60 --hold stale-2 -- sleep 10` as the takeover process, kill the previous holder via SIGKILL, wait 1s, take over, then read the JSON. Since this is timing-sensitive, document the alternative: assert the takeover succeeded (exit 0 on the takeover invocation) and trust the LOCK-01 unit tests to prove the holder shape.
  </behavior>
  <action>
    Create `tests/multiprocess/test_lock_stale.py`. Mirror `test_lock_hold.py` shape.

    Imports: `import json, os, signal, subprocess, time` + `import pytest` + `from tests.conftest import EM_PROJ_BIN, TEST_DB`. The literal key prefix `"state:lock:"` may be inlined for readability or imported from `em_proj.state.lock` as `KEY_PREFIX` (KEY_PREFIX is a PUBLIC constant — not underscore-prefixed — so importing it from test code is fine; only state/__init__.py's verb body is forbidden from importing private symbols).

    Use `clean_db` fixture for raw client access. Use `subprocess.Popen` directly (signal injection requires live process control; multiproc_race only returns RaceResults after children exit).

    EM_PROJ_REDIS_DB=15 env injection is MANDATORY in every Popen call. Without it the children write to db=0 (production Redis) and corrupt the user's real state. Document this inline: `env = {**os.environ, "EM_PROJ_REDIS_DB": str(TEST_DB)}` — DO NOT mutate `os.environ` directly.

    For SIGKILL injection in Test 1: `os.kill(proc.pid, signal.SIGKILL)`. The child's exit_code after wait will be `-signal.SIGKILL` (= -9 on POSIX). Document inline why SIGKILL specifically (vs. SIGTERM): SIGKILL CANNOT be caught — the atexit + signal handlers do NOT run — so the lock is guaranteed-stale, exercising the takeover path (not the cleanup path).

    Tolerances for wall-time: the 0.5s "wait for lock to land" is generous (LOCK-01 acquire is ~50ms typically); the 2s "wait for OS reap after SIGKILL" is generous; the 1s default block-poll on the takeover invocation means the entire test runs in ~2-3s.

    All CLI invocations use the canonical option order: options BEFORE positional name BEFORE `--`. Reference Warning #5 pin in a top-of-file comment so anyone editing this test file preserves the convention.

    Document each test docstring with the D-10 or success-criterion being verified. Include a comment block at the top of the file explaining that these tests are intentionally SIGKILL-driven (vs. SIGINT in test_lock_hold.py) to bypass cleanup and produce a stale lock.
  </action>
  <verify>
    <automated>bash scripts/test.sh multiprocess -k test_lock_stale</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/multiprocess/test_lock_stale.py` exists
    - `bash scripts/test.sh multiprocess -k test_lock_stale` reports >= 2 tests collected, 0 failed (Test 3 is optional per the behavior bullet)
    - `grep -c '^def test_' tests/multiprocess/test_lock_stale.py` returns >= 2
    - `grep -cE 'signal\.SIGKILL|SIGKILL' tests/multiprocess/test_lock_stale.py` returns >= 1 (SIGKILL is the chosen failure-injection)
    - `grep -c 'EM_PROJ_REDIS_DB' tests/multiprocess/test_lock_stale.py` returns >= 1 (mandatory env injection)
    - `grep -c 'multiprocessing' tests/multiprocess/test_lock_stale.py` returns 0 (Phase 1 pitfall #6 carry)
    - `bash scripts/test.sh multiprocess` passes overall (no regressions)
    - `bash scripts/test.sh all` passes overall: full suite green; expect total count to be Phase 2 final + ~55 new Phase 3 tests (the 3 new structural pins + the earlier-plan additions)
  </acceptance_criteria>
  <done>D-10 opportunistic stale-takeover proven end-to-end at the CLI boundary with a real SIGKILL'd holder; ROADMAP SC#2 closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final phase verification via scripts/verify-phase.sh 03</name>
  <files>(no new files; this task runs the verification dispatcher)</files>
  <read_first>
    - scripts/verify-phase.sh (the dispatcher being invoked — read its `help` output to know exactly what it checks)
    - .planning/phases/02-cli-shell-kv-primitive/02-05-SUMMARY.md (the Phase 2 precedent for invoking verify-phase.sh + interpreting its output)
  </read_first>
  <behavior>
    - `bash scripts/verify-phase.sh 03` exits 0.
    - The verify script's output shows all checks passing: test suite (`test.sh all` + `test.sh structural`), Redis backend check, em-proj on PATH + `--version`, anti-pattern grep (TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER) on `src/ tests/ scripts/`, SUMMARY.md presence for every PLAN.md in the phase directory, recent commit traceability.
    - The em-proj editable shim points at the canonical repo path (`/Users/emonical/projects/personal/ai-tools/em-proj`), NOT at a stale worktree (per the Phase 2 carry-forward; Plan 03-05's --hold tests will have failed if the shim was stale, so this is mostly a confirmation step).
  </behavior>
  <action>
    Run `bash scripts/verify-phase.sh 03`.

    If verify-phase.sh reports SUMMARY.md missing for any 03-NN-PLAN.md, write the corresponding SUMMARY (per each plan's `<output>` block instructions in this Phase 3 plan set). The executor that runs 03-01..05 should have written each SUMMARY at its task completion; Plan 03-06 is the last-line-of-defense check.

    If the anti-pattern grep flags new occurrences of TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER in Phase 3 source files (`src/em_proj/identity.py`, `src/em_proj/state/lock.py`, `tests/multiprocess/test_lock_hold.py`, etc.), audit each one: legitimate forward-pointers (e.g., "Phase 4 will add claim model" markers) are acceptable but must be phrased without the anti-pattern keywords; ad-hoc TODOs are not acceptable and must be cleaned up.

    If the em-proj shim points at a stale worktree, re-install via `uv tool install --editable . --force --reinstall` from the repo root (per the Phase 2 02-04 / 02-05 SUMMARY pattern).

    If `bash scripts/test.sh all` reports any failures, the prior plans did not converge cleanly; investigate before declaring Phase 3 complete.

    Do NOT push to remote; this is a local-verify step.
  </action>
  <verify>
    <automated>bash scripts/verify-phase.sh 03</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-phase.sh 03` exits 0
    - The verify output reports the following all passing: test suite (unit + structural + multiprocess + harness), Redis backend reachable on db=15, em-proj on PATH, anti-pattern grep clean, SUMMARY.md present for 03-01..03-06, recent commits traceable to plan IDs (`feat(03-01)`, `feat(03-02)`, etc.)
    - `bash scripts/test.sh all` exits 0 with a passing test count >= (Phase 2 final count) + 55 (rough lower bound on new Phase 3 tests across this plan set)
    - The em-proj editable install resolves to the canonical repo (`em-proj --version` works without ModuleNotFoundError)
    - No file under `src/em_proj/` or `tests/` contains a fresh `TBD`/`FIXME`/`XXX`/`HACK`/`TODO`/`PLACEHOLDER` marker (existing pre-Phase-3 markers, if any, are out of scope for this task)
  </acceptance_criteria>
  <done>bash scripts/verify-phase.sh 03 is green; Phase 3 is end-to-end verifiable via the single dispatcher call; the gsd-verifier subagent spawn for Phase 3 reduces to "run verify-phase.sh 03 + apply judgment".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structural test ↔ source files | The test reads .py files via ast.parse + Path.read_text(); a malicious source file could attempt to crash the parser, but this is in our own repo |
| SIGKILL injection in test_lock_stale | Test uses os.kill on a process it spawned; no cross-user concern |
| verify-phase.sh ↔ git inspection | The verifier shells out to git for commit traceability; reads only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06-01 | Tampering | structural test bypass | accept | Structural tests are themselves source files in the repo; a bad actor with commit access could disable them, but that's a process-level concern (code review), not an automation-mitigable one. |
| T-3-06-02 | DoS — SIGKILL test orphans a child | test_lock_stale | mitigate | After `os.kill(SIGKILL)`, `proc.wait(timeout=2)` reaps the zombie. If the child is somehow not reapable in 2s (impossible under normal POSIX), the pytest process exit will reap it. |
| T-3-06-03 | Race — SIGKILL'd lock persists beyond test boundary | test_lock_stale | mitigate | `clean_db` fixture is function-scoped (FLUSHDB on db=15 before AND after each test). Even if a test leaves the lock behind, the next test starts clean. The 60s TTL is also a backstop for db=15. |
| T-3-06-04 | Verifier false-pass | scripts/verify-phase.sh 03 | accept | The verifier runs a deterministic check suite; bugs in the verifier itself manifest as Phase 2's existing tests catching the issue. Per Phase 2 02-05 SUMMARY, the verifier is hypothesis-stage and earns trust through observed runs. |
| T-3-06-05 | Anti-pattern grep false-positive | TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER in Phase 3 source | accept | The convention is "no anti-patterns in committed source for ship-ready code"; legitimate forward-pointers ("Phase 4 will add ...") are phrased without the trigger keywords. If a Phase 3 source file truly needs one, document the exception and the dispatcher will report it. |
| T-3-06-06 | Structural regex drift | Blocker #3 refresher-handler regex | mitigate | The regex `except\s*\(\s*redis\.ConnectionError\s*,\s*redis\.TimeoutError\s*\)` permits whitespace flexibility but pins the exact exception tuple shape. A refactor that changes the catch to `except redis.ConnectionError` (single-arg) or `except (TimeoutError, ConnectionError)` (reordered) WILL fail this test — which is by design: the recovery posture's narrow tuple is the documented contract. If a future plan legitimately needs a different shape, update both the lock.py source AND the regex in one commit. |
</threat_model>

<verification>
- `bash scripts/test.sh structural` — all structural tests pass (Phase 1 + Phase 2 + new Phase 3 = ~53 tests, +3 from the revision pins)
- `bash scripts/test.sh multiprocess` — all multiprocess tests pass (Phase 1 + Phase 2 + Phase 3 = harness + KV atomicity + lock_hold + lock_stale)
- `bash scripts/test.sh all` — full suite green; total count substantially exceeds Phase 2's 126
- `bash scripts/verify-phase.sh 03` — exits 0
- `tests/structural/test_phase_03_shape.py::test_every_phase_3_decision_cited_in_at_least_one_plan` — passes (all D-01..D-12 cited)
- `tests/structural/test_phase_03_shape.py::test_lock_force_displace_is_exported` — passes (Blocker #1 pin a)
- `tests/structural/test_phase_03_shape.py::test_state_init_imports_no_private_symbols_from_lock` — passes (Blocker #1 pin b)
- `tests/structural/test_phase_03_shape.py::test_refresher_catches_redis_transients` — passes (Blocker #3 pin)
- `em-proj state --help` lists 6 commands; `em-proj state lock --help` shows --warn, --hold, --ttl, --reason, --json
- The Phase 2 structural tests (`test_phase_02_shape.py::test_no_direct_redis_redis_construction_outside_chokepoint`) still pass (lock.py and identity.py respect the D-18 chokepoint tree-wide; lock.py's `import redis` for exception types is NOT a `redis.Redis(` construction call)
</verification>

<success_criteria>
- ROADMAP success criterion #2 FULLY closed at the end-to-end layer (test_lock_stale.py proves the IDENT-02 stale-detection composite catches abandoned locks across PID reuse via the SIGKILL'd-then-takeover flow).
- Decision Coverage Gate for Phase 3 is green: every D-01..D-12 is cited in at least one 03-NN-PLAN.md.
- Blocker #1 resolution is structurally enforced: lock_force_displace is exported; verb code does not import private symbols from lock.py.
- Blocker #3 resolution is structurally enforced: the refresher's narrow Redis-transient catch is present and shape-pinned.
- Phase 3 is verifiable via a single allowlisted dispatcher call: `bash scripts/verify-phase.sh 03` returns 0.
- The structural test file becomes the regression gate for every subsequent phase that might accidentally violate a Phase 3 invariant (e.g., Phase 4's claim verb importing redis for direct construction, a refactor that loses the --warn double-isatty check, a refactor that drops the refresher's recovery handler, or a backslide that re-introduces `_encode_holder` imports into verb code).
- Phase 3 is ready for `gsd-verify-phase 3` — the verifier sub-agent's job becomes: "run verify-phase.sh 03; read output; apply judgment about whether the phase GOAL is delivered (not just whether checks pass); write VERIFICATION.md".
</success_criteria>

<output>
After completion, create `.planning/phases/03-identity-advisory-locks/03-06-SUMMARY.md` documenting:
- Test count in test_phase_03_shape.py (>= 20, with the 3 new Blocker #1/#3 pins)
- Test count in test_lock_stale.py
- Final `bash scripts/verify-phase.sh 03` output (paste the structured markdown report)
- Any pre-existing anti-pattern grep hits not introduced by Phase 3 (carry-forward; not blocking)
- Any deviations from the planning sketch encountered during execution
- Confirmation that all three revision-iteration blockers are structurally pinned: Blocker #1 (lock_force_displace exported + no private imports in verb code), Blocker #2 (no "not_implemented" string in state/__init__.py post-Plan-03-05 + no NotImplementedError anywhere on the --hold path), Blocker #3 (refresher exception handler shape pinned)
- Phase 3 readiness statement: "Phase 3 verifiable via verify-phase.sh; ready for gsd-verify-phase 3"
</output>
</content>
</invoke>