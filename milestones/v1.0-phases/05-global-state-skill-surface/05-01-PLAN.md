---
phase: 05-global-state-skill-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/em_proj/state/lock.py
  - tests/unit/test_lock_list.py
autonomous: true
requirements: [SKILL-01]
must_haves:
  truths:
    - "lock_list_by_prefix returns an array of lock holder dicts for keys matching the KEY_PREFIX"
    - "Filtering by session_id (--mine analog) returns only locks held by the current session"
    - "Filtering by stale probe (--stale analog) returns only locks whose holder fails is_holder_stale"
  artifacts:
    - path: "src/em_proj/state/lock.py"
      provides: "lock_list_by_prefix pure op"
      contains: "def lock_list_by_prefix"
    - path: "tests/unit/test_lock_list.py"
      provides: "unit tests for lock_list_by_prefix"
  key_links:
    - from: "lock_list_by_prefix"
      to: "KEY_PREFIX"
      via: "SCAN MATCH state:lock:*"
      pattern: "KEY_PREFIX"
    - from: "lock_list_by_prefix"
      to: "is_holder_stale"
      via: "--stale filter"
      pattern: "is_holder_stale"
---

<objective>
Add `lock_list_by_prefix` as a pure-ops function in `src/em_proj/state/lock.py`.
This is the pure-ops foundation for the `em-proj state lock list` verb wired in Plan 05-03.

Purpose: Provide a testable, Redis-scanning function that enumerates active locks,
returning typed holder dicts with optional filter predicates for `--mine` and `--stale`.

Output: `lock_list_by_prefix` in `lock.py` + unit tests in `tests/unit/test_lock_list.py`.
</objective>

<execution_context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lock_list_by_prefix pure op to lock.py</name>
  <files>src/em_proj/state/lock.py</files>
  <read_first>
    - src/em_proj/state/lock.py — full file; understand KEY_PREFIX, _decode_holder, is_holder_stale, HeldByAnother, current_process_composite
    - src/em_proj/identity.py — resolve_session_id, resolve_project_hash, is_holder_stale signatures
    - src/em_proj/state/kv.py — kv_list pattern for SCAN (reference for how prefix-scanning is done)
  </read_first>
  <behavior>
    - lock_list_by_prefix() with no filters returns a list of all holder dicts for keys under KEY_PREFIX
    - When mine=True, only dicts where holder["session_id"] == resolve_session_id() are returned
    - When stale=True, only dicts where is_holder_stale(holder) is True are returned
    - mine and stale may be combined (AND logic: must satisfy both conditions)
    - Keys that fail JSON decode are silently skipped (malformed key = defensive omission, not a crash)
    - Returns empty list when no locks exist (not an error)
    - Function does NOT catch redis.ConnectionError (D-18 chokepoint: verb layer owns that)
    - Return type: list[dict] — each dict has same shape as lock_acquire return value
  </behavior>
  <action>
    Append `lock_list_by_prefix` as a new public function at the bottom of the public operations
    section in `src/em_proj/state/lock.py`, BEFORE the hold-runner section. Follow the D-17
    thin-ops discipline: no typer imports, no emit_* calls.

    Function signature:
      def lock_list_by_prefix(
          mine: bool = False,
          stale: bool = False,
      ) -> list[dict]:

    Implementation shape:
    1. Get client = get_client()
    2. Use client.scan_iter(match=KEY_PREFIX + "*", count=100) to iterate keys
    3. For each key: client.get(key) → if None, skip (key expired mid-scan); else
       _decode_holder(raw) in a try/except (ValueError, KeyError) → skip malformed
    4. Filter: if mine and holder["session_id"] != resolve_session_id(), skip
    5. Filter: if stale and not is_holder_stale(holder), skip
    6. Collect and return the list

    Add docstring noting: "MODULE-PUBLIC: wired by Plan 05-03's lock_list verb.
    Does NOT catch redis.ConnectionError (D-18 carry)."

    After writing the function, also write the unit tests in tests/unit/test_lock_list.py
    before considering this task complete (TDD: tests first, then verify they pass).
  </action>
  <verify>
    <automated>bash scripts/test.sh unit tests/unit/test_lock_list.py -v</automated>
  </verify>
  <done>
    lock_list_by_prefix defined in lock.py; all unit tests in tests/unit/test_lock_list.py pass;
    no import of typer, no emit_* calls in lock.py
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for lock_list_by_prefix</name>
  <files>tests/unit/test_lock_list.py</files>
  <read_first>
    - tests/unit/ — check for existing lock unit test files to follow naming and fixture patterns
    - tests/conftest.py — redis_precheck, clean_db fixtures (available in unit suite? verify)
    - src/em_proj/state/lock.py — lock_list_by_prefix signature (just written in Task 1)
    - src/em_proj/identity.py — resolve_session_id for constructing mock session_ids
  </read_first>
  <behavior>
    - test_lock_list_empty: no locks in Redis → returns []
    - test_lock_list_returns_holder: after lock_acquire("testkey"), lock_list_by_prefix() returns list with one entry; holder has session_id, pid, expires_at fields
    - test_lock_list_mine_filter: two locks in Redis (one with current session_id, one with a fabricated other session_id via direct Redis HSET); mine=True returns only current-session lock
    - test_lock_list_stale_filter: one lock in Redis with a fabricated dead PID composite (pid=99999999, proc_start_epoch=0.0, boot_id="deadbeef00000000"); stale=True returns it; stale=False does not
    - test_lock_list_malformed_skip: a key under state:lock: containing invalid JSON is silently skipped; no exception raised
    - test_lock_list_mine_and_stale_combined: with mine=True and stale=True, only dicts matching BOTH conditions are returned
  </behavior>
  <action>
    Write tests/unit/test_lock_list.py. Use the redis_precheck and clean_db fixtures from conftest.py.
    For the "fabricated other session" test: write a holder dict with a different session_id directly
    via client.set() using the KEY_PREFIX + name key and a JSON-encoded holder.
    For the "dead PID" stale test: write a holder with pid=99999999 (guaranteed absent),
    proc_start_epoch=0.0, boot_id="deadbeef00000000" directly via client.set().
    Use lock_release() to clean up after test_lock_list_returns_holder to avoid leaking locks
    across tests.

    Import only from: em_proj.state.lock, em_proj.identity, em_proj.redis_client.
    No subprocess invocations in unit tests (those belong in multiprocess suite).
  </action>
  <verify>
    <automated>bash scripts/test.sh unit tests/unit/test_lock_list.py -v</automated>
  </verify>
  <done>
    All 6 test functions pass; no test imports typer or subprocess; tests use clean_db fixture
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis → Python | SCAN returns arbitrary key names; holder JSON is untrusted input |
| project_hash scope | lock list scoped to KEY_PREFIX only; no cross-project leakage at this layer (project-scoping is done in claim.py via project_hash in the key; lock keys are user-global per Phase 3 design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01-01 | I (Information Disclosure) | lock_list_by_prefix | mitigate | Caller (verb layer) applies _HOLDER_DISCLOSURE_KEYS filter before emitting; pure op returns full dicts internally — verb is responsible for redacting boot_id and proc_start_epoch per output.py contract |
| T-5-01-02 | T (Tampering) | malformed holder JSON | accept | Silently skip malformed keys; malformed key cannot cause takeover or crash; best-effort listing surface |
| T-5-01-03 | D (Denial of Service) | unbounded SCAN | accept | scan_iter with count=100 is cursor-based and non-blocking; lock namespace is developer-facing and bounded in practice; no user-controlled key count |
</threat_model>

<verification>
- `bash scripts/test.sh unit tests/unit/test_lock_list.py -v` → all tests pass
- `grep -n "def lock_list_by_prefix" src/em_proj/state/lock.py` returns a match
- `grep "typer" src/em_proj/state/lock.py` returns nothing new (no new typer import)
</verification>

<success_criteria>
- lock_list_by_prefix exists in lock.py as a public module-level function
- mine= and stale= filter parameters work as independently verifiable predicates
- 6 unit tests covering empty-list, single-holder, mine-filter, stale-filter,
  malformed-skip, and combined-filter cases all pass via bash scripts/test.sh unit
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-state-skill-surface/05-01-SUMMARY.md`
</output>
