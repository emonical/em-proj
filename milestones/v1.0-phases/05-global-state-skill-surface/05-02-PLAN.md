---
phase: 05-global-state-skill-surface
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/em_proj/state/claim.py
  - tests/unit/test_claim_list.py
autonomous: true
requirements: [SKILL-01]
must_haves:
  truths:
    - "claim_list_by_prefix returns an array of claim holder dicts for all claims under the current project_hash"
    - "Filtering by session_id (--mine analog) returns only claims held by the current session"
    - "Filtering by active (--active analog) returns only claims whose Redis key still has TTL > 0"
    - "Filtering by stale (--stale analog) returns claims where TTL has expired or holder probe fails"
  artifacts:
    - path: "src/em_proj/state/claim.py"
      provides: "claim_list_by_prefix pure op"
      contains: "def claim_list_by_prefix"
    - path: "tests/unit/test_claim_list.py"
      provides: "unit tests for claim_list_by_prefix"
  key_links:
    - from: "claim_list_by_prefix"
      to: "KEY_PREFIX + project_hash"
      via: "SCAN MATCH state:claim:<project_hash>:*"
      pattern: "KEY_PREFIX"
    - from: "claim_list_by_prefix"
      to: "_hgetall_to_holder"
      via: "HGETALL result coercion"
      pattern: "_hgetall_to_holder"
---

<objective>
Add `claim_list_by_prefix` as a pure-ops function in `src/em_proj/state/claim.py`.
This is the pure-ops foundation for the `em-proj state claim list` verb wired in Plan 05-03.

Purpose: Provide a testable, Redis-scanning function that enumerates active claims
scoped to the current project_hash, returning typed holder dicts with optional filter
predicates for `--mine`, `--active`, and `--stale`.

Output: `claim_list_by_prefix` in `claim.py` + unit tests in `tests/unit/test_claim_list.py`.
</objective>

<execution_context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/05-global-state-skill-surface/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add claim_list_by_prefix pure op to claim.py</name>
  <files>src/em_proj/state/claim.py</files>
  <read_first>
    - src/em_proj/state/claim.py — full file; understand KEY_PREFIX, _build_redis_key, _hgetall_to_holder, resolve_project_hash, resolve_session_id
    - src/em_proj/state/lock.py — lock_list_by_prefix (just written in Plan 05-01) for structural analog
    - src/em_proj/identity.py — resolve_session_id, resolve_project_hash
  </read_first>
  <behavior>
    - claim_list_by_prefix() with no filters returns list of all holder dicts for
      keys matching "state:claim:<current_project_hash>:*"
    - When mine=True, only dicts where holder["session_id"] == resolve_session_id() are returned
    - When active=True, only claims with Redis TTL > 0 are returned (use client.ttl(key))
    - When stale=True, only claims where Redis TTL <= 0 (expired but key still present,
      i.e., key exists AND ttl == -1 or ttl == 0) are returned; note claims rely
      entirely on TTL for liveness — no PID composite probe needed
    - active and stale are mutually exclusive filters at the caller's discretion
      (function applies both if set; results would be empty, but no error raised)
    - mine may be combined with active or stale (AND logic)
    - Keys that fail HGETALL decode (empty dict or missing fields) are silently skipped
    - Returns empty list when no claims exist (not an error)
    - Function does NOT catch redis.ConnectionError (D-18 chokepoint carry)
    - Scope: ONLY scans keys under KEY_PREFIX + current_project_hash + ":" —
      cross-project listing is out of scope (REQUIREMENTS.md out-of-scope table)
    - Return type: list[dict] — each dict has same 5-field shape as claim_take return value
      (session_id, project_hash, reason, claimed_at, expires_at)
  </behavior>
  <action>
    Append `claim_list_by_prefix` as a new public function at the bottom of the public
    operations section in `src/em_proj/state/claim.py`. Follow D-17 discipline: no typer
    imports, no emit_* calls.

    Function signature:
      def claim_list_by_prefix(
          mine: bool = False,
          active: bool = False,
          stale: bool = False,
      ) -> list[dict]:

    Implementation shape:
    1. project_hash = resolve_project_hash()
    2. scan_prefix = KEY_PREFIX + project_hash + ":"
    3. client = get_client()
    4. Use client.scan_iter(match=scan_prefix + "*", count=100) to iterate keys
    5. For each key:
       a. raw = client.hgetall(key) — if empty dict, skip (key expired mid-scan)
       b. try _hgetall_to_holder(raw) except (KeyError, ValueError) → skip malformed
       c. Filter mine: if mine and holder["session_id"] != resolve_session_id(), skip
       d. Filter active: if active → ttl = client.ttl(key); skip if ttl <= 0
       e. Filter stale: if stale → ttl = client.ttl(key); skip if ttl > 0
    6. Collect and return the list

    IMPORTANT: if both active and stale need ttl, fetch ttl once per key, not twice.
    Use a lazy ttl fetch pattern: `_ttl = None` then `_ttl = client.ttl(key)` on first need.

    Add docstring noting: "MODULE-PUBLIC: wired by Plan 05-03's claim_list verb.
    Scopes to current project_hash only — cross-project listing is out of scope (M2).
    Does NOT catch redis.ConnectionError (D-18 carry)."

    After writing the function, also write unit tests in tests/unit/test_claim_list.py
    before considering this task complete (TDD: tests first, then verify they pass).
  </action>
  <verify>
    <automated>bash scripts/test.sh unit tests/unit/test_claim_list.py -v</automated>
  </verify>
  <done>
    claim_list_by_prefix defined in claim.py; all unit tests in tests/unit/test_claim_list.py pass;
    no import of typer in claim.py; no cross-project scanning
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for claim_list_by_prefix</name>
  <files>tests/unit/test_claim_list.py</files>
  <read_first>
    - tests/unit/test_lock_list.py — structural analog (just written in Plan 05-01)
    - tests/conftest.py — available fixtures
    - src/em_proj/state/claim.py — claim_list_by_prefix signature + claim_take, claim_release
    - src/em_proj/identity.py — resolve_session_id for constructing mock session_ids
  </read_first>
  <behavior>
    - test_claim_list_empty: no claims in Redis → returns []
    - test_claim_list_returns_holder: after claim_take("test-area"), claim_list_by_prefix()
      returns list with one entry having session_id, project_hash, claimed_at, expires_at fields
    - test_claim_list_mine_filter: two claims (one current session, one with a fabricated
      other session_id written directly via client.hset + client.expire); mine=True returns
      only current-session claim
    - test_claim_list_active_filter: one live claim (TTL > 0) and one expired-but-present claim
      (write claim key via hset with no expire, so ttl=-1); active=True returns only live claim
    - test_claim_list_stale_filter: same setup as active; stale=True returns only the expired-but-present one
    - test_claim_list_malformed_skip: a key under the claim prefix containing an empty HASH
      (or a HASH missing fields) is silently skipped; no exception raised
    - test_claim_list_scoped_to_project: a claim key written under a DIFFERENT project_hash
      (e.g. KEY_PREFIX + "other-project:" + "area") is NOT returned; scope is current project only
  </behavior>
  <action>
    Write tests/unit/test_claim_list.py. Use redis_precheck and clean_db fixtures.
    For the "fabricated other session" test: write the holder fields via client.hset() +
    client.expire() using KEY_PREFIX + project_hash + ":other-area" as the key.
    For the "expired but present" test: write the holder fields via client.hset() WITHOUT
    client.expire(), so Redis reports ttl=-1 for that key (persistent key, simulating
    TTL-expired claim that wasn't evicted yet).
    For the "other project" test: use KEY_PREFIX + "other-project-hash:area" as the key.

    Import only from: em_proj.state.claim, em_proj.identity, em_proj.redis_client.
    Use claim_release() to clean up live claims to avoid leaking across tests.
    Set CLAUDE_CODE_SESSION_ID env var in tests that need a known session_id
    (use monkeypatch.setenv("CLAUDE_CODE_SESSION_ID", "test-session-abc")).
  </action>
  <verify>
    <automated>bash scripts/test.sh unit tests/unit/test_claim_list.py -v</automated>
  </verify>
  <done>
    All 7 test functions pass; no test imports typer or subprocess; claim_list_by_prefix
    correctly scopes to current project_hash only
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis → Python | HGETALL returns arbitrary string fields; holder data is untrusted input |
| project_hash scope | SCAN prefix scoped to KEY_PREFIX + current project_hash; no cross-project data returned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02-01 | I (Information Disclosure) | claim_list_by_prefix | mitigate | Verb layer applies disclosure filtering before emit; pure op returns full 5-field holder (all claim fields are disclosure-safe; claims lack pid/boot_id per Phase 4 design) |
| T-5-02-02 | E (Elevation of Privilege) | cross-project listing | mitigate | SCAN prefix is KEY_PREFIX + current_project_hash + ":" — no wildcard across project hashes; cross-project listing explicitly out of scope per REQUIREMENTS.md |
| T-5-02-03 | T (Tampering) | malformed HASH | accept | Empty/malformed HGETALL silently skipped; cannot cause data corruption |
</threat_model>

<verification>
- `bash scripts/test.sh unit tests/unit/test_claim_list.py -v` → all tests pass
- `grep -n "def claim_list_by_prefix" src/em_proj/state/claim.py` returns a match
- `grep "typer" src/em_proj/state/claim.py` returns nothing new
</verification>

<success_criteria>
- claim_list_by_prefix exists in claim.py as a public module-level function
- mine=, active=, and stale= filter parameters work as independently verifiable predicates
- Scope is enforced: only current project_hash keys returned
- 7 unit tests pass via bash scripts/test.sh unit
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-state-skill-surface/05-02-SUMMARY.md`
</output>
