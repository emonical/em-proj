---
phase: 07-project-scoped-reservation-registry
plan: "03"
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/structural/test_phase_07_shape.py
  - /Users/emonical/.claude/skills/em-global-state/SKILL.md
autonomous: true
requirements:
  - RESERVE-03
  - RESERVE-04

must_haves:
  truths:
    - "tests/structural/test_phase_07_shape.py exists and passes via `bash scripts/test.sh structural`"
    - "Structural test asserts KEY_PREFIX disjointness: claim.py KEY_PREFIX == 'state:claim:' AND reserve.py KEY_PREFIX == 'state:reserve:' AND claim.py source does NOT contain 'state:reserve:' AND reserve.py source does NOT contain 'state:claim:' AND claim.py source does NOT contain 'upstream_identity' (Pitfall #8 two-namespace invariant)"
    - "Structural test asserts reserve.py defines all 3 Lua scripts (LUA_RESERVE_REFRESH_OR_TAKE, LUA_RESERVE_COMPARE_AND_DELETE, LUA_RESERVE_CHECK) as module-level string constants"
    - "Structural test asserts state/__init__.py contains @state_app.command(\"reserve\") AND @state_app.command(\"reserve-list\") AND the `check` verb's signature mentions --upstream"
    - "Structural test asserts EVERY subprocess.Popen call in tests/multiprocess/test_reserve_*.py is followed by a `cwd=` kwarg (Pitfall #6 mitigation — machine-enforced)"
    - "Structural test asserts the exact 'workstream unresolved — set it via' actionable-error substring is present in state/__init__.py (locks the RESEARCH §Pattern 4 wording)"
    - "Structural test asserts every 07-*-PLAN.md in the phase dir has a corresponding 07-*-SUMMARY.md (SUMMARY coverage, same pattern as Phase 4/5/6)"
    - "~/.claude/skills/em-global-state/SKILL.md contains a new `### /em-global-state reservations` verb subsection wrapping `em-proj state reserve-list`"
    - "SKILL.md verb subsection documents --category and --upstream flags + the 7-field+area holder schema + exit-0-on-empty contract"
    - "bash scripts/verify-phase.sh 07 exits 0 (full phase acceptance gate: tests + anti-pattern grep + SUMMARY presence + commit traceability)"
  artifacts:
    - path: "tests/structural/test_phase_07_shape.py"
      provides: "AST + source-grep structural invariants for Phase 7 — encoded plan acceptance criteria as runtime assertions"
      contains: "KEY_PREFIX"
    - path: "/Users/emonical/.claude/skills/em-global-state/SKILL.md"
      provides: "/em-global-state reservations verb subsection — read view over Phase 7 reservations from any session"
      contains: "reservations"
  key_links:
    - from: "test_phase_07_shape.py"
      to: "src/em_proj/state/reserve.py AND src/em_proj/state/claim.py"
      via: "(REPO_ROOT / 'src/em_proj/state/{file}.py').read_text() + source-text and AST assertions"
      pattern: "KEY_PREFIX"
    - from: "test_phase_07_shape.py"
      to: "tests/multiprocess/test_reserve_race.py + test_reserve_three_clones_list.py"
      via: "regex assertion that every subprocess.Popen( … ) call contains a cwd= kwarg"
      pattern: "subprocess\\.Popen\\([^)]*cwd="
    - from: "SKILL.md reservations verb"
      to: "em-proj state reserve-list CLI"
      via: "documented `em-proj state reserve-list [--category <name>] [--upstream <url-or-id>] --json` invocation"
      pattern: "em-proj state reserve-list"
---

<objective>
Encode Phase 7 acceptance criteria as runtime pytest assertions in
`tests/structural/test_phase_07_shape.py`, extend the existing
`em-global-state` skill with a `reservations` verb subsection, then run
`bash scripts/verify-phase.sh 07` as the final acceptance gate.

Purpose:
  - Phase 7's two-namespace invariant (Pitfall #8) needs machine enforcement
    or it WILL regress under a future refactor that consolidates claim.py
    and reserve.py. The structural test is the regression guard.
  - Pitfall #6 (per-child cwd= in multi-clone tests) is a NEW pattern for
    em-proj. Without machine enforcement, a future test that mirrors the
    Phase 4-6 env-only pattern will silently produce false-positive passes.
    The structural test source-greps every subprocess.Popen( call in
    test_reserve_*.py and asserts cwd= appears as a kwarg.
  - The locked actionable error copy in `_resolve_workstream` ("workstream
    unresolved — set it via …") is part of the user contract for RESERVE-05.
    A structural assertion locks the wording so casual refactors don't
    silently break it.
  - RESERVE-03 + RESERVE-04 require a skill surface. The existing
    `/em-global-state` skill already has 6 verbs; adding a 7th
    `reservations` verb per RESEARCH Open Q-E + RESEARCH §Example 5 is
    cheaper than maintaining a parallel `/em-check-state` skill file. The
    SKILL.md edit is CROSS-REPO (lives at ~/.claude/skills/em-global-state/
    outside em-proj's git tree, same posture as Phase 6's gsd-sdk patch).
  - `bash scripts/verify-phase.sh 07` is the deterministic dispatcher gate
    that the human (or /gsd-verify-work) runs to confirm the phase is
    structurally done.

Output:
  - tests/structural/test_phase_07_shape.py (6+ test functions)
  - ~/.claude/skills/em-global-state/SKILL.md (new ### /em-global-state
    reservations subsection)
  - bash scripts/verify-phase.sh 07 exits 0
  - 07-01-SUMMARY.md, 07-02-SUMMARY.md, 07-03-SUMMARY.md all present
</objective>

<execution_context>
@/Users/emonical/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emonical/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/PROJECT.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/ROADMAP.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/STATE.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-RESEARCH.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-PATTERNS.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-01-SUMMARY.md
@/Users/emonical/projects/personal/ai-tools/em-proj/.planning/phases/07-project-scoped-reservation-registry/07-02-SUMMARY.md
@/Users/emonical/projects/personal/ai-tools/em-proj/CLAUDE.md
</context>

<interfaces>
<!-- Self-contained convention (Phase 1+2+3+4+5+6 precedent — locked) -->

Every structural test file copies helpers verbatim from a sibling phase
rather than importing from a shared module. Phase 7 does NOT need the
Phase 6 `_resolve_workstream_artifact` cross-repo resolver because the
SKILL.md is the only cross-repo artifact and its absence is acceptable
(skill not installed → still a valid em-proj checkout); no xfail required
on the structural side.

<!-- Path constants Phase 7 needs -->

REPO_ROOT = Path(__file__).resolve().parent.parent.parent
PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "07-project-scoped-reservation-registry"
RESERVE_PY = REPO_ROOT / "src" / "em_proj" / "state" / "reserve.py"
CLAIM_PY = REPO_ROOT / "src" / "em_proj" / "state" / "claim.py"
STATE_INIT_PY = REPO_ROOT / "src" / "em_proj" / "state" / "__init__.py"
IDENTITY_PY = REPO_ROOT / "src" / "em_proj" / "identity.py"
RESERVE_RACE_TEST = REPO_ROOT / "tests" / "multiprocess" / "test_reserve_race.py"
RESERVE_THREE_CLONES_TEST = REPO_ROOT / "tests" / "multiprocess" / "test_reserve_three_clones_list.py"

<!-- SKILL.md insertion point -->

The existing skill file is ~/.claude/skills/em-global-state/SKILL.md (244
lines). Per RESEARCH §Example 5 lines 894-920, the new verb subsection
lands between the existing `### /em-global-state claims [--mine] [--active]
[--stale]` subsection and the `### /em-global-state unlock <name> [--force]`
subsection — read verbs grouped together, write verbs grouped together. The
new subsection follows the same shape as the existing 6 verbs (subsection
header, bash invocation block, schema block, exit-code note).

<!-- SKILL.md verb content (locked by RESEARCH §Example 5) -->

The new verb's canonical CLI surface is:
  em-proj state reserve-list [--category <name>] [--upstream <url-or-id>] --json

Output schema (per Plan 07-02 emit_ok envelope):
  {"schema_version":"1","status":"ok","data":{
     "upstream_identity":"<canonical>",
     "items":[<reservation_holder>...]
  }}

Each reservation_holder contains 7 fields plus an injected `area`:
  area, session_id, project_hash, upstream_identity, workstream, reason,
  claimed_at, expires_at.

Exit 0 = success (empty list is still exit 0).

<!-- Phase 6 analog for structural-test shape -->

tests/structural/test_phase_06_shape.py is the most-recent structural test;
its self-contained-helper preamble, REPO_ROOT/PHASE_DIR constants pattern,
and test_phase_NN_summaries_present function shape are the templates to
copy. Phase 7 does NOT need the cross-repo xfail dance because:
  - reserve.py / claim.py / state/__init__.py are em-proj source — straight
    Path.read_text().
  - SKILL.md absence is NOT a regression (the developer may not have the
    skill installed on this checkout); the SKILL.md assertion uses
    pytest.skip on missing-file, NOT xfail (different semantics than Phase
    6's npm-install reversion case).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/structural/test_phase_07_shape.py with all Phase 7 invariants</name>
  <files>tests/structural/test_phase_07_shape.py</files>

  <read_first>
    - tests/structural/test_phase_06_shape.py — full file (~260 lines). Closest analog. Note: module docstring (lines 1-23), self-contained-helper preamble comment, REPO_ROOT/PHASE_DIR path constants (lines 33-34), test_phase_06_summaries_present pattern (lines 287-302). Phase 7 reuses the SUMMARY pattern verbatim with phase-number swap.
    - tests/structural/test_phase_04_shape.py — supplement; line 192-199 has the simpler SUMMARY-coverage pattern.
    - tests/structural/test_phase_05_shape.py — the SKILL.md handling pattern (lines 56-59 SKILL_PATH resolver + xfail dance for cross-repo missing). Phase 7 uses pytest.skip not xfail (different scope — skill absence is acceptable on a fresh checkout; em-proj source presence is the structural concern).
    - src/em_proj/state/reserve.py (Plan 07-01) and src/em_proj/state/__init__.py (Plan 07-02) — confirm the symbols the structural test will assert exist.
    - 07-RESEARCH.md §"Validation Architecture" lines 1099-1102 + 07-PATTERNS.md §"tests/structural/test_phase_07_shape.py" (lines 681-748) — the Phase-7-specific invariants to encode.
  </read_first>

  <action>
    Create tests/structural/test_phase_07_shape.py. Self-contained (no
    imports from other tests/structural/*.py files — copy helpers verbatim
    per the Plan 02-05 SUMMARY convention reinforced through Phases 3+4+5+6).

    Module layout (top-to-bottom):

    1. Future-imports + docstring:
       from __future__ import annotations
       """Phase 7 structural invariants — source-grep and AST assertions.

       Encodes plan acceptance criteria as runtime pytest assertions:
         - Two-namespace disjointness (Pitfall #8): claim.py and reserve.py
           KEY_PREFIX values are different; neither file references the
           other's prefix literal; claim.py has no 'upstream_identity'
           reference (it's a Phase 7-only field).
         - reserve.py shape: 3 Lua scripts as module-level string constants;
           7-field holder enforced via _RESERVE_ARGV_ORDER tuple presence.
         - Verb wiring: state/__init__.py contains @state_app.command('reserve'),
           @state_app.command('reserve-list'), AND the check verb references
           --upstream.
         - Per-child cwd= in multi-clone tests (Pitfall #6 — machine-enforced):
           every subprocess.Popen(…) site in test_reserve_*.py contains a
           cwd= kwarg.
         - Actionable-error copy lock (RESEARCH §Pattern 4): the exact
           'workstream unresolved — set it via' substring is present in
           state/__init__.py.
         - SUMMARY coverage: every 07-*-PLAN.md has a 07-*-SUMMARY.md sibling.

       Each structural file is self-contained (no imports from sibling
       test_phase_*_shape.py files) per Phase 1+2+3+4+5+6 precedent.
       """

    2. Imports (minimal):
       import re
       from pathlib import Path
       import pytest

       # Self-contained — no imports from sibling test_phase_*_shape.py files.

    3. Path constants:
       REPO_ROOT = Path(__file__).resolve().parent.parent.parent
       PHASE_DIR = REPO_ROOT / ".planning" / "phases" / "07-project-scoped-reservation-registry"
       RESERVE_PY = REPO_ROOT / "src" / "em_proj" / "state" / "reserve.py"
       CLAIM_PY = REPO_ROOT / "src" / "em_proj" / "state" / "claim.py"
       STATE_INIT_PY = REPO_ROOT / "src" / "em_proj" / "state" / "__init__.py"
       IDENTITY_PY = REPO_ROOT / "src" / "em_proj" / "identity.py"
       RESERVE_RACE_TEST = REPO_ROOT / "tests" / "multiprocess" / "test_reserve_race.py"
       RESERVE_THREE_CLONES_TEST = REPO_ROOT / "tests" / "multiprocess" / "test_reserve_three_clones_list.py"

       # Cross-repo (NOT in em-proj git tree) — skill-doc invariant only
       SKILL_PATH = Path.home() / ".claude" / "skills" / "em-global-state" / "SKILL.md"

    4. Test functions (8 minimum):

       Test A — test_reserve_py_exists_and_has_three_lua_scripts:
         assert RESERVE_PY.exists(), "Plan 07-01 must create reserve.py"
         src = RESERVE_PY.read_text()
         for lua_name in ("LUA_RESERVE_REFRESH_OR_TAKE",
                          "LUA_RESERVE_COMPARE_AND_DELETE",
                          "LUA_RESERVE_CHECK"):
             assert lua_name in src, f"{lua_name} missing from reserve.py"

       Test B — test_key_prefixes_are_disjoint (Pitfall #8 invariant):
         assert RESERVE_PY.exists() and CLAIM_PY.exists()
         reserve_src = RESERVE_PY.read_text()
         claim_src = CLAIM_PY.read_text()
         # Strip docstrings/comments to avoid false positives — but the
         # tighter check is on the actual KEY_PREFIX assignment lines.
         # Use regex for the precise assignment shape:
         m_reserve = re.search(r'^KEY_PREFIX\s*:\s*str\s*=\s*"([^"]+)"',
                               reserve_src, re.MULTILINE)
         m_claim = re.search(r'^KEY_PREFIX\s*:\s*str\s*=\s*"([^"]+)"',
                             claim_src, re.MULTILINE)
         assert m_reserve and m_reserve.group(1) == "state:reserve:", \
             "reserve.py KEY_PREFIX must be 'state:reserve:'"
         assert m_claim and m_claim.group(1) == "state:claim:", \
             "claim.py KEY_PREFIX must be 'state:claim:'"

       Test C — test_namespaces_dont_cross_contaminate (Pitfall #8 deeper):
         """Source-text assertion that the two modules don't reference each
         other's namespace string. A future consolidation refactor that
         parameterizes the prefix would necessarily include both strings in
         one file — that should fail this test."""
         claim_src = CLAIM_PY.read_text()
         reserve_src = RESERVE_PY.read_text()
         # claim.py must NOT reference reserve namespace
         assert "state:reserve:" not in claim_src, (
             "claim.py source contains 'state:reserve:' — two-namespace "
             "invariant violated (Pitfall #8)"
         )
         # reserve.py must NOT reference claim namespace
         assert "state:claim:" not in reserve_src, (
             "reserve.py source contains 'state:claim:' — two-namespace "
             "invariant violated (Pitfall #8)"
         )
         # claim.py must NOT have upstream_identity (Phase 7-only field).
         # NOTE: if a future Phase consolidates and uses 'upstream_identity'
         # in claim.py, this assertion will fail — that's the point.
         assert "upstream_identity" not in claim_src, (
             "claim.py source contains 'upstream_identity' — Phase 7 holder "
             "field has leaked into the claim namespace (Pitfall #8)"
         )

       Test D — test_state_init_has_reserve_verbs:
         src = STATE_INIT_PY.read_text()
         assert '@state_app.command("reserve")' in src or \
                "@state_app.command('reserve')" in src, (
             "state/__init__.py missing @state_app.command('reserve') — "
             "Plan 07-02 verb wiring not landed"
         )
         assert '@state_app.command("reserve-list")' in src or \
                "@state_app.command('reserve-list')" in src, (
             "state/__init__.py missing @state_app.command('reserve-list')"
         )
         # The check verb must mention --upstream
         assert "--upstream" in src, (
             "state/__init__.py has no '--upstream' reference — the check "
             "verb extension from Plan 07-02 not landed"
         )

       Test E — test_actionable_error_copy_locked:
         """RESEARCH §Pattern 4 + Q-F lock the exact error wording. If a
         future refactor accidentally drops 'set it via' or rephrases the
         message, this test catches it."""
         src = STATE_INIT_PY.read_text()
         assert "workstream unresolved — set it via" in src, (
             "Locked actionable error copy 'workstream unresolved — set it "
             "via' missing from state/__init__.py. Plan 07-02's "
             "_resolve_workstream helper must preserve this exact phrasing."
         )

       Test F — test_multiproc_tests_use_per_child_cwd (Pitfall #6 invariant):
         """Every subprocess.Popen call in test_reserve_*.py must include
         cwd= as a kwarg. A test that varies only env= would produce a
         false-positive race outcome because both children would resolve
         the SAME upstream_identity from the test runner's cwd."""
         for test_file in (RESERVE_RACE_TEST, RESERVE_THREE_CLONES_TEST):
             assert test_file.exists(), f"Plan 07-02 must create {test_file}"
             src = test_file.read_text()
             # Find every subprocess.Popen(... open paren.
             popen_matches = list(re.finditer(r"subprocess\.Popen\s*\(",
                                              src))
             if not popen_matches:
                 # test_reserve_three_clones_list.py uses subprocess.run, not
                 # Popen — that's fine; Popen is only for the race test.
                 continue
             for m in popen_matches:
                 # Take the slice from the open paren forward and find
                 # the matching close paren. For simplicity, look at the
                 # next 600 chars (Popen calls are short) and check for cwd=.
                 chunk = src[m.start():m.start() + 600]
                 assert "cwd=" in chunk, (
                     f"subprocess.Popen at offset {m.start()} in "
                     f"{test_file.name} missing cwd= kwarg (Pitfall #6). "
                     "Without per-child cwd=, both children would resolve "
                     "the same upstream_identity from the test runner's "
                     "cwd, producing a false-positive race outcome."
                 )

       Test G — test_phase_07_summaries_present:
         """SUMMARY coverage check, identical to Phase 4/5/6."""
         if not PHASE_DIR.exists():
             pytest.skip(
                 f"{PHASE_DIR.relative_to(REPO_ROOT)} not present — "
                 "planning worktree may not be attached on this checkout"
             )
         plans = sorted(PHASE_DIR.glob("07-*-PLAN.md"))
         if not plans:
             pytest.skip(f"no 07-*-PLAN.md files yet under "
                         f"{PHASE_DIR.relative_to(REPO_ROOT)}")
         for plan in plans:
             summary = plan.parent / plan.name.replace("-PLAN.md", "-SUMMARY.md")
             assert summary.exists(), (
                 f"Missing SUMMARY for {plan.name}: expected {summary.name} "
                 f"in {PHASE_DIR.relative_to(REPO_ROOT)}"
             )

       Test H — test_skill_has_reservations_verb:
         """Cross-repo skill assertion. Uses pytest.skip (NOT xfail) on
         missing skill — the developer's checkout may not have the
         em-global-state skill installed (different from Phase 6's
         npm-install reversion case, which IS a regression). Absence of
         a personal Claude skill is NOT a Phase 7 failure."""
         if not SKILL_PATH.exists():
             pytest.skip(
                 f"{SKILL_PATH} not present — em-global-state skill not "
                 "installed on this checkout. Phase 7 still ships the "
                 "verb in em-proj; the skill is a separate user surface."
             )
         skill_src = SKILL_PATH.read_text()
         assert "reservations" in skill_src, (
             "SKILL.md missing the 'reservations' verb — Plan 07-03 "
             "Task 2 not applied OR skill was overwritten since"
         )
         assert "em-proj state reserve-list" in skill_src, (
             "SKILL.md reservations verb does not invoke "
             "`em-proj state reserve-list` — schema documentation broken"
         )

    NOTE on xfail-vs-skip discipline (carried from Phase 6 with adjustment):
      - Phase 6 used xfail for cross-repo missing because the gsd-sdk
        patch in workstream.js is RUNTIME-CRITICAL — its absence is a
        regression.
      - Phase 7's SKILL.md is a USER convenience surface; em-proj's
        CLI still works without it. Absence is acceptable, so skip
        (not xfail) is the right semantic.

    NOTE on regex resilience for Test F: the 600-char window is a heuristic;
      adjust if Popen calls in the actual test files are longer. If a Popen
      call exceeds 600 chars without containing cwd= early enough, refactor
      the test to put cwd= near the top of the kwargs OR widen the window
      here.
  </action>

  <verify>
    <automated>bash scripts/test.sh structural -k test_phase_07_shape</automated>
  </verify>

  <done>
    - File tests/structural/test_phase_07_shape.py exists with 8+ test functions
    - bash scripts/test.sh structural -k test_phase_07_shape exits 0 (tests pass OR pytest.skip with reason; no FAIL)
    - bash scripts/test.sh structural exits 0 (full structural suite; Phases 2-6 invariants still hold)
    - No imports from any other tests/structural/*.py file (self-contained convention)
    - The path constants section uses REPO_ROOT-relative paths (no hardcoded /Users/emonical/ except for SKILL_PATH which is Path.home()-based, NOT a literal string)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ~/.claude/skills/em-global-state/SKILL.md with the reservations verb subsection</name>
  <files>/Users/emonical/.claude/skills/em-global-state/SKILL.md</files>

  <read_first>
    - /Users/emonical/.claude/skills/em-global-state/SKILL.md — full file (~244 lines). Note the existing 6 verb subsections (list / get / locks / claims / unlock / release) — Phase 7 follows the same subsection shape (header → bash invocation block → JSON schema → exit-code note → trailing horizontal rule).
    - 07-RESEARCH.md §"Example 5" lines 894-920 — canonical content for the new verb subsection.
    - CLAUDE.md global rule §"Planning artifact storage" — confirms SKILL.md is OUTSIDE em-proj's git tree (lives in user's global Claude skills dir). Same cross-repo handling posture as Phase 6's gsd-sdk patch: the SKILL.md edit produces NO em-proj-side commit on main. Plan 07-03's planning-branch SUMMARY records the edit; recovery on a fresh checkout (or a Claude-config reset) requires re-applying this task.
  </read_first>

  <action>
    Edit /Users/emonical/.claude/skills/em-global-state/SKILL.md to insert a
    new `### /em-global-state reservations` subsection.

    INSERTION POINT: between the existing `### /em-global-state claims
    [--mine] [--active] [--stale]` subsection (the LAST READ verb) and the
    `### /em-global-state unlock <name> [--force]` subsection (the FIRST
    WRITE verb). The read verbs cluster together; reservations is a read
    verb.

    EXACT CONTENT TO INSERT (per RESEARCH §Example 5 lines 894-920):

      ---

      ### /em-global-state reservations [--category <name>] [--upstream <url-or-identity>]

      List reservations against the upstream-repo identity. Auto-resolves the identity
      from the current cwd's `git remote get-url origin`; sibling clones of the same
      upstream see the same reservations.

      ```bash
      em-proj state reserve-list [--category <name>] [--upstream <url-or-identity>] --json
      ```

      Pass `--category <name>` to filter to a single category prefix (e.g., `migrations`).
      Pass `--upstream <url-or-identity>` to query reservations against an upstream
      other than the one rooted at the current cwd.

      Emit stdout verbatim. Output schema:

      ```json
      {"schema_version":"1","status":"ok","data":{"upstream_identity":"<canonical>","items":[<reservation_holder>...]}}
      ```

      Each `reservation_holder` contains 7 fields plus an injected `area`:
      `area, session_id, project_hash, upstream_identity, workstream, reason, claimed_at, expires_at`.

      Exit 0 = success (empty list is still exit 0).

      ---

    Two additional edits to the existing file for consistency:

      Edit 1 — update <scope> READ surface bullet list (around line 213-217):
        Add a new bullet after the `claims [--mine] [--active] [--stale]` bullet:
          - `/em-global-state reservations [--category <name>] [--upstream <url-or-identity>]` —
            reservation enumeration scoped to the calling cwd's upstream-repo identity

      Edit 2 — extend <related> section (around lines 237-244):
        Add a line:
          - Phase 7 (em-proj) — reservation registry implementation (project-scoped via
            upstream-repo identity).

    DO NOT touch the existing 6 verb subsections, the <objective>, <when_to_invoke>,
    <action>'s top boilerplate, the WRITE surface bullets, or the NEVER USED FOR section.

    This is a CROSS-REPO edit. It does NOT produce an em-proj-side commit on main.
    The planning-branch 07-03-SUMMARY.md is the traceability record. If a future
    machine reinstall or skill-reset removes ~/.claude/skills/em-global-state/SKILL.md,
    re-applying this task restores the verb.

    Verification:
      grep -c '^### /em-global-state reservations' /Users/emonical/.claude/skills/em-global-state/SKILL.md
    must return >= 1.
  </action>

  <verify>
    <automated>test "$(grep -c '^### /em-global-state reservations' /Users/emonical/.claude/skills/em-global-state/SKILL.md)" -ge 1</automated>
  </verify>

  <done>
    - SKILL.md contains a new `### /em-global-state reservations` subsection inserted between `claims` and `unlock` verbs
    - The subsection documents --category, --upstream, the JSON schema with 7+area fields, and exit-0-on-empty
    - The <scope> READ surface bullet list is updated to include the new verb
    - The <related> section references Phase 7
    - test "$(grep -c '^### /em-global-state reservations' …)" -ge 1 returns 0
    - The structural test from Task 1 (test_skill_has_reservations_verb) now passes (was skipping or passing before — re-run to confirm pass)
  </done>
</task>

<task type="auto">
  <name>Task 3: Run bash scripts/verify-phase.sh 07 as the final acceptance gate</name>
  <files></files>

  <read_first>
    - scripts/verify-phase.sh — full dispatcher surface (run `bash scripts/verify-phase.sh help`). Understand which checks fire for phase 07: full test suite (unit + multiprocess + structural), Redis backend check, em-proj on PATH + --version, anti-pattern grep on src/ tests/ scripts/ (TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER), SUMMARY.md presence for every 07-*-PLAN.md, recent commit traceability.
    - .planning/phases/07-project-scoped-reservation-registry/ — confirm 07-01-SUMMARY.md and 07-02-SUMMARY.md already exist (created by Plans 07-01 and 07-02). 07-03-SUMMARY.md is created as the FINAL step of THIS task, AFTER verify-phase.sh exits 0.
    - CLAUDE.md §"verify-phase.sh — phase verification dispatcher" — operating instructions.
    - tests/structural/test_phase_07_shape.py (from Task 1) — confirm the test file is in place before running the gate.
  </read_first>

  <action>
    Run the deterministic phase-verification dispatcher:

      bash scripts/verify-phase.sh 07

    Read the structured markdown report. Checks that must pass:

      1. bash scripts/test.sh all → 0 failures, 0 errors (full pytest suite —
         includes Phase 7's new unit + multiprocess + structural tests).
      2. bash scripts/test.sh structural → green (test_phase_07_shape.py
         from Task 1 inclusive).
      3. verify-redis-config.sh → PASS or SKIP (redis-unreachable is not a
         Phase 7 failure; production redis-config is a Phase 1 concern).
      4. em-proj on PATH → must be found.
      5. em-proj --version → must return a version string.
      6. Anti-pattern grep: no TBD/FIXME/XXX/HACK/TODO/PLACEHOLDER tokens
         in src/, tests/, scripts/.
      7. SUMMARY.md presence:
           07-01-PLAN.md → 07-01-SUMMARY.md (exists from Plan 07-01)
           07-02-PLAN.md → 07-02-SUMMARY.md (exists from Plan 07-02)
           07-03-PLAN.md → 07-03-SUMMARY.md (created at END of this task)
      8. Commit traceability: recent commits reference Phase 07 plans
         (feat/test commits from Plans 07-01 and 07-02 land on main as
         normal em-proj-side commits; Task 1 of THIS plan also lands on
         main as a test commit; the SKILL.md edit from Task 2 does NOT
         produce a main commit because it's outside the repo, identical to
         Phase 6's gsd-sdk-patch posture).

    Special handling for check #8 (Phase-7-specific commit-traceability nuance):

      Plans 07-01, 07-02, AND Task 1 of Plan 07-03 all produce em-proj-side
      commits on main (source files + test files). The skill-doc edit in
      Task 2 produces NO main-branch commit because SKILL.md is in
      ~/.claude/skills/em-global-state/. Document this in 07-03-SUMMARY.md
      so future archaeology knows why the "feat(07-03):" line in `git log`
      includes only the structural test, not the SKILL.md edit.

      If verify-phase.sh's commit-traceability check flags Plan 07-03 as
      having a partial commit footprint (test commit yes; SKILL.md edit
      no), that is EXPECTED and not a failure.

    Order of operations:

      1. Run `bash scripts/verify-phase.sh 07`. Expect failure on check #7
         (07-03-SUMMARY.md doesn't exist yet — about to be created).
      2. Inspect the report. ALL OTHER CHECKS (1-6, 8) must pass.
         If any non-SUMMARY check fails: diagnose root cause and fix.
         NEVER silence the check.
      3. Commit test_phase_07_shape.py to em-proj main:
           git add tests/structural/test_phase_07_shape.py
           git commit -m "test(07-03): structural shape assertions + verify-phase.sh 07 gate"
         NO Co-Authored-By trailer (per global rule + project CLAUDE.md).
      4. Create 07-03-SUMMARY.md per the <output> block below (lands on the
         planning branch worktree).
      5. Re-run `bash scripts/verify-phase.sh 07`. All checks must pass
         (exit 0).

    Common failure modes to watch for:
      - A new TBD/TODO accidentally introduced in the new test or source
        files → remove before re-running.
      - Plan 07-02's race-test cleanup left a stale reservation in db=15 →
        run `EM_PROJ_REDIS_DB=15 em-proj state reserve-list --json` to
        inspect; manually clean up via redis-cli FLUSHDB on db=15 if needed.
      - Test F regex (per-child cwd= check in multi-clone tests) fails
        because a Popen call is longer than the 600-char window → widen
        the window or refactor the test's Popen call to put cwd= earlier.
      - The structural test asserts the actionable-error copy substring
        verbatim — if Plan 07-02's verb implementation drifted from the
        locked wording, the test fails actionably.

    After verify-phase.sh 07 exits 0:
      - 07-03-SUMMARY.md is the FINAL artifact; create it AFTER the green
        verify-phase.sh, then the phase is structurally complete and ready
        for /gsd-verify-work (which spawns a verifier that applies
        judgment about whether the phase GOAL — cross-clone reservation
        coordination — is delivered, not just whether checks pass).
  </action>

  <verify>
    <automated>bash scripts/verify-phase.sh 07</automated>
  </verify>

  <done>
    - bash scripts/verify-phase.sh 07 exits 0
    - Report shows all checks PASS (or SKIP for redis-unreachable, which is allowed)
    - test_phase_07_shape.py committed to main as `test(07-03): …` (single commit; no Co-Authored-By trailer)
    - 07-03-SUMMARY.md exists on the planning branch
    - 07-01-SUMMARY.md, 07-02-SUMMARY.md, 07-03-SUMMARY.md all present in `.planning/phases/07-project-scoped-reservation-registry/`
    - Phase 7 is structurally complete and ready for /gsd-verify-work
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structural test → src/em_proj/ files | Read-only Path.read_text(); no execution surface |
| Structural test → tests/multiprocess/*.py | Read-only source-grep; the regex check enforces a code-shape invariant (cwd= present in Popen) |
| SKILL.md edit boundary | The file lives at ~/.claude/skills/em-global-state/SKILL.md, OUTSIDE em-proj's git tree. Any Claude-skill-reset or machine reinstall will require re-applying Task 2. |
| verify-phase.sh dispatcher → full test suite | Runs all tests including Phase 7's multi-process tests against Redis db=15; clean_db isolates from prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | T (Tampering) | Two-namespace invariant breach via future refactor consolidating claim.py and reserve.py | mitigate | Tests B + C of test_phase_07_shape.py grep the actual source files for the disjoint-prefix property AND assert claim.py contains NO 'upstream_identity' string. A consolidation refactor that parameterized the prefix would FAIL these tests — visible at structural-test runtime. |
| T-07-06 carry | T (Tampering) | Multi-clone fixture loses per-child cwd= | mitigate | Test F's regex on tests/multiprocess/test_reserve_*.py source enforces that every subprocess.Popen( … ) call contains cwd= as a kwarg. This is machine-enforced; a future test that mirrors Phase 4-6's env-only pattern fails this assertion. |
| T-07-15 | T (Tampering) | Actionable-error copy drift | mitigate | Test E grep-asserts the exact substring "workstream unresolved — set it via" in state/__init__.py. A refactor that rephrases the message FAILS this test. The user-contract wording is locked. |
| T-07-16 | I (Information Disclosure) | SKILL.md leaks Phase 7 schema | accept | The schema in the SKILL.md verb subsection is the SAME data the em-proj reserve-list verb returns in its JSON envelope. SKILL.md is documentation of a public surface; no additional disclosure. |
| T-07-17 | T (Tampering) | SKILL.md overwrite by future skill-reset | mitigate | This task is idempotent (re-applying is safe). The 07-03-SUMMARY.md records the deliverable; if SKILL.md is reset, the SUMMARY tells future-Claude to re-apply Task 2. (Identical recovery posture to Phase 6's gsd-sdk patch.) |
</threat_model>

<verification>
- bash scripts/test.sh structural -k test_phase_07_shape exits 0 (Task 1)
- bash scripts/test.sh structural exits 0 (no Phase 2-6 regression)
- test "$(grep -c '^### /em-global-state reservations' /Users/emonical/.claude/skills/em-global-state/SKILL.md)" -ge 1 returns 0 (Task 2)
- bash scripts/verify-phase.sh 07 exits 0 (Task 3)
- bash scripts/test.sh all exits 0 (full em-proj suite green)
- All three Phase 7 SUMMARY.md files exist after Task 3 completes
- src/em_proj/state/claim.py UNTOUCHED by this plan (no edits to the Phase 4 surface)
</verification>

<success_criteria>
- tests/structural/test_phase_07_shape.py exists with 8+ test functions encoding the namespace-disjointness, 3-Lua-script-presence, verb-wiring, per-child-cwd, actionable-error-copy, SUMMARY-coverage, and SKILL.md-verb invariants
- ~/.claude/skills/em-global-state/SKILL.md has a new `reservations` verb subsection (cross-repo deliverable; outside em-proj's git tree)
- bash scripts/verify-phase.sh 07 exits 0
- Phase 7 deliverables complete:
  - RESERVE-01 substrate via Plan 07-01 (identity resolver + canonicalizer)
  - RESERVE-02 substrate via Plan 07-01 (reserve.py 7-field holder + Lua atomicity) AND verb-layer via Plan 07-02 (anonymous refusal + workstream auto-stamp)
  - RESERVE-03 satisfied via Plan 07-02 (reserve-list verb) AND THIS plan (SKILL.md surface)
  - RESERVE-04 satisfied via Plan 07-02 (--category + --upstream flags) AND THIS plan (SKILL.md documentation)
  - RESERVE-05 satisfied via Plan 07-02 (verb shape + TTY prompt + locked error message), structurally locked via Test E in Task 1
  - SC#1 (sibling clones share namespace): proven by Plan 07-01 substrate + Plan 07-02 race test
  - SC#2 (loser sees winner's workstream): proven by Plan 07-02 race test assertion on holder.workstream
  - SC#3 (3-clone reserve-list visibility): proven by Plan 07-02 three-clones test
  - SC#4 (TTY prompt + non-TTY exit 1): proven by Plan 07-02 unit tests + locked by Test E
  - SC#5 (namespace disjointness): structural Tests B + C in this plan
- Phase 7 is ready for /gsd-verify-work to confirm DELIVERY (vs. just check-passing)
</success_criteria>

<output>
After completion of Task 3 (verify-phase.sh 07 exits 0), create
`.planning/phases/07-project-scoped-reservation-registry/07-03-SUMMARY.md`
on the planning-branch worktree.

The SUMMARY must include:

  1. Standard summary preamble (link to PLAN, completion date, no deviations
     of substance beyond Q-H findings already recorded in 07-01 / 07-02 SUMMARYs).

  2. Phase 7 closeout notes:
     - RESERVE-01 delivered (Plan 07-01 substrate).
     - RESERVE-02 delivered (Plan 07-01 substrate + Plan 07-02 verb).
     - RESERVE-03 delivered (Plan 07-02 reserve-list + Plan 07-03 SKILL.md).
     - RESERVE-04 delivered (Plan 07-02 --category/--upstream + Plan 07-03 SKILL.md).
     - RESERVE-05 delivered (Plan 07-02 verb + TTY prompt + locked error message;
       Plan 07-03 Test E locks the wording).
     - All 5 ROADMAP Phase 7 Success Criteria satisfied.

  3. CRITICAL cross-repo recovery notes (mirror Phase 6's posture):
     - The SKILL.md edit in Task 2 lives at ~/.claude/skills/em-global-state/SKILL.md.
       This file is NOT in em-proj's git repository.
     - Any future Claude-skill-reset, machine reinstall, or manual edit to
       SKILL.md will remove the `reservations` verb subsection. The
       structural test in test_phase_07_shape.py uses pytest.skip on
       missing SKILL.md (NOT xfail — see RESEARCH §Q-E "Confidence: MEDIUM"
       discussion + Phase 7 Plan 07-03 Task 1 docstring), so absence is
       SILENT on a fresh checkout. To restore: re-run Task 2 of this plan.

  4. Q-H findings already recorded in 07-01-SUMMARY and 07-02-SUMMARY are
     final: Phase 6 does NOT store workstream name in the claim holder;
     the Phase 7 TTY prompt fires regardless of workstream.active claim
     presence. test_reserve_phase_6_claim_set_but_name_unknown_still_prompts
     in test_reserve_verbs.py (Plan 07-02) pins this behavior.

  5. Commit traceability summary:
     - Plan 07-01: feat(07-01) main commits — identity.py edit, reserve.py creation, test files
     - Plan 07-02: feat(07-02) + test(07-02) main commits — state/__init__.py edit, verb tests, multi-process tests
     - Plan 07-03: test(07-03) main commit — test_phase_07_shape.py
       (SKILL.md edit produces NO main commit because it's outside the repo, same as Phase 6's gsd-sdk patch)
     - All three plans have planning-branch SUMMARY.md commits via the em-execute-phase wrapper's filesystem rescue (per project CLAUDE.md and Phase 6 precedent)

  6. Hand-off to /gsd-verify-work: verify-phase.sh 07 has validated all
     deterministic checks; /gsd-verify-work spawns a verifier that applies
     judgment about whether the phase GOAL (cross-clone reservation
     coordination via shared upstream_identity namespace) is DELIVERED end-
     to-end. Suggested manual verification: set up three real sibling clones
     of `git@github.com:emonical/em-proj.git` in /tmp/em-proj-{a,b,c}/, run
     the workflow from 07-VALIDATION.md "Manual-Only Verifications" Table.
</output>
